defmodule CredenceTest do
  use ExUnit.Case

  # test "flags Enum.at/2 inside a for loop" do
  #   code = """
  #   defmodule Example do
  #     def run(list) do
  #       for i <- 0..10 do
  #         Enum.at(list, i)
  #       end
  #     end
  #   end
  #   """

  #   issues = Credence.analyze(code)

  #   IO.inspect(issues)
  # end
end
