# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Real-world corpus: source of popular hex packages fetched (once) by the
# over-firing test layer via `mix hex.package fetch`. See test/support/corpus.ex.
/corpus/

# Where third-party dependencies like ExDoc output generated docs.
/doc/

# Temporary files, for example, from tests.
/tmp/

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Ignore package tarball (built via "mix hex.build").
credence-*.tar


# Autonomous review-loop verdict channel (transient)
maintainer_tools/_verdict

# Autonomous review-loop per-row agent transcripts
maintainer_tools/stage_1_promote_fixable_rules/.review_logs/
maintainer_tools/stage_2_promote_non_fixable/.review_logs/
maintainer_tools/stage_3_resurrect_followups/.review_logs/

.claudemaintainer_tools/corpus_whitelist_validator/accepted_findings.txt
maintainer_tools/corpus_whitelist_validator/data/
maintainer_tools/corpus_whitelist_validator/reports/
maintainer_tools/corpus_whitelist_validator/.logs/
