# Finding summary

Generated: 2026-08-25T04:22:22Z

Progress: 761/798 files reviewed.

- Blockers: 720
- Concerns: 245
- Nits: 136

## Repeated root-cause candidates

None detected.

## Blockers

- **lib/syntax/close_unclosed_brace.ex** — lib/syntax/close_unclosed_brace.ex:223 — the "placement must be beyond doubt" guard only ever appends `}` to the *end* of lines *before* the target line, so it is structurally inert whenever the literal sits on one line: for `x = {1, 2 |> IO.inspect()` with `end` on the next line, `target_line/3` picks that same line (index == open_line - 1), the scan range `(start_line - 1)..(index - 1)//1` is empty, and the rule commits `x = {1, 2 |> IO.inspect()}` — even though `x = {1, 2} |> IO.inspect()` also parses and means something else. The rule's own moduledoc and test `test/syntax/close_unclosed_brace_fix_test.exs:242` declare exactly this ambiguity must be refused; it is, but only when the pipe happens to be on the next line. The same hole applies to a mid-line `}` on the target line of a multi-line literal (`x = {1,` / `2 |> IO.inspect()` is committed for the same reason).
- **lib/syntax/close_unclosed_brace.ex** — lib/syntax/close_unclosed_brace.ex:231 — the "placement must be beyond doubt" guard can only ever test end-of-line placements on lines *before* the target line, so it is structurally inert whenever the literal opens on the line the `}` is appended to. On `defmodule E do` / `  def foo do` / `    x = {1, 2 |> IO.inspect()` / `  end` / `end`, `target_line/3` picks that same line (index == open_line - 1), the scan range `(start_line - 1)..(index - 1)//1` is empty, `sole_placement/4` returns `:ok` unconditionally, and the rule commits `x = {1, 2 |> IO.inspect()}` — the tuple `{1, IO.inspect(2)}` — although `x = {1, 2} |> IO.inspect()` parses just as well and is a different program. The identical ambiguity spread over two lines is refused (pinned by test/syntax/close_unclosed_brace_fix_test.exs:242), so the rule's answer depends on where the author happened to break the line. The same hole covers any mid-line placement on the target line of a multi-line literal: `x = {1,` / `    2 |> IO.inspect()` / `end` commits for the same reason (the `x = {1,` line is skipped by the trailing-comma rule and no mid-line `}` is ever tried). The moduledoc's "Only one placement is faithful: at the very end of the literal's last line" (lines 26-30) states this as policy, but the second guard's worked `|>` example (lines 46-58) promises the rule stays silent for exactly this shape.
- **lib/syntax/close_unclosed_brace.ex** — lib/syntax/close_unclosed_brace.ex:235 — the trailing-comma exemption in `sole_placement/4` is a raw text test (`String.ends_with?(line, ",")`) that short-circuits *before* `competing_placement?/4`, so it also skips the swallowed-brace probe at :256. An earlier line of the literal whose trailing *comment* ends in a comma is therefore treated as "pinned by a trailing comma" and never probed. On `defmodule E do` / `  def foo do` / `    x = {1 # oops,` / `    |> g()` / `  end` / `end`, the target line is `    |> g()`, one `}` makes the whole source parse, the only earlier line ends in `,` and is skipped, and the rule commits `x = {1 # oops,` / `    |> g()}` — i.e. `{1 |> g()}` — where the author's `{1} |> g()` is equally faithful. The same source with the comma removed from the comment is refused, and that refusal is a deliberate, tested guard (test/syntax/close_unclosed_brace_fix_test.exs:293, whose own comment names this exact corruption). The comma reasoning quoted in the moduledoc ("closing after a trailing comma drops an element") only holds for a comma in code; nothing here distinguishes code bytes from comment bytes. The same text-level test at :174 is harmless because it only over-refuses.
- **lib/syntax/close_unclosed_doc_heredoc.ex** — lib/syntax/close_unclosed_doc_heredoc.ex:73 — the rule's whole admission test is "the first non-blank line after `@doc \"\"\"` starts with `def`", which is also true of a *correctly closed* doc heredoc whose first content line is a `def` example. On such a file the fix inserts `\"\"\"` right after the opener, truncating the doc to empty, promoting the documented example to real code, and turning the doc's real closing `\"\"\"` into an *opening* heredoc that swallows everything down to the next bare `\"\"\"` line. That output can still parse — e.g. `@doc \"\"\"` / `def example do` / `:ok` / `end` / `\"\"\"` / `def a, do: 1` / blank / `@doc \"\"\"` / `Docs for b.` / `\"\"\"` / `def b, do: 2` becomes a module where `def a/0` has been eaten into a string literal and `def example/0` is a real function. Because the result parses, `ProgressGuard.verdict/4` short-circuits on `after_stage == :parses` (lib/syntax/progress_guard.ex:148) and `commit_or_roll_back/4` commits it, so neither the per-rule guard nor the round's all-or-nothing gate can see the deletion. The Syntax round only runs on source that does not parse, so reaching this needs one unrelated syntax error in the file — and if that error sits in the code the runaway heredoc swallows, swallowing it is precisely what makes the file parse again.
- **lib/syntax/close_unclosed_doc_heredoc.ex** — lib/syntax/close_unclosed_doc_heredoc.ex:83 — the test that admits the input and the place the fix edits are two different lines. `def_below?`/`closing_quotes_below?` scan to end of file (a `-> false` branch in `Enum.find_value/3` means "keep looking", as lines 115-119 now correctly state), so the rule fires whenever there is a `def` *anywhere* below and no `"""` anywhere below — but `find_next_nonblank_offset/1` then inserts the closing `"""` before the **first non-blank line**. Whenever the unclosed doc actually has text in it (the shape the moduledoc itself describes: the LLM wrote the doc and forgot the closer), those differ and the terminator lands above the doc text instead of above the `def`. On `defmodule Sample do\n  @doc """\n  1 + 1\n\n  def get(m, k), do: Map.get(m, k)\nend\n` the output is `@doc """` / `"""` / `1 + 1` / `def get…`: the doc is emptied and its content becomes live module-body code. That output *parses*, so neither guard can catch it — ProgressGuard sees tokenize_error → parse_error (rank up, always `:keep`), and the phase-level all-or-nothing gate only rolls back sources that still fail to parse. This is the exact failure mode the new comment on line 38-44 claims the `\b` prevents; the `\b` only narrows the no-`def`-at-all case, not this one. When the doc text does not parse (`Finds the min and max.`) the same rewrite is kept per-rule and then discarded with the whole round, so the rule silently repairs nothing on its commonest realistic input — and if another rule repairs the remaining error in the same round, the emptied doc commits. Pre-existing in shape (base `next_nonblank_is_def?` had the same whole-file scan), but this PR documents the whole-file scan as intended without reconciling the insertion point. Correct behaviour is to insert immediately before the line that matched `@def_line`, not before the first non-blank line.
- **lib/syntax/close_unclosed_doc_heredoc.ex** — lib/syntax/close_unclosed_doc_heredoc.ex:55 — `@def_line` is `^\s*def…\b`, so an *indented example* inside the doc text counts as "the next definition". On an unclosed doc whose body shows a `def` example — `defmodule S do` / `  @doc """` / `  Example:` / blank / `      def add(a, b), do: a + b` / `  def add(a, b), do: a + b` / `end` — `def_offset/1` returns the example line, and the closer is inserted above it: the doc is truncated to `"Example:\n\n"` and the documented example becomes a live second `add/2` clause. The output *parses*, so `ProgressGuard.verdict/4` keeps it and `commit_or_roll_back/4` commits it — the same silent, unrevertable damage class as the two blockers fixed in rounds 1 and 2, just entered through the insertion point instead of the admission test. No test in either test file feeds an *unclosed* doc that contains a `def`-shaped line in its body; the three "def example" fixtures (fix_test.exs:85, :106, :124) are all correctly closed docs, which the `"""` veto declines before the boundary is ever consulted. Anchoring the def scan to the indent already captured from the `@doc` line would exclude a deeper-indented example while still matching every fixture in the suite.
- **lib/syntax/close_unclosed_doc_heredoc.ex** — lib/syntax/close_unclosed_doc_heredoc.ex:134 — because the scan stops only at a `def…` line, every module attribute between the doc text and the definition is swallowed into the doc. On `defmodule S do` / `  @doc """` / `  Adds.` / blank / `  @spec add(integer, integer) :: integer` / `  def add(a, b), do: a + b` / `end` the closer lands under the `@spec`, so the emitted module's `@doc` string ends with the spec text and the `@spec` attribute itself is gone. The result parses and compiles without a warning, so neither the per-rule guard nor the round's all-or-nothing gate can see the deleted contract. `@doc` → `@spec`/`@impl` → `def` is the ordering LLMs emit most often, and it is exactly the shape this rule exists to repair. Note this is a behaviour change from the base version, which declined this input outright (`next_nonblank_is_def?` saw the prose line and returned `false`): the PR trades "repairs nothing" for "repairs it wrongly, and the wrong answer ships".
- **lib/syntax/close_unclosed_doc_heredoc.ex** — lib/syntax/close_unclosed_doc_heredoc.ex:153 — `doc_end_offset/2` searches for the next definition all the way to end-of-file with nothing stopping it at the enclosing module's `end`, so on a two-module file whose first module has an unclosed `@doc """` and which contains no `"""` anywhere below (e.g. `defmodule Helpers do` / `  @doc """` / `  Shared helpers.` / `end` / blank / `defmodule Main do` / `  def run, do: :ok` / `end`) the closer is inserted before `def run` — inside the *second* module. The result is `defmodule Helpers do` with `@doc "Shared helpers.\nend\n\ndefmodule Main do\n"` and `def run/0` as its own function: module `Main` has been swallowed into a docstring and silently deleted. This passes every downstream gate — the two outdented lines inside the heredoc are only a *warning* in this Elixir (the repo's own `test/semantic/outdented_heredoc_check_test.exs` pins `severity: :warning`), the output parses under Sourceror, and `compiles_no_worse?/2` compares error sets, not warnings. This is new in this delta: the pre-PR `next_nonblank_is_def?` required the *first non-blank* line below the opener to be a `def`, so the old code declined this input outright and never spanned an outdented line.
- **lib/syntax/close_unclosed_doc_heredoc.ex** — lib/syntax/close_unclosed_doc_heredoc.ex:173 — `doc_end_offset/2` now ends the doc at the first *def-or-attribute* line at the doc's indent, so every other module-body line between the opener and that def is swallowed into the doc string and deleted. On `defmodule Server do\n  @doc """\n  Starts the server.\n  use GenServer\n\n  def start_link(opts), do: ...\nend` the closer is inserted above `def start_link`, `use GenServer` becomes doc text, and the module loses the GenServer behaviour, its default callbacks and `child_spec/1`. The output parses and compiles (a stale `@impl` only warns), so `commit_or_roll_back/4` keeps it and nothing downstream reverts it. The same absorption eats an Ecto `schema "users" do ... end` block, an Nx `defn`, or `import`/`alias`/`require` lines sitting between a doc and its def — none of which `@def_word` or `@attribute_word` recognise. This is new in this delta: the base version required the *first non-blank* line below the opener to be a `def` and inserted immediately above it, so it could never absorb an intervening line. The rule already declines when the first definition-form line is one `@doc` cannot document (`defstruct`, `defmodule`, …); the gap is that non-`def…` module-level constructs get no such treatment.
- **lib/syntax/close_unclosed_fn_delimiter.ex** — lib/syntax/close_unclosed_fn_delimiter.ex:162 — `stray_end_error/1` accepts ANY "`(` closed by `end`" mismatch anywhere in the file, not only one caused by the `end` this rule just inserted; with the whole-file parse gate removed by this PR, a file whose *second, unrelated* fault has that shape makes the rule invent a stray `end` that does not exist and delete a real one. Input `defmodule Mixed do\n  def a(list) do\n    Enum.max_by(list, fn {_, s} -> s)\n  end\n\n  def broken(, do: 1\nend` — the parser's first error is the `fn`/`)` mismatch on line 3, so `detect/1` fires; inserting `end` there makes lines 1-4 perfectly balanced (this is NoUnclosedFnDelimiter's plain-unclosed-`fn` shape, which this rule's moduledoc:41-44 promises it never touches), but the tokenizer then reports the unrelated `def broken(` on line 6 as a `(` closed by the `end` on line 7, so `stray_end_error/1` returns `{:ok, {6, 13}}` and the rule proceeds to delete a line. Output: `def a`'s own `  end` (line 4) is deleted. Under the base version this was rejected because the candidate did not parse.
- **lib/syntax/close_unclosed_fn_delimiter.ex** — lib/syntax/close_unclosed_fn_delimiter.ex:259 — the identity guard `same_stray_end?/2` compares the `(` position captured *before* the deletion against the one reported *after* it, relying on the invariant stated at :159-161 and :236-240 that "the `(` is always above the line being deleted". Nothing checks that invariant. When `stray_end_error/1` picked up a `(` that is *below* the insertion point (the blocker above), deleting any bare `end` line shifts that `(` up by one line, the two positions differ, and the guard reads the unchanged error as "the stray `end` is gone" — so the very first bare `end` line at or below the insertion point is accepted no matter what it closes. The same positional-not-semantic weakness lets a deletion be accepted whenever the identical structural error is merely re-reported at a *different* enclosing `(`.
- **lib/syntax/close_unclosed_fn_delimiter.ex** — lib/syntax/close_unclosed_fn_delimiter.ex:210 — an unclosed `(` sitting ABOVE the repaired line produces the very same "`(` closed by `end`" complaint the rule reads as "there is a stray `end` of mine to delete", and `opening_line(meta) <= repair_line` admits it, so the rule deletes a real block terminator. On the input `defmodule M do` / `  def broken(, do: 1` / `` / `  def go(list) do` / `    Enum.max_by(list, fn {_, s} -> s)` / `  end` / `end` the first parse error is the `fn`/`)` mismatch on line 5, so `end` is inserted there; the leftover error is then the `(` opened on line 2 (above line 5) being closed by the `end` on line 7, which `stray_end_above?` accepts. `delete_stray_end` deletes the first `end` token below line 5 — line 6, which is `def go`'s own terminator, not a stray one — and `clears_stray_end?` (line 318) accepts the result because the remaining error is now an EOF "missing terminator: )" whose `closing_delimiter` is nil (so `stray_end_meta?` is false) and whose `end_line` is past the deleted line. Output is the file with `def go`'s `end` gone, plus a bogus issue reported at line 5. This is exactly the plain-unclosed-fn shape the moduledoc promises to leave to `NoUnclosedFnDelimiter`, and it is a regression introduced by this PR: the base version's whole-file parse gate declined here. Every test added for the unrelated-fault case ("declines the plain-unclosed-fn shape even when an unrelated ( is closed by an end below", "does not delete a real end on the strength of an unrelated ( closed by an end below") places the unrelated `(` BELOW the repair; the above-the-repair direction the new guard does not cover is untested. The Syntax round's all-or-nothing rollback masks this only while the file still fails to parse at the end of the round — it does not apply under `syntax_partial_repairs: true`, nor when a later rule repairs the remaining `(`.
- **lib/syntax/close_unclosed_fn_delimiter.ex** — lib/syntax/close_unclosed_fn_delimiter.ex:305 — "first `end` token below the repair is the stray one" is an assumption, not a property of the bug, and when it is false the rule deletes a real block terminator and the result parses, so nothing catches it. Input: the documented bug shape where the outer `fn` body continues with a complete `do…end` block before the dangling `end` — `defmodule M do / def go(m) do / Enum.map(m, fn r -> / Enum.map(r, fn e -> / if e == 0 do 1 else e end) / if r > 0 do / IO.puts("big") / end / r * 2 / end / end) / end / end`. After inserting `end` on line 5 the leftover complaint is `(` (line 3) closed by `end` (line 11), 3 <= 5, so `stray_end_above?` passes; `first_end_token_line/2` then stops at line 8 — the `if`'s own terminator — not at the real stray on line 10. Deleting line 8 yields `if r > 0 do IO.puts("big"); r * 2 end`, which parses and compiles cleanly, so `clears_stray_end?/3`'s `{:ok, _ast} -> true` arm accepts it unconditionally. The function now returns `nil` for `r <= 0` instead of always returning `r * 2` — a silent behaviour change, and `analyze/1` reports line 5 with "a stray `end` follows", which is a false diagnosis of that line. Pre-existing: the base version's "first bare `end` line whose removal makes the whole file parse" scan picks the same line 8 and commits the same output. What the PR changes is that the moduledoc (lines 79-83) and the `clears_stray_end?` comment (lines 339-346) now assert this deletion is ruled out "by the shape of the bug", so the hazard is now documented as handled. Note the guard is one-sided: it bounds walking *past* the stray but not stopping *short* of it.
- **lib/syntax/fix_bare_tuple_zero_in_type.ex** — lib/syntax/fix_bare_tuple_zero_in_type.ex:109 — `toggle_heredoc/2` decides "am I inside a heredoc?" by counting `"""`/`'''` per line and flipping on an odd count, so any line whose delimiter count is odd for a reason other than being a delimiter inverts the state for the rest of the file. On input like `@moduledoc """` / `Charlist heredocs open with '''.` / (blank) / `  @type t :: () -> any()` / `"""`, the `'''` line flips the tracker off and the rule rewrites the documentation prose to `  @type t :: (() -> any())` — silently changing the text the module's `@moduledoc` produces, which is exactly the corruption the moduledoc at :46-50 claims this tracking prevents. The same flip runs the other way (a code line containing one `"""`, e.g. `String.split(x, ~s("""))`, turns tracking *on* and the rule goes inert for the remainder of the file with every gate green). The project already has the primitive for this, with the opposite failure direction stated in its own docs: `Credence.SourceMask.lines/1` + `self_contained?/2` (lib/source_mask.ex:224, :255) — "the failure direction is a missed fix on a line inside a multi-line literal, never a corrupted one" — and `PreferSpecArrowOperator` (lib/syntax/prefer_spec_arrow_operator.ex:25-31) documents that it was converted to it precisely because it had been rewriting the `## Bad` example in its own moduledoc.
- **lib/syntax/fix_bare_tuple_zero_in_type.ex** — lib/syntax/fix_bare_tuple_zero_in_type.ex:92 — A multiline range return such as `@type t :: () -> 1` followed by `.. 10` is not recognized as a continuation, so the fix closes the function type on the first line and changes it to `(() -> 1) .. 10` instead of wrapping the complete `1..10` return type.
- **test/syntax/fix_bare_tuple_zero_in_type_fix_test.exs** — test/syntax/fix_bare_tuple_zero_in_type_fix_test.exs:297 — Every repair test calls the rule’s `fix/1` directly; none runs realistic invalid source through `Credence.Syntax.fix/1`. The suite can therefore pass if the new rule is missing from discovery, never reached in round order, or reverted/rolled back by round guards, while users receive the original broken typespec.
- **lib/syntax/fix_capture_operator_syntax.ex** — lib/syntax/fix_capture_operator_syntax.ex:84 — Only whole-line comments and double-quoted strings are masked. Valid source such as `msg = 'compare &> here'`, `msg = ~s(compare &> here)`, or `x = 1 # compare &> here` is rewritten, changing literal values or non-code bytes.
- **lib/syntax/fix_capture_operator_syntax.ex** — lib/syntax/fix_capture_operator_syntax.ex:48 — The leading-boundary regex omits valid term boundaries such as `=` and `->`, so malformed captures in inputs like `cmp=&>` or `fn ->&> end` are neither reported nor repaired.
- **lib/syntax/fix_capture_operator_syntax.ex** — lib/syntax/fix_capture_operator_syntax.ex:89 — Any odd `"""` byte sequence toggles heredoc state even inside a comment or ordinary literal; for example, `# """` causes a later `cmp = &>` line to be silently skipped instead of repaired.
- **test/syntax/fix_capture_operator_syntax_analyze_test.exs** — test/syntax/fix_capture_operator_syntax_analyze_test.exs:110 — The masking tests cover only whole-line comments and double-quoted strings. Inputs such as `x = 1 # compare &> here`, `msg = 'compare &> here'`, and `msg = ~s(compare &> here)` are valid code but are reported by the rule, allowing its fix to corrupt non-code bytes while this suite stays green.
- **test/syntax/fix_capture_operator_syntax_analyze_test.exs** — test/syntax/fix_capture_operator_syntax_analyze_test.exs:9 — Every positive fixture places whitespace or an opening delimiter before the capture. Real malformed inputs such as `cmp=&>` and `fn ->&> end` are silently missed by the rule, so the tests do not prove the repair fires across valid term boundaries.
- **test/syntax/fix_capture_operator_syntax_analyze_test.exs** — test/syntax/fix_capture_operator_syntax_analyze_test.exs:118 — The heredoc test covers only a normal heredoc. A `"""` sequence inside a comment or ordinary literal incorrectly puts the analyzer into heredoc mode, causing a later real capture such as `cmp = &>` to be skipped without any test failure.
- **test/syntax/fix_capture_operator_syntax_fix_test.exs** — test/syntax/fix_capture_operator_syntax_fix_test.exs:155 — The masking tests cover only whole-line comments, double-quoted strings, and heredocs. Valid inputs such as `x = 1 # compare &> here`, `msg = 'compare &> here'`, and `msg = ~s(compare &> here)` are rewritten inside non-code text, changing comments or literal values while this suite stays green.
- **test/syntax/fix_capture_operator_syntax_fix_test.exs** — test/syntax/fix_capture_operator_syntax_fix_test.exs:11 — Every positive fixture puts whitespace or an opening delimiter before the capture. Malformed captures after other valid term boundaries, such as `cmp=&>` and `fn ->&> end`, are silently left unrepaired without any test failing.
- **test/syntax/fix_capture_operator_syntax_fix_test.exs** — test/syntax/fix_capture_operator_syntax_fix_test.exs:165 — The heredoc test covers only a normal heredoc. A `"""` sequence in a comment or ordinary literal incorrectly makes the rule treat later code as heredoc content, so a subsequent `cmp = &>` is silently skipped while this suite stays green.
- **test/syntax/fix_capture_operator_syntax_fix_test.exs** — test/syntax/fix_capture_operator_syntax_fix_test.exs:9 — Every repair assertion calls `FixCaptureOperatorSyntax.fix/1` directly; none calls `Credence.Syntax.fix/1` or the full pipeline. The rule could be undiscovered, ordered too late, or rolled back by pipeline validation and repair nothing in production while all these tests pass.
- **lib/syntax/fix_div_rem.ex** — lib/syntax/fix_div_rem.ex:211 — For `def f, do: if ok, do: a div b`, the new function-head pattern rewrites the whole keyword expression as `def f, do: div(if ok, do: a, b)`, changing the program instead of fixing the `if` body.
- **lib/syntax/fix_div_rem.ex** — lib/syntax/fix_div_rem.ex:211 — Pre-existing: the right operand always extends to the end of the line, so `x = a div b + 1` becomes `x = div(a, b + 1)` instead of `x = div(a, b) + 1`, changing operator precedence and the result.
- **test/syntax/fix_div_rem_test.exs** — test/syntax/fix_div_rem_test.exs:439 — The new “declines” tests enshrine an analyze/fix mismatch: both `IO.puts("a div b"); x = n div 2` and `fn n -> n div 2 end` are reported as infix-operator issues, but `fix/1` returns them unchanged, so admitted inputs receive no repair while the tests pass.
- **lib/syntax/fix_do_block_fusion.ex** — lib/syntax/fix_do_block_fusion.ex:63 — pre-existing: on valid single-line nesting such as `defmodule M do def f, do: 1 end`, the rule mistakes the outer module’s `end` for a stray one-liner terminator and removes it, producing invalid code.
- **lib/syntax/fix_elsif_in_if_chain.ex** — lib/syntax/fix_elsif_in_if_chain.ex:112 — `fix/1` rewrites only the first `elsif`/`elif` chain. A file containing two independent bad chains remains unparsable after this rule’s single Syntax-round invocation, so the pipeline rolls the repair back and fixes neither chain.
- **lib/syntax/fix_elsif_in_if_chain.ex** — lib/syntax/fix_elsif_in_if_chain.ex:317 — the multiline-string safety check misses multiline sigils such as `~S(...)` and ordinary multiline charlists. In a repaired branch, `build_cond/3` trims and re-indents every line, changing the literal’s contents and therefore the program’s answer.
- **lib/syntax/fix_ets_match_spec_erlang_less_than.ex** — lib/syntax/fix_ets_match_spec_erlang_less_than.ex:87 — The rule treats every non-comment line as code and only masks double-quoted strings. On an unparseable file containing an inline comment, charlist, or bracket-delimited sigil such as `note = ~S([:=<,])`, it rewrites those non-code bytes to `:"=<"`, silently changing user data while repairing the real syntax error elsewhere.
- **test/syntax/fix_ets_match_spec_erlang_less_than_analyze_test.exs** — test/syntax/fix_ets_match_spec_erlang_less_than_analyze_test.exs:7 — Every positive test calls the rule module directly, so the suite can pass even if the Syntax phase never discovers or invokes this new rule; on realistic `guards = [{:=<, :"$1", cutoff}]` input, the production pipeline could leave the invalid atom unrepaired. Add an end-to-end assertion through `Credence.Syntax` or `Credence.fix`.
- **test/syntax/fix_ets_match_spec_erlang_less_than_fix_test.exs** — test/syntax/fix_ets_match_spec_erlang_less_than_fix_test.exs:162 — Comment protection is tested only on whole-line comments. The implementation rewrites `:=<` in trailing comments such as `value = 1 # [{:=<, x}]`, changing non-code bytes; this test suite would not catch that corruption.
- **test/syntax/fix_ets_match_spec_erlang_less_than_fix_test.exs** — test/syntax/fix_ets_match_spec_erlang_less_than_fix_test.exs:11 — Every positive test calls the rule’s `fix/1` directly. None passes realistic unparseable input through `Credence.Syntax.fix/1`, so the suite can stay green if discovery, ordering, or the syntax-round parse gate makes the new rule inert.
- **lib/syntax/fix_extra_brace_in_ets_match.ex** — lib/syntax/fix_extra_brace_in_ets_match.ex:163 — The rule commits its one-brace deletion only if the entire file immediately parses. On a file containing two malformed `:ets.*` calls (or this error plus another repairable syntax error), the candidate still fails to parse, so the rule repairs neither occurrence. This defeats the Syntax round’s incremental multi-rule repair design and leaves an admitted error untouched.
- **lib/syntax/fix_for_comprehension_in_keyword_value.ex** — lib/syntax/fix_for_comprehension_in_keyword_value.ex:140 — A bare comprehension with a newline after `for`, such as `%{foo: for\n  x <- xs, do: x}`, is valid comprehension layout once parenthesized, but `check_for_keyword/3` accepts only a space or tab after `for`; the rule stays inert and the syntax pipeline leaves the file unparseable.
- **lib/syntax/close_unclosed_brace.ex** — lib/syntax/close_unclosed_brace.ex:190 — On CRLF input, the repair appends `}` after the retained `\r`, turning the target line’s `\r\n` into `\r}\n`; the result may parse, but it corrupts that line’s newline encoding instead of inserting the brace before the CRLF.
- **test/syntax/close_unclosed_brace_fix_test.exs** — test/syntax/close_unclosed_brace_fix_test.exs:32 — Every repair assertion calls `CloseUnclosedBrace.fix/1` directly. The realistic unclosed-tuple input is never repaired through `Credence.Syntax.fix_with_trace/2` with the live rule set, so the test stays green if ordering or the progress/rollback guards prevent this rule’s repair from reaching users.
- **lib/syntax/close_unclosed_doc_heredoc.ex** — lib/syntax/close_unclosed_doc_heredoc.ex:180 — An unclosed doc followed by module-level code such as `create unique_index(:users, [:email])` and then a `def` is admitted, but the shape regex misses that macro call; the inserted closer goes below it, silently converting executable module code into doc text.
- **lib/syntax/close_unclosed_doc_heredoc.ex** — lib/syntax/close_unclosed_doc_heredoc.ex:301 — Any doc-text line beginning with a bare attribute-like word at the doc’s indentation, such as `@timeout - how long to wait`, is treated as the doc boundary; the fix truncates the documentation and promotes that prose to live module-body code.
- **test/syntax/close_unclosed_doc_heredoc_fix_test.exs** — test/syntax/close_unclosed_doc_heredoc_fix_test.exs:459 — The test blesses a corrupting repair: when doc prose begins with `@timeout - how long to wait`, the expected output closes the heredoc above that text, truncating the documentation and promoting the prose to executable module-body code. The rule must preserve meaning or decline this input, not lock the corruption in as expected behavior.
- **lib/syntax/close_unclosed_fn_delimiter.ex** — lib/syntax/close_unclosed_fn_delimiter.ex:337 — The delimiter scan treats atom literals such as `:do` and `:fn` as block openers because `:` is allowed before the regex match. If one appears between the repaired `)` and the stray `end`, the scanner consumes the stray `end` as though it closed that atom, then can delete a later real `end`; the stray `end` may take over that real terminator’s role, yielding parseable code with subsequent expressions moved into a different scope or executed at module load.
- **test/syntax/close_unclosed_fn_delimiter_fix_test.exs** — test/syntax/close_unclosed_fn_delimiter_fix_test.exs:571 — The 101-occurrence test requires `fix/1` to return a silently partial, still-invalid rewrite after its pass limit. On an admitted input with more than 100 occurrences, user code is modified without completing the promised repair; the test should require an all-or-nothing result rather than pinning this unsafe behavior.
- **test/syntax/fix_capture_operator_syntax_fix_test.exs** — test/syntax/fix_capture_operator_syntax_fix_test.exs:11 — Every repair test calls the rule’s `fix/1` callback directly; none passes realistic broken source through `Credence.Syntax.fix/1` or `fix_with_trace/1`. The rule can therefore be undiscovered, skipped, reverted, or rolled back by the production Syntax round while this entire test file remains green.
- **lib/syntax/fix_inline_keyword_if_in_with_clause.ex** — lib/syntax/fix_inline_keyword_if_in_with_clause.ex:75 — On input containing more than 50 repairable bare keyword-style `if` bindings, `fix/1` stops after 50 and returns still-invalid, partially rewritten source; this can escape when `syntax_partial_repairs: true` is enabled.
- **test/syntax/fix_inline_keyword_if_in_with_clause_fix_test.exs** — test/syntax/fix_inline_keyword_if_in_with_clause_fix_test.exs:385 — The test treats a repairable binding as a required no-op merely because an `if` branch string contains the text `<- if`. On this input the rule leaves the whole file invalid instead of wrapping the outer `if`, and this assertion locks that defect in as expected behavior.
- **lib/syntax/fix_keyword_before_positional_argument.ex** — lib/syntax/fix_keyword_before_positional_argument.ex:197 — Unicode keyword names are valid Elixir identifiers, but the ASCII-only regex does not recognize them; input such as `foo(árg: 1, positional)` receives the owned parser diagnostic yet `analyze/1` stays silent and `fix/1` leaves it broken.
- **lib/syntax/fix_keyword_before_positional_argument.ex** — lib/syntax/fix_keyword_before_positional_argument.ex:63 — The hard 20-pass limit leaves the 21st broken call unrepaired, so a file containing more than 20 otherwise-supported keyword-before-positional calls remains syntactically invalid after `fix/1`.
- **lib/syntax/fix_keyword_block_as_function_arg.ex** — lib/syntax/fix_keyword_block_as_function_arg.ex:91 — On a file containing more than 20 affected calls, `fix/1` repairs only the first 20 and then returns while another admitted syntax error remains, leaving the file unparsable and still reported by `analyze/1`.
- **lib/syntax/fix_keyword_list_colon_syntax.ex** — lib/syntax/fix_keyword_list_colon_syntax.ex:41 — An input containing 51 malformed entries such as `[:k1: 1, ..., :k51: 51]` hits the 50-pass cap with one error left. The rule’s direct output remains invalid, so the Syntax phase rolls back all 50 repairs and returns the entirely unchanged input.
- **lib/syntax/fix_mismatched_closing_brace.ex** — lib/syntax/fix_mismatched_closing_brace.ex:71 — The parser diagnostic proves only that `[` and `}` disagree, not which delimiter is wrong. On input `def f, do: [1, 2}`, where the intended value is the tuple `{1, 2}`, the rule changes the closer and silently produces the list `[1, 2]`, giving a different answer.
- **lib/syntax/fix_mismatched_closing_brace.ex** — lib/syntax/fix_mismatched_closing_brace.ex:55 — An input containing 51 mismatched `[`/`}` pairs is admitted, but the hard 50-fix limit leaves the last mismatch unrepaired and returns source that still does not parse.
- **test/syntax/fix_mismatched_closing_brace_analyze_test.exs** — test/syntax/fix_mismatched_closing_brace_analyze_test.exs:9 — The positive tests assume the `}` is wrong, but the same parser diagnostic occurs when the opener is wrong; there is no regression case such as `def f, do: [1, 2}`, whose intended tuple `{1, 2}` would instead be silently changed into the list `[1, 2]`.
- **lib/syntax/fix_misplaced_when_guard.ex** — lib/syntax/fix_misplaced_when_guard.ex:112 — A `for` generator whose expression contains a bare keyword atom, such as `for x <- :def, when String.length(inspect(x)) > 0, do: x`, is misclassified as a clause head; the rule deletes the comma instead of `when`, leaving parseable but non-compiling code that the Syntax round can commit.
- **lib/syntax/fix_misplaced_when_guard.ex** — lib/syntax/fix_misplaced_when_guard.ex:109 — A file with 21 misplaced guards has only the first 20 repaired; the remaining syntax error causes the default all-or-nothing Syntax round to roll every repair back, so the new rule does nothing on that admitted input.
- **lib/syntax/fix_missing_module_end.ex** — lib/syntax/fix_missing_module_end.ex:55 — On source with more than 100 unclosed `do` blocks, `fix/1` appends only 100 `end`s and returns altered source that still does not parse and is still reported by `analyze/1`; with partial syntax repairs enabled, this corrupt partial repair can escape the pipeline.
- **lib/syntax/fix_mixed_required_optional_map_keys.ex** — lib/syntax/fix_mixed_required_optional_map_keys.ex:196 — When a comment appears between the map opener or preceding comma and the offending key, `arrow_ify/3` treats the comment as part of the key and inserts `:` before it; the result remains invalid, so the Syntax round rolls back and repairs nothing.
- **lib/syntax/fix_mixed_required_optional_map_keys.ex** — lib/syntax/fix_mixed_required_optional_map_keys.ex:116 — A file with more than 20 offending entries exhausts the fixed edit budget while still invalid; the Syntax round therefore rolls back every repair and leaves the original parse error untouched.
- **lib/syntax/fix_python_augmented_assignment.ex** — lib/syntax/fix_python_augmented_assignment.ex:120 — Pre-existing: the rule rewrites every bare-variable `+=` as numeric addition, but Python `+=` is type-dependent. On `items += [1]` or `text += "x"`, it emits `items = items + ([1])` or `text = text + ("x")`, which raises at runtime instead of preserving Python list or string concatenation.
- **test/syntax/fix_python_augmented_assignment_test.exs** — test/syntax/fix_python_augmented_assignment_test.exs:316 — This mixed heredoc/code test uses substring assertions, so it still passes if the fix rewrites or deletes other bytes while retaining those two fragments and producing parseable output; compare the complete expected source exactly.
- **test/syntax/fix_python_augmented_assignment_test.exs** — test/syntax/fix_python_augmented_assignment_test.exs:8 — Every repair test calls the rule module directly, so the suite can pass even if the rule is unregistered or never dispatched by the real syntax pipeline; there is no end-to-end test proving a realistic `count += 1` input is repaired through that pipeline.
- **lib/syntax/fix_python_floor_div.ex** — lib/syntax/fix_python_floor_div.ex:144 — Adjacent matches are spliced without a separator: `x = Kernel.//n // 2` becomes `x = divdiv(n, 2)`, inventing a different function call instead of repairing the expression.
- **lib/syntax/fix_python_floor_div.ex** — lib/syntax/fix_python_floor_div.ex:155 — pre-existing: legitimate qualified range-step calls are rewritten as division; for example, `Kernel.//(1..10, 2)` becomes `div(1..10, 2)`, changing a range value into a runtime arithmetic error.
- **lib/syntax/fix_python_floor_div.ex** — lib/syntax/fix_python_floor_div.ex:158 — pre-existing: replacing Python floor division with `div/2` gives the wrong answer for negative operands; code such as `a // b` becomes truncating division, so inputs `a = -3, b = 2` produce `-1` instead of Python’s `-2`.
- **lib/syntax/fix_python_modulo.ex** — lib/syntax/fix_python_modulo.ex:3 — pre-existing: Python `%` and Elixir `rem/2` differ for negative operands. For example, Python `-5 % 2` is `1`, but the rule emits `-rem(5, 2)`, which is `-1`; an identifier holding `-5` has the same wrong-answer problem.
- **lib/syntax/fix_scientific_notation.ex** — lib/syntax/fix_scientific_notation.ex:27 — Pre-existing: the pattern also matches valid hexadecimal literals and identifiers; for example, fixing `x = 0x1e5` produces invalid `x = 0x1.0e5`, while `value1e5` becomes `value1.0e5`.
- **lib/syntax/no_after_in_anon_fn.ex** — lib/syntax/no_after_in_anon_fn.ex:113 — The closing-`end` search counts `do`, `fn`, and `end` inside strings, comments, and heredocs. For an affected function whose `after` body contains `IO.puts("end")`, the fix mistakes that line for the function’s closing `end`, removes only the `after` line, and returns malformed code with the cleanup body still present.
- **lib/syntax/no_after_in_anon_fn.ex** — lib/syntax/no_after_in_anon_fn.ex:115 — Keyword syntax such as `if condition, do: cleanup()` is counted as opening a block even though it has no matching `end`. On that realistic `after` body, the search reaches EOF without finding the anonymous function’s closing `end`, so `analyze/1` reports an issue that `fix/1` leaves untouched and the syntax error remains.
- **lib/syntax/no_after_in_anon_fn.ex** — lib/syntax/no_after_in_anon_fn.ex:64 — The parser error token alone does not establish that `after` is inside an anonymous function. Invalid `after` clauses in constructs such as `if` or `case` are also admitted, then reported as anonymous-function errors and rewritten by deleting the clause.
- **test/syntax/no_after_in_anon_fn_analyze_test.exs** — test/syntax/no_after_in_anon_fn_analyze_test.exs:22 — Negative coverage includes only parseable code, so it misses that the rule flags any parser error at `after`, even without an anonymous function. For `if true do\n  work()\nafter\n  cleanup()\nend`, the rule reports `:no_after_in_anon_fn` and its fix deletes the `after` clause, silently discarding `cleanup()`.
- **test/syntax/no_after_in_anon_fn_fix_test.exs** — test/syntax/no_after_in_anon_fn_fix_test.exs:67 — The nested-body coverage omits `end` inside strings or comments. For an admitted input whose `after` body contains `IO.puts("end")`, the rule mistakes that text for the function’s closing `end`, removes only the `after` line, and turns the cleanup body into ordinary function code.
- **lib/syntax/no_atom_as_function_name.ex** — lib/syntax/no_atom_as_function_name.ex:130 — `Regex.run(..., return: :index)` returns byte offsets, but `drop_colon/3` passes that offset to grapheme-based `String.slice/3`; when non-ASCII text precedes `:name(` on the same line, the fix deletes a later character instead of the colon and can repeatedly corrupt the call.
- **lib/syntax/no_atom_as_function_name.ex** — lib/syntax/no_atom_as_function_name.ex:79 — the regex lacks Unicode mode, so repairable Unicode identifiers such as `:café(1)` are declined even though dropping the colon produces valid `café(1)`.
- **lib/syntax/no_atom_as_function_name.ex** — lib/syntax/no_atom_as_function_name.ex:108 — the hard 20-edit limit leaves the 21st malformed call untouched; the default all-or-nothing Syntax round then rolls back all 20 repairs, so a file with 21 occurrences receives no repair despite the rule claiming every occurrence is handled.
- **test/syntax/no_atom_as_function_name_fix_test.exs** — test/syntax/no_atom_as_function_name_fix_test.exs:88 — The “repairs every occurrence” tests cover at most three occurrences and miss the rule’s 20-edit cap. An input containing 21 bad calls has only 20 colons removed, remains invalid, and the Syntax round rolls the entire repair back, leaving all 21 calls untouched.
- **lib/syntax/no_case_closed_with_brace.ex** — lib/syntax/no_case_closed_with_brace.ex:57 — The fix is committed only when the entire file parses after one replacement. A file containing two `do … }` mistakes, or this mistake plus any later syntax error, is reported but left unchanged; the single-pass Syntax round may therefore never repair it.
- **lib/syntax/no_case_closed_with_brace.ex** — lib/syntax/no_case_closed_with_brace.ex:124 — Parser columns count Unicode codepoints, but `String.split_at/2` counts graphemes. If the offending `}` has a combining character, flag emoji, or another multi-codepoint grapheme earlier on the same line, the split misses the brace and the reported defect is not repaired.
- **lib/syntax/no_catch_after_anon_fn.ex** — lib/syntax/no_catch_after_anon_fn.ex:131 — The raw `end)` regex accepts comments as anonymous-function closings. For `dangerous_call(); # end)` followed by an orphaned `catch`, the repair produces valid code by wrapping only the comment in an empty `try`, leaving `dangerous_call()` outside the catch; the rule reports success but installs the wrong exception boundary.
- **test/syntax/no_catch_after_anon_fn_fix_test.exs** — test/syntax/no_catch_after_anon_fn_fix_test.exs:39 — Every repair test calls `NoCatchAfterAnonFn.fix/1` directly; none passes realistic malformed input through `Credence.Syntax.fix/1`. If rule discovery or syntax-round dispatch omits this new rule, this entire file still passes while the real pipeline leaves the invalid `catch`/`after` source unrepaired.
- **lib/syntax/no_extra_bracket_after_end.ex** — lib/syntax/no_extra_bracket_after_end.ex:73 — The rule requires its one-character repair to make the entire file parse, so it cannot participate in the multi-rule Syntax round. For input containing an earlier `end]` and a later `fn x -> x)` error, this rule declines the bracket repair; the later rule cannot see past the bracket error, and the pipeline returns the still-invalid source.
- **lib/syntax/no_extra_bracket_after_end.ex** — lib/syntax/no_extra_bracket_after_end.ex:113 — Parser columns and whole-line grapheme slicing diverge when a string begins with a combining mark (for example `_ -> "\u0301" end]`). The split lands past the parser-reported `]`, so the rule does not fire and leaves this genuine `end]` error unrepaired.
- **test/syntax/no_extra_bracket_after_end_analyze_test.exs** — test/syntax/no_extra_bracket_after_end_analyze_test.exs:7 — Every positive test calls the rule module directly, so the suite can pass even if the default `Credence.Syntax` pipeline never discovers or applies this new rule; an `end]` input could therefore remain unchanged in real use without any test failing.
- **lib/syntax/no_fn_as_variable.ex** — lib/syntax/no_fn_as_variable.ex:203 — After one parser-pinned rename, the fallback regex searches unmasked strings and comments for `fn.`. For input beginning `"fn.foo"; defmodule M do` and containing `def foo([fn]), do: fn`, it renames the string to `"func.foo"`; because that shifts the blamed `do` column, the progress check at line 123 accepts the corruption, and later repairs yield parseable but behavior-changing code.
- **lib/syntax/no_fn_with_capture.ex** — lib/syntax/no_fn_with_capture.ex:46 — The pattern admits `fn(&0 > 0)`, but `&0` is not a valid capture argument; the fix produces `&(&0 > 0)`, so it consumes the finding without repairing the syntax error.
- **test/syntax/no_fn_with_capture_fix_test.exs** — test/syntax/no_fn_with_capture_fix_test.exs:111 — The new preservation tests use `confirm_fix/2`, which strips all trailing newlines before comparing. If `fix/1` deletes or appends trailing newlines while processing a moduledoc, comment, string, or its own source, these tests still pass despite changing user bytes; use an exact whole-string `==` assertion.
- **lib/syntax/no_hash_quantifier_in_regex_sigil.ex** — lib/syntax/no_hash_quantifier_in_regex_sigil.ex:81 — The raw line regex also matches text in comments, strings, heredocs, and moduledocs. For example, fixing `# example: ~r/#{1,2}/` rewrites the comment to `# example: ~r/[#]{1,2}/`, corrupting non-code bytes in otherwise valid source.
- **lib/syntax/no_hash_quantifier_in_regex_sigil.ex** — lib/syntax/no_hash_quantifier_in_regex_sigil.ex:51 — Splitting before matching makes the rule inert for a slash-delimited regex sigil spanning multiple lines; an unescaped `#{1,6}` on an interior line is neither reported nor repaired, so the source remains unparsable.
- **test/syntax/no_hash_quantifier_in_regex_sigil_analyze_test.exs** — test/syntax/no_hash_quantifier_in_regex_sigil_analyze_test.exs:62 — The negative cases never put the complete trigger text inside a string, heredoc, moduledoc, or comment. For valid input such as `message = "~r/#{1,6}/"`, the rule reports an issue and its fix changes the string’s contents to `~r/[#]{1,6}/`, corrupting non-code data.
- **test/syntax/no_hash_quantifier_in_regex_sigil_fix_test.exs** — test/syntax/no_hash_quantifier_in_regex_sigil_fix_test.exs:127 — Negative coverage omits regex-looking text in comments or non-interpolating strings. On valid input such as `# example: ~r/#{1,6}/`, the rule rewrites the comment to `# example: ~r/[#]{1,6}/`, corrupting non-code bytes while this suite still passes.
- **lib/syntax/no_keyword_if_bare_in_tuple.ex** — lib/syntax/no_keyword_if_bare_in_tuple.ex:75 — On a file containing more than 50 repairable bare conditionals, `fix/1` stops after the fiftieth and leaves invalid syntax; the Syntax round then rolls back every repair and returns the original broken file.
- **test/syntax/no_keyword_if_bare_in_tuple_fix_test.exs** — test/syntax/no_keyword_if_bare_in_tuple_fix_test.exs:313 — `compiles?/1` passes the repaired fixture to a helper that uses raw `Code.compile_string/1`; compiling executes top-level fixture code without the required heap ceiling or deadline, so a future runaway fixture can hang or exhaust the test process.
- **lib/syntax/no_keyword_inside_tuple_brace.ex** — lib/syntax/no_keyword_inside_tuple_brace.ex:70 — A source containing 51 repairable `{key: value}` forms is admitted, but `fix/1` stops after 50 and leaves the source invalid; the syntax pipeline consequently rolls back every repair and returns the original broken file.
- **lib/syntax/no_map_arrow_syntax_in_tuple_brace.ex** — lib/syntax/no_map_arrow_syntax_in_tuple_brace.ex:107 — The rule requires its first edit to make the whole file parse, so a file containing two malformed maps such as `a = {"x" => 1}\nb = {"y" => 2}` is left completely unchanged. The syntax round invokes each rule only once, so neither unambiguous error is ever repaired.
- **test/syntax/no_map_arrow_syntax_in_tuple_brace_fix_test.exs** — test/syntax/no_map_arrow_syntax_in_tuple_brace_fix_test.exs:235 — `compiles?/1` reaches raw `Code.compile_string/1`, executing the fixture without the required heap and deadline bounds; use `Credence.RuleHelpers.compile_and_capture/1`.
- **lib/syntax/no_markdown_code_fences.ex** — lib/syntax/no_markdown_code_fences.ex:49 — The fence regex accepts only an immediately adjacent word-only tag. Valid Markdown wrappers such as ```` ``` elixir ```` or ```` ```elixir-livebook ```` are admitted by the documented “optional language tag” contract but are not removed, so otherwise-valid Elixir remains unparseable.
- **lib/syntax/no_postfix_if_expression.ex** — lib/syntax/no_postfix_if_expression.ex:122 — “Previously mentioned” is not “previously bound”: identifiers in comments, strings, or another function are added to `seen`, so a later `x = foo() if flag` can be rewritten with `else: x` where `x` is unbound, causing compilation to fail.
- **lib/syntax/no_postfix_if_expression.ex** — lib/syntax/no_postfix_if_expression.ex:147 — The heredoc guard recognizes only `"""`; postfix-looking text inside valid `'''` charlist heredocs is treated as code and rewritten, corrupting the heredoc’s contents.
- **test/syntax/no_postfix_if_expression_analyze_test.exs** — test/syntax/no_postfix_if_expression_analyze_test.exs:137 — The “variable is not bound” coverage tests only a file with no earlier mention. It misses an earlier mention in a comment, string, or different function; the analyzer treats that mention as a binding and flags a later assignment whose generated `else: variable` is unbound, allowing a repair that does not compile.
- **test/syntax/no_postfix_if_expression_analyze_test.exs** — test/syntax/no_postfix_if_expression_analyze_test.exs:141 — Heredoc coverage checks only `"""`. A valid `'''` charlist heredoc containing a previously mentioned variable and postfix-looking example is reported as code, allowing the fixer to rewrite documentation bytes.
- **test/syntax/no_postfix_if_expression_fix_test.exs** — test/syntax/no_postfix_if_expression_fix_test.exs:92 — The compile assertion uses `Credence.RuleCase.compiles?/1`, which calls raw `Code.compile_string/1`; fixture source executes at compile time without the required heap and deadline bounds, so a future adversarial fixture can hang or exhaust the test runner. Use `Credence.RuleHelpers.compile_and_capture/1`.
- **lib/syntax/no_python_multi_return.ex** — lib/syntax/no_python_multi_return.ex:824 — The paren-less-call heuristic mistakes any first expression beginning with an identifier and whitespace for a call; for example `a + b, c` is a Python-style multi-return, but the rule leaves it unreported and unfixed, so the file remains invalid.
- **lib/syntax/no_python_multi_return.ex** — lib/syntax/no_python_multi_return.ex:195 — An inline comment is placed inside the generated tuple: `a, b # explanation` becomes the candidate `{a, b # explanation}`, whose closing brace is commented out. The parser check rejects it, so the rule leaves this multi-return invalid instead of producing `{a, b} # explanation`.
- **test/syntax/no_python_multi_return_fix_test.exs** — test/syntax/no_python_multi_return_fix_test.exs:32 — The repair is tested only by calling `NoPythonMultiReturn.fix/1` directly. Nothing proves the unparseable flagship input is discovered, accepted by the progress guard, and committed by `Credence.Syntax.fix/1`, so the rule could be inert in the real syntax pipeline while this entire file remains green.
- **lib/syntax/no_unclosed_fn_delimiter.ex** — lib/syntax/no_unclosed_fn_delimiter.ex:37 — When a source contains multiple unclosed `fn` delimiters, `fix/1` repairs all of them but `analyze/1` emits only one issue for the first occurrence, so later repaired defects are omitted from diagnostics.
- **lib/syntax/prefer_comma_in_tuple_literal.ex** — lib/syntax/prefer_comma_in_tuple_literal.ex:122 — A file with 101 missing tuple commas is left completely unchanged: after 100 successful insertions the repair budget reaches zero and discards every repair, even though inserting the 101st comma would make the file parse. This contradicts the claim that the budget is not a cap on normal repairs and makes the rule inert for that admitted input.
- **test/syntax/prefer_comma_in_tuple_literal_fix_test.exs** — test/syntax/prefer_comma_in_tuple_literal_fix_test.exs:9 — Every repair test calls the rule module directly; none sends realistic invalid source through `Credence.Syntax.fix/1`. The suite therefore still passes if discovery or pipeline ordering leaves this new rule inert, so it does not prove the repair fires end to end.
- **lib/syntax/prefer_cond_do_keyword.ex** — lib/syntax/prefer_cond_do_keyword.ex:96 — Pre-existing: a file containing two real `cond ->` mistakes is left completely unchanged because neither single replacement makes the whole file parse; the Syntax round runs this rule only once, so both advertised syntax errors survive and the pipeline returns the broken input.
- **lib/syntax/prefer_spec_arrow_operator.ex** — lib/syntax/prefer_spec_arrow_operator.ex:79 — On `@spec foo(:"a)") integer()`, analysis correctly ignores the `)` inside the quoted atom by using the masked line, but the fix locates the closing parenthesis again in raw text and mistakes that literal byte for the argument-list close, producing `@spec foo(:"a) :: ") integer()` and corrupting the specification.
- **lib/syntax/progress_guard.ex** — lib/syntax/progress_guard.ex:249 — `eof_stop?/2` treats every error reported on the last physical line as an EOF stop. For a file without a trailing newline, a real offending token on that line also has `line == total_lines`; if a rule then introduces an earlier parse error, `position_verdict/4` returns `:keep` without comparing positions. The guard therefore fails to revert a same-stage regression, allowing the damaged intermediate source to continue through later rules and potentially be committed if they make it parse.
- **lib/syntax/when_guard_position.ex** — lib/syntax/when_guard_position.ex:223 — The keyword regex treats atom patterns such as `:for` as enclosing constructs. On `def f :for, when true, do: :ok`, it classifies the clause as a comprehension and deletes `when` instead of the stray comma, changing or invalidating user code.
- **lib/semantic/fix_after_or_rescue_in_case.ex** — lib/semantic/fix_after_or_rescue_in_case.ex:99 — The rewrite preserves the invalid clauses’ original order. On admitted input such as `case x do ... after ... rescue ... end`, it emits `try do ... after ... rescue ... end`; `after` must be last in a `try`, so the repaired source still fails to compile even though the moduledoc explicitly claims this shape is fixed.
- **test/semantic/fix_after_or_rescue_in_case_fix_test.exs** — test/semantic/fix_after_or_rescue_in_case_fix_test.exs:84 — The only combined-clause test covers `rescue` before `after`. There is no test for the admitted `after`-then-`rescue` input, whose fix preserves that invalid order and emits a `try` where `after` is not last, so the supposed repair still fails to compile.
- **lib/semantic/fix_apply_arity_one.ex** — lib/semantic/fix_apply_arity_one.ex:94 — Any `apply` definition anywhere in the file, including an unrelated sibling module, makes the rule leave a genuine `apply/1` error unchanged even though that sibling definition cannot affect the failing module.
- **lib/semantic/fix_apply_arity_one.ex** — lib/semantic/fix_apply_arity_one.ex:119 — The rewrite visits every `apply/1` AST node on the diagnostic line, including calls inside `quote`; when a valid quoted call shares a line with the reported call, the rule silently changes the generated code.
- **test/semantic/fix_apply_arity_one_fix_test.exs** — test/semantic/fix_apply_arity_one_fix_test.exs:158 — The test wrongly treats an `apply/1` definition in a different module as grounds to leave the diagnosed call unchanged. Such a sibling definition cannot satisfy `FixApplyArityOneExample`’s unqualified call, so this fixture locks in a failure to repair real compiler-invalid code; the defdelegate case at line 172 has the same defect.
- **lib/semantic/fix_case_branch_assignment_scope.ex** — lib/semantic/fix_case_branch_assignment_scope.ex:220 — `uses_var?/2` descends through scope boundaries such as `def`, so a module-level `case` followed by `def f, do: label` is accepted and rewritten even though the hoisted module-body variable is still unavailable inside the function; the rule reports a repair but leaves the undefined-variable error intact.
- **test/semantic/fix_cyclic_struct_reference_nested_test.exs** — test/semantic/fix_cyclic_struct_reference_nested_test.exs:87 — The “other statements keep their order” test never checks that `def answer` survives; an implementation that drops that trailing statement still compiles and satisfies both substring-order assertions, masking user-code loss.
- **test/semantic/fix_cyclic_struct_reference_nested_test.exs** — test/semantic/fix_cyclic_struct_reference_nested_test.exs:39 — The test repeatedly sends fixture source through `RuleCase.compiles?/1`, which uses raw `Code.compile_string/1`; compiling executes top-level fixture code without the required heap and deadline bounds of `RuleHelpers.compile_and_capture/1`.
- **lib/semantic/fix_cyclic_struct_reference.ex** — lib/semantic/fix_cyclic_struct_reference.ex:357 — The topological sort moves every dependency-free statement ahead of a blocked statement, including statements originally after the nested struct module. For `@x 1; def build, do: %U{x: @x}; defmodule U, do: defstruct([:x]); @x 2`, the repair moves `@x 2` before `build/0`, so it compiles but changes the returned struct from `%U{x: 1}` to `%U{x: 2}`.
- **lib/semantic/fix_erlang_bitwise_bif.ex** — lib/semantic/fix_erlang_bitwise_bif.ex:92 — The generated `Bitwise` alias can be shadowed. On input containing `alias SomeModule, as: Bitwise`, fixing `bsl(a, b)` produces `Bitwise.bsl(a, b)` and calls `SomeModule.bsl/2`; if that function exists with different behavior, the repaired program compiles but gives the wrong answer.
- **lib/semantic/fix_erlang_bitwise_bif.ex** — lib/semantic/fix_erlang_bitwise_bif.ex:89 — `Macro.prewalk/2` descends into quoted data. If a flagged line contains both an executable `bsl(a, b)` and `quote(do: bsl(1, 2))`, the fix rewrites both, changing the quoted AST even though only the executable call caused the diagnostic.
- **test/semantic/fix_erlang_bitwise_bif_fix_test.exs** — test/semantic/fix_erlang_bitwise_bif_fix_test.exs:76 — The fix tests cover only six of the rule’s twelve admitted spellings; bare `bor/2`, `bsr/2`, `bxor/2`, `bnot/1` and operators `&&&/2`, `<<</2` have no repair assertion, so any of those diagnostics could remain unfixed while this suite stays green.
- **lib/semantic/fix_fn_arity_in_keyword_value.ex** — lib/semantic/fix_fn_arity_in_keyword_value.ex:139 — If the raise already has a later `arity:` option, such as `function: :push/4, arity: 3`, the fix inserts `arity: 4` but retains the later duplicate; the later value wins, so the repaired exception still reports the wrong arity.
- **lib/semantic/fix_fn_arity_in_keyword_value.ex** — lib/semantic/fix_fn_arity_in_keyword_value.ex:104 — The unrestricted AST walk enters quoted code and rewrites every matching raise on the diagnostic’s line. If a real offending raise and a quoted `raise FunctionClauseError, function: :other/2` share a line, fixing the real diagnostic also corrupts the quoted data even though it produced no diagnostic.
- **test/semantic/fix_fn_arity_in_keyword_value_fix_test.exs** — test/semantic/fix_fn_arity_in_keyword_value_fix_test.exs:158 — `compiles?/1` invokes raw `Code.compile_string/1`, so this fixture executes unbounded top-level code during the suite; use the bounded `Credence.RuleHelpers.compile_and_capture/1` path instead.
- **lib/semantic/fix_function_in_module_attribute_inline_usages.ex** — lib/semantic/fix_function_in_module_attribute_inline_usages.ex:130 — The safety check only rejects Kernel collisions, not functions or macros already defined by the module. For example, if `foo/0` already exists and `@foo` holds an anonymous function, the rewrite creates another `foo/0`; it can fail compilation due to a `def`/`defp` conflict or silently call the pre-existing clause and return the wrong value.
- **lib/semantic/fix_function_in_module_attribute_inline_usages.ex** — lib/semantic/fix_function_in_module_attribute_inline_usages.ex:189 — `def_bodies/1` treats the entire definition, including patterns and guards, as a function body. An attribute reference in a pattern or guard is therefore admitted and rewritten to a capture or local call, which is illegal there, so the rule changes the source without repairing the compilation error.
- **test/semantic/fix_function_in_module_attribute_inline_usages_fix_test.exs** — test/semantic/fix_function_in_module_attribute_inline_usages_fix_test.exs:284 — This and the compile assertions at lines 299 and 311 use `Credence.RuleCase.compiles?/1`, which calls raw `Code.compile_string/1`. A fixture containing a nonterminating or memory-exhausting top-level expression can hang or crash the test run; compile these strings through bounded `Credence.RuleHelpers.compile_and_capture/1`.
- **lib/semantic/fix_hallucinated_calendar_iso_accessor.ex** — lib/semantic/fix_hallucinated_calendar_iso_accessor.ex:66 — `match?/1` claims expanded diagnostics from aliased calls such as `alias Calendar.ISO; ISO.date(dt)`, but `rename_at/5` requires the literal `Calendar.ISO.` before the diagnostic column and returns the source unchanged. Semantic dispatch therefore reports this rule as the owner while leaving the undefined call unrepaired.
- **test/semantic/fix_hallucinated_calendar_iso_accessor_fix_test.exs** — test/semantic/fix_hallucinated_calendar_iso_accessor_fix_test.exs:201 — This test blesses an unfixed `ISO.date/1` call even though its compiler diagnostic expands the alias to `Calendar.ISO.date/1`, which this rule claims. The rule then consumes the diagnostic but returns unchanged, leaving invalid code and preventing another rule from repairing it.
- **lib/semantic/fix_hallucinated_enum_range.ex** — lib/semantic/fix_hallucinated_enum_range.ex:59 — `match?/1` claims every `Enum.range/2` warning, including captures, piped calls, prefixed calls, ambiguous line-only positions, and other shapes that `fix/2` deliberately leaves unchanged. On those admitted inputs Credence reports the issue but returns the same invalid source, violating the fix-or-drop rule; admission must use the same source-aware predicate as the repair.
- **test/semantic/fix_hallucinated_enum_range_fix_test.exs** — test/semantic/fix_hallucinated_enum_range_fix_test.exs:213 — `compiles?/1` sends the fixture to raw `Code.compile_string/1`, so a fixture containing a nonterminating or memory-exhausting top-level expression can hang or kill the test process; compile fixtures must use bounded `RuleHelpers.compile_and_capture/1`.
- **lib/semantic/fix_hallucinated_enum_take_drop_right.ex** — lib/semantic/fix_hallucinated_enum_take_drop_right.ex:64 — The rule claims every matching diagnostic, but `fix/2` declines admitted forms such as `list |> Enum.take_right(2)`, captures, and `Elixir.Enum.take_right(list, 2)`. Credence therefore reports this rule’s finding while leaving the undefined call unrepaired; the piped form could be safely rewritten as `list |> Enum.take(-2)`.
- **test/semantic/fix_hallucinated_enum_take_drop_right_fix_test.exs** — test/semantic/fix_hallucinated_enum_take_drop_right_fix_test.exs:252 — `compiles?/1` passes the fixture to raw `Code.compile_string/1`; compiling executes top-level fixture code without the required heap ceiling or deadline, so a future adversarial fixture can hang or exhaust the test runner. Use the bounded `RuleHelpers.compile_and_capture/1` path.
- **lib/semantic/fix_hallucinated_map_put_arity.ex** — lib/semantic/fix_hallucinated_map_put_arity.ex:193 — `match?/1` admits a matching warning with no `position`, but `to_issue/1` then calls `line/1`, which has no fallback clause; analyzing that admitted diagnostic raises `FunctionClauseError` and crashes the pipeline.
- **test/semantic/fix_hallucinated_map_put_arity_check_test.exs** — test/semantic/fix_hallucinated_map_put_arity_check_test.exs:9 — Every accepted diagnostic fixture includes `position`, so the test misses an admitted warning without that field: `match?/1` returns true, but `to_issue/1` then raises `FunctionClauseError`, crashing analysis.
- **lib/semantic/fix_hallucinated_naive_datetime_accessor.ex** — lib/semantic/fix_hallucinated_naive_datetime_accessor.ex:65 — `match?/1` reports every listed warning, but `fix/2` only repairs one narrow ASCII-variable spelling. Real inputs such as `NDT.minute(dt)`, piped/captured calls, computed arguments, multiline calls, or Unicode variable names are therefore reported while deliberately left unchanged, violating the fix-or-drop requirement and leaving the compiler defect unresolved.
- **test/semantic/fix_hallucinated_naive_datetime_accessor_fix_test.exs** — test/semantic/fix_hallucinated_naive_datetime_accessor_fix_test.exs:292 — `compiles?/1` ultimately uses raw `Code.compile_string/1`, executing fixture top-level code without the required heap ceiling or deadline; route this compilation through `RuleHelpers.compile_and_capture/1`.
- **lib/semantic/fix_hallucinated_stream_data_flat_map.ex** — lib/semantic/fix_hallucinated_stream_data_flat_map.ex:145 — A call through an alias, such as `alias StreamData, as: SD; SD.flat_map(gen, fun)`, produces the matched `StreamData.flat_map/2 is undefined or private` diagnostic, but the candidate search accepts only the literal `StreamData.flat_map` spelling. The rule therefore leaves this admitted, safely repairable call broken.
- **test/semantic/fix_hallucinated_stream_data_flat_map_fix_test.exs** — test/semantic/fix_hallucinated_stream_data_flat_map_fix_test.exs:235 — `Credence.RuleCase.compiles?/1` uses raw `Code.compile_string/1`, so this fixture executes arbitrary top-level code without a heap ceiling or deadline; a future hostile or accidentally nonterminating fixture can hang or exhaust the test VM. Route the compilation through bounded `Credence.RuleHelpers.compile_and_capture/1`.
- **lib/semantic/fix_invalid_capture_with_arguments.ex** — lib/semantic/fix_invalid_capture_with_arguments.ex:95 — `Macro.prewalk/3` rewrites every matching AST node, including nodes inside `quote`. If a file contains an actual invalid capture plus `quote do: &foo(:x)/0`, fixing the real diagnostic also changes the quoted data into `quote do: fn -> foo(:x) end`, altering unrelated program behavior.
- **lib/semantic/fix_invalid_list_typespec_syntax.ex** — lib/semantic/fix_invalid_list_typespec_syntax.ex:77 — An invalid `list([type1, type2])` split across lines is never repaired: `fix/2` passes only the diagnostic’s single line to a scanner that requires the closing `])` on that same line, so the matched compiler error survives unchanged.
- **lib/semantic/fix_invalid_list_typespec_syntax.ex** — lib/semantic/fix_invalid_list_typespec_syntax.ex:86 — The byte scanner rewrites every `list([...])` occurrence on the flagged line without masking comments or strings; for example, fixing a valid trigger followed by `# list([foo(), bar()])` also changes the user’s comment to `# list([foo() | bar()])`.
- **test/semantic/fix_invalid_list_typespec_syntax_fix_test.exs** — test/semantic/fix_invalid_list_typespec_syntax_fix_test.exs:50 — This test and the compile assertions at lines 51, 83, 102, 121, and 140 pass fixture source to `Credence.RuleCase.compiles?/1`, which uses raw `Code.compile_string/1`; compiling executes top-level fixture expressions without the required heap ceiling or deadline, so a hostile or accidentally nonterminating fixture can hang or exhaust the test runner.
- **lib/semantic/fix_jason_decode_error_message_field.ex** — lib/semantic/fix_jason_decode_error_message_field.ex:145 — The rule only recognizes the literal `%Jason.DecodeError{...}` AST. On common admitted input using `alias Jason.DecodeError` followed by `%DecodeError{message: msg}`, it claims the compiler diagnostic but returns the source unchanged, leaving the compile error unrepaired.
- **lib/semantic/fix_jason_decode_error_message_field.ex** — lib/semantic/fix_jason_decode_error_message_field.ex:212 — Variable replacement descends into quoted code. For `%Jason.DecodeError{message: msg} -> quote(do: msg)`, the quoted `msg` does not read the pattern binding, but the fix rewrites it to quoted `Exception.message(error)`, changing the generated AST and program meaning.
- **test/semantic/fix_jason_decode_error_message_field_check_test.exs** — test/semantic/fix_jason_decode_error_message_field_check_test.exs:7 — The test calls `match?/1` directly but never runs this rule through the Semantic pipeline on realistic source, so it cannot detect the known failure mode where the compiler diagnostic is dropped before dispatch and the rule repairs nothing in production.
- **test/semantic/fix_jason_decode_error_message_field_fix_test.exs** — test/semantic/fix_jason_decode_error_message_field_fix_test.exs:225 — This and the later “no-op” cases pass the rule the exact diagnostic it claims, then require the fix to leave the uncompilable `%Jason.DecodeError{message: ...}` source unchanged. The Semantic dispatcher has already assigned that diagnostic to this rule, so these tests endorse reported findings that repair nothing, contrary to the fix-or-drop requirement.
- **lib/semantic/fix_keyword_double_colon.ex** — lib/semantic/fix_keyword_double_colon.ex:102 — With two keyword `::` typos on different lines, replacing either one leaves the other parse error, so the whole-file parse check rejects both repairs and the semantic phase returns the source unchanged.
- **lib/semantic/fix_local_function_in_guard.ex** — lib/semantic/fix_local_function_in_guard.ex:130 — Helper lookup and guard rewriting traverse the entire file without respecting module or `quote` boundaries or the diagnostic position. If module A defines `allowed/1` and module B has the reported undefined `allowed/1` guard, A's body is inlined into B; calls inside quoted code are also rewritten, changing unrelated generated code.
- **lib/semantic/fix_local_function_in_guard.ex** — lib/semantic/fix_local_function_in_guard.ex:187 — Guard safety is decided only by the called atom's name, not what that call resolves to. For example, after `import Kernel, except: [is_map: 1]`, a local non-guard `is_map/1` used by an otherwise inlinable helper is accepted; the fix emits that local call directly in the guard and leaves the file with another compile error.
- **test/semantic/fix_local_function_in_guard_check_test.exs** — test/semantic/fix_local_function_in_guard_check_test.exs:55 — The only `should_report?/2` assertion expects `false`; if that callback always returned false, every test here and every direct fix test would still pass while Semantic dispatch never reports or repairs any realistic input. Add an end-to-end positive case proving a fixable compiler diagnostic is reported and repaired.
- **test/semantic/fix_local_function_in_guard_fix_test.exs** — test/semantic/fix_local_function_in_guard_fix_test.exs:38 — `compiles?/1` uses raw `Code.compile_string/1`, so compiling a fixture executes its top-level expressions without a heap ceiling or deadline; a nonterminating or memory-hungry fixture can hang or take down the test box. Route this through `RuleHelpers.compile_and_capture/1`.
- **lib/semantic/fix_malformed_spec.ex** — lib/semantic/fix_malformed_spec.ex:110 — The diagnostic also occurs for a spec that merely omits its return type, such as `@spec f(count :: integer())`; the rule misreads the named argument separator as a return separator and rewrites it to `@spec f(count) :: integer()`, changing its meaning and typically introducing an undefined `count/0` type.
- **lib/semantic/fix_malformed_spec.ex** — lib/semantic/fix_malformed_spec.ex:111 — A successful rewrite discards everything after the spec’s closing parenthesis, so `@spec f(integer() :: atom()) # important` silently loses its comment, and same-line code after a semicolon is deleted too.
- **test/semantic/fix_malformed_spec_test.exs** — test/semantic/fix_malformed_spec_test.exs:88 — No test covers bytes after the malformed spec’s closing parenthesis. On `@spec f(integer() :: atom()) # keep`, the rule rebuilds only the spec and silently deletes `# keep`; the current ordinary and end-to-end fixtures all end at `)`, so this source corruption passes.
- **lib/semantic/fix_map_fetch_none_clause.ex** — lib/semantic/fix_map_fetch_none_clause.ex:111 — The fix ignores the diagnostic position and walks into quoted AST. If a valid-looking `case Map.fetch(...)` appears inside an earlier `quote` and a later executable case emits the warning, the rule rewrites `:none` to `:error` inside the quoted data instead of at the diagnosed clause, silently changing generated code unrelated to the diagnostic.
- **test/semantic/fix_map_fetch_none_clause_check_test.exs** — test/semantic/fix_map_fetch_none_clause_check_test.exs:79 — No test ties `should_report?/2` to the diagnostic’s position. With two `Map.fetch/2` cases, a diagnostic for an unsafe case that already has an `:error` or catch-all clause can be claimed because the rule instead finds and rewrites an earlier eligible case, changing unrelated code while leaving the diagnosed clause untouched.
- **lib/semantic/fix_mixed_arities_in_anon_fn.ex** — lib/semantic/fix_mixed_arities_in_anon_fn.ex:180 — When the padding candidate is already a parameter of the shorter clause, the rule duplicates that name. For example, `fn x, y -> x; y -> y end` becomes a function with a `y, y` clause, which matches only equal arguments instead of acting as the original catch-all and can raise `FunctionClauseError`.
- **lib/semantic/fix_mixed_arities_in_anon_fn.ex** — lib/semantic/fix_mixed_arities_in_anon_fn.ex:180 — Candidate reuse checks only the clause body, not its guard. For `fn x, y -> x; x when y > 0 -> x end`, it inserts `_` instead of `y`, leaving the guard’s `y` undefined and the repaired source uncompilable.
- **test/semantic/fix_mixed_arities_in_anon_fn_check_test.exs** — test/semantic/fix_mixed_arities_in_anon_fn_check_test.exs:15 — The tests call `match?/1`, `to_issue/1`, and `fix/2` only in isolation; none sends realistic invalid source through `Credence.Semantic.fix/2`. They therefore stay green if the compiler emits a different diagnostic, the rule is undiscovered or loses dispatch ownership, `should_report?/2` suppresses it, or the pipeline reverts its repair, allowing this new rule to ship inert.
- **test/semantic/fix_mixed_arities_in_anon_fn_fix_test.exs** — test/semantic/fix_mixed_arities_in_anon_fn_fix_test.exs:59 — This test and three others pass fixture source to `compiles?/1`, which uses raw `Code.compile_string/1`; top-level expressions in a fixture execute without the required heap ceiling or deadline and can hang or exhaust the test runner.
- **lib/semantic/fix_multiple_default_args.ex** — lib/semantic/fix_multiple_default_args.ex:107 — If the source already has a default-declaring function header and its implementation clauses repeat that default, the rule treats the header as another clause, inserts a second header, and strips the original into a bodyless declaration without defaults; the output remains invalid instead of repairing the compiler error.
- **lib/semantic/fix_multiple_default_args.ex** — lib/semantic/fix_multiple_default_args.ex:69 — The diagnostic regex accepts only `\w+` function names, so valid Elixir names ending in `?` or `!` (for example `enabled?/2`) never match and their “defines defaults multiple times” errors remain unrepaired.
- **test/semantic/fix_multiple_default_args_fix_test.exs** — test/semantic/fix_multiple_default_args_fix_test.exs:64 — `compiles?/1` passes generated fixture text to raw `Code.compile_string/1`, so a fixture containing a non-terminating top-level expression can hang or exhaust the test runner; compilation must use the bounded `RuleHelpers.compile_and_capture/1`.
- **lib/semantic/fix_negated_capture_with_arity.ex** — lib/semantic/fix_negated_capture_with_arity.ex:98 — The fix ignores the diagnostic position and rewrites every matching AST node, including nodes inside `quote`; when a file contains one genuinely invalid negated capture plus quoted `&(!fun/1)` data, repairing the real error also changes the quoted program data to `&(!fun(&1))`.
- **lib/semantic/fix_negated_capture_with_arity.ex** — test/semantic/fix_negated_capture_with_arity_fix_test.exs:17 — The new rule has no end-to-end test through `Credence.Semantic.fix/2`, so the tests do not prove that compilation produces the expected diagnostic, dispatch selects this rule, and the repair actually fires on realistic source.
- **test/semantic/fix_negated_capture_with_arity_fix_test.exs** — test/semantic/fix_negated_capture_with_arity_fix_test.exs:12 — Every repair test bypasses semantic diagnostic generation and ordered dispatch by calling the rule’s `fix/2` directly with a fabricated diagnostic. The suite can therefore pass even if realistic `&(!Enum.empty?/1)` input is never selected or repaired by `Credence.Semantic.fix/1`; add an exact whole-output end-to-end assertion through that production entry point.
- **lib/semantic/fix_negation_in_guard.ex** — lib/semantic/fix_negation_in_guard.ex:80 — Replacing every `!value` with `not value` changes truthiness semantics: `!nil` is `true`, while `not nil` fails the strict-boolean guard and rejects the clause. A guard such as `when !x` therefore gives the wrong answer for `nil`.
- **lib/semantic/fix_negation_in_guard.ex** — lib/semantic/fix_negation_in_guard.ex:56 — The unrestricted AST walk enters quoted data, so repairing one real invalid guard also rewrites unrelated `quote(do: (x when !x))` expressions elsewhere in the file and changes the AST returned by that code.
- **lib/semantic/fix_nimble_csv_direct_parse.ex** — lib/semantic/fix_nimble_csv_direct_parse.ex:159 — The regex rewrites the first matching bytes on the diagnostic line even inside a string. For `IO.puts("NimbleCSV.parse_string"); NimbleCSV.parse_string(csv)`, it changes the string and leaves the invalid call untouched; `should_report?/2` still claims the diagnostic, starving the fallback rule.
- **lib/semantic/fix_nimble_csv_direct_parse.ex** — lib/semantic/fix_nimble_csv_direct_parse.ex:140 — The rule copies a parser alias without preserving its lexical scope. If `NimbleCSV.define(MyParser, ...)` is at top level and the diagnosed call is inside `defmodule Loader`, the replacement `MyParser.parse_string(...)` resolves to `Loader.MyParser`, not the defined top-level `MyParser`, leaving an undefined-module call.
- **test/semantic/fix_nimble_csv_direct_parse_check_test.exs** — test/semantic/fix_nimble_csv_direct_parse_check_test.exs:58 — The test calls `should_report?/2` directly but never sends a realistic diagnostic through Semantic dispatch, so it does not prove this new rule wins the shared undefined-function diagnostic and repairs the source end-to-end; incorrect registration or priority could leave the rule inert while every test here passes.
- **test/semantic/fix_nimble_csv_direct_parse_fix_test.exs** — test/semantic/fix_nimble_csv_direct_parse_fix_test.exs:174 — The suite never places `"NimbleCSV.parse_string"` in a string before the real call on the reported line. The line-based fix replaces that string occurrence first, leaves the invalid call untouched, and corrupts user data.
- **test/semantic/fix_nimble_csv_direct_parse_fix_test.exs** — test/semantic/fix_nimble_csv_direct_parse_fix_test.exs:89 — The tests assume the textual alias passed to `NimbleCSV.define/2` has the same meaning at the call site. If the definition and call are in different module scopes, inserting that alias can resolve to a different parser module; no cross-scope test covers this.
- **lib/semantic/fix_pin_atom_in_exception_case.ex** — lib/semantic/fix_pin_atom_in_exception_case.ex:116 — The rewrite uses only the diagnostic line and variable name, so if two bare `^exception ->` clauses occur on that line, it rewrites both. Only one clause produced the warning; the unrelated clause can consequently change from matching the pinned value to matching a struct, changing program behavior.
- **test/semantic/fix_pin_in_ets_match_spec_fix_test.exs** — test/semantic/fix_pin_in_ets_match_spec_fix_test.exs:20 — Every positive test calls the rule directly with a hand-written diagnostic, so the suite can pass even if the compiler emits a different position/message, discovery or dispatch never selects the rule, or the production semantic pipeline leaves realistic ETS input unchanged. Add an end-to-end test using `Credence.Semantic.fix/1` and the bounded compiler path that asserts the exact repaired source.
- **lib/semantic/fix_plug_dependency_module_order.ex** — lib/semantic/fix_plug_dependency_module_order.ex:228 — The hand-written `do`/`end` scanner reads heredoc contents as code. If the dependency module contains a heredoc with a column-0 line containing only `end`, that literal line is treated as the module boundary; the fixer moves only the module prefix and leaves the remainder behind, producing malformed user code.
- **lib/semantic/fix_plug_dependency_module_order.ex** — lib/semantic/fix_plug_dependency_module_order.ex:253 — Intervening top-level code is moved from before the dependency module to after it. On input where the gap defines an `alias`, `import`, `require`, attribute, or other setup used by the dependency, the reordered module loses that context and can compile differently or fail.
- **lib/semantic/fix_plug_dependency_module_order.ex** — lib/semantic/fix_plug_dependency_module_order.ex:171 — Plug calls are searched in raw text without masking strings, heredocs, or moduledocs. If an earlier module contains a literal line such as `plug Foo.Dep` and a later router contains the real call that caused the diagnostic, the fixer selects the earlier module and moves the dependency across unrelated module code, changing compile-time behavior beyond the required repair.
- **test/semantic/fix_plug_dependency_module_order_fix_test.exs** — test/semantic/fix_plug_dependency_module_order_fix_test.exs:18 — No test covers `defmodule Foo.Plugs.Bar do` text inside a heredoc; the rule treats that text as a real top-level module and can move heredoc lines, corrupting valid source.
- **lib/semantic/fix_raise_in_keyword_value.ex** — lib/semantic/fix_raise_in_keyword_value.ex:52 — `should_report?/2` ignores the diagnostic position and returns true when any bare `raise` exists anywhere in the file. A generic missing-parentheses diagnostic for an unrelated call can therefore be falsely claimed by this rule, and `fix/2` rewrites the unrelated `raise` while leaving the diagnosed ambiguity unresolved.
- **test/semantic/fix_raise_in_keyword_value_check_test.exs** — test/semantic/fix_raise_in_keyword_value_check_test.exs:55 — The negative `should_report?` test contains no `raise` elsewhere, so it misses cross-diagnostic consumption: when the diagnostic points to a non-raise ambiguity but another line contains a bare `raise` in a `do:` value, `should_report?/2` returns true and the rule consumes the unrelated diagnostic while rewriting the other line.
- **test/semantic/fix_raise_in_keyword_value_fix_test.exs** — test/semantic/fix_raise_in_keyword_value_fix_test.exs:127 — The realistic-input test compiles the fixture but invokes the rule’s `fix/2` callback directly; no test calls `Credence.Semantic.fix/1`. The rule could be undiscovered, lose diagnostic dispatch, or be rejected by pipeline reporting while every test stays green, leaving the warning unrepaired in actual use.
- **lib/semantic/fix_remote_call_in_pattern.ex** — lib/semantic/fix_remote_call_in_pattern.ex:111 — `match?/1` claims every remote-call-in-match diagnostic, but `fix/2` only repairs `receive` and `case` clauses (plus assignments); the same diagnostic from a function head, anonymous-function clause, or `with` pattern is consumed and returned unchanged, preventing any other semantic rule from repairing it.
- **lib/semantic/fix_remote_call_in_pattern.ex** — lib/semantic/fix_remote_call_in_pattern.ex:204 — the assignment repair replaces later bare `state` uses with the updated map but deliberately leaves later `state.field` accesses unchanged; after repairing `state.field = value`, code returning `{state, state.field}` therefore observes the new field through `state` and the old field through `state.field`.
- **lib/semantic/fix_remote_call_in_pattern.ex** — lib/semantic/fix_remote_call_in_pattern.ex:94 — the unrestricted whole-AST walk ignores the diagnostic location and descends into quoted code, so a real matching diagnostic elsewhere can also rewrite `state.field = ...` inside `quote`, changing the quoted value even though that code caused no diagnostic.
- **lib/semantic/fix_reraise_keyword_in_catch.ex** — lib/semantic/fix_reraise_keyword_in_catch.ex:218 — The recursive replacement descends into `quote` blocks. If a catch clause contains both `quote do: reraise` and a real bare `reraise`, fixing the real diagnostic also rewrites the quoted AST, silently changing runtime data that was not erroneous.
- **lib/semantic/fix_reraise_keyword_in_catch.ex** — lib/semantic/fix_reraise_keyword_in_catch.ex:202 — The replacement reconstructs caught values using only their variable names. If `kind` or `reason` is rebound or shadowed before a bare `reraise`, the generated `:erlang.raise/3` uses the newer value instead of the value caught by the clause, so it raises the wrong term.
- **test/semantic/fix_reraise_keyword_in_catch_fix_test.exs** — test/semantic/fix_reraise_keyword_in_catch_fix_test.exs:183 — The recursive-body tests omit quoted code. If a catch clause contains both a real bare `reraise` and `quote do: reraise`, the rule traverses into the quote and rewrites its literal AST too, corrupting the quoted value while repairing the diagnostic.
- **lib/semantic/fix_spawn_monitor_pattern_match.ex** — lib/semantic/fix_spawn_monitor_pattern_match.ex:62 — The fix ignores the diagnostic position and rewrites every matching assignment in the file, including unrelated local or imported `spawn_monitor` functions that legitimately return `{:ok, pid}`; one genuine Kernel diagnostic can therefore cause valid code elsewhere to be changed to the wrong pattern.
- **lib/semantic/fix_spawn_monitor_pattern_match.ex** — lib/semantic/fix_spawn_monitor_pattern_match.ex:75 — `_ref` is a real binding, not a discard pattern. If the surrounding function already uses `_ref` after this assignment, the repair overwrites its value with the monitor reference and changes subsequent behavior; use the anonymous `_` pattern or a demonstrably fresh variable.
- **test/semantic/fix_spawn_monitor_pattern_match_check_test.exs** — test/semantic/fix_spawn_monitor_pattern_match_check_test.exs:27 — The test calls `match?/1` only with a hand-written diagnostic; it never runs realistic source through the Semantic pipeline and verifies an exact repaired result. The rule can therefore ship inert because the compiler message differs, the rule is not dispatched, or another rule owns the diagnostic, while every test here still passes.
- **lib/semantic/fix_struct_test_in_guard.ex** — lib/semantic/fix_struct_test_in_guard.ex:124 — `fix/2` ignores the diagnostic’s function and position and rewrites every matching AST node. A `String.length/1` guard diagnostic can therefore be claimed merely because an unrelated `Map.get(..., :__struct__)` definition exists inside `quote`; the quoted data is changed while the diagnosed error remains unfixed.
- **lib/semantic/fix_struct_test_in_guard.ex** — lib/semantic/fix_struct_test_in_guard.ex:187 — the matcher treats every syntactic `Map.get/2` as Elixir’s standard `Map`, but `Map` can be aliased to an application module whose `get/2` has different semantics. On such input the rewrite replaces an arbitrary call with `%Struct{}` matching and changes which arguments select the clause.
- **test/semantic/fix_struct_test_in_guard_check_test.exs** — test/semantic/fix_struct_test_in_guard_check_test.exs:132 — The end-to-end repair assertion checks only that one fragment appears. On this input, the pipeline could also delete the fallback clause, alter the function body, or append malformed code and the test would still pass; assert the exact whole output instead.
- **test/semantic/fix_struct_test_in_guard_fix_test.exs** — test/semantic/fix_struct_test_in_guard_fix_test.exs:107 — `compiles?/1` and the `call_fixed/4` calls at line 123 compile fixture strings through raw `Code.compile_string/1`, without the required bounded `RuleHelpers.compile_and_capture/1`; a fixture with a nonterminating or memory-exhausting top-level expression can hang or take down the test process.
- **test/semantic/fix_struct_test_in_guard_fix_test.exs** — test/semantic/fix_struct_test_in_guard_fix_test.exs:43 — five repair tests use substring assertions (lines 43, 57, 68, 79, and 91), so output that contains the expected clause fragment but deletes or corrupts the fallback clause or surrounding module still passes; these cases need exact whole-output assertions.
- **lib/semantic/fix_task_async_stream_case_match.ex** — lib/semantic/fix_task_async_stream_case_match.ex:99 — The rewrite is not behavior-preserving: on every admitted case, the original evaluates the stream and raises `CaseClauseError`, while the replacement executes the `{:ok, variable}` body and can return a value or perform new side effects.
- **lib/semantic/fix_task_async_stream_case_match.ex** — lib/semantic/fix_task_async_stream_case_match.ex:90 — The fix ignores the diagnostic position and rewrites the first matching AST anywhere in the file. An earlier matching case inside `quote` produces no compiler diagnostic but is still traversed, so a warning for a later real case can corrupt the quoted code while leaving the diagnosed case untouched.
- **lib/semantic/fix_task_async_stream_case_match.ex** — lib/semantic/fix_task_async_stream_case_match.ex:74 — `match?/1` accepts warnings from cases containing a live catch-all, but `fix/2` refuses those cases. Such a diagnostic is attributed to this rule and then produces no repair, violating the fix-or-drop requirement.
- **test/semantic/fix_task_async_stream_case_match_check_test.exs** — test/semantic/fix_task_async_stream_case_match_check_test.exs:35 — The positive tests pass handcrafted diagnostics directly to `match?/1`; none compiles realistic source or runs the Semantic pipeline, so they still pass if the compiler’s actual diagnostic differs, the rule is undiscovered, or another rule owns the diagnostic, leaving this new rule inert end to end.
- **test/semantic/fix_task_async_stream_case_match_fix_test.exs** — test/semantic/fix_task_async_stream_case_match_fix_test.exs:30 — The test approves a behavior-changing rewrite: for any reachable call, the original `case` raises `CaseClauseError` because `Task.async_stream/3` returns a stream, while the expected replacement consumes that stream and returns values. This violates the required same-answer guarantee.
- **test/semantic/fix_task_id_field_access_fix_test.exs** — test/semantic/fix_task_id_field_access_fix_test.exs:15 — Every test fabricates a diagnostic and calls the fixer directly, so none proves that realistic source produces this diagnostic and reaches the rule through the Semantic pipeline; the rule could ship undiscovered or otherwise inert while this entire file remains green.
- **lib/semantic/fix_truncated_special_form.ex** — lib/semantic/fix_truncated_special_form.ex:119 — A stale diagnostic position is accepted whenever it happens to point at the same bytes elsewhere, including inside `"__MODULE"` or `:__MODULE`; the rule inserts `__` there and returns the parseable result, corrupting a string or changing an atom instead of safely declining the stale diagnostic.
- **lib/semantic/fix_with_else_bare_value.ex** — lib/semantic/fix_with_else_bare_value.ex:91 — On a file containing one genuinely malformed `with` plus a bare-value `with` inside `quote`, the unrestricted `Macro.prewalk/2` repairs both. The quoted form is data and causes no compiler error, so rewriting it changes the AST produced by otherwise unrelated, valid metaprogramming code.
- **test/semantic/fix_with_else_bare_value_check_test.exs** — test/semantic/fix_with_else_bare_value_check_test.exs:50 — `RuleCase.compiles?/1` uses raw `Code.compile_string/1`; both calls at lines 50 and 85 execute fixture code without the required heap and deadline bounds, so a future top-level expression or faulty repair can hang or kill the test process.
- **lib/semantic/missing_use_exunit_case.ex** — lib/semantic/missing_use_exunit_case.ex:59 — Pre-existing: `match?/1` does not recognize `undefined function setup_all/1`, although the fixer explicitly supports top-level `setup_all`; a module whose only ExUnit diagnostic is `setup_all/1` is handed to the catch-all and remains unrepaired.
- **lib/semantic/missing_use_exunit_case.ex** — lib/semantic/missing_use_exunit_case.ex:99 — Pre-existing: `needs_fix?/1` skips an entire `defmodule` subtree when that outer module has no direct ExUnit calls, so a nested test module missing `use ExUnit.Case` produces a matching diagnostic but the fix returns the source unchanged.
- **lib/semantic/no_bare_doc_attribute.ex** — lib/semantic/no_bare_doc_attribute.ex:64 — pre-existing: a bare attribute followed by a comment, such as `@doc # TODO`, produces the matched no-effect diagnostic, but `bare_doc_line?/1` rejects that line, so the rule reports a repair and then leaves the source unchanged.
- **lib/semantic/no_bare_names_in_spec.ex** — lib/semantic/no_bare_names_in_spec.ex:33 — pre-existing: `match?/1` claims every undefined zero-arity type diagnostic, including errors in `@type`, `@callback`, and other non-`@spec` declarations; `analyze/2` then falsely attributes those errors to this rule, while `fix/2` cannot change them because it only visits `@spec` nodes.
- **test/semantic/no_bare_names_in_spec_fix_test.exs** — test/semantic/no_bare_names_in_spec_fix_test.exs:182 — The new tests pass fixture source to `Credence.RuleCase.compiles?/1`, which uses unbounded `Code.compile_string/1`; top-level fixture code can execute indefinitely or exhaust memory. Use the bounded `Credence.RuleHelpers.compile_and_capture/1` instead.
- **lib/semantic/no_bare_return_in_unless.ex** — lib/semantic/no_bare_return_in_unless.ex:127 — The catch-all strips `return` without preserving early-exit control flow. For example, `if flag do return(:error) else :ok end; perform_update()` becomes `if flag do :error else :ok end; perform_update()`, so `perform_update/0` runs even when the author intended to return before it. The same corruption occurs for `return` in non-final `case` branches and conditional branches with `else`.
- **lib/semantic/no_bare_return_in_unless.ex** — lib/semantic/no_bare_return_in_unless.ex:161 — `return(nil)` is used as the helper’s “not found” sentinel, so an early-exit guard such as `if invalid?, do: return(nil); continue()` is not restructured. The later catch-all merely removes `return`, and `continue/0` runs on the invalid path.
- **lib/semantic/no_bare_return_in_unless.ex** — lib/semantic/no_bare_return_in_unless.ex:155 — Early-exit detection only accepts a branch whose entire body is the `return` call. A realistic branch with preceding work, such as `if invalid? do; log(); return(:error); end; continue()`, is instead handled by the catch-all, causing `continue/0` to run after the supposed early return.
- **test/semantic/no_bare_return_in_unless_check_test.exs** — test/semantic/no_bare_return_in_unless_check_test.exs:13 — The negative-match coverage omits other `return` arities. The rule matches `undefined function return/0`, `return/2`, etc., but its fixer only handles one-argument calls; those diagnostics are claimed while the source remains broken, potentially starving the generic undefined-function rule.
- **test/semantic/no_bare_return_in_unless_fix_test.exs** — test/semantic/no_bare_return_in_unless_fix_test.exs:236 — The behavioral test passes fixture source to `call_fixed/4`, which uses unbounded `Code.compile_string/1`; a future fixture with a nonterminating or memory-exhausting top-level expression can hang or kill the test runner instead of being contained by `RuleHelpers.compile_and_capture/1`.
- **lib/semantic/no_capture_as_bitwise_and.ex** — lib/semantic/no_capture_as_bitwise_and.ex:162 — When the diagnostic column does not identify a safe bitwise match, the fallback rewrites the first match elsewhere on the line. For `x & 1; foo(&2)`, the diagnostic for stray `&2` makes this rule change the unrelated `x & 1`, falsely consuming a diagnostic it cannot repair.
- **lib/semantic/no_conflicting_default_args.ex** — lib/semantic/no_conflicting_default_args.ex:66 — The rule assumes every conflicting lower-arity clause is redundant and deletes it without comparing behavior. For `def foo(x, y \\ 0), do: {:default, x, y}; def foo(x), do: {:special, x}`, it silently discards the explicit `foo/1` behavior.
- **lib/semantic/no_conflicting_default_args.ex** — lib/semantic/no_conflicting_default_args.ex:38 — `match?/1` accepts the compiler diagnostic for valid function names such as `ready?/1`, but `\w+` cannot parse the `?`; `to_issue/1` then pattern-matches on `nil` and crashes issue reporting.
- **test/semantic/no_conflicting_default_args_check_test.exs** — test/semantic/no_conflicting_default_args_check_test.exs:12 — The negative test covers only a wholly unrelated message, so it misses malformed errors containing “conflicts with defaults from”; the rule admits such input, then `to_issue/1` crashes when its stricter regex cannot parse it.
- **test/semantic/no_conflicting_default_args_fix_test.exs** — test/semantic/no_conflicting_default_args_fix_test.exs:20 — The test blesses deleting a guarded `sequence/1` clause as “redundant,” but the default-generated `sequence/1` is unguarded; the expected repair therefore accepts non-atoms that the explicit clause rejects, changing program behavior.
- **test/semantic/no_conflicting_default_args_fix_test.exs** — test/semantic/no_conflicting_default_args_fix_test.exs:8 — Every test fabricates a diagnostic and calls the rule directly, so none proves that a real compiler diagnostic reaches this rule at the expected line and fires through `Credence.Semantic.fix/1`; the rule can ship inert while this file remains green.
- **lib/semantic/no_define_match_fn.ex** — lib/semantic/no_define_match_fn.ex:91 — The rule admits public `def match?/2` definitions as well as the documented private helper; it renames the public function, so existing callers of `Module.match?/2` fail with an undefined-function error.
- **lib/semantic/no_define_match_fn.ex** — lib/semantic/no_define_match_fn.ex:78 — `defines_local_match?/1` searches recursively through nested modules. If a nested module defines `match?/2`, the rule treats its enclosing module as conflicting and renames legitimate unqualified `Kernel.match?/2` calls throughout the enclosing module, changing their meaning or making them undefined.
- **lib/semantic/no_define_match_fn.ex** — lib/semantic/no_define_match_fn.ex:105 — Once a module has a conflicting `match?/2`, the rewrite renames every arity named `match?`. For example, an unrelated public `match?/1` API becomes `match_pattern?/1`, breaking callers even though that arity cannot conflict with `Kernel.match?/2`.
- **test/semantic/no_define_match_fn_check_test.exs** — test/semantic/no_define_match_fn_check_test.exs:8 — The only positive test passes a hand-written diagnostic directly to `match?/1`; it never compiles realistic conflicting code or runs the Semantic pipeline, so it still passes if the compiler message is unreachable, the rule is unregistered, another rule wins dispatch, or the repair never fires end-to-end.
- **test/semantic/no_define_match_fn_fix_test.exs** — test/semantic/no_define_match_fn_fix_test.exs:60 — The capture test endorses renaming `&match?/N` without restricting it to arity 2. On a module that also has a valid `match?/1`, the fixer renames that separate function and its calls even though the diagnostic concerns only `match?/2`, changing program behavior.
- **test/semantic/no_define_match_fn_fix_test.exs** — test/semantic/no_define_match_fn_fix_test.exs:14 — The suite never covers a module that already defines `match_pattern?/2`; renaming `match?/2` then merges or conflicts with the existing function instead of preserving the program’s answer.
- **lib/semantic/no_defp_already_defined_as_def.ex** — lib/semantic/no_defp_already_defined_as_def.ex:71 — The “duplicate” decision compares only argument patterns and ignores guards and bodies. For `def foo(x) when is_integer(x)` plus `defp foo(x) when is_binary(x)`, it deletes the private clause instead of preserving its distinct behavior under a renamed helper.
- **lib/semantic/no_defp_already_defined_as_def.ex** — lib/semantic/no_defp_already_defined_as_def.ex:167 — The rename clauses check the function name but not the diagnostic arity, so repairing `foo/1` also renames every private `foo/2`, `foo/3`, etc. in the file, changing unrelated functions.
- **lib/semantic/no_defp_already_defined_as_def.ex** — lib/semantic/no_defp_already_defined_as_def.ex:197 — Call-site rewriting traverses the entire file without using the diagnostic position or module scope. A `foo/1` call in another module, or a recursive call intended for the public `foo/1`, is silently changed to `do_foo/1`.
- **lib/semantic/no_defp_already_defined_as_def.ex** — lib/semantic/no_defp_already_defined_as_def.ex:77 — When no matching private definition exists, the rename path still rewrites matching bare calls throughout the source. A stale or mismatched diagnostic therefore changes valid code instead of returning it unchanged.
- **test/semantic/no_defp_already_defined_as_def_check_test.exs** — test/semantic/no_defp_already_defined_as_def_check_test.exs:47 — The “real captured diagnostic” test only constructs a map and calls `match?/1`; no test compiles realistic faulty code and runs the Semantic pipeline, so this new rule can be unregistered, lose diagnostic dispatch, or never repair input end-to-end while every test here still passes.
- **test/semantic/no_defp_already_defined_as_def_fix_test.exs** — test/semantic/no_defp_already_defined_as_def_fix_test.exs:10 — Every test calls the rule’s `fix/2` callback directly, so the suite can pass even if real compiler diagnostics never dispatch to this rule or the production fix path rejects/reverts its output; there is no end-to-end test proving this new Semantic rule actually repairs realistic input through `Credence.Semantic.fix/1`.
- **test/semantic/no_deprecated_not_in_fix_test.exs** — test/semantic/no_deprecated_not_in_fix_test.exs:152 — `call_fixed/4` uses raw `Code.compile_string/1` and executes the generated module without the bounded compilation helper; a malformed repair that introduces non-terminating compile-time or runtime code can hang or exhaust the test process.
- **lib/semantic/no_doc_on_private_function.ex** — lib/semantic/no_doc_on_private_function.ex:81 — Removing the entire `@doc` line also deletes any trailing comment. On input such as `@doc "helper" # credo:disable-for-next-line`, user-authored instructions or notes disappear and tooling behavior can change; the moduledoc’s assumption that every trailing comment describes stale documentation cannot be inferred from the source.
- **lib/semantic/no_duplicate_defstruct.ex** — lib/semantic/no_duplicate_defstruct.ex:52 — The rule claims every duplicate-`defstruct` compiler diagnostic, but repairs only direct calls inside a file containing exactly one outermost module. A duplicate inside a nested module, conditional block, or one module in a multi-module file returns the source unchanged, so the compilation error remains.
- **test/semantic/no_duplicate_defstruct_fix_test.exs** — test/semantic/no_duplicate_defstruct_fix_test.exs:71 — The test blesses leaving nested-module duplicates unchanged, even though this rule matches their compiler diagnostic; such input remains uncompilable and the matched finding is not repaired.
- **test/semantic/no_duplicate_defstruct_fix_test.exs** — test/semantic/no_duplicate_defstruct_fix_test.exs:10 — Every repair test calls `fix/2` with a fabricated diagnostic, so none proves that realistic duplicate-`defstruct` source produces the expected diagnostic and reaches this rule through `Credence.Semantic.fix/1`; the rule can be inert in production while this file stays green.
- **lib/semantic/no_hallucinated_datetime_info.ex** — lib/semantic/no_hallucinated_datetime_info.ex:54 — `match?/1` claims every `DateTime.info?/1` diagnostic, but `fix/2` only handles calls with one explicit AST argument. Valid inputs such as `x |> DateTime.info?()` and `Elixir.DateTime.info?(x)` produce the claimed diagnostic yet remain unchanged, so this rule consumes an error it does not repair.
- **test/semantic/no_hallucinated_datetime_info_fix_test.exs** — test/semantic/no_hallucinated_datetime_info_fix_test.exs:10 — Every test calls `fix/2` directly with a fabricated diagnostic, so none proves that a real `DateTime.info?/1` compiler diagnostic reaches this rule through Semantic dispatch and is repaired; the rule can ship inert despite this file passing.
- **lib/semantic/no_hallucinated_datetime_zone.ex** — lib/semantic/no_hallucinated_datetime_zone.ex:67 — The unmasked, global line-wide regex also rewrites non-code text. For example, fixing `dt.zone <> "dt.zone"` changes the string literal to `"dt.time_zone"`, and the same corruption occurs in comments on the flagged line.
- **test/semantic/no_hallucinated_datetime_zone_check_test.exs** — test/semantic/no_hallucinated_datetime_zone_check_test.exs:19 — Negative coverage only uses a wholly unrelated message. A real `unknown key .zone` diagnostic for any non-DateTime struct still matches this rule, causing the pipeline to rewrite (for example) `user.zone` to `user.time_zone`; this test needs a realistic non-DateTime `.zone` diagnostic that must be rejected.
- **test/semantic/no_hallucinated_datetime_zone_fix_test.exs** — test/semantic/no_hallucinated_datetime_zone_fix_test.exs:50 — No test covers trigger text inside a string or comment on the flagged line; the current line-wide replacement changes both the real `dt.zone` access and non-code text such as `"dt.zone"`, corrupting user content.
- **test/semantic/no_hallucinated_datetime_zone_fix_test.exs** — test/semantic/no_hallucinated_datetime_zone_fix_test.exs:147 — The end-to-end witness uses a substring assertion, so it passes even if the pipeline makes additional incorrect edits; assert the exact whole output instead.
- **lib/semantic/no_hallucinated_defpstruct.ex** — lib/semantic/no_hallucinated_defpstruct.ex:315 — Block-form repair uses an unmasked whole-file `String.replace/3`; for `defpstruct Node`, every literal `%Node{` sequence is rewritten, including comments, strings, heredocs, and legitimate references to another `Node` struct, corrupting unrelated bytes and potentially changing program meaning.
- **lib/semantic/no_hallucinated_defpstruct.ex** — lib/semantic/no_hallucinated_defpstruct.ex:98 — The repair ignores the diagnostic position and always selects the first matching AST call; when a file contains multiple `defpstruct`/`defpstructp` calls, or both block and keyword forms, a diagnostic for a later call can rewrite the wrong occurrence while leaving the reported compile error intact.
- **lib/semantic/no_hallucinated_guard_fn.ex** — lib/semantic/no_hallucinated_guard_fn.ex:62 — The fix ignores the diagnostic position and rewrites every `is_regex/1` AST node, including calls outside guards and calls stored inside `quote`; a file with one offending guard plus `quote(do: is_regex(x))` has the quoted program silently changed to `is_struct(x, Regex)`.
- **test/semantic/no_hallucinated_guard_fn_fix_test.exs** — test/semantic/no_hallucinated_guard_fn_fix_test.exs:14 — The only positive fixture contains one `is_regex/1` occurrence, so it misses that the fix rewrites every such AST node in the file, including legitimate calls or definitions outside the reported guard; on a file containing both a hallucinated guard and an unrelated local `is_regex/1`, the unrelated code is changed and user semantics can be corrupted.
- **test/semantic/no_hallucinated_guard_fn_fix_test.exs** — test/semantic/no_hallucinated_guard_fn_fix_test.exs:10 — Every test calls the rule’s `fix/2` directly; none sends realistic uncompilable source through the Semantic pipeline and proves diagnostic matching, dispatch, and repair occur end-to-end, so the new rule can ship inert while this file remains green.
- **lib/semantic/no_hallucinated_self_bang.ex** — lib/semantic/no_hallucinated_self_bang.ex:55 — The fix ignores the diagnostic location and rewrites every `self!/1` AST node, including valid definitions, calls in other modules or import scopes, and calls inside `quote`; one undefined call elsewhere in the file therefore changes or breaks unrelated valid code.
- **test/semantic/no_hallucinated_self_bang_fix_test.exs** — test/semantic/no_hallucinated_self_bang_fix_test.exs:71 — This test requires the fix to rewrite every `self!/1` in the file, although one compiler diagnostic identifies only one invalid call. If the same file also contains a quoted `self!(:value)` expression or a nested module that legitimately defines `self!/1`, the rule changes that unrelated code and its meaning.
- **test/semantic/no_hallucinated_self_bang_fix_test.exs** — test/semantic/no_hallucinated_self_bang_fix_test.exs:10 — Every test calls the rule callback directly with a fabricated diagnostic; none sends realistic broken source through `Credence.Semantic.fix/1`. The suite therefore stays green if diagnostic capture, rule discovery, or dispatch prevents this new rule from ever repairing real input.
- **lib/semantic/no_hallucinated_task_timeout_error_struct.ex** — lib/semantic/no_hallucinated_task_timeout_error_struct.ex:53 — The rule admits the expression-position diagnostic even though its fix only recognizes a surrounding `{:exit, {%Task.TimeoutError{}, stacktrace}}` tuple. For input such as `x = %Task.TimeoutError{}`, Semantic dispatch assigns the diagnostic to this rule, the fix returns the source unchanged, and the compile error remains.
- **lib/semantic/no_hallucinated_task_timeout_error_struct.ex** — lib/semantic/no_hallucinated_task_timeout_error_struct.ex:80 — The context-free whole-file traversal rewrites every matching tuple, not just the pattern responsible for the diagnostic. A file containing one triggering pattern plus the same form inside quoted code or an unrelated expression has those additional forms changed to `{:exit, :timeout}`, corrupting program data or behavior.
- **test/semantic/no_hallucinated_task_timeout_error_struct_fix_test.exs** — test/semantic/no_hallucinated_task_timeout_error_struct_fix_test.exs:160 — Both end-to-end tests pass generated source to `Credence.RuleCase.compiles?/1`, which uses raw `Code.compile_string/1`; compiling executes top-level expressions without a heap ceiling or deadline. Use bounded `Credence.RuleHelpers.compile_and_capture/1` instead (also line 175).
- **lib/semantic/no_impl_true_for_undeclared_callback.ex** — lib/semantic/no_impl_true_for_undeclared_callback.ex:42 — `match?/1` accepts diagnostics for callback names such as `valid?/1` or `run!/0`, but `parse_undeclared/1` only accepts `\w+`; `to_issue/1` then pattern-matches on `nil` and crashes the semantic pipeline.
- **lib/semantic/no_impl_true_for_undeclared_callback.ex** — lib/semantic/no_impl_true_for_undeclared_callback.ex:68 — the fix only handles a source whose root AST is one `defmodule`; a valid file with multiple top-level modules or an affected nested module is matched but returned unchanged, leaving the compiler warning unrepaired.
- **lib/semantic/no_impl_true_for_undeclared_callback.ex** — lib/semantic/no_impl_true_for_undeclared_callback.ex:121 — the fix requires `@impl true` to be the immediately preceding top-level clause; valid intervening attributes such as `@impl true`, then `@doc false`, then `def handle_call(...)` cause the rule to claim the diagnostic but make no repair.
- **test/semantic/no_impl_true_for_undeclared_callback_check_test.exs** — test/semantic/no_impl_true_for_undeclared_callback_check_test.exs:13 — The negative case tests only a wholly unrelated message and misses malformed near-matches. A warning such as `got "@impl true" for function unknown but no behaviour specifies such callback` is accepted by `match?/1`, but `to_issue/1` cannot extract a name/arity and crashes the semantic pipeline with a match error.
- **test/semantic/no_impl_true_for_undeclared_callback_fix_test.exs** — test/semantic/no_impl_true_for_undeclared_callback_fix_test.exs:136 — The end-to-end fixture covers only a source containing one top-level module. On realistic input where the warned function is inside a nested module or the second of multiple modules, the rule cannot locate that function and leaves `@impl true` unchanged, so the compiler warning survives while this test still presents the rule as pipeline-proven.
- **lib/semantic/no_map_has.ex** — lib/semantic/no_map_has.ex:63 — The fix applies an unmasked regex to the whole diagnostic line and replaces its first textual match. On input such as `{"Map.has?", Map.has?(map, key)}`, it changes the string value to `"Map.has_key?"` while leaving the erroneous call untouched, corrupting user data without repairing the diagnostic.
- **lib/semantic/no_map_has.ex** — lib/semantic/no_map_has.ex:63 — The check identifies the resolved `Map.has?/2` diagnostic, but the fix only accepts source spelled literally as `Map.has?`. Calls through an alias such as `alias Map, as: M; M.has?(map, key)` produce the same resolved diagnostic yet are left unchanged, so this rule claims the diagnostic and prevents any later semantic rule from repairing it.
- **test/semantic/no_map_has_check_test.exs** — test/semantic/no_map_has_check_test.exs:8 — The test fabricates a diagnostic and calls `match?/1` directly; it never compiles realistic `Map.has?/2` input and runs the Semantic round, so mismatched real diagnostic text or broken dispatch/registration could leave the typo unrepaired while every test here passes.
- **test/semantic/no_map_has_fix_test.exs** — test/semantic/no_map_has_fix_test.exs:34 — The fix tests omit strings and comments on the diagnostic line. If `"Map.has?"` or `# Map.has?` precedes the real call, the rule rewrites that non-code text first and leaves the undefined call unchanged, so user text is corrupted without repairing the diagnostic.
- **lib/semantic/no_map_update_missing_key.ex** — lib/semantic/no_map_update_missing_key.ex:77 — The fix rewrites every literal assignment to the named variable across the entire file, ignoring lexical scope and diagnostic position. If two functions assign `state`, a warning in one function adds the key to both functions’ maps, changing unrelated behavior.
- **lib/semantic/no_map_update_missing_key.ex** — lib/semantic/no_map_update_missing_key.ex:78 — The matcher treats every `state = %{...}` AST node as map construction, including nodes used as patterns. For example, a case pattern `state = %{counter: 0}` becomes `state = %{counter: 0, timer_ref: nil}`, so inputs that previously matched no longer do.
- **lib/semantic/no_map_update_missing_key.ex** — lib/semantic/no_map_update_missing_key.ex:90 — Adding the missing key to an earlier map literal changes the map before the diagnosed update. If that map is inspected, returned, or passed to another function on a path before the update, it now contains an extra `key: nil`, so the repair changes answers outside the failing expression.
- **lib/semantic/no_match_with_method_string_in_plug_router.ex** — lib/semantic/no_match_with_method_string_in_plug_router.ex:53 — The rule claims every compile error containing `no function clause matching in Access.get/3`, including unrelated `Access.get/3` failures; when no targeted router call exists, it consumes the diagnostic and reports a repair while `fix/2` returns the source unchanged.
- **lib/semantic/no_match_with_method_string_in_plug_router.ex** — lib/semantic/no_match_with_method_string_in_plug_router.ex:73 — The fix rewrites every unqualified `match "GET|POST|PUT|PATCH|DELETE", path do ... end` in the entire file without verifying that the call is inside a module using `Plug.Router` or corresponds to the diagnostic location, so an unrelated macro named `match` can be silently changed to a different call.
- **test/semantic/no_match_with_method_string_in_plug_router_fix_test.exs** — test/semantic/no_match_with_method_string_in_plug_router_fix_test.exs:10 — Every positive case calls the rule’s `fix/2` directly with a fabricated diagnostic; none sends realistic Plug.Router source through the Semantic pipeline, so the suite can pass even if production never captures, dispatches, or applies this repair.
- **lib/semantic/no_message_access_on_rescue_variable.ex** — lib/semantic/no_message_access_on_rescue_variable.ex:120 — Replacing `e.message` with `Exception.message(e)` changes results for custom exceptions whose `message/1` callback returns something different from their `:message` field; rescued code that previously returned the stored field will instead return the callback result.
- **lib/semantic/no_message_access_on_rescue_variable.ex** — lib/semantic/no_message_access_on_rescue_variable.ex:111 — Any later binding of the rescue variable makes the entire clause a no-op, including an earlier `e.message` that caused the admitted diagnostic; for example, `value = e.message; e = normalize(e); value` remains unfixed and still fails under warnings-as-errors.
- **lib/semantic/no_message_access_on_rescue_variable.ex** — lib/semantic/no_message_access_on_rescue_variable.ex:68 — The fix ignores the diagnostic position and recursively rewrites every matching rescue in the AST, including rescue syntax inside `quote`; when a file has one real warning plus quoted `rescue e -> e.message` code, the quoted program is silently changed even though it produced no diagnostic.
- **test/semantic/no_message_access_on_rescue_variable_check_test.exs** — test/semantic/no_message_access_on_rescue_variable_check_test.exs:8 — The test fabricates the real type-checker diagnostic with `severity: :warning` and calls `match?/1` directly. On Elixir 1.19 this diagnostic is emitted as an error, which the Semantic pipeline drops before dispatch, so the rule is inert on realistic input while this test passes; add a bounded `RuleHelpers.compile_and_capture/1` end-to-end pipeline test.
- **test/semantic/no_message_access_on_rescue_variable_fix_test.exs** — test/semantic/no_message_access_on_rescue_variable_fix_test.exs:8 — Every positive test calls `fix/2` directly with a truncated “real” diagnostic that the rule’s own `match?/1` rejects because it lacks the bare-rescue hint. No test sends realistic source through `Credence.Semantic.fix/1`, so the suite can stay green even if dispatch never applies this repair.
- **lib/semantic/no_module_level_init.ex** — lib/semantic/no_module_level_init.ex:95 — The fix deletes the entire physical line containing `init()`. On input such as `init(); @mode :ready` or `init() # explanation`, it silently removes unrelated code or comment bytes.
- **lib/semantic/no_module_level_init.ex** — lib/semantic/no_module_level_init.ex:103 — The rewrite installs any matching `init/0` as an `@on_load` callback without verifying that it returns `:ok`; an initializer returning any other value makes the repaired module fail to load.
- **lib/semantic/no_module_level_init.ex** — lib/semantic/no_module_level_init.ex:144 — `match?/1` claims every undefined `init/0` diagnostic, but the fix recognizes only a `def init` head represented with `nil` arguments. A valid explicit-parentheses definition such as `def init(), do: :ok` is declined, leaving the diagnostic unrepaired after this rule wins Semantic dispatch.
- **test/semantic/no_module_level_init_check_test.exs** — test/semantic/no_module_level_init_check_test.exs:24 — The negative cases omit names containing `init/0`; `"undefined function deinit/0"` matches this rule’s substring predicate, so it can steal the diagnostic from `UndefinedFunction` and then leave the source unchanged.
- **test/semantic/no_module_level_init_check_test.exs** — test/semantic/no_module_level_init_check_test.exs:8 — The test only feeds hand-built diagnostics directly to the rule; it never compiles realistic source through the Semantic pipeline and proves this rule wins dispatch and changes the output, so an inert or mis-dispatched rule can pass the entire test.
- **test/semantic/no_module_level_init_fix_test.exs** — test/semantic/no_module_level_init_fix_test.exs:14 — Every positive test calls the rule’s `fix/2` callback directly with a fabricated diagnostic; none compiles realistic faulty input and runs `Credence.Semantic.fix/1`, so the suite still passes if diagnostic capture, registration, priority, or dispatch leaves this new rule completely inert end to end.
- **lib/semantic/no_naive_datetime_new_with_tuple.ex** — lib/semantic/no_naive_datetime_new_with_tuple.ex:65 — Four-element tuples with an integer microsecond, including the rule’s own `{hour, minute, 0, 0}` example, become `Time.new!(hour, minute, 0, 0)`; `Time.new!/4` requires a `{microsecond, precision}` tuple as its fourth argument, so repaired code raises instead of constructing a datetime.
- **lib/semantic/no_naive_datetime_new_with_tuple.ex** — lib/semantic/no_naive_datetime_new_with_tuple.ex:67 — The inserted `Time.new!` respects local aliases; in a module with `alias SomeModule, as: Time`, the repair calls `SomeModule.new!` and can fail or produce the wrong value instead of using `Elixir.Time`.
- **lib/semantic/no_naive_datetime_new_with_tuple.ex** — lib/semantic/no_naive_datetime_new_with_tuple.ex:61 — The whole-file AST walk descends into `quote` blocks and rewrites quoted `NaiveDateTime.new!` syntax unrelated to the compiler diagnostic, changing data or generated code that was never executed or warned about.
- **test/semantic/no_naive_datetime_new_with_tuple_fix_test.exs** — test/semantic/no_naive_datetime_new_with_tuple_fix_test.exs:20 — Every repair test calls the rule’s `fix/2` callback directly; none runs realistic input through `Credence.Semantic.fix/1`, so the suite still passes if the rule is absent from dispatch or another rule consumes its diagnostic, leaving user code unrepaired.
- **lib/semantic/no_or_in_case_pattern.ex** — lib/semantic/no_or_in_case_pattern.ex:56 — The matcher claims the compiler diagnostic for every pattern context, including function clauses and the left side of `=`, but the fixer only handles `case`; those admitted inputs remain unchanged and uncompilable.
- **lib/semantic/no_or_in_case_pattern.ex** — lib/semantic/no_or_in_case_pattern.ex:143 — The fixer recognizes `or` only when it is the entire clause pattern. A nested pattern such as `case value do {nil or "", x} -> x end` produces the matched diagnostic but is returned unchanged and still cannot compile.
- **lib/semantic/no_or_in_case_pattern.ex** — lib/semantic/no_or_in_case_pattern.ex:75 — The unrestricted AST walk also rewrites `case` syntax held inside `quote` blocks. If a file contains a real erroneous case plus quoted code containing an `or` pattern, repairing the real error silently changes the quoted AST and therefore the program’s output.
- **lib/semantic/no_pipe_into_unary_arithmetic.ex** — lib/semantic/no_pipe_into_unary_arithmetic.ex:106 — Nested input such as `x |> +(y |> +1)` keeps only the outer patch, but rendering the outer operand does not apply the discarded inner patch. It produces `x |> Kernel.+(y |> +1)`, which still does not compile; sufficiently deep nesting also outlives the Semantic round’s limited passes.
- **test/semantic/no_pipe_into_unary_arithmetic_fix_test.exs** — test/semantic/no_pipe_into_unary_arithmetic_fix_test.exs:145 — `call_fixed/4` compiles the fixture with raw, unbounded `Code.compile_string/1`; compilation executes top-level expressions, so a malformed repair can hang or exhaust the test runner instead of being contained by `RuleHelpers.compile_and_capture/1`.
- **lib/semantic/no_redefine_builtin_type.ex** — lib/semantic/no_redefine_builtin_type.ex:81 — The whole-file AST walk ignores the diagnostic’s module and line. If one module illegally defines `node/0` while another module validly uses the built-in `node/0`, the second module’s reference is also changed to undefined `trie_node/0`, corrupting otherwise valid code.
- **lib/semantic/no_redefine_builtin_type.ex** — lib/semantic/no_redefine_builtin_type.ex:55 — The replacement name is always `trie_<name>` without checking whether that type already exists. For a module containing both `@type trie_node :: ...` and the rejected `@type node :: ...`, the fix creates two `trie_node/0` definitions and leaves the file uncompilable.
- **lib/semantic/no_redefine_builtin_type.ex** — lib/semantic/no_redefine_builtin_type.ex:78 — References are renamed only inside `@type` and `@typep`. A source such as `@type node :: atom(); @spec f() :: node` is repaired to define `trie_node/0` while the spec still denotes Erlang’s built-in `node/0`, silently changing the declared return type.
- **test/semantic/no_redefine_builtin_type_fix_test.exs** — test/semantic/no_redefine_builtin_type_fix_test.exs:193 — The “any builtin name” test fabricates an impossible `my_type/0` diagnostic and never covers parameterized built-ins. On real input such as `@type list(element) :: [element]`, the compiler reports `list/1`, but the fix only recognizes zero-argument type definitions and returns the source unchanged, leaving the compilation error unresolved.
- **test/semantic/no_redefine_builtin_type_fix_test.exs** — test/semantic/no_redefine_builtin_type_fix_test.exs:33 — No test covers a module that already defines the generated `trie_node` name. For input containing both invalid `@type node :: atom()` and existing `@type trie_node :: integer()`, the fix creates duplicate `trie_node/0` definitions, so the repaired source still fails to compile.
- **lib/semantic/no_rescue_in_cond.ex** — lib/semantic/no_rescue_in_cond.ex:62 — `Macro.prewalk/2` also rewrites `cond` syntax stored inside `quote`; when a file contains one genuinely broken `cond` plus a quoted `cond` with `rescue` or `catch`, fixing the real compiler diagnostic silently changes the quoted AST and therefore the program’s data.
- **test/semantic/no_rescue_in_cond_fix_test.exs** — test/semantic/no_rescue_in_cond_fix_test.exs:11 — Every test calls `NoRescueInCond.fix/2` directly with a fabricated diagnostic, so none proves that realistic broken source produces the expected compiler diagnostic and reaches this rule through Semantic dispatch; the rule can ship unregistered or otherwise inert while this entire file remains green.
- **lib/semantic/no_rescue_in_exception.ex** — lib/semantic/no_rescue_in_exception.ex:140 — Replacing `rescue e in Exception` with `rescue e` is not behavior-preserving: for any raised exception, the original dead clause lets it propagate, while the rewritten clause catches it and runs the handler. The diagnostic does not prove that widening the clause to catch every exception was the author’s intent.
- **lib/semantic/no_rescue_in_with_expression.ex** — lib/semantic/no_rescue_in_with_expression.ex:84 — The unrestricted `Macro.prewalk/2` also rewrites `with ... rescue/catch` syntax inside `quote` blocks. If a file contains one genuine diagnosed `with` plus quoted syntax intended as AST data, fixing the genuine error silently changes the quoted program by wrapping its `with` in `try`.
- **test/semantic/no_rescue_in_with_expression_fix_test.exs** — test/semantic/no_rescue_in_with_expression_fix_test.exs:11 — Every positive case calls the rule’s `fix/2` callback directly; none runs realistic invalid source through Semantic diagnostic dispatch, so the tests remain green if the new rule is unregistered, misordered, or otherwise inert end-to-end.
- **lib/semantic/no_stream_data_integer_two_args.ex** — lib/semantic/no_stream_data_integer_two_args.ex:144 — Import detection tracks only module boundaries, not lexical blocks. If an earlier function or branch contains `import StreamData` but the flagged `integer(0, 10)` is in another function or outside that branch, the rule wrongly rewrites an unrelated undefined `integer/2` call to `integer(0..10)`.
- **lib/semantic/no_stream_data_integer_two_args.ex** — lib/semantic/no_stream_data_integer_two_args.ex:157 — Every `import StreamData` is treated as importing `integer/1`, even when options exclude it, such as `import StreamData, only: [constant: 1]` or `except: [integer: 1]`. A subsequent undefined `integer/2` is rewritten to an equally undefined `integer/1`, changing code without performing the promised repair.
- **test/semantic/no_stream_data_integer_two_args_fix_test.exs** — test/semantic/no_stream_data_integer_two_args_fix_test.exs:83 — `compiles?/1` sends the generated fixture to raw `Code.compile_string/1`, so its top-level code executes without the required heap ceiling or deadline; use `RuleHelpers.compile_and_capture/1` for both compilation assertions at lines 83 and 104.
- **lib/semantic/no_stream_data_tuple_with_list.ex** — lib/semantic/no_stream_data_tuple_with_list.ex:202 — Source ranges use codepoint-based columns, but `String.slice/3` counts graphemes. If a decomposed character or multi-codepoint emoji appears before the list on its line, the extracted span starts at the wrong character, so `fix/2` returns the unchanged invalid call and `should_report?/2` suppresses its diagnostic.
- **test/semantic/no_stream_data_tuple_with_list_fix_test.exs** — test/semantic/no_stream_data_tuple_with_list_fix_test.exs:68 — `compiles?/1` calls raw `Code.compile_string/1`, so compiling a changed or adversarial fixture can execute unbounded top-level code and hang or exhaust the test runner; use the bounded `Credence.RuleHelpers.compile_and_capture/1` path instead.
- **lib/semantic/no_string_replace_arity_mismatch.ex** — lib/semantic/no_string_replace_arity_mismatch.ex:86 — The fix ignores the diagnostic position and rewrites every matching AST node, including calls inside `quote`; an unrelated `String.replace` guard failure can therefore alter quoted code that never caused the diagnostic.
- **lib/semantic/no_string_replace_arity_mismatch.ex** — lib/semantic/no_string_replace_arity_mismatch.ex:93 — The replacement emits the alias-sensitive name `Regex.replace`; if the source aliases another module as `Regex`, the repaired call invokes that module instead of `Elixir.Regex`, potentially crashing or changing behavior.
- **test/semantic/no_string_replace_arity_mismatch_fix_test.exs** — test/semantic/no_string_replace_arity_mismatch_fix_test.exs:183 — Both fixture strings are passed through `compiles?/1`, which uses unbounded `Code.compile_string/1`; compiling executes module attributes, so a nonterminating fixture can hang or exhaust the test runner. Use the bounded `RuleHelpers.compile_and_capture/1`.
- **lib/semantic/no_struct_update_on_untyped_variable.ex** — lib/semantic/no_struct_update_on_untyped_variable.ex:161 — Adding a struct pattern changes observable failures for non-struct arguments: a call that currently reaches `%Saga{value | ...}` and raises `BadStructError`, `KeyError`, or `BadMapError` instead fails clause matching with `FunctionClauseError`. Treating every raise as the same answer violates the required answer-preservation rule.
- **test/semantic/no_struct_update_on_untyped_variable_check_test.exs** — test/semantic/no_struct_update_on_untyped_variable_check_test.exs:27 — This async test compiles the common top-level module `Saga`, which is also used by other tests; concurrent compilation can redefine or purge the module and make the suite flaky. Use a uniquely named fixture module.
- **test/semantic/no_struct_update_on_untyped_variable_fix_test.exs** — test/semantic/no_struct_update_on_untyped_variable_fix_test.exs:18 — The test blesses adding a struct pattern without checking behavioral equivalence. On a non-struct argument, the original body raises `BadStructError`, `KeyError`, or `BadMapError`, while the rewritten function rejects the call with `FunctionClauseError`; these are not the exact same answer required for every admitted input.
- **lib/semantic/no_undefined_module_in_rescue.ex** — lib/semantic/no_undefined_module_in_rescue.ex:29 — An undefined-at-compile-time exception module can be loaded or defined before the function runs; the original rescue list then catches that exception, but this fix removes it and lets the exception propagate, so the rewrite is not behaviour-preserving for every admitted input.
- **test/semantic/no_undefined_module_in_rescue_check_test.exs** — test/semantic/no_undefined_module_in_rescue_check_test.exs:90 — The tests fabricate a diagnostic and call the rule directly; none runs realistic source through the semantic pipeline and proves the compiler warning is dispatched to this rule and repaired. The rule could be unregistered or incompatible with real diagnostic positions/shapes while every test here remains green.
- **test/semantic/no_undefined_module_in_rescue_fix_test.exs** — test/semantic/no_undefined_module_in_rescue_fix_test.exs:10 — Every positive test calls the rule’s `fix/2` callback directly; none feeds realistic source through the Semantic pipeline with a real compiler diagnostic. The new rule can therefore be unregistered, lose dispatch ownership, or fail end-to-end while this entire test file stays green.
- **lib/semantic/no_underscore_in_expression.ex** — lib/semantic/no_underscore_in_expression.ex:163 — The new `==` rewrite changes numeric equality semantics: for `s == {:reply, 200, _}`, a value such as `{:reply, 200.0, :anything}` would satisfy the original `==` comparison once the wildcard is repaired, but `match?({:reply, 200, _}, s)` returns false because pattern matching distinguishes integers from floats.
- **lib/semantic/no_unreachable_case_clause_by_type.ex** — lib/semantic/no_unreachable_case_clause_by_type.ex:147 — Deleting an unreachable clause is not behavior-preserving when its body invokes a macro with compile-time effects: the original compilation expands and executes that macro even though the clause cannot match, while the repaired source removes the invocation and therefore changes compilation behavior.
- **lib/semantic/no_unused_type_declaration.ex** — lib/semantic/no_unused_type_declaration.ex:95 — When the unused `@typep` is the module’s only declaration (optionally preceded only by its `@typedoc`), `drop_from_body/4` rejects the empty result, so `fix/2` does nothing and `should_report?/2` silently suppresses the compiler-proven warning instead of repairing it.
- **lib/semantic/no_use_plug_conn.ex** — lib/semantic/no_use_plug_conn.ex:120 — `Macro.prewalk/3` descends into `quote` bodies, so when a file contains both the failing `use Plug.Conn` and a quoted standalone `use Plug.Conn`, the fix rewrites both; this silently changes generated/data AST that did not cause the diagnostic.
- **test/semantic/no_use_plug_conn_check_test.exs** — test/semantic/no_use_plug_conn_check_test.exs:62 — The test uses a hand-built diagnostic and calls `should_report?/2` directly, but never compiles the realistic `@fixable` source and runs it through semantic dispatch and repair. It can therefore stay green if the compiler’s actual exception is captured differently or the pipeline never invokes this rule, allowing the new rule to ship inert.
- **lib/semantic/outdented_heredoc.ex** — lib/semantic/outdented_heredoc.ex:119 — Outdented sigil heredocs with closing modifiers, such as a `~r"""` heredoc closed by `"""i`, produce the matched compiler warning but are never repaired because the closing-line search accepts only a bare `"""` or `'''`; the semantic pass therefore reports a no-op and leaves the warning in place.
- **lib/semantic/prefer_explicit_range_step.ex** — lib/semantic/prefer_explicit_range_step.ex:80 — pre-existing: one real range warning causes the fix to rewrite every descending range in the file, including ranges inside `quote`; quoted syntax is program data, so changing `quote(do: 5..1)` to `quote(do: 5..1//-1)` changes the returned AST and can change program behavior.
- **lib/semantic/prefer_kernel_max_over_local.ex** — lib/semantic/prefer_kernel_max_over_local.ex:132 — pre-existing: `defps_named/2` combines clauses from every module in the file instead of locating the module named by the diagnostic. If two modules each contain one complementary `max/2` clause, their unrelated partial implementations are accepted as one canonical pair; the fixer then deletes both and rewrites their calls to total `Kernel.max/2`, changing inputs that previously raised `FunctionClauseError` into returned values.
- **lib/semantic/prefer_rescue_before_catch.ex** — lib/semantic/prefer_rescue_before_catch.ex:96 — The unrestricted `Macro.postwalk/2` descends into `quote` bodies. If a file has one real out-of-order `try` diagnostic and also quotes an out-of-order `try` as data, the fix reorders both, changing the quoted AST returned at runtime even though the quoted form caused no warning.
- **lib/semantic/require_defmodule_wrapper.ex** — lib/semantic/require_defmodule_wrapper.ex:131 — pre-existing: relocation treats attribute names as unique, so an orphaned `@spec f()` or `@type first()` is silently deleted whenever the module already contains any `@spec` or `@type`, even for a different function or type; the repair loses valid declarations and changes the program’s documented/type-checked meaning.
- **lib/semantic/require_defmodule_wrapper.ex** — lib/semantic/require_defmodule_wrapper.ex:36 — pre-existing: `match?/1` claims every `redefining @...` diagnostic, including duplicate attributes wholly inside an ordinary module, but `fix/2` returns that source unchanged because there is no attribute before a top-level module; the semantic dispatcher therefore assigns the diagnostic to a repair that does nothing.
- **test/semantic/require_defmodule_wrapper_fix_test.exs** — test/semantic/require_defmodule_wrapper_fix_test.exs:83 — pre-existing: `compiles?/1` delegates to raw `Code.compile_string/1`; a fixture containing a nonterminating top-level expression can hang or exhaust the test runner instead of being bounded by `RuleHelpers.compile_and_capture/1`.
- **test/semantic/require_defmodule_wrapper_fix_test.exs** — test/semantic/require_defmodule_wrapper_fix_test.exs:100 — every test calls `RequireDefmoduleWrapper.fix/2` directly with a fabricated diagnostic; no test sends realistic failing source through diagnostic generation and Semantic dispatch, so an unregistered or dispatch-starved rule can remain completely inert while this file and its check test pass.
- **test/semantic/undefined_function_local_fix_test.exs** — test/semantic/undefined_function_local_fix_test.exs:593 — The new `exit/2` repair is tested only by calling `UndefinedFunction.fix/2` with a hand-written diagnostic. No end-to-end test proves that realistic source produces the expected compiler diagnostic, dispatches to this rule, and is repaired; the new mapping could therefore ship inert while every test here passes.
- **test/semantic/undefined_function_qualified_fix_test.exs** — test/semantic/undefined_function_qualified_fix_test.exs:1015 — The new Base, keystore, queue, and docs/23 tests call `UndefinedFunction.fix/2` directly with `severity: :error`, but the rule’s qualified-call matcher accepts only warnings. They can all pass even if production dispatch never selects the rule; no test proves these repairs fire end-to-end on realistic compiler diagnostics.
- **lib/semantic/undefined_function.ex** — lib/semantic/undefined_function.ex:490 — pre-existing: `replace_drop_module/5` edits raw text, so when a string or comment containing `Enum.length(` precedes the real call on the diagnostic line, it rewrites the non-code text and leaves the undefined call unchanged; the same unmasked corruption exists in `rename_negate_arg_on_line/5` at line 666 and `to_range_on_line/3` at line 810.
- **lib/semantic/undefined_function.ex** — lib/semantic/undefined_function.ex:625 — `rename_add_arg_on_line/5` only uses the mask to establish that some code call exists, then `do_rename_add_arg/4` selects the first raw-text occurrence; input such as `{"List.second(x)", List.second(xs)}` corrupts the string and leaves the real call undefined. `wrap_args_on_line/4` has the same split masked-check/raw-selection defect at line 774.
- **lib/semantic/undefined_function.ex** — lib/semantic/undefined_function.ex:639 — pre-existing: byte offsets from `:binary.match/2` are passed to grapheme-based `String.slice/2`; a multibyte character before `List.second(...)` makes the splice start at the wrong location and can produce malformed code. The same byte/grapheme mismatch occurs in the negate, wrapping, and range helpers.
- **lib/semantic/undefined_function.ex** — lib/semantic/undefined_function.ex:851 — pre-existing: three-argument Python `range(a, b, step)` excludes `b`, but the replacement `a..b//step` includes `b` whenever the step lands on it; for example, `range(0, 10, 2)` gains an extra `10`, changing the program’s answer.
- **lib/semantic/undefined_string_alphanumeric.ex** — lib/semantic/undefined_string_alphanumeric.ex:86 — pre-existing: the direct-call regex stops at the first closing parenthesis, so `String.alphanumeric?(String.trim(c))` becomes `String.match?(String.trim(c, ~r/^[a-zA-Z0-9]$/))`; this changes the nested call and can fail at runtime.
- **lib/semantic/undefined_string_alphanumeric.ex** — lib/semantic/undefined_string_alphanumeric.ex:83 — pre-existing: replacement is applied to every matching byte sequence on the diagnostic line without masking strings or comments, so a valid call beside `"String.alphanumeric?(x)"` repairs the call but also silently changes the string value.
- **lib/semantic/undefined_string_alphanumeric.ex** — lib/semantic/undefined_string_alphanumeric.ex:86 — pre-existing: a multiline call such as `String.alphanumeric?(\n  c\n)` is admitted by the compiler diagnostic but the line-scoped regex cannot match it, so the rule reports the issue and leaves the undefined call unrepaired.
- **lib/semantic/unused_variable.ex** — lib/semantic/unused_variable.ex:185 — Collision detection searches only the diagnostic’s line. For a multiline pattern such as `def f(\n  _ref,\n  ref\n), do: :ok`, fixing unused `ref` produces a second `_ref`; repeated variables in a pattern impose equality, so calls with different arguments stop matching. The generated name must be unique across the whole binding pattern, not merely the current line.
- **test/semantic/unused_variable_fix_test.exs** — test/semantic/unused_variable_fix_test.exs:331 — pre-existing: end-to-end fixtures define globally named modules such as `Nested`, `Multi`, and `Both`, which are also defined by other test files; parallel tests can redefine/purge the same modules and become order-dependent or flaky. Use uniquely namespaced fixture modules.
- **lib/semantic/used_underscore_variable.ex** — lib/semantic/used_underscore_variable.ex:90 — Renaming does not check whether the stripped name already exists in the clause. On `def f(_x, x), do: {_x, x}`, it produces `def f(x, x), do: {x, x}`, changing which arguments match and the returned value. (pre-existing)
- **lib/semantic/used_underscore_variable.ex** — lib/semantic/used_underscore_variable.ex:88 — The ASCII-only variable regex makes the rule claim diagnostics it then leaves unrepaired for valid Elixir identifiers such as `_café` or `_ready?`; under warnings-as-errors, the reported warning remains and no later Semantic rule can own it.
- **lib/semantic/used_underscore_variable.ex** — lib/semantic/used_underscore_variable.ex:141 — Clause boundaries are detected from unmasked text and indentation rather than syntax. A `def`/`end` line inside a heredoc can be mistaken for the enclosing clause, and a valid nested block whose `end` shares the function’s indentation can truncate the range; the fix can then rename only the use but not the parameter, producing undefined-variable code. (pre-existing)
- **test/semantic/used_underscore_variable_fix_test.exs** — test/semantic/used_underscore_variable_fix_test.exs:356 — The new successful-rewrite and byte-scope tests use substring assertions instead of exact whole-output comparisons. On these inputs, a fix that preserves the checked fragments while also rewriting another string/comment, truncating code, or appending corrupt text still passes.
- **lib/pattern/avoid_graphemes_enum_count_with_predicate.ex** — lib/pattern/avoid_graphemes_enum_count_with_predicate.ex:249 — `graphemes_call?/1` accepts any arity, so parsed partial code such as `String.graphemes() |> Enum.count(&(&1 == "a"))` is reported, but `fix_pipe/2` cannot extract a subject and deletes the `Enum.count/2` step, producing `String.graphemes()` instead of repairing the call. Since the Pattern round now admits non-compiling parsed source, the unchanged `String.graphemes/0` compile error can let this behavior-changing patch pass the “no worse” gate.
- **lib/pattern/avoid_graphemes_enum_count.ex** — lib/pattern/avoid_graphemes_enum_count.ex:116 — `graphemes_call?/1` accepts every arity, so after the Pattern pipeline began processing parseable-but-noncompiling files, inputs such as `Enum.count(String.graphemes())` produce a finding that `fix_patches/2` declines, while `x |> String.graphemes(extra) |> Enum.count()` can be rewritten to `String.length(x)`, erasing the original invalid call.
- **lib/pattern/avoid_graphemes_enum_count.ex** — lib/pattern/avoid_graphemes_enum_count.ex:44 — the rule treats any `String` alias as the standard library module. In code with `alias CustomString, as: String`, it rewrites `Enum.count(String.graphemes(value))` to `String.length(value)` even though those custom functions may return different answers.
- **lib/pattern/avoid_graphemes_length.ex** — lib/pattern/avoid_graphemes_length.ex:77 — pre-existing: `String.graphemes() |> length()` is reported, but fixing it reaches the explicit `raise("unreachable")` and can crash the repair pipeline on this parseable input.
- **lib/pattern/avoid_graphemes_length.ex** — lib/pattern/avoid_graphemes_length.ex:114 — pre-existing: the matcher treats a locally shadowed `String` alias as Elixir’s `String` module; for example, after `alias CustomString, as: String`, it rewrites `length(String.graphemes(x))` to `String.length(x)`, which can return a different answer.
- **lib/pattern/avoid_length_guard_less_than2.ex** — lib/pattern/avoid_length_guard_less_than2.ex:176 — A guarded variable nested inside a parameter pattern (for example, `def f({list}) when length(list) < 2, do: :ok`) is reported by `check/2`, but `replace_param_with_patterns/3` only replaces top-level parameters and emits no patch, so the pipeline leaves a reported defect unrepaired.
- **lib/pattern/avoid_length_guard_less_than2.ex** — lib/pattern/avoid_length_guard_less_than2.ex:181 — When the guarded variable occurs more than once in the function head, every occurrence is independently replaced. For example, `def f(list, list) when length(list) < 2, do: :ok` becomes singleton clauses matching any two one-element lists, losing the original requirement that both lists—including their elements—be equal.
- **test/pattern/fix_ets_new_string_name_equivalence_test.exs** — test/pattern/fix_ets_new_string_name_equivalence_test.exs:50 — `call_fixed/4` compiles and executes both fixture strings with raw `Code.compile_string/1`, bypassing the required bounded `RuleHelpers.compile_and_capture/1`; a fixture or fix regression containing runaway top-level code can hang or exhaust the test runner.
- **lib/pattern/fix_ets_new_string_name.ex** — lib/pattern/fix_ets_new_string_name.ex:83 — `String.to_atom/1` permanently interns every matched source string in the Credence VM; a file containing sufficiently many distinct valid names such as `:ets.new("table_1", [])`, `table_2`, etc. can exhaust the atom table and crash the entire pipeline.
- **test/pattern/fix_ets_options_bare_keypos_equivalence_test.exs** — test/pattern/fix_ets_options_bare_keypos_equivalence_test.exs:42 — Both fixture strings are compiled through `call_fixed/4`, which uses unbounded `Code.compile_string/1`; compiling executes top-level fixture code and can hang or exhaust the test process instead of being contained by `RuleHelpers.compile_and_capture/1`.
- **lib/pattern/fix_map_fetch_case_match.ex** — lib/pattern/fix_map_fetch_case_match.ex:97 — The matcher treats every syntactic `Map.fetch/2` as Elixir’s standard `Map.fetch/2`, but `Map` can be a lexical alias (for example, `alias MyApp.Map`) whose `fetch/2` legitimately returns a bare map. On that admitted input, the current map clause works, while the fix wraps it in `{:ok, ...}` and causes a `CaseClauseError`.
- **test/pattern/fix_map_fetch_case_match_check_test.exs** — test/pattern/fix_map_fetch_case_match_check_test.exs:209 — The guarded-map test also includes an unguarded bare map, so `flagged?/2` passes even if the rule completely ignores `%{y: y} when is_integer(y)`. The implementation does ignore guarded bare-map clauses, leaving a valid `Map.fetch/2` success path dead and uncaught; this test masks that defect instead of proving the guarded clause is diagnosed.
- **test/pattern/fix_regex_match_swapped_args_equivalence_test.exs** — test/pattern/fix_regex_match_swapped_args_equivalence_test.exs:5 — The repair exemption falsely claims every admitted attribute use has a non-binary left operand. An external `use` or macro can set `@re` to a binary without exposing `Module.put_attribute` in this file’s AST; a later explicit `@re ~r/x/` makes the rule classify the attribute as regex-only, so an earlier valid `@re =~ string` substring check is swapped and changes its answer.
- **lib/pattern/fix_regex_match_swapped_args.ex** — lib/pattern/fix_regex_match_swapped_args.ex:145 — The matcher treats interpolated `~r` sigils as literals and swaps them across the right operand. For `~r/#{left_effect()}/ =~ right_effect()`, the original evaluates `left_effect/0` before `right_effect/0`, while the fix reverses that order; this can change observable effects, exceptions, or termination.
- **test/pattern/fix_regex_match_swapped_args_fix_test.exs** — test/pattern/fix_regex_match_swapped_args_fix_test.exs:6 — The positive test uses an unconstrained variable as the right operand and checks only rewritten text. The rule also admits `~r/a/ =~ ~r/b/`; its fix merely reverses the regexes, still crashes, and the next pass reverses them again. Add coverage requiring a genuinely repaired binary-right-hand case and rejecting or otherwise handling non-binary right operands.
- **lib/pattern/fix_task_shutdown_brutal_kill.ex** — lib/pattern/fix_task_shutdown_brutal_kill.ex:26 — The matcher treats every `Task.shutdown/2` syntax as `Elixir.Task.shutdown/2`. With `alias MyApp.Task, as: Task`, where that module legitimately accepts `:brutal`, the fix silently changes the argument to `:brutal_kill` and alters program behavior.
- **lib/pattern/fix_task_shutdown_brutal_kill.ex** — lib/pattern/fix_task_shutdown_brutal_kill.ex:26 — The rule misses equivalent calls such as `task |> Task.shutdown(:brutal)` and `Elixir.Task.shutdown(task, :brutal)`, so those inputs retain the invalid atom and still raise at runtime; the tests explicitly lock in the piped false negative.
- **test/pattern/fix_task_shutdown_brutal_kill_check_test.exs** — test/pattern/fix_task_shutdown_brutal_kill_check_test.exs:22 — The test requires `task |> Task.shutdown(:brutal)` to remain clean, even though this is exactly `Task.shutdown(task, :brutal)` after pipe expansion and fails for the same invalid shutdown option. It codifies an AST-shape blind spot and prevents the rule from repairing realistic piped input.
- **test/pattern/graphemes_count_family_test.exs** — test/pattern/graphemes_count_family_test.exs:91 — This does not prove convergence “in any order”: it places the weaker rewrite in the initial input, so the currently earlier `AvoidGraphemesLength` can repair it. If ordering becomes `AvoidGraphemesLength`, then `NoEnumCountForLength`, then `AvoidGraphemesEnumCount`, the original piped `Enum.count` is weakened only after the finishing rule has passed and ships as `String.graphemes(text) |> length()`.
- **test/pattern/graphemes_count_family_test.exs** — test/pattern/graphemes_count_family_test.exs:40 — All fix-result checks use substring assertions, so malformed output, retained original expressions, or unrelated occurrences of `String.length(...)` can satisfy the tests; compare each complete output string exactly.
- **lib/pattern/hallucinated_guard.ex** — lib/pattern/hallucinated_guard.ex:74 — pre-existing: the rule recognizes only locally defined `defguard` names, so a valid local `def`/`defp is_pos_integer/1` is treated as hallucinated; the fixer rewrites both its definition head and calls, producing invalid or behavior-changing code.
- **lib/pattern/hallucinated_guard.ex** — lib/pattern/hallucinated_guard.ex:44 — pre-existing: any `import` or `use` anywhere in the parsed file disables the rule for the entire file, even in unrelated modules or when importing unrelated functions; a hallucinated guard then remains unrepaired and the invalid code still fails to compile.
- **test/pattern/no_agent_get_and_modify_equivalence_test.exs** — test/pattern/no_agent_get_and_modify_equivalence_test.exs:11 — The repair exemption falsely claims every `Agent.get_and_modify/2` call fails. With `alias MyAgent, as: Agent`, the original can call a valid user-defined function, while the fix changes it to `MyAgent.get_and_update/2`, which may crash or behave differently; this test silently certifies that unsafe rewrite without covering the admitted alias case.
- **lib/pattern/no_agent_get_and_modify.ex** — lib/pattern/no_agent_get_and_modify.ex:37 — The rule treats every syntactic `Agent.get_and_modify(...)` as a call to Elixir’s built-in `Agent`, even when `Agent` is an alias or local module that validly defines `get_and_modify`. For input such as `alias MyApp.Agent; Agent.get_and_modify(pid, fun)`, it silently changes the called API to `MyApp.Agent.get_and_update`, altering behavior or causing an undefined-function error.
- **test/pattern/no_agent_get_and_modify_check_test.exs** — test/pattern/no_agent_get_and_modify_check_test.exs:33 — The tests cover supported replacement arities 2 and 3 but never reject other arities. The rule therefore admits calls such as `Agent.get_and_modify(pid)` or `&Agent.get_and_modify/99` and rewrites them to equally nonexistent `Agent.get_and_update` forms, leaving broken code and potentially changing its failure instead of repairing it.
- **lib/pattern/no_anon_fn_application_in_pipe.ex** — lib/pattern/no_anon_fn_application_in_pipe.ex:73 — The replacement calls unqualified `then/2`. On admitted code where `Kernel.then/2` is excluded and a local or imported `then/2` has different behavior, the original anonymous function returns one value while the fixed code invokes that other function and returns a different value.
- **lib/pattern/no_anon_fn_application_in_pipe.ex** — lib/pattern/no_anon_fn_application_in_pipe.ex:71 — The fix assumes the opening `(` is exactly one column before `fn`. Valid forms with whitespace, a comment, or a line break between `(` and `fn` are still reported, but the patch consumes whitespace instead of the parenthesis, produces invalid source, and is rejected by the shared patch-safety gate, leaving the finding unfixed.
- **lib/pattern/no_bare_value_in_map_new.ex** — lib/pattern/no_bare_value_in_map_new.ex:105 — The fix renames an underscore-prefixed mapper parameter but does not rename references inside the body. For `Map.new(items, fn _item -> [_item] end)`, the rule emits `fn item -> {item, [_item]} end`, leaving `_item` unbound and turning a runtime `ArgumentError` into code that does not compile.
- **lib/pattern/no_capture_fn_apply.ex** — lib/pattern/no_capture_fn_apply.ex:184 — pre-existing: raw placeholder substitution is not capture-safe around local bindings. For example, `def run(x), do: (& (x = 1; &1)).(x)` is rewritten to a block equivalent to `x = 1; x`, which returns `1` instead of the argument’s original value.
- **lib/pattern/no_case_destructure_in_pipe.ex** — lib/pattern/no_case_destructure_in_pipe.ex:121 — pre-existing: `irrefutable_var?/1` mistakes the special form `__MODULE__` for a variable because its AST has atom name and context. For `value |> case do __MODULE__ -> body end`, a value other than the enclosing module originally raises `CaseClauseError`; the fix rewrites it to `then(fn __MODULE__ -> body end)`, which raises `FunctionClauseError`, changing the answer.
- **lib/pattern/no_case_on_param_dispatch.ex** — lib/pattern/no_case_on_param_dispatch.ex:220 — A clause body can observe the parameter without explicitly naming it: for `def f(x), do: case x do 0 -> binding(); _ -> [] end`, `mentions_var?/2` returns false and the fix emits `def f(0), do: binding()`. Consequently `f(0)` changes from `[x: 0]` to `[]`, violating behavior preservation.
- **lib/pattern/no_case_true_false.ex** — lib/pattern/no_case_true_false.ex:140 — pre-existing: `provably_boolean?/1` admits `and`/`or` expressions and any `?`-suffixed call even though these can return non-booleans. For example, `case true and :unknown do true -> :yes; false -> :no end` raises `CaseClauseError`, but the auto-fix returns `:yes`; a `ready?()` function returning `:unknown` has the same behavior change.
- **lib/pattern/no_case_tuple_guard_dispatch.ex** — lib/pattern/no_case_tuple_guard_dispatch.ex:176 — On an otherwise eligible `case`, a comment inside the scrutinee tuple or a discarded tuple pattern is lost when the node is rebuilt as `cond`; the shared comment-preservation gate then rejects the patch, but `check/2` still reports the issue, leaving the diagnostic unrepaired.
- **lib/pattern/no_chunk_by_identity_for_dedup.ex** — lib/pattern/no_chunk_by_identity_for_dedup.ex:51 — A direct-looking `Enum.map(Enum.chunk_by(list, & &1), &List.first/1)` used as the right side of another pipe is actually an `Enum.map/3` call after pipe insertion, but the recursive walk still reports and rewrites it. For `foo |> Enum.map(Enum.chunk_by(list, & &1), &List.first/1)`, the fix produces `foo |> Enum.dedup(list)`, changing the invalid call and its compiler error rather than performing the advertised equivalent repair.
- **lib/pattern/no_codepoint_string_reverse.ex** — lib/pattern/no_codepoint_string_reverse.ex:162 — pre-existing: the nested-call guard accepts the unintended cross-product `IO.join(...)` and `Enum.iodata_to_binary(...)`; if `IO` or `Enum` is aliased to an application module implementing that valid call, the rule replaces its arbitrary behavior with `String.reverse/1`, changing the program’s answer.
- **lib/pattern/no_crypto_hash_pipe_swapped_args.ex** — lib/pattern/no_crypto_hash_pipe_swapped_args.ex:104 — The predicate accepts every literal atom, including invalid algorithms such as `:bogus` or `nil`. On `"data" |> :crypto.hash(:bogus)`, the rewrite produces `:crypto.hash(:bogus, "data")`, which still raises `ArgumentError`, so the rule records an applied repair without fixing the program; restrict admission to algorithms accepted by `:crypto.hash/2`.
- **test/pattern/no_crypto_hash_pipe_swapped_args_fix_test.exs** — test/pattern/no_crypto_hash_pipe_swapped_args_fix_test.exs:38 — The tests omit valid one-argument pipes such as `:sha256 |> :crypto.hash(:payload)`, which already means `:crypto.hash(:sha256, :payload)` and returns a digest; the rule admits this input and rewrites it to `:crypto.hash(:payload, :sha256)`, turning valid code into an `ArgumentError`.
- **lib/pattern/no_dead_map_update.ex** — lib/pattern/no_dead_map_update.ex:72 — The matcher treats every syntactic `Map` alias as Elixir’s standard `Map` module. On input such as `alias Custom, as: Map`, it rewrites calls to `Custom.update/drop/delete` under assumptions that need not hold, changing behavior or removing side effects.
- **lib/pattern/no_destructure_reconstruct.ex** — lib/pattern/no_destructure_reconstruct.ex:198 — pre-existing: the fix always introduces the variable `items` without checking whether that name is already bound. For example, fixing `def f([a, b], items), do: {[a, b], items}` makes both arguments share one binding, so the clause only matches when they are equal and returns the wrong value otherwise.
- **lib/pattern/no_doc_false_on_private.ex** — lib/pattern/no_doc_false_on_private.ex:33 — Pre-existing: the rule only detects adjacent AST statements, so valid ordering such as `@doc false`, then `@spec helper(integer()) :: integer()`, then `defp helper(x)` is silently missed and the redundant private documentation remains.
- **lib/pattern/no_double_filter.ex** — lib/pattern/no_double_filter.ex:116 — The first assignment may rebind the source or operand variable used by the second filter. For `numbers = Enum.filter(numbers, &(&1 >= 0)); neg = Enum.filter(numbers, &(&1 < 0))`, the original second filter reads the newly filtered `numbers`, but the rewrite partitions the original list, changing the answer.
- **lib/pattern/no_double_filter.ex** — lib/pattern/no_double_filter.ex:172 — `slice/2` assumes every admitted source and predicate range is single-line and treats Sourceror columns as grapheme offsets. A multiline capture such as `&(\n &1 >= 0\n)` can produce a negative slice length and crash fixing; non-ASCII text before a range can extract the wrong bytes and yield a rejected or incorrect patch.
- **lib/pattern/no_double_sort_same_list.ex** — lib/pattern/no_double_sort_same_list.ex:62 — `Enum.sort(list, :desc)` is not equivalent to `Enum.reverse(Enum.sort(list))` when distinct terms compare equal. For `[1, 1.0]`, stable descending sort preserves their input order, while reversing the ascending result swaps them, so the fix changes the returned values.
- **lib/pattern/no_double_sort_same_list.ex** — lib/pattern/no_double_sort_same_list.ex:27 — Sorts are grouped only by source-variable name across the entire AST, ignoring function scope and statement order. Separate functions using an `arr` parameter, or a descending sort before the ascending binding, can therefore be rewritten to reference an undefined or unrelated ascending variable, producing invalid or incorrect code.
- **lib/pattern/no_double_sort_same_list.ex** — lib/pattern/no_double_sort_same_list.ex:148 — A piped sort is identified only by the pipeline’s root variable. Different pipelines such as `arr |> Enum.map(f) |> Enum.sort()` and `arr |> Enum.filter(p) |> Enum.sort(:desc)` are treated as sorting the same list, and the latter is replaced with the reverse of the former despite operating on different values.
- **lib/pattern/no_duplicate_spec.ex** — lib/pattern/no_duplicate_spec.ex:42 — The fix removes an `@spec` while declaring no safety assumption, but duplicate specs are observable through `Code.Typespec.fetch_specs/1`: input containing two identical specs returns two type entries before the fix and one afterward, so the rewrite does not preserve the exact answer for every admitted input.
- **lib/pattern/no_eager_with_index_in_reduce.ex** — lib/pattern/no_eager_with_index_in_reduce.ex:120 — pre-existing: replacing eager `Enum.with_index/1` with lazy `Stream.with_index/1` changes evaluation order for admitted effectful enumerables. If enumeration and the reducer have side effects, or the reducer raises early, the original fully enumerates before reducing while the fix interleaves or stops enumeration, changing observable behavior.
- **lib/pattern/no_empty_map_new.ex** — lib/pattern/no_empty_map_new.ex:155 — pre-existing: the matcher ignores lexical aliases, so `alias MyMap, as: Map; Map.new()` is rewritten to `%{}` even when `MyMap.new/0` returns a non-map value, changing program behavior.
- **lib/pattern/no_empty_map_new.ex** — lib/pattern/no_empty_map_new.ex:136 — pre-existing: the generic AST walk descends into quoted code, so `quote do: Map.new()` is rewritten to `quote do: %{}`, changing the AST value produced at runtime rather than replacing an evaluated empty-map construction.
- **lib/pattern/no_empty_map_new.ex** — lib/pattern/no_empty_map_new.ex:58 — pre-existing: every pipe RHS is discarded wholesale, so a standalone nested call such as `items |> process(Map.new())` is neither reported nor repaired even though the pipe supplies an argument to `process/2`, not to the nested `Map.new/0`.
- **lib/pattern/no_enum_into_empty_mapset.ex** — lib/pattern/no_enum_into_empty_mapset.ex:68 — Pre-existing: the rule treats any alias spelled `Enum` or `MapSet` as the standard module. With `alias CustomSet, as: MapSet`, it rewrites `Enum.into(items, MapSet.new())` to `MapSet.new(items)` even though those custom functions need not be equivalent, changing behavior or causing a runtime failure.
- **lib/pattern/no_enum_into_empty_mapset.ex** — lib/pattern/no_enum_into_empty_mapset.ex:107 — Pre-existing: both check clauses accept arbitrary trailing arguments, so calls such as `Enum.into(items, MapSet.new(), fun, extra)` produce a diagnostic, while every fix clause requires exactly the supported arity and emits no patch. The rule therefore promises a repair it silently declines.
- **test/pattern/no_enum_sort_then_map_values_equivalence_test.exs** — test/pattern/no_enum_sort_then_map_values_equivalence_test.exs:16 — The test marks every admitted match as an always-failing repair, but `Map` can be a local alias. For example, with `alias ListMap, as: Map` where `ListMap.values/1` accepts a list, `Map.values(Enum.sort(m))` returns normally; the rule rewrites that valid call to `Enum.map/2` and changes its answer. This test therefore certifies an unsafe rule without covering the alias-resolved input that disproves its premise.
- **lib/pattern/no_enum_sort_then_map_values.ex** — lib/pattern/no_enum_sort_then_map_values.ex:187 — The fix assumes every sorted element is a `{key, value}` pair, but the check admits any enumerable. For `Map.values(Enum.sort([2, 1]))`, it rewrites the existing `BadMapError` into an `Enum.map/2` whose tuple pattern raises `FunctionClauseError`, so the reported defect remains unrepaired.
- **lib/pattern/no_enum_sort_then_map_values.ex** — lib/pattern/no_enum_sort_then_map_values.ex:132 — The matcher treats the source aliases `Map` and `Enum` as the standard library modules without resolving lexical aliases. Code such as `alias MyEnum, as: Enum` can make `Map.values(Enum.sort(x))` valid when `MyEnum.sort/1` returns a map; the rule then rewrites valid code to a call to `MyEnum.map/2`, changing its answer or causing an undefined-function crash.
- **test/pattern/no_enum_sort_then_map_values_fix_test.exs** — test/pattern/no_enum_sort_then_map_values_fix_test.exs:33 — The purported realistic `payments |> Enum.sort_by(& &1.date)` case only checks rewritten text. For a map, `Enum.sort_by/2` passes `{key, value}` tuples to the sorter, so `& &1.date` fails before `Map.values/1`; for a list of payment structs, the emitted tuple-pattern mapper fails. The test therefore does not prove this advertised field shape is repaired end-to-end and can bless a rewrite that still crashes on every plausible input.
- **lib/pattern/no_enum_take_negative.ex** — lib/pattern/no_enum_take_negative.ex:146 — pre-existing: the diagnostic tells users to sort in the opposite direction and take a positive count, which changes results for unsorted inputs; for `[2, 1, 3]` with `-2`, `Enum.take/2` returns `[1, 3]`, while the suggested descending sort and positive take returns `[3, 2]`.
- **lib/pattern/no_explicit_product_reduce.ex** — lib/pattern/no_explicit_product_reduce.ex:58 — pre-existing: the rule treats every syntactic `Enum.reduce/3` as the standard library call, even when `Enum` is aliased to a custom module; valid code such as `alias MyEnum, as: Enum` is rewritten from `MyEnum.reduce/3` to `MyEnum.product/1`, which may return a different value or be undefined.
- **lib/pattern/no_explicit_sum_reduce.ex** — lib/pattern/no_explicit_sum_reduce.ex:81 — pre-existing: the matcher ignores the initial accumulator, so `Enum.reduce(list, 10, fn x, acc -> x + acc end)` is rewritten to `Enum.sum(list)`, losing 10; an effectful initializer is skipped entirely.
- **lib/pattern/no_fetch_then_update.ex** — lib/pattern/no_fetch_then_update.ex:237 — For `Map.update/4`, the rewrite drops the default argument without evaluating it. On a present key, an input such as `Map.update(map, key, raise("boom"), fun)` originally raises but is changed to a successful `Map.put`, and any side effects in the default are also lost.
- **lib/pattern/no_fetch_then_update.ex** — lib/pattern/no_fetch_then_update.ex:160 — The walker descends into quoted code. An `Map.update!` expression inside `quote do ... end` in the successful fetch branch is rewritten even though it is AST data rather than an executed update, changing the value returned by the program.
- **lib/pattern/no_fetch_then_update.ex** — lib/pattern/no_fetch_then_update.ex:186 — The rule identifies `Map` textually and does not verify that it refers to Elixir’s `Map` module. Under `alias CustomMap, as: Map`, it rewrites `CustomMap.update!/3` or `update/4` into `CustomMap.put/3`, which need not have equivalent behavior.
- **lib/pattern/no_filter_then_first.ex** — lib/pattern/no_filter_then_first.ex:59 — Pre-existing: for a valid pipeline with steps after the match, such as `Stream.filter(nums, pred) |> Enum.at(0) |> inspect()`, `Macro.prewalk/3` emits overlapping patches for both the whole pipeline and its matching left subtree. Sourceror then applies original-coordinate replacements to the same bytes, which can corrupt the repaired source or cause the safety gate to discard the fix while the rule still reports the issue.
- **lib/pattern/no_find_value_default_case.ex** — lib/pattern/no_find_value_default_case.ex:148 — A variable default is treated as eagerly safe, but the rewrite moves it before evaluation of the callback expression. For `Enum.find_value(list, fun = fn _ -> nil end) || fun`, the original binds `fun` before reading it; the fix produces `Enum.find_value(list, fun, fun = fn _ -> nil end)`, which reads `fun` before that binding and can fail compilation or read a different outer binding.
- **lib/pattern/no_grapheme_palindrome.ex** — lib/pattern/no_grapheme_palindrome.ex:178 — Variable bindings and comparisons are matched globally by name, without lexical scope. If one function binds `g = String.graphemes(x)` while another function compares its own `g == Enum.reverse(g)`, the rule can rewrite the unrelated comparison using `x`; when that function also has an `x` variable, the result compiles but changes its answer.
- **lib/pattern/no_group_by_for_frequencies.ex** — lib/pattern/no_group_by_for_frequencies.ex:291 — The rule treats every local `length(group)` call as `Kernel.length/1`. In a valid module that excludes the Kernel import and defines its own `length/1`, the original callback can return another value or perform side effects, while the fix replaces it with `Enum.frequencies_by/2`, changing behavior.
- **lib/pattern/no_guard_equality_for_pattern_match.ex** — lib/pattern/no_guard_equality_for_pattern_match.ex:262 — pre-existing: multiple equalities for the same parameter are collapsed with `Map.new/2`. For `def f(x) when x == :a and x == :b, do: :impossible`, both guards are removed and only the last literal becomes the head pattern, producing `def f(:b), do: :impossible`; input `:b`, which previously fell through, now executes this clause.
- **lib/pattern/no_hallucinated_ets_keytype_option.ex** — lib/pattern/no_hallucinated_ets_keytype_option.ex:143 — When an option list contains `keytype: :term` more than once, the rule deletes only the first pair; the remaining invalid pair still makes `:ets.new/2` raise, so the single-pass repair reports success without repairing the call.
- **lib/pattern/no_hallucinated_ets_keytype_option.ex** — lib/pattern/no_hallucinated_ets_keytype_option.ex:67 — The matcher never verifies that the call has exactly two arguments. It therefore rewrites the last list argument of nonexistent `:ets.new/1`, `:ets.new/3`, and other arities, changing unrelated invalid calls while leaving their actual arity error unfixed.
- **test/pattern/no_hallucinated_ets_keytype_option_check_test.exs** — test/pattern/no_hallucinated_ets_keytype_option_check_test.exs:6 — The tests never assert that non-`/2` calls are clean. The current matcher consequently flags nonexistent `:ets.new/1`, `:ets.new/3`, and other arities when their last argument contains `keytype: :term`, misdiagnosing and rewriting calls whose actual defect is their arity.
- **test/pattern/no_hallucinated_ets_keytype_option_fix_test.exs** — test/pattern/no_hallucinated_ets_keytype_option_fix_test.exs:6 — No case covers multiple `keytype: :term` entries in one literal option list. The implementation deletes only the first, so input such as `:ets.new(:t, [:set, keytype: :term, keytype: :term])` remains invalid after the single repair pass.
- **lib/pattern/no_hd_tl_when_cons_bound.ex** — lib/pattern/no_hd_tl_when_cons_bound.ex:244 — The matcher treats every unqualified `hd(var)` or `tl(var)` as a Kernel call. If the module excludes Kernel’s function and imports a custom `hd/1` or `tl/1`, the fix replaces that custom call with cons destructuring and changes the returned value.
- **lib/pattern/no_hd_tl_when_cons_bound.ex** — lib/pattern/no_hd_tl_when_cons_bound.ex:274 — Fresh-name generation gives up after `head1000`/`tail1000`. A valid clause whose patterns already bind all 1,001 candidate names is still reported, but the replacement name becomes `nil`, so the matching `hd/1` or `tl/1` call is left unrepaired.
- **lib/pattern/no_identity_enum_map.ex** — lib/pattern/no_identity_enum_map.ex:57 — The matcher treats any written `Enum.map/2` as Elixir’s standard `Enum`, even when `Enum` is a local alias for another module. On input such as `alias Custom, as: Enum`, the fix changes `Custom.map(value, identity)` into `Custom.to_list(value)`, which need not return the same answer and may not exist.
- **lib/pattern/no_identity_enum_map.ex** — lib/pattern/no_identity_enum_map.ex:138 — The rule assumes written `&Function.identity/1` is the standard identity function. With `alias Custom, as: Function`, that capture can transform its argument, yet the rule removes it and rewrites the call to `Enum.to_list/1`, changing the result.
- **lib/pattern/no_identity_function_in_enum.ex** — lib/pattern/no_identity_function_in_enum.ex:52 — The matcher treats any `Enum` alias as the standard library module and likewise assumes `Function` in `&Function.identity/1` is canonical. With `alias MyEnum, as: Enum` or `alias MyCallbacks, as: Function`, it rewrites calls to user modules into unrelated standard-library calls, changing results or causing runtime failures.
- **lib/pattern/no_if_empty_for_enum_min_max.ex** — lib/pattern/no_if_empty_for_enum_min_max.ex:16 — pre-existing: the rule is not equivalent for stateful enumerables. On a `Stream.resource/3` whose result changes each time enumeration starts, the original calls `Enum.empty?/1` and then starts enumeration again for `Enum.min/1`, while the rewrite enumerates only once, so it can return a different minimum; the new moduledoc’s claim that these forms “rewrite identically” is therefore false.
- **lib/pattern/no_if_true_false.ex** — lib/pattern/no_if_true_false.ex:136 — pre-existing: the rule treats any AST call spelled `Enum.all?/2`, `Enum.any?/2`, or `Enum.empty?/1` as boolean without resolving aliases. With `alias MyEnum, as: Enum` and a function returning `5`, `if Enum.all?(...) do true else false end` returns `true`, but the fix returns `5`, changing the program’s answer.
- **lib/pattern/no_is_nil_guard.ex** — lib/pattern/no_is_nil_guard.ex:124 — The fix checks whether a nil-tested parameter is used only in the function body, not in the remaining guard. For `def foo(x, y) when is_nil(x) and x == y, do: :ok`, it emits `def foo(nil, y) when x == y`, where `x` is unbound; the production compile-safety gate then reverts the repair and leaves the reported issue unfixed.
- **lib/pattern/no_kernel_op_in_pipeline.ex** — lib/pattern/no_kernel_op_in_pipeline.ex:99 — A quoted pipeline fragment with a zero-arity head is rewritten unless it contains `unquote`; a literal fragment such as `quote do: Enum.count() |> Kernel.==(0)` can later be spliced after an upstream pipe, and the fix changes it to an infix comparison that the outer pipeline cannot accept, causing macro expansion to fail.
- **lib/pattern/no_keyword_get_integer_key.ex** — lib/pattern/no_keyword_get_integer_key.ex:89 — `:timeout |> Keyword.get(5000)` is claimed and rewritten to `:timeout |> Enum.at(5000)`, even though the atom-first-argument sibling rule owns this defect and would repair it to `5000`; the piped detector ignores the pipe’s left-hand side, causing diagnostic contention and a wrong repair.
- **test/pattern/no_keyword_get_keyword_key_equivalence_test.exs** — test/pattern/no_keyword_get_keyword_key_equivalence_test.exs:20 — This test performs none of the 12 claimed probes: `mark_equivalence_repair/1` returns `:ok` for any non-empty string. It never proves the original crashes, the rule fires, or the repaired code returns the intended values, so it passes even if the repair is inert or wrong.
- **lib/pattern/no_keyword_get_keyword_key.ex** — lib/pattern/no_keyword_get_keyword_key.ex:60 — The matcher treats every `Keyword.get` alias as Elixir’s standard `Keyword` module. If code declares `alias MyKeyword, as: Keyword` and that module’s `get/2` accepts a keyword-list argument, the rule rewrites a valid call to `get/3`, changing behavior or causing an undefined-function error.
- **lib/pattern/no_keyword_get_keyword_key.ex** — lib/pattern/no_keyword_get_keyword_key.ex:106 — Nested matches such as `Keyword.get(Keyword.get(opts, inner: 1), outer: 2)` produce overlapping patches for the outer and inner calls. The outer replacement still contains the unfixed inner AST, so applying both ranges can corrupt/reject the repair or leave a reported inner defect unfixed.
- **test/pattern/no_keyword_get_keyword_key_check_test.exs** — test/pattern/no_keyword_get_keyword_key_check_test.exs:110 — The test codifies a false negative for `opts |> Keyword.get(partial: false)`. This is still a two-argument `Keyword.get(opts, [partial: false])` call and always crashes; the unambiguous repair is `opts |> Keyword.get(:partial, false)`, but the test requires the rule to ignore it.
- **test/pattern/no_keyword_get_keyword_key_fix_test.exs** — test/pattern/no_keyword_get_keyword_key_fix_test.exs:9 — The new rule has no end-to-end test through `Credence.Pattern.fix/2`; every positive case calls the rule’s patch helper directly, so the suite can pass even if production rule discovery, check-to-fix dispatch, or single-pass application leaves realistic `Keyword.get(opts, partial: false)` unchanged.
- **test/pattern/no_keyword_get_with_atom_first_arg_equivalence_test.exs** — test/pattern/no_keyword_get_with_atom_first_arg_equivalence_test.exs:18 — The test claims every matched `Keyword.get(:atom, ...)` call raises, but the rule matches syntax without resolving aliases. In `alias Custom, as: Keyword; Keyword.get(:clock, :fallback)`, a custom `get/2` may return normally; the fix then replaces that valid call with `:fallback`, changing its answer. This no-op marker falsely certifies the rule as an always-failing repair.
- **lib/pattern/no_keyword_get_with_atom_first_arg.ex** — lib/pattern/no_keyword_get_with_atom_first_arg.ex:58 — For three-argument calls, the fix drops the key expression even though Elixir evaluates it before calling `Keyword.get/3`. On `Keyword.get(:key, raise("boom"), :fallback)`, the original raises `RuntimeError`, but the repair returns `:fallback`; effectful keys such as `send(pid, :seen)` are likewise silently skipped.
- **test/pattern/no_keyword_get_with_atom_first_arg_fix_test.exs** — test/pattern/no_keyword_get_with_atom_first_arg_fix_test.exs:55 — The test incorrectly approves deleting `compute_key()`: Elixir evaluates that argument before `Keyword.get/3` raises, so on input where `compute_key/0` has a side effect, the repair silently removes an observable effect.
- **lib/pattern/no_length_based_indexing.ex** — lib/pattern/no_length_based_indexing.ex:92 — pre-existing: the rule admits `n = Enum.count(enum)` for any enumerable, then may remove that enumeration and replace a later `Enum.at(enum, n - K)` with a single negative-index traversal. For a stateful enumerable such as a finite `Stream.repeatedly/1` backed by an Agent, the original traverses it twice while the fixed code traverses it once, producing a different value and different side effects.
- **lib/pattern/no_length_guard_to_pattern.ex** — lib/pattern/no_length_guard_to_pattern.ex:192 — Replacing `length(list) > 0` with `[_ | _]` changes behavior for improper lists: the original guard fails and falls through because `length([1 | 2])` is invalid, while the new pattern accepts `[1 | 2]` and executes the guarded clause.
- **test/pattern/no_length_on_mapset_new_equivalence_test.exs** — test/pattern/no_length_on_mapset_new_equivalence_test.exs:3 — The repair exemption assumes `MapSet` always means Elixir’s standard module. In code such as `alias ListFactory, as: MapSet`, where `MapSet.new/1` returns a list, the original `length(MapSet.new(arg))` succeeds, while the rewrite calls `MapSet.size/1` and can crash or return a different answer; this test therefore certifies a behaviour-changing rewrite as universally broken input.
- **lib/pattern/no_length_on_mapset_new.ex** — lib/pattern/no_length_on_mapset_new.ex:86 — The matcher assumes the alias `MapSet` denotes Elixir’s standard module. With `alias ListFactory, as: MapSet`, where `MapSet.new/1` returns a list, the original `length(MapSet.new(arg))` succeeds, but the rewrite calls `MapSet.size/1` and can crash or return a different answer.
- **test/pattern/no_length_on_mapset_new_check_test.exs** — test/pattern/no_length_on_mapset_new_check_test.exs:167 — The no-issue cases miss rebinding the exact `MapSet` alias. With `alias ListFactory, as: MapSet`, where `ListFactory.new/1` returns a list, the test suite blesses a check that flags valid `length(MapSet.new(arg))`; the corresponding repair can then crash or change the answer by calling `ListFactory.size/1`.
- **test/pattern/no_length_on_mapset_new_check_test.exs** — test/pattern/no_length_on_mapset_new_check_test.exs:7 — Every positive test invokes the rule directly, so none proves that this newly added rule is discovered and its repair survives the real Pattern pipeline; the rule could remain inert end-to-end while this entire file passes.
- **test/pattern/no_length_on_mapset_new_fix_test.exs** — test/pattern/no_length_on_mapset_new_fix_test.exs:148 — The no-op coverage omits lexical shadowing: with `alias MySet, as: MapSet`, or a locally imported `length/1`, the rule still rewrites `length(MapSet.new(items))` even though those names do not refer to `Kernel.length/1` and the standard `MapSet`; valid user behavior can become a wrong result or an undefined-function crash.
- **lib/pattern/no_list_append_in_recursion.ex** — lib/pattern/no_list_append_in_recursion.ex:271 — pre-existing: the fix reverses the caller’s initial accumulator as well as newly accumulated elements. For `build([1, 2], [:a, :b])`, the original returns `[:a, :b, 2, 4]`, while the rewrite returns `[:b, :a, 2, 4]`.
- **lib/pattern/no_list_append_in_recursion.ex** — lib/pattern/no_list_append_in_recursion.ex:138 — pre-existing: analysis groups functions globally by name and arity, ignoring module boundaries. A recursive `build/2` without a base case in one module can be paired with an unrelated base-like `build/2` in another module, causing both functions to be reported and rewritten as though they were one function.
- **lib/pattern/no_list_append_in_reduce.ex** — lib/pattern/no_list_append_in_reduce.ex:260 — The rule accepts an appended expression that reads `acc`, but the fix changes the accumulator’s order during every iteration, not only after the reduce. For `Enum.reduce([1, 2, 9], [], fn item, acc -> acc ++ [if(acc == [1, 2], do: 7, else: item)] end)`, the original returns `[1, 2, 7]` while the rewrite returns `[1, 2, 9]`.
- **lib/pattern/no_list_concat_with_recursive_result.ex** — lib/pattern/no_list_concat_with_recursive_result.ex:102 — The rule treats every unqualified `++` AST node as Kernel list concatenation. In a module that excludes Kernel’s `++/2` and imports or defines a custom `++/2` macro, `[h] ++ build(t)` can have arbitrary semantics, but the fix replaces it with list-cons syntax and changes the program’s answer.
- **lib/pattern/no_list_delete_at_length.ex** — lib/pattern/no_list_delete_at_length.ex:98 — The bare `length(x)` matcher does not prove that the call is `Kernel.length/1`. In a module that excludes the Kernel import and defines its own `length/1`, such as one returning `1`, the original deletes index `0` while the fix deletes the last element.
- **lib/pattern/no_list_delete_at_length.ex** — lib/pattern/no_list_delete_at_length.ex:77 — The claim that the rewrite raises identically on non-lists is false. With `list = :not_a_list`, the original fails while evaluating `length/1` with an `ArgumentError`; the replacement instead invokes `List.delete_at/2` and fails with a `FunctionClauseError`, changing observable behavior.
- **lib/pattern/no_list_duplicate_flatten.ex** — lib/pattern/no_list_duplicate_flatten.ex:95 — The matcher treats every `Enum` alias as Elixir’s standard `Enum`. With `alias MyEnum, as: Enum`, an admitted call to `MyEnum.concat/1` is rewritten into `MyEnum.flat_map/2`; custom implementations can return different values or lack `flat_map/2`, changing the answer or causing a runtime error.
- **lib/pattern/no_list_duplicate_join.ex** — lib/pattern/no_list_duplicate_join.ex:47 — The matcher treats the written aliases `List` and `Enum` as the standard-library modules without resolving aliases. For example, after `alias MyList, as: List`, it rewrites `Enum.join(List.duplicate("=", n))` to `String.duplicate("=", n)`, changing the called function and potentially the result.
- **lib/pattern/no_list_duplicate_join.ex** — lib/pattern/no_list_duplicate_join.ex:29 — The rule admits any count expression and claims invalid counts raise identically, but negative counts do not preserve the exact failure: `List.duplicate("=", -1)` and `String.duplicate("=", -1)` raise different exception types. The rewrite therefore changes observable behavior for an admitted input.
- **lib/pattern/no_list_foldl.ex** — lib/pattern/no_list_foldl.ex:108 — Every `++` expression is treated as a proper list, but the right operand may be any term: `List.foldl([] ++ :not_a_list, acc, fun)` raises `FunctionClauseError`, while the rewrite calls `Enum.reduce(:not_a_list, acc, fun)` and raises `Protocol.UndefinedError`.
- **lib/pattern/no_list_foldl.ex** — lib/pattern/no_list_foldl.ex:100 — The matcher ignores lexical aliases. With `alias MyFold, as: List`, it rewrites `MyFold.foldl(...)` as though it were the standard `List.foldl/3`; aliases such as `alias MyMap, as: Map` can likewise make the “list-returning” proof false.
- **lib/pattern/no_list_foldl.ex** — lib/pattern/no_list_foldl.ex:96 — The replacement emits the shadowable alias `Enum`. In a scope containing `alias MyEnum, as: Enum`, valid standard `List.foldl(...)` is changed into `MyEnum.reduce(...)`, which may return a different result or be undefined.
- **lib/pattern/no_manual_count_with_predicate.ex** — lib/pattern/no_manual_count_with_predicate.ex:500 — pre-existing: the fix replaces repeated `acc + 1` operations with one `acc + count`; with a nonnumeric accumulator and no matches, the original returns the accumulator but the rewrite raises, and floating-point accumulators can also produce different values because addition is regrouped.
- **lib/pattern/no_manual_count_with_predicate.ex** — lib/pattern/no_manual_count_with_predicate.ex:372 — pre-existing: the 2-clause rewrite admits predicates that reference the changing accumulator, but the generated `Enum.count/2` closure sees only its initial value; for example, `if rem(acc, 2) == 0` can alternate during recursion while the rewrite makes the same decision for every element.
- **lib/pattern/no_manual_count_with_predicate.ex** — lib/pattern/no_manual_count_with_predicate.ex:227 — pre-existing: guard-pattern clauses are analyzed in every permutation without preserving dispatch order; when the unguarded skip clause appears before the guarded increment clause, the original never increments, but the rewrite counts matching elements.
- **lib/pattern/no_manual_find.ex** — lib/pattern/no_manual_find.ex:383 — pre-existing: the replacement adds `when is_list(list)`, but `is_list/1` is false for improper lists. For input such as `[1 | :tail]`, an original function guarded by `h > 0` returns `1`, while the rewritten function raises `FunctionClauseError`; the claim at line 34 that `is_list/1` accepts improper lists is incorrect.
- **lib/pattern/no_manual_frequencies.ex** — lib/pattern/no_manual_frequencies.ex:148 — A derived key such as `binding()` does not explicitly reference `acc`, so the rule rewrites it to `Enum.frequencies_by/2`. In the original reducer, `binding()` includes both `item` and the evolving accumulator; in the generated one-argument function it does not include `acc`, producing different map keys and therefore a different answer.
- **lib/pattern/no_manual_list_last.ex** — lib/pattern/no_manual_list_last.ex:65 — pre-existing: clauses are grouped only by function name and arity, not by enclosing module. Two modules containing complementary `last/1` clauses are falsely treated as one function; the fix replaces one module’s clause, deletes the other’s, and rewrites unrelated `last/1` calls throughout the file.
- **lib/pattern/no_manual_list_last.ex** — lib/pattern/no_manual_list_last.ex:34 — pre-existing: the rewrite is not exactly behaviour-preserving for admitted inputs. On `[]` the original raises `FunctionClauseError`, while `hd(Enum.reverse([]))` raises `ArgumentError`; on other non-lists the exception can also differ, so callers that rescue or inspect exceptions receive a different answer.
- **lib/pattern/no_manual_list_reduce.ex** — lib/pattern/no_manual_list_reduce.ex:256 — pre-existing: the base clause’s non-list parameters are never checked against the recursive clause. For `f([], :allowed, acc)` plus `f([h | t], mode, acc)`, the rewrite produces a clause accepting every `mode`, so calls such as `f([], :rejected, 0)` return `0` instead of raising `FunctionClauseError`.
- **lib/pattern/no_manual_list_reduce.ex** — lib/pattern/no_manual_list_reduce.ex:151 — pre-existing: non-adjacent clauses are collapsed at the first clause’s position. If an intervening module attribute, alias, or import affects the recursive update expression, moving that expression above the intervening statement changes its meaning; for example, an update using an attribute assigned between the clauses can receive `nil` instead of the assigned value and crash.
- **lib/pattern/no_manual_max.ex** — lib/pattern/no_manual_max.ex:145 — pre-existing: the rule admits repeated expressions with side effects, such as `if f() >= g(), do: f(), else: g()`. The original calls the selected function twice, while `max(f(), g())` calls each function once, changing results, exceptions, and side effects.
- **lib/pattern/no_manual_min.ex** — lib/pattern/no_manual_min.ex:112 — pre-existing: the rule admits arbitrary repeated expressions, but `if f() <= g(), do: f(), else: g()` calls the selected function twice while `min(f(), g())` calls each function once, changing side effects and possibly the returned value.
- **lib/pattern/no_manual_min.ex** — lib/pattern/no_manual_min.ex:147 — pre-existing: the replacement emits unqualified `min/2`; in code that excludes or shadows `Kernel.min/2`, the repaired code can call a different local/imported function and return a different answer. Emit `Kernel.min/2` explicitly.
- **lib/pattern/no_manual_string_reverse.ex** — lib/pattern/no_manual_string_reverse.ex:143 — Pre-existing: the pipeline matcher accepts `IO.iodata_to_binary` with any explicit argument count. On parseable input such as `s |> String.graphemes() |> Enum.reverse() |> IO.iodata_to_binary(extra)`, it rewrites an undefined two-argument call into valid `String.reverse(s)`, changing the program’s behavior.
- **lib/pattern/no_manual_string_reverse.ex** — lib/pattern/no_manual_string_reverse.ex:156 — Pre-existing: the nested matcher independently checks the module and function, accidentally admitting nonexistent cross-pairings such as `Enum.iodata_to_binary(Enum.reverse(String.graphemes(s)))` and `IO.join(...)`; the fixer replaces these undefined calls with working `String.reverse(s)`.
- **lib/pattern/no_map_keys_enum_lookup.ex** — lib/pattern/no_map_keys_enum_lookup.ex:152 — pre-existing: rewriting `Map.keys(m) |> Enum.any?/all?(callback)` changes callback evaluation order for maps over 32 entries; on callbacks with side effects, exceptions, or process messages, different keys are evaluated before short-circuiting, so observable behavior changes despite the same boolean result.
- **lib/pattern/no_map_keys_enum_lookup.ex** — lib/pattern/no_map_keys_enum_lookup.ex:230 — pre-existing: the fix always introduces a binding named `v`; for input such as `Map.keys(m) |> Enum.all?(fn k -> m[k] == v end)`, it captures the existing `v` reference and rewrites the predicate to `v == v`, producing a different answer.
- **lib/pattern/no_map_keys_enum_lookup.ex** — lib/pattern/no_map_keys_enum_lookup.ex:263 — pre-existing: replacing `Map.get(m, k, default_expression)` with `v` removes eager evaluation of `default_expression`; if it has side effects or raises, the repaired program behaves differently even though every iterated key exists.
- **lib/pattern/no_map_keys_enum_lookup.ex** — lib/pattern/no_map_keys_enum_lookup.ex:250 — pre-existing: lookup replacement compares variable names without respecting lexical scope; in `Map.keys(m) |> Enum.any?(fn k -> Enum.any?(keys, fn k -> m[k] end) end)`, the nested callback’s lookup is incorrectly replaced with the outer entry value, changing the result.
- **lib/pattern/no_map_keys_or_values_for_iteration.ex** — lib/pattern/no_map_keys_or_values_for_iteration.ex:73 — Pre-existing: `all?`, `any?`, predicate `count`, and `frequencies_by` are not order-independent when callbacks have side effects, depend on process state, or raise. On maps whose direct traversal order differs from `Map.keys/values`, the rewrite can invoke callbacks in a different order, stop after a different callback, produce a different result, or raise at a different point.
- **lib/pattern/no_map_keys_or_values_for_iteration.ex** — lib/pattern/no_map_keys_or_values_for_iteration.ex:460 — Pre-existing: `on_first/2` and the `count` branches accept multiple trailing arguments but silently retain only the first. For example, malformed `Enum.all?(Map.values(m), fn _ -> true end, fn _ -> false end)` is rewritten from an undefined three-argument call into a valid two-argument call that returns a value.
- **lib/pattern/no_map_keys_or_values_for_iteration.ex** — lib/pattern/no_map_keys_or_values_for_iteration.ex:107 — Pre-existing: the triple-pipe matcher does not require `Map.keys/values` to have zero explicit arguments. `m |> Map.values(extra) |> Enum.count()` is an invalid two-argument `Map.values` call, but the fixer discards `extra` and produces valid `Enum.count(m)`.
- **lib/pattern/no_map_then_aggregate.ex** — lib/pattern/no_map_then_aggregate.ex:321 — pre-existing: the rewrite blindly substitutes every matching variable, including shadowed bindings. On `list |> Enum.map(fn x -> (fn x -> x end).(2) end) |> Enum.sum()`, it rewrites the inner function to use the reduce element, changing each mapped value from `2` to the list element and producing a different sum.
- **lib/pattern/no_map_update_then_fetch.ex** — lib/pattern/no_map_update_then_fetch.ex:147 — If the key variable is rebound between the update and fetch, the rule treats the two same-named key expressions as identical and moves the fetch before the rebinding. For `map = Map.update(map, key, ...); key = :other; val = Map.fetch!(map, key)`, the repaired code fetches the original key instead of `:other`, changing the result.
- **lib/pattern/no_map_update_then_fetch.ex** — lib/pattern/no_map_update_then_fetch.ex:162 — The matcher admits `Map.get/3` because it accepts arbitrary trailing arguments, but the replacement removes the third argument without evaluating it. Since Elixir eagerly evaluates call arguments, a side effect or exception in that default occurs before repair but disappears afterward. The analogous rewrite at line 258 also conditionally evaluates `Map.update/4`’s eagerly evaluated default and function expressions, changing behavior when either expression has effects or raises.
- **lib/pattern/no_missing_require_logger.ex** — lib/pattern/no_missing_require_logger.ex:91 — pre-existing: `has_logger_require?/1` treats a `require Logger` anywhere in the module as satisfying every Logger call. On a module where one function requires Logger but another calls `Logger.info/1`, the rule reports nothing, leaving the latter call without the required lexical import.
- **test/pattern/no_multi_arity_fn_in_string_replace_equivalence_test.exs** — test/pattern/no_multi_arity_fn_in_string_replace_equivalence_test.exs:13 — The unconditional repair marker blesses every match, but the rule also admits string patterns such as `String.replace("abc", "a", fn _, group -> group end)`. Its rewrite passes `"a"` to `Regex.replace/3`, which requires a regex, so the “repair” still crashes instead of producing the claimed working replacement.
- **lib/pattern/no_multi_arity_fn_in_string_replace.ex** — lib/pattern/no_multi_arity_fn_in_string_replace.ex:53 — The rule admits any pattern expression, including a string or a regex whose capture count does not match the callback arity. It rewrites these to `Regex.replace/3`, which requires a regex and compatible callback arity, so the supposed repair still crashes instead of producing the claimed corrected behavior.
- **lib/pattern/no_multi_arity_fn_in_string_replace.ex** — lib/pattern/no_multi_arity_fn_in_string_replace.ex:36 — The matcher assumes every syntactic `String` alias means Elixir’s standard `String` module. Under `alias MyString, as: String`, a valid custom `String.replace/3` call accepting a multi-arity function is flagged and rewritten to unrelated `Regex.replace/3`, changing working user code into a crash or different result.
- **test/pattern/no_multi_arity_fn_in_string_replace_fix_test.exs** — test/pattern/no_multi_arity_fn_in_string_replace_fix_test.exs:60 — The realistic fixture calls the rule’s patch helper directly, bypassing `Credence.Pattern.fix/1`; therefore the test can pass even if discovery, check-driven dispatch, or pipeline acceptance leaves this new rule inert. Add an exact whole-output assertion through the Pattern pipeline.
- **lib/pattern/no_negative_step_in_string_slice.ex** — lib/pattern/no_negative_step_in_string_slice.ex:56 — The matcher treats every `String.slice` syntax as Elixir’s standard `String`, even after `alias MyRangeModule, as: String`; for a custom `slice/2` that observes the range, rewriting `2..-1` (descending) to `2..-1//1` (empty) changes the program’s answer.
- **test/pattern/no_nested_enum_on_same_enumerable_equivalence_test.exs** — test/pattern/no_nested_enum_on_same_enumerable_equivalence_test.exs:28 — pre-existing: the equivalence test admits only eager lists, although the rule accepts any variable-backed enumerable. With a stateful stream, the original interleaves outer traversal with repeated inner traversals, while the fix fully consumes the stream into a MapSet before traversing it again; callbacks therefore run in a different order and count, but this test remains green because it compares only returned list values.
- **lib/pattern/no_nested_enum_on_same_enumerable.ex** — lib/pattern/no_nested_enum_on_same_enumerable.ex:103 — pre-existing: the repair always binds a user-visible variable named `set`; if the surrounding code already uses `set`, such as returning `{set, Enum.member?(list, x)}` from the callback, the repair overwrites that binding and changes the returned value.
- **lib/pattern/no_nested_enum_on_same_enumerable.ex** — lib/pattern/no_nested_enum_on_same_enumerable.ex:55 — pre-existing: “same enumerable” is decided only by variable name, ignoring lexical rebinding. For `Enum.map(list, fn list -> Enum.member?(list, value) end)`, the inner `list` is the current element, but the repair tests membership in a `MapSet` built from the outer list, changing behavior.
- **lib/pattern/no_nested_enum_on_same_enumerable.ex** — lib/pattern/no_nested_enum_on_same_enumerable.ex:106 — pre-existing: hoisting `MapSet.new/1` forces the entire enumerable before the outer traversal, which is not equivalent for lazy, effectful, or infinite enumerables. For example, `Enum.any?(Stream.iterate(0, &(&1 + 1)), fn x -> Enum.member?(stream, x) end)` can return immediately before the repair, while the repaired code hangs building the set.
- **lib/pattern/no_piped_regex_replace.ex** — lib/pattern/no_piped_regex_replace.ex:49 — pre-existing: the rule treats every syntactic `Regex.replace` as Elixir’s standard `Regex` module. With `alias MyRegex, as: Regex`, a valid custom `value |> Regex.replace(~r/x/, replacement)` call is rewritten to `String.replace`, changing its meaning or crashing.
- **lib/pattern/no_reduce_for_group_by.ex** — lib/pattern/no_reduce_for_group_by.ex:170 — pre-existing: the rule accepts any key expression without checking that it is independent of the reducer accumulator. For `Map.update(acc, map_size(acc), [x], &[x | &1])`, the fix emits a key function that references `acc` outside its scope, so previously valid code becomes uncompilable.
- **lib/pattern/no_reduce_while_without_halt.ex** — lib/pattern/no_reduce_while_without_halt.ex:71 — The rule treats every `Enum.reduce_while` AST as Elixir’s standard `Enum`, even when the file has `alias SomeModule, as: Enum`. On such valid input it rewrites `SomeModule.reduce_while/3` to `SomeModule.reduce/3`; those functions need not be equivalent, so the fix can change the program’s result.
- **lib/pattern/no_redundant_case_nil_clause.ex** — lib/pattern/no_redundant_case_nil_clause.ex:172 — Pre-existing: an underscore-prefixed variable is treated as a wildcard even though it binds the matched value. If both duplicate bodies reference that name, the `nil` body can read an outer binding while the final clause reads its newly bound subject; `Macro.to_string/1` still considers the bodies identical. Removing the `nil` clause then changes results—for example, with outer `_fallback = :outer`, `nil -> _fallback; n when is_integer(n) -> n; _fallback -> _fallback` returns `:outer` before the fix and `nil` afterward.
- **lib/pattern/no_redundant_comparison_guard.ex** — lib/pattern/no_redundant_comparison_guard.ex:69 — pre-existing: clauses are grouped only by function name and arity, not by enclosing module. Given module A with `f(n) when is_number(n) and n < 0` and module B with `f(n) when is_number(n) and n >= 0`, the rule treats A’s clause as preceding B’s and removes B’s comparison; B then incorrectly accepts negative numbers.
- **lib/pattern/no_redundant_dedup_before_mapset.ex** — lib/pattern/no_redundant_dedup_before_mapset.ex:90 — The pipe matcher accepts `MapSet.new/2`, but its replacement always emits `MapSet.new/1`. For `Enum.uniq(items) |> MapSet.new(transform)`, the fix deletes `transform`, producing a different set and skipping any side effects or exceptions from the function. The non-pipe matcher at line 102 also accepts `MapSet.new(Enum.uniq(items), transform)` and removes deduplication even though that changes how many times a stateful or raising transform runs.
- **lib/pattern/no_redundant_enum_join_separator.ex** — lib/pattern/no_redundant_enum_join_separator.ex:27 — The rule treats every `Enum` alias AST as the standard library module. On input such as `alias MyEnum, as: Enum`, where `MyEnum.join/2` and `MyEnum.join/1` intentionally return different values, it rewrites `Enum.join(list, "")` to `Enum.join(list)` and changes the program’s answer; the same defect affects `map_join`.
- **lib/pattern/no_redundant_list_traversal.ex** — lib/pattern/no_redundant_list_traversal.ex:498 — pre-existing: two assignments may bind the same result variable, such as `x = Enum.min(numbers); x = Enum.max(numbers)`. The fix emits `{x, x} = Enum.min_max(numbers)`, which raises `MatchError` whenever the minimum and maximum differ instead of returning the original maximum.
- **lib/pattern/no_redundant_list_traversal.ex** — lib/pattern/no_redundant_list_traversal.ex:235 — pre-existing: an inline call uses the hard-coded binding `minimum` or `maximum` without checking whether that name is already live. For example, after `maximum = 100`, fixing `minimum = Enum.min(numbers); minimum + Enum.max(numbers) + maximum` overwrites `maximum` with the list maximum and changes the answer.
- **lib/pattern/no_redundant_list_traversal.ex** — lib/pattern/no_redundant_list_traversal.ex:309 — pre-existing: rebinding is checked only strictly between the two traversal statements, so the first traversal assignment itself may overwrite the traversed variable. `numbers = Enum.min(numbers); maximum = Enum.max(numbers)` originally calls `Enum.max/1` on the rebound scalar, but the fix evaluates `Enum.min_max/1` on the original enumerable and changes a failure into a result.
- **lib/pattern/no_redundant_list_traversal.ex** — lib/pattern/no_redundant_list_traversal.ex:276 — pre-existing: the matcher accepts any plain-variable enumerable, not only immutable lists. Collapsing two traversals of a stateful or one-shot enumerable into one changes enumeration side effects and potentially returned values, violating equivalence for admitted inputs.
- **lib/pattern/no_redundant_local_capture.ex** — lib/pattern/no_redundant_local_capture.ex:97 — Nested calls such as `f = &foo/1; f.(f.(x))` produce overlapping patches for both the inner and outer applications; applying overlapping Sourceror ranges can corrupt the output or crash the fix pipeline.
- **lib/pattern/no_redundant_local_capture.ex** — lib/pattern/no_redundant_local_capture.ex:183 — The reference scan descends into quoted code. For `f = &foo/1` followed by `quote do: f.(x)`, the rule removes the runtime capture and changes the returned AST from `f.(x)` to `foo(x)`, even though the `f` inside `quote` is not the captured variable.
- **lib/pattern/no_redundant_negated_guard.ex** — lib/pattern/no_redundant_negated_guard.ex:211 — Pre-existing: the rule treats every equality/inequality operator pairing as complementary. With `x === y` followed by `x != y`, input `1, 1.0` matches neither clause before repair but matches the now-unguarded second clause afterward, changing the answer.
- **lib/pattern/no_redundant_negated_guard.ex** — lib/pattern/no_redundant_negated_guard.ex:136 — Pre-existing: matching variable names does not prove the clauses compare the same values or admit the same inputs. For `def f([x], y) when x == y` followed by `def f(x, y) when x != y`, input `{[1], [1]}` has no match before repair but reaches the second clause after its guard is removed.
- **lib/pattern/no_redundant_negated_guard.ex** — lib/pattern/no_redundant_negated_guard.ex:206 — Pre-existing: clauses are grouped only by function name and arity, without module scope, so an equality clause in one module can cause the rule to remove an inequality guard from a same-named function in another module, changing that unrelated function’s dispatch.
- **lib/pattern/no_redundant_to_list.ex** — lib/pattern/no_redundant_to_list.ex:49 — The rule also rewrites `MapSet.new(Enum.to_list(items), transform)`, but this is not equivalent for effectful enumerables: `Enum.to_list/1` exhausts the enumerable before `transform` runs, while the replacement interleaves enumeration and transformation. A stream producer and transform communicating through the process dictionary can therefore produce a different set.
- **lib/pattern/no_redundant_to_list.ex** — lib/pattern/no_redundant_to_list.ex:30 — The matcher treats any aliases spelled `Enum`, `Map`, or `MapSet` as the standard-library modules without resolving them. With `alias MyEnum, as: Enum` or `alias MySet, as: MapSet`, it removes a potentially meaningful custom `to_list/1` call and changes the program’s result.
- **lib/pattern/no_repeated_div_rem.ex** — lib/pattern/no_repeated_div_rem.ex:66 — Declaring `unsafe_in_dsl/0` as `[]` leaves cross-boundary Ash rewrites unguarded. For `def f(x) do; q = div(x, 2); expr(div(x, 2)); end`, the matcher treats the ordinary Kernel call and the Ash-interpreted call as identical and rewrites the latter to `expr(q)`, changing a DSL division of field `x` into a reference to field `q`.
- **lib/pattern/no_sort_for_top_k.ex** — lib/pattern/no_sort_for_top_k.ex:245 — pre-existing: the check admits `Enum.sort(list, &>=/2) |> Enum.at(0)` and recommends `Enum.min`, but that descending sort returns the maximum; the fixer then declines the same input because it only supports `sort/1`, leaving a false diagnostic whose suggested rewrite gives the wrong answer.
- **lib/pattern/no_sort_then_at.ex** — lib/pattern/no_sort_then_at.ex:232 — The `:desc`/first rewrite changes which tied value is returned: `Enum.sort([1, 1.0], :desc) |> Enum.at(0)` returns the first stable maximum (`1`), but `Enum.max([1, 1.0], &>/2, fn -> nil end)` keeps the last (`1.0`), so the fix is not `===`-equivalent.
- **test/pattern/no_sort_then_at_fix_test.exs** — test/pattern/no_sort_then_at_fix_test.exs:51 — The test blesses a behavior-changing repair for descending-first input. With `nums = [1, 1.0]`, stable `Enum.sort(nums, :desc) |> Enum.at(0)` returns the first tied maximum (`1`), while `Enum.max(nums, &>/2, fn -> nil end)` selects the last (`1.0`). The same incorrect expectation appears for piped and anonymous descending-first forms.
- **lib/pattern/no_sort_then_reverse.ex** — lib/pattern/no_sort_then_reverse.ex:7 — pre-existing: the claimed equivalence fails for mixed numerically equal terms. For `[1, 1.0]`, ascending stable sort followed by reverse returns `[1.0, 1]`, while `Enum.sort(list, :desc)` preserves the original tie order as `[1, 1.0]`, changing the exact result.
- **lib/pattern/no_string_concat_in_loop.ex** — lib/pattern/no_string_concat_in_loop.ex:78 — The rewrite changes behavior when an element or mapped expression is not a binary: `acc <> value` raises, while `Enum.join/1` and `Enum.map_join/2` convert values implementing `String.Chars` (for example integers), so previously failing input can return a string.
- **lib/pattern/no_string_concat_in_loop.ex** — lib/pattern/no_string_concat_in_loop.ex:74 — Matching the syntactic alias `Enum` does not prove the call targets Elixir’s `Enum`; after `alias MyEnum, as: Enum`, the rule rewrites `MyEnum.reduce/3` into `MyEnum.join/1` or `map_join/2`, which can change behavior or introduce an undefined-function failure.
- **lib/pattern/no_string_length_for_char_check.ex** — lib/pattern/no_string_length_for_char_check.ex:72 — The matcher treats any `String.length/1` syntax as Elixir’s standard `String`, but `String` can be rebound with `alias OtherModule, as: String`. If that module’s `length/1` and `graphemes/1` differ, the rewrite changes the boolean result; for example, `length/1` returning `1` and `graphemes/1` returning `[]` changes `true` to `false`.
- **lib/pattern/no_take_while_length_check.ex** — lib/pattern/no_take_while_length_check.ex:173 — pre-existing: the generated callback binds `elem` and `acc`, so a predicate that references an outer variable with either name is silently changed; for example, `Enum.take_while(items, &(&1 > elem)) |> length()` compares each item with itself after repair instead of with the original outer `elem`, producing a wrong count.
- **lib/pattern/no_tautological_if.ex** — lib/pattern/no_tautological_if.ex:147 — pre-existing: branch comparison removes all AST metadata, but metadata can affect meaning. For `if false, do: __ENV__.line, else: __ENV__.line`, the branches are treated as identical even though they return their distinct source lines; replacing the `if` with the first branch therefore changes the result.
- **lib/pattern/no_trailing_newline_in_doc.ex** — lib/pattern/no_trailing_newline_in_doc.ex:123 — A doc containing an escaped literal backslash before `n` internally and a real trailing `\n`, such as `@doc "Path C:\\new.\n"`, is incorrectly ignored. The raw check treats every internal `\n` byte pair as a newline without checking backslash parity, so this admitted trailing-newline case is neither reported nor repaired.
- **lib/pattern/no_underscore_function_name.ex** — lib/pattern/no_underscore_function_name.ex:78 — pre-existing: the rule admits public `def _helper`, then renames its exported API to `do_helper`; callers in other modules remain unchanged and fail with `undefined function _helper/...`.
- **lib/pattern/no_underscore_function_name.ex** — lib/pattern/no_underscore_function_name.ex:207 — pre-existing: when a local `_helper` exists, the atom-wide rewrite also changes unrelated uses such as `Other._helper()` and the data literal `:_helper`, altering external calls and returned data.
- **lib/pattern/no_underscore_function_name.ex** — lib/pattern/no_underscore_function_name.ex:221 — pre-existing: the rule never checks whether the suggested `do_helper` already exists; fixing a module containing both names merges their clauses under one name, changing dispatch or causing a compile error.
- **lib/pattern/no_unless_else.ex** — lib/pattern/no_unless_else.ex:42 — An unqualified `unless` imported from a custom DSL is indistinguishable here from `Kernel.unless`; the rule reports it and line 104 rewrites it to `if`, changing arbitrary DSL semantics and potentially corrupting the program.
- **lib/pattern/no_unless_else.ex** — lib/pattern/no_unless_else.ex:37 — `defines_unless?/1` suppresses the entire file: if one module defines a custom `unless`, genuine `Kernel.unless ... else` calls in unrelated modules are neither reported nor repaired.
- **lib/pattern/no_unused_computation.ex** — lib/pattern/no_unused_computation.ex:192 — pre-existing: the rule treats every `_`-prefixed variable as unused without checking later references; on `_n = length([1]); _n`, it deletes the binding and leaves `_n` unbound, producing invalid code or forcing the pipeline to revert the reported fix.
- **lib/pattern/no_unused_computation.ex** — lib/pattern/no_unused_computation.ex:81 — pre-existing: `String.to_charlist/1` is classified as total for every binary, but it raises on invalid UTF-8 such as `<<255>>`; deleting `_chars = String.to_charlist(<<255>>)` changes the program from raising to continuing.
- **lib/pattern/no_unused_underscore_assignment.ex** — lib/pattern/no_unused_underscore_assignment.ex:113 — Pre-existing: the underscore-name check admits special forms such as `__MODULE__`. On input `def f do; __MODULE__ = :ok; :done; end`, the rule deletes the invalid match and turns code that fails to compile into a function returning `:done`.
- **lib/pattern/no_zip_then_map.ex** — lib/pattern/no_zip_then_map.ex:50 — The rewrite is not behavior-preserving for effectful enumerables: `Enum.zip/2` fully builds the zipped list before `Enum.map/2` runs, while `Enum.zip_with/3` invokes the mapping function during enumeration. For example, if a stream updates process state for each element and the mapper reads that state, the original mapper sees the final state for every pair, while the replacement sees incremental states and returns different values.
- **lib/pattern/non_grouped_clauses.ex** — lib/pattern/non_grouped_clauses.ex:262 — `check/2` can report a function that `fix_patches/2` refuses to repair: with `foo/1`, another function, a stray `foo/1`, then `@threshold 9` and another consecutive `foo/1`, the reported stray is movable, but `groupable_keys/1` rejects the entire function because the later non-stray clause follows a value attribute, so the fix emits no patch.
- **lib/pattern/non_grouped_clauses.ex** — lib/pattern/non_grouped_clauses.ex:81 — nested modules can produce overlapping patches when both the outer and inner module have scattered clauses; the outer whole-body patch contains the inner module while the inner patch targets part of that same range, so applying both can corrupt the source or crash patch application.
- **lib/pattern/non_grouped_clauses.ex** — lib/pattern/non_grouped_clauses.ex:175 — the repair partitions statements by whole source lines, so admitted modules containing multiple definitions on one line cannot be reordered safely: adjacent AST nodes have the same start line, producing empty/reversed slices or repeated copies of the shared line and corrupting the module.
- **lib/pattern/prefer_comprehension_for_filtered_range.ex** — lib/pattern/prefer_comprehension_for_filtered_range.ex:56 — The matcher treats every `Enum.reduce`/`Enum.reverse` alias AST as the standard library. With `alias MyEnum, as: Enum`, it rewrites calls to `MyEnum` into a built-in comprehension, changing the program’s result.
- **lib/pattern/prefer_comprehension_for_filtered_range.ex** — lib/pattern/prefer_comprehension_for_filtered_range.ex:40 — A matching reduction containing a comment on the removed `if` or pipeline node is still reported, but the rewrite drops that comment and the shared comment-preservation gate rejects the patch; the pipeline silently leaves the reported issue unfixed.
- **lib/pattern/prefer_concat_over_flat_map_identity.ex** — lib/pattern/prefer_concat_over_flat_map_identity.ex:119 — The matcher treats every `&Function.identity/1` as the standard identity function, but `Function` can be a local alias. With `alias MyCallbacks, as: Function`, the callback may transform each item; rewriting `Enum.flat_map(items, &Function.identity/1)` to `Enum.concat(items)` can return a different value or crash.
- **test/pattern/prefer_concat_over_flat_map_identity_check_test.exs** — test/pattern/prefer_concat_over_flat_map_identity_check_test.exs:153 — The new `&Function.identity/1` cases only prove that `check/2` reports an issue; neither direct nor piped input is passed through `fix/2` with an exact expected output, so the repair could remain inert or decline these newly admitted inputs while this regression test stays green.
- **lib/pattern/prefer_cond_for_nested_if.ex** — lib/pattern/prefer_cond_for_nested_if.ex:36 — The rule replaces `if` with `cond` but inherits `unsafe_in_dsl/0 == []`; nested conditionals inside control-flow-reinterpreting DSL blocks are therefore rewritten without the shared DSL guard, potentially changing the generated query/expression’s meaning.
- **lib/pattern/prefer_cond_for_nested_if.ex** — lib/pattern/prefer_cond_for_nested_if.ex:96 — The matcher admits nested conditionals with comments attached to `else`/`do` syntax, but the replacement renders only the five expression ASTs and drops those comments; the shared comment-safety check then rejects the patch while `check/2` still reports the issue, leaving a reported diagnostic permanently unrepaired.
- **lib/pattern/prefer_counts_for_length.ex** — lib/pattern/prefer_counts_for_length.ex:153 — The matcher treats any bare `length/1` call as `Kernel.length/1`. In code that excludes the Kernel import and defines or imports a different `length/1`, the fix replaces that function’s result with the codepoint count, changing the program’s answer.
- **lib/pattern/prefer_counts_for_length.ex** — lib/pattern/prefer_counts_for_length.ex:182 — The rule does not verify that `String`, `Enum`, and `Map` resolve to the standard-library modules. Aliases such as `alias MyMap, as: Map` are admitted, so the generated `Enum.sum(Map.values(counts))` can call different modules and return a different value from the original expression.
- **lib/pattern/prefer_counts_for_length.ex** — lib/pattern/prefer_counts_for_length.ex:109 — `_ = string |> String.codepoints() |> Enum.frequencies()` is accepted as a counts assignment, but the repair emits `Map.values(_)`; `_` cannot be read as a value, so a compiling input is turned into code that fails compilation when the rule is applied directly and is left unrepaired by the production compile-revert gate.
- **lib/pattern/prefer_desc_sort_over_negative_take.ex** — lib/pattern/prefer_desc_sort_over_negative_take.ex:67 — Pre-existing: once one adjacent sort/take pair matches, the fix rewrites every plain sort and every negative take in the pipeline. For `xs |> Enum.sort() |> Enum.take(-3) |> Enum.sort()`, it changes the final sort to descending, so the repaired expression returns a different order.
- **lib/pattern/prefer_desc_sort_over_negative_take.ex** — lib/pattern/prefer_desc_sort_over_negative_take.ex:103 — Pre-existing: a piped comparator variable is mistaken for a direct-call list argument. `xs |> Enum.sort(comparator) |> Enum.take(-3)` is admitted, then rewritten as a piped `Enum.sort(comparator, :desc)`, producing a nonexistent three-argument call and breaking valid code.
- **lib/pattern/prefer_enum_count.ex** — lib/pattern/prefer_enum_count.ex:230 — `build_capture/2` replaces every variable with the element’s name without respecting lexical scope. For a condition such as `x > 0 and Enum.any?(others, fn x -> x > 1 end)`, the inner `x` is a new binding, but the fix rewrites its parameter and body to `&1`; this produces an invalid nested function, so the patch is rejected and the reported issue remains unrepaired.
- **lib/pattern/prefer_enum_reverse_two.ex** — lib/pattern/prefer_enum_reverse_two.ex:34 — The rewrite changes evaluation order: in `Enum.reverse(bad_or_effectful_enumerable) ++ effectful_tail`, the original completes or raises during `reverse/1` before evaluating the tail, while `reverse/2` evaluates the tail first; observable side effects and exceptions therefore differ.
- **lib/pattern/prefer_enum_reverse_two.ex** — lib/pattern/prefer_enum_reverse_two.ex:36 — The matcher treats every written `Enum` alias as the standard library module. If source contains `alias CustomEnum, as: Enum`, it rewrites `CustomEnum.reverse/1` plus concatenation into `CustomEnum.reverse/2`; when that function exists with different semantics, the repaired program compiles but returns a different answer.
- **lib/pattern/prefer_enum_slice.ex** — lib/pattern/prefer_enum_slice.ex:68 — The rule admits every enumerable, but the rewrite is only equivalent for list-like inputs: `Enum.drop(stream, start)` consumes the entire remaining enumerable before `Enum.take/2`, while `Enum.slice(stream, start, len)` can stop after the requested window. On finite side-effecting streams it performs fewer effects, and on infinite streams it changes nontermination into a returned result.
- **lib/pattern/prefer_enum_split.ex** — lib/pattern/prefer_enum_split.ex:95 — The matcher cannot prove `src` is a list, so it also rewrites streams and other stateful enumerables. `Enum.take(src, n)` followed by `Enum.drop(src, n)` enumerates the source twice, while `Enum.split(src, n)` enumerates it once; callbacks, messages, I/O, or stateful results therefore change.
- **lib/pattern/prefer_enum_split.ex** — lib/pattern/prefer_enum_split.ex:63 — Collapsing the two statements through `patches_from_ast_transform/3` replaces their entire source span. A comment between the `Enum.take` and `Enum.drop` assignments lies inside that span and is deleted by the fix.
- **lib/pattern/prefer_enum_split.ex** — lib/pattern/prefer_enum_split.ex:47 — Both walks descend into `quote` blocks. Code that intentionally returns quoted adjacent `Enum.take`/`Enum.drop` assignments is diagnosed and rewritten, changing the AST value produced at runtime rather than optimizing executed code.
- **lib/pattern/prefer_erlang_float.ex** — lib/pattern/prefer_erlang_float.ex:210 — pre-existing: the rule admits operands of any type, but changes failure behavior for non-numbers. For example, inside `try/rescue`, `:bad * 1.0` is catchable as `ArithmeticError`, while the repaired `:erlang.float(:bad)` raises `ArgumentError` and can escape, changing the program’s answer.
- **lib/pattern/prefer_erlang_float.ex** — lib/pattern/prefer_erlang_float.ex:210 — pre-existing: rewriting `x + 0.0` or `0.0 + x` is not exact when `x` is negative zero. The arithmetic produces positive zero, while `:erlang.float(-0.0)` preserves negative zero; code that observes the sign through float serialization therefore changes behavior.
- **lib/pattern/prefer_frequencies_over_group_by.ex** — lib/pattern/prefer_frequencies_over_group_by.ex:127 — The matcher treats every unqualified `length(values)` as `Kernel.length/1`. If the surrounding module defines or imports a different `length/1`, the original pipeline uses that function, but the fix removes the call and computes real frequencies, changing the returned count.
- **lib/pattern/prefer_frequencies_over_group_by.ex** — lib/pattern/prefer_frequencies_over_group_by.ex:108 — The rule assumes the written `Enum` alias means Elixir’s standard `Enum` module. Inside code such as `alias MyEnum, as: Enum`, it rewrites calls on `MyEnum` to `MyEnum.frequencies/1`, which may be missing or have different semantics, causing a compile/runtime failure or a different answer.
- **lib/pattern/prefer_function_capture.ex** — lib/pattern/prefer_function_capture.ex:62 — On a flagged function containing a comment, such as `fn x -> # explain delegation\n to_string(x) end`, the fresh capture AST drops that comment; the shared patch guard therefore rejects the repair, so the rule reports an issue but leaves the source unchanged.
- **lib/pattern/prefer_function_clauses_for_list_patterns.ex** — lib/pattern/prefer_function_clauses_for_list_patterns.ex:407 — pre-existing: removing the `is_list/1` guard makes promoted cons heads accept improper lists such as `[1 | :tail]`; the original guarded clause rejects or falls through for that input, while the rewritten function executes a case branch and can return a different answer.
- **lib/pattern/prefer_function_clauses_for_list_patterns.ex** — lib/pattern/prefer_function_clauses_for_list_patterns.ex:398 — pre-existing: if a case pattern shadows the guarded parameter (for example `[list]`) while the remaining function guard uses the original `list`, the rewrite treats both bindings as one variable; the preserved guard then examines the element instead of the whole input list, changing which clause runs.
- **lib/pattern/prefer_graphemes_for_character_uniqueness.ex** — lib/pattern/prefer_graphemes_for_character_uniqueness.ex:52 — Pre-existing: the check accepts any pipeline containing `String.to_charlist/1` before `Enum.count/1`, without requiring the documented `Enum.uniq/1` chain. For `"a"`, `String.to_charlist(s) |> Enum.filter(&is_integer/1) |> Enum.count() |> (&(&1 == 1)).()` returns `true`, but the fix replaces the integer list with grapheme strings, so the filter removes everything and the result becomes `false`, even though `single_codepoint_graphemes` is satisfied.
- **lib/pattern/prefer_guard_over_if.ex** — lib/pattern/prefer_guard_over_if.ex:520 — The fix renames parameters it considers unused, but macros can observe those bindings without explicit variable AST nodes. For `def f(x) do if x > 0, do: binding(), else: binding() end`, the else clause renames `x` to `_x`, so `binding()` returns `_x: value` instead of `x: value`; custom macros inspecting caller bindings can likewise change behavior.
- **lib/pattern/prefer_lookup_for_digit_conversion.ex** — lib/pattern/prefer_lookup_for_digit_conversion.ex:270 — The diagnostic recommends an unguarded uppercase lookup. Following it for a newly admitted lowercase table changes 10–15 from `"a"`–`"f"` to `"A"`–`"F"`, and for either alphabet changes out-of-range calls from `FunctionClauseError` to `nil` or negative indexing. The automatic repair correctly preserves case and adds a guard, but the user-facing recommendation and moduledoc do not.
- **test/pattern/prefer_lookup_for_digit_conversion_check_test.exs** — test/pattern/prefer_lookup_for_digit_conversion_check_test.exs:214 — The new equivalence test passes fixture source to `call_fixed/4`, which uses raw `Code.compile_string/1`. Compilation executes top-level expressions without the required heap ceiling or deadline, so a hostile or accidentally nonterminating fixture can hang or exhaust the test runner; use the bounded `RuleHelpers.compile_and_capture/1` path.
- **lib/pattern/prefer_map_intersect_over_mapset_intersection.ex** — lib/pattern/prefer_map_intersect_over_mapset_intersection.ex:422 — `mentions_any?/2` replaces the accumulated result for every variable instead of preserving an earlier match. An intervening rebinding such as `freq1 = replacement` ends by visiting `replacement`, resets `found` to false, and is accepted; the original computes keys from the old `freq1` but fetches from the replacement, while the repair computes both from the replacement, changing results or suppressing a `KeyError`.
- **lib/pattern/prefer_map_new_with_transform.ex** — lib/pattern/prefer_map_new_with_transform.ex:51 — The rule admits transforms that can return a non-pair. On `[1, 2]`, if the transform returns `:bad` for `1` and performs a side effect for `2`, `Enum.map/2 |> Map.new/1` performs both transforms before raising, while `Map.new/2` raises on the first element and skips the second side effect, changing observable behavior.
- **lib/pattern/prefer_map_new_with_transform.ex** — lib/pattern/prefer_map_new_with_transform.ex:109 — Matching bare `Enum` and `Map` alias AST does not prove they refer to the standard modules. Inside code that aliases custom modules as `Enum` or `Map`, the fix changes calls from custom `Enum.map/2` plus custom `Map.new/1` to custom `Map.new/2`, which may return a different result or be undefined.
- **lib/pattern/prefer_map_new.ex** — lib/pattern/prefer_map_new.ex:52 — The rule treats any written `Enum` and `Map` aliases as the standard modules. With `alias CustomEnum, as: Enum`, it changes `Enum.into(items, %{})` from `CustomEnum.into/2` to `Elixir.Map.new/1`; with `alias CustomMap, as: Map`, it changes the call to `CustomMap.new/1`. Either input can produce a different result or fail.
- **lib/pattern/prefer_map_new.ex** — lib/pattern/prefer_map_new.ex:46 — A comment attached to the removed `%{}` argument, as in a multiline `Enum.into(items, # target\n%{})`, is discarded by the AST transformation; the shared comment-preservation gate consequently rejects the patch after `check/2` has reported it, leaving the admitted diagnostic unrepaired.
- **lib/pattern/prefer_map_put_new.ex** — lib/pattern/prefer_map_put_new.ex:175 — pre-existing: `pure?/1` treats every bare identifier AST as a variable, but an unbound bare identifier can resolve to a zero-arity function call. For `Map.put(map, key, default)` where `default` is a side-effecting or raising local function written without parentheses, the original calls it only when the key is absent, while the rewrite calls it eagerly even when the key exists.
- **lib/pattern/prefer_mapset_for_set_equality.ex** — lib/pattern/prefer_mapset_for_set_equality.ex:163 — pre-existing: the replacement emits the alias-sensitive name `MapSet`; when the surrounding scope has `alias SomeModule, as: MapSet`, the fixed code calls `SomeModule.new/1` instead of `Elixir.MapSet.new/1`, changing the answer or crashing even though the original code never used that alias.
- **lib/pattern/prefer_multi_clause_reduce_fn.ex** — lib/pattern/prefer_multi_clause_reduce_fn.ex:174 — Renaming an unused first parameter can collide with a variable already present in the second parameter pattern. For example, `fn element, {_element, count} -> ... end` becomes a clause headed `_element, {_element, count}`, which newly requires both arguments to be equal and can select a different clause or raise `FunctionClauseError`.
- **lib/pattern/prefer_negate_if_true_false.ex** — lib/pattern/prefer_negate_if_true_false.ex:67 — pre-existing: nested matching `if` expressions produce overlapping whole-node patches; for example, when an outer matching `if` has another matching `if` as its else body, the outer replacement contains the unchanged inner expression while a second patch targets bytes inside that replacement, so one repair can be lost or the patched source corrupted.
- **lib/pattern/prefer_no_question_mark_for_non_boolean.ex** — lib/pattern/prefer_no_question_mark_for_non_boolean.ex:76 — The fixer renames every AST tuple whose first atom matches the function name, including variables. For a flagged `count?/0`, code such as `count = 1; count? = 2; count + count?` becomes `count = 1; count = 2; count + count`, changing the result from 3 to 4.
- **lib/pattern/prefer_pattern_match_over_conditional_in_recursive_count.ex** — lib/pattern/prefer_pattern_match_over_conditional_in_recursive_count.ex:156 — The safety check admits a count variable that rebinds one of the function’s remaining parameters. For example, `def count([head | tail], target) do target = if head == target, do: 1, else: 0; target + count(tail, target) end` recursively passes the newly computed `0` or `1`, but the generated clauses recursively pass the original `target`, changing the result.
- **lib/pattern/prefer_pipe_mapset_intersection.ex** — lib/pattern/prefer_pipe_mapset_intersection.ex:111 — The rule admits later `MapSet.new/1` arguments that reference earlier assigned sets. For `set_a = MapSet.new([1]); set_b = MapSet.new(set_a); MapSet.intersection(set_a, set_b) |> MapSet.to_list()`, the fix removes both assignments but leaves `MapSet.new(set_a)`, so `set_a` becomes undefined and the replacement does not compile; pipeline validation can only revert it, leaving a reported issue with no repair.
- **lib/pattern/prefer_reduce_while_with_halt_value.ex** — lib/pattern/prefer_reduce_while_with_halt_value.ex:298 — pre-existing: the fix removes the callback accumulator’s third variable without checking whether the body uses it; an admitted callback such as `fn x, {a, b, flag} -> if flag or x > 0, ... end` is rewritten with `flag` unbound, so valid code becomes uncompilable.
- **lib/pattern/prefer_reduce_while_with_halt_value.ex** — lib/pattern/prefer_reduce_while_with_halt_value.ex:334 — pre-existing: replacing `{:halt, {first, second, true}}` with `{:halt, true}` discards evaluation of `first` and `second`; admitted expressions with side effects or exceptions therefore produce a different answer or stop raising.
- **lib/pattern/prefer_reduce_while_with_halt_value.ex** — lib/pattern/prefer_reduce_while_with_halt_value.ex:140 — pre-existing: detection accepts the first top-level `if` anywhere in the callback block, even when later statements determine the callback result, while the fix rewrites every top-level `if`; such admitted callbacks can retain a three-element final accumulator after the parameter and seed were shortened, causing a match failure on the next iteration or a wrong final result.
- **lib/pattern/prefer_reduce_while_with_halt_value.ex** — lib/pattern/prefer_reduce_while_with_halt_value.ex:182 — pre-existing: checking a three-element halt or continue tuple whose last element is not a literal boolean calls `unwrap_boolean/1` with no matching clause; ordinary non-matching input such as `{:halt, {a, b, flag}}` can crash the rule instead of being left unchanged.
- **lib/pattern/prefer_regex_match.ex** — lib/pattern/prefer_regex_match.ex:122 — The matcher accepts any alias spelled `Regex`, including `alias CustomMatcher, as: Regex`; if that module’s `run/2` returns a non-empty list but its `match?/2` returns a different boolean, the rewrite changes the selected branch.
- **lib/pattern/prefer_regex_match.ex** — lib/pattern/prefer_regex_match.ex:158 — The rewrite replaces the `case` special form with an unqualified `if`, whose meaning can be changed by imports; on admitted code importing a custom `if/2` macro, the rewritten program can compile but return a different answer.
- **lib/pattern/prefer_string_slice_for_trim_last_char.ex** — lib/pattern/prefer_string_slice_for_trim_last_char.ex:98 — The widened matcher accepts a case with only `[_last] -> ""` followed by `[_head | _tail] -> String.slice(...)`. On `""`, the original raises `CaseClauseError`, while the replacement returns `""`.
- **lib/pattern/prefer_string_slice_for_trim_last_char.ex** — lib/pattern/prefer_string_slice_for_trim_last_char.ex:132 — A named catch-all can shadow the subject variable, such as `str -> String.slice(str, 0, String.length(str) - 1)`. The original then passes a grapheme list to `String.slice/3` and raises, but the rule rewrites it to a successful slice of the outer string.
- **lib/pattern/prefer_string_slice_for_trim_last_char.ex** — lib/pattern/prefer_string_slice_for_trim_last_char.ex:177 — Pre-existing: the same shadowing bug exists for a cons pattern such as `[str | tail] -> String.slice(str, 0, String.length(str) - 1)`. The original slices only the newly bound first grapheme, while the replacement slices the outer string, changing the answer.
- **test/pattern/prefer_string_slice_for_trim_last_char_check_test.exs** — test/pattern/prefer_string_slice_for_trim_last_char_check_test.exs:171 — `call_fixed/4` compiles fixture and repaired source through raw `Code.compile_string/1`, so top-level expressions introduced by a faulty repair execute without the required heap limit or deadline and can hang or exhaust the test runner; use the bounded `RuleHelpers.compile_and_capture/1` path.
- **lib/pattern/prefer_string_split_trim.ex** — lib/pattern/prefer_string_split_trim.ex:110 — The matcher treats any lexical alias written as `String` as Elixir’s standard `String` module. With `alias MySplitter, as: String`, it rewrites `MySplitter.split/2 |> Enum.filter/2` into `MySplitter.split/3` with `trim: true`; that custom function need not have the same semantics and may return a different answer or crash.
- **lib/pattern/prefer_then_over_capture_invocation.ex** — lib/pattern/prefer_then_over_capture_invocation.ex:81 — The replacement calls unqualified `then/2`, not `Kernel.then/2`. In a module that excludes Kernel’s import and defines its own `then/2`, the repaired pipeline invokes that local function and can return a different answer.
- **lib/pattern/prefer_then_over_capture_invocation.ex** — lib/pattern/prefer_then_over_capture_invocation.ex:77 — The patch assumes the opening parenthesis is exactly one column before `&`. Valid input such as `x |> (  &(&1 + 1)).()` is still reported, but the patch starts in whitespace, leaves the opening parenthesis behind, and is rejected as invalid, so the advertised repair never occurs.
- **lib/pattern/prefer_tuple_destructure_after_with_index.ex** — lib/pattern/prefer_tuple_destructure_after_with_index.ex:101 — The matcher treats every written `Enum` alias as the standard library module. Under `alias MyEnum, as: Enum`, valid custom `with_index`/`map` calls can be rewritten from a two-argument callback to a one-argument tuple callback, changing behavior or causing an arity crash.
- **lib/pattern/prefer_tuple_destructure_after_with_index.ex** — lib/pattern/prefer_tuple_destructure_after_with_index.ex:137 — A comment between the two function parameters lies inside the replacement range and is deleted. The shared comment-preservation guard rejects the patch, but the rule still reports the finding, so `fn row, # note\n index ->` remains broken and violates the fix-or-drop contract.
- **test/pattern/prefer_zip_with_over_zip_then_count_equivalence_test.exs** — test/pattern/prefer_zip_with_over_zip_then_count_equivalence_test.exs:16 — The battery uses only eager lists and compares final values, so it misses an admitted behavioral change: `Enum.zip/2` consumes both enumerables before running the count predicate, while `Enum.zip_with/3` interleaves enumeration and predicate calls. With a source that raises on its second element and a predicate that raises with a different exception on the first pair, the original and repaired expressions raise different exceptions; this test still presents the rewrite as equivalent.
- **lib/pattern/prefer_zip_with_over_zip_then_count.ex** — lib/pattern/prefer_zip_with_over_zip_then_count.ex:112 — The rule admits arbitrary enumerables and predicate bodies, but the rewrite changes evaluation order: `Enum.zip/2` consumes the zipped inputs before `Enum.count/2` runs its predicate, while `Enum.zip_with/3` runs the predicate during input consumption. If a later stream element raises and the predicate also raises on the first pair, the original reports the stream error but the rewritten code reports the predicate error; side effects and resource consumption can likewise differ.
- **lib/pattern/redundant_list_guard.ex** — lib/pattern/redundant_list_guard.ex:161 — The check finds `is_list(t)` anywhere inside a guard, including negated or compared forms such as `when not is_list(t)` and `when is_list(t) == false`. Under `proper_lists` these guards are always false, not removable; nevertheless the rule reports that removing them is safe, while `simplify_guard/2` leaves them unchanged, so the pipeline emits a false finding and records an unrepaired no-op.
- **lib/pattern/remove_unreachable_clauses_after_catchall.ex** — lib/pattern/remove_unreachable_clauses_after_catchall.ex:121 — pre-existing: `filter_def_nodes/1` discards every non-`def` statement before grouping clauses, but `delete_patch/1` deletes the entire source range between the clauses. On input such as `def f(x), do: x; IO.puts("side effect"); def f(_), do: nil`, the rule treats the two `f/1` clauses as consecutive and deletes both the unreachable clause and the intervening executable expression, changing program behaviour.
- **lib/pattern/unnecessary_grapheme_chunking.ex** — lib/pattern/unnecessary_grapheme_chunking.ex:161 — The replacement evaluates both the pipeline subject and chunk-size expression multiple times, while the original evaluates each once. On stateful, nondeterministic, or side-effecting expressions, the fixed code can return different chunks or repeat effects.
- **lib/pattern/unnecessary_grapheme_chunking.ex** — lib/pattern/unnecessary_grapheme_chunking.ex:169 — The replacement introduces a non-hygienic variable named `i`. If the original subject or chunk-size expression refers to an outer `i`, occurrences placed in the comprehension body resolve to the generated integer instead, producing a wrong answer or a runtime error.
- **lib/pattern/unnecessary_grapheme_chunking.ex** — lib/pattern/unnecessary_grapheme_chunking.ex:133 — The anonymous-function match ignores whether `Enum.join` receives the function argument, and the catch-all accepts any function whose printed AST merely contains `Enum.join`. For example, `Enum.map(fn x -> Enum.join(other) end)` is reported and rewritten even though it returns the same unrelated join for every chunk.
- **lib/pattern/use_map_join.ex** — lib/pattern/use_map_join.ex:71 — The rewrite changes evaluation order on valid inputs. In `Enum.map(xs, mapper_expr) |> Enum.join(separator_expr)`, mapping finishes before `separator_expr` is evaluated; the generated `Enum.map_join(xs, separator_expr, mapper_expr)` evaluates the separator first and interleaves mapping with string conversion. Side effects or exceptions therefore occur in a different order.
- **lib/pattern/use_map_join.ex** — lib/pattern/use_map_join.ex:113 — `remote_call?/3` ignores arity, so parsed calls such as `Enum.map() |> Enum.join()` are reported, then `extract_mapper/1` raises because its argument list matches no clause. Production crash isolation skips the rule, leaving its own diagnostic unrepaired.
- **lib/credence.ex** — lib/credence.ex:138 — pre-existing: `rule_status/1` ignores `:syntax_rules` and `:semantic_rules`, always reports every default rule as enabled, and `enabled_rules/1` inherits that wrong answer; on those admitted options, `fix/2` actually runs only the supplied rule lists.
- **lib/credence/corpus/analysis_cache.ex** — lib/credence/corpus/analysis_cache.ex:67 — The cache sorts the explicit `rules:` list, but `Credence.Pattern.analyze/2` runs explicit rules in caller order. Calls with reversed rule lists therefore produce differently ordered findings on a cache miss but share one cache entry, so the answer depends on which call ran first.
- **lib/credence/corpus/budget.ex** — lib/credence/corpus/budget.ex:278 — A grandfathered rule can fall from its adoption ceiling and later grow back to that ceiling without any violation. For example, a rule grandfathered at 500 may drop to 200 and then return to 500; `over_grandfather/2` only compares against the original 500, so the claimed one-way paydown ratchet has refill room and can mask renewed over-firing.
- **lib/credence/mutation.ex** — lib/credence/mutation.ex:358 — Passing `cap: 0` or any negative integer, including through the documented CLI’s `--cap`, matches no `apply_cap/2` clause and crashes the mutation sweep instead of returning zero mutants or rejecting the option clearly.
- **lib/credence/mutation/sweep.ex** — lib/credence/mutation/sweep.ex:132 — The subprocess has a wall-clock limit but no heap or output ceiling. Compiling a mutant executes top-level expressions, so a mutant that allocates indefinitely—or continuously prints while `System.cmd/3` buffers its output—can exhaust the host before `timeout(1)` kills it.
- **lib/credence/mutation/sweep.ex** — lib/credence/mutation/sweep.ex:94 — A non-empty `test_files` list whose files define zero runnable tests is accepted as a green baseline. Every valid mutant is then classified as survived, producing a misleading 0.0 kill rate from a vacuous test battery.
- **lib/dsl_guard.ex** — lib/dsl_guard.ex:300 — pre-existing: every qualified call is resolved through an alias keyed only by its final segment, so a file containing `alias MyApp.Query` causes an explicit `Ecto.Query.from(...)` call to be interpreted as `MyApp.Query.from(...)`; the DSL block is not guarded, allowing Pattern fixes inside the Ecto expression to change its meaning or make it fail to compile.
- **lib/mix/tasks/credence.corpus.ex** — lib/mix/tasks/credence.corpus.ex:180 — `--only-rule` accepts a partially fetched corpus because it raises only when every entry is missing. For a new rule with no snapshot entries and no findings in the few fetched packages, it prints `RESULT=clean` without scanning the missing packages, potentially hiding over-firing there.
- **lib/mix/tasks/credence.equiv.ex** — lib/mix/tasks/credence.equiv.ex:190 — With `--compare-messages`, exceptions are represented as `{:raise, module, message}`, but `raised?/1` recognizes only two-element tuples. If every admitted input raises identically, the task therefore still reports EQUIVALENT instead of SKIPPED; the same tuple-shape bug also prevents REPAIR classification.
- **lib/mix/tasks/credence.equiv.ex** — lib/mix/tasks/credence.equiv.ex:185 — The widened `repair?/1` accepts every input where the original raises without checking what the replacement does. For example, if both versions return the same value on one input but raise different exception classes on another, the task reports REPAIR even though its own equivalence policy defines that exception-class change as a divergence.
- **lib/mix/tasks/credence.equiv.ex** — lib/mix/tasks/credence.equiv.ex:124 — pre-existing: user-supplied expressions are executed directly through the unbounded `BehaviourEquivalence.eval_outcome/2`; an expression such as `Stream.cycle([1]) |> Enum.to_list()` never returns and hangs the Mix task instead of being stopped by a deadline and heap ceiling.
- **lib/mix/tasks/credence.equiv.ex** — lib/mix/tasks/credence.equiv.ex:223 — pre-existing: `--inputs-file` evaluates the file as arbitrary Elixir in the Mix-task VM with raw `Code.eval_string/1`; a nonterminating or memory-exhausting top-level expression can hang or kill the task.
- **lib/mix/tasks/credence.fix_tests.ex** — lib/mix/tasks/credence.fix_tests.ex:291 — `escape_heredoc/1` does not escape `"""`; when a rule’s real output contains an Elixir heredoc, the generated delimiter closes `expected` early and `fix_file/1` writes a syntactically invalid or meaning-changed test file without validating the patched source.
- **lib/mix/tasks/credence.mutants.ex** — lib/mix/tasks/credence.mutants.ex:97 — Parse errors are discarded. A misspelled filter such as `--rul no_manual_max` is silently ignored, so the task sweeps every rule and publishes corpus-wide results instead of rejecting the invalid command.
- **lib/mix/tasks/credence.mutants.ex** — lib/mix/tasks/credence.mutants.ex:251 — Independent rounded layer quotas can sum to less than the requested sample; for example, with the current layer proportions `--sample 8` selects only seven rules, contradicting the requested sample size and the reproducibility claim.
- **lib/mix/tasks/credence.mutants.ex** — lib/mix/tasks/credence.mutants.ex:433 — Survivor source context is inserted into a Markdown table without escaping `|`; a surviving mutant on a line containing common Elixir syntax such as `|>` breaks the row into extra columns and corrupts the triage report.
- **lib/pattern.ex** — lib/pattern.ex:125 — `fix_with_trace/2` calls each rule’s `check/2` directly, outside `isolate/4`; a rule whose check raises, throws, or exits still crashes the entire fix pipeline and prevents later healthy rules from running.
- **lib/pattern.ex** — lib/pattern.ex:269 — compile errors are compared as a set of message strings, so if the input already has one compile error and a fix introduces another error with the same message at a different location, the duplicate collapses and the unsafe fix is accepted.
- **lib/rule_helpers.ex** — lib/rule_helpers.ex:270 — The heap ceiling and timeout cover only the compiler process, not processes started by top-level source. Input such as `spawn(fn -> Enum.flat_map(1..10, &Stream.cycle([&1])) end)` finishes compiling while leaving an unbounded child alive, so arbitrary analyzed code can still exhaust the VM after `compile_and_capture/1` returns.
- **lib/rule_helpers.ex** — lib/rule_helpers.ex:217 — The compile lock uses the complete set of module names as one key, so overlapping sets do not contend. Concurrent sources defining `{A, B}` and `{A}` acquire different locks, then compile and delete `A` concurrently, recreating the false-negative/module-removal race this lock is intended to prevent.
- **lib/rule_helpers.ex** — lib/rule_helpers.ex:442 — `compile_errors/1` returns an empty set for every `{:ok, diagnostics}` result, even though successful compilation can include `severity: :error` diagnostics such as an unknown struct key in a pattern. Consequently `compiles_no_worse?/2` accepts a Pattern fix that introduces such an error into previously clean code.
- **lib/rule_helpers.ex** — lib/rule_helpers.ex:450 — Compile errors are reduced to a set of message strings, discarding multiplicity. If a broken source has one error and a fix duplicates that same error elsewhere, the before and after sets are identical, so `compiles_no_worse?/2` accepts code with an additional compile error.
- **lib/semantic.ex** — lib/semantic.ex:552 — Semantic dispatch now tries every rule matching one diagnostic until a fix changes the source. This violates priority ownership: when the designated specific rule conservatively declines an input, a lower-priority catch-all can rewrite it anyway, potentially applying the wrong repair instead of leaving uncertain code unchanged.
- **lib/semantic.ex** — lib/semantic.ex:556 — pre-existing: calls to semantic rule callbacks have no crash isolation. A matching rule whose `fix/2` raises, throws, or exits crashes the whole pipeline; `should_report?/2` has the same problem during analysis at line 521.
- **lib/source_mask.ex** — lib/source_mask.ex:343 — Paired sigils can nest, but the scanner closes at the first matching delimiter. For `~s(outer (a % b) tail)`, `a % b` is exposed as code, so a rule can rewrite bytes inside the sigil.
- **lib/source_mask.ex** — lib/source_mask.ex:347 — After a sigil’s closing delimiter, its modifiers are scanned as ordinary code. For inputs such as `~r/foo/iu`, a code replacement matching `i` or `u` can corrupt the sigil modifiers even though `mask/1` promises to blank the entire sigil.
- **lib/syntax.ex** — lib/syntax.ex:116 — pre-existing: a syntax rule whose `fix/1` raises, exits, or returns a non-string crashes the entire repair pipeline instead of being isolated and recorded as `{rule, :crashed}`; one defective rule therefore prevents every later syntax rule from repairing the file.
- **test/behaviour_equivalence_self_test.exs** — test/behaviour_equivalence_self_test.exs:187 — The over-match control uses `{1, 2, 3, 4}`, which fails the normalizer’s module/function atom checks and never tests the dangerous boundary. Ordinary returned data such as `[{Foo, :bar, 1, :left}]` is treated as a stacktrace; if a rewrite changes `:left` to `:right`, both results collapse to `:__stacktrace__` and the equivalence checker falsely approves the behavior change.
- **test/comment_preservation_test.exs** — test/comment_preservation_test.exs:128 — pre-existing: `safe_fix/2` converts every rule-fix exception into unchanged source. If a fix crashes on an original fixture, `fires?/2` skips it; if it crashes only after comment injection, the retained probe token is treated as success. The meta-gate therefore silently masks fixes that crash on commented code.
- **test/concurrent_analysis_test.exs** — test/concurrent_analysis_test.exs:33 — Both fixtures have exactly the same single-module set, so this regression test misses partial overlap. If one source defines `Shared` plus `AOnly` and another defines `Shared` plus `BOnly`, the current composite lock keys differ; both compilations can race on `Shared` and silently lose diagnostics.
- **test/corpus/corpus_task_test.exs** — test/corpus/corpus_task_test.exs:20 — No test invokes `CorpusTask.run/1`, so the advertised `--only-rule` command-line gate could stop dispatching to these helpers, report clean, or return success on drift while every test here still passes.
- **test/corpus/corpus_task_test.exs** — test/corpus/corpus_task_test.exs:81 — `assert_enabled!/3` is not tested with a missing status entry; on `statuses = []` the implementation returns `:ok`, allowing a scoped scan to proceed without proving the requested rule is enabled and potentially report a vacuous clean result.
- **test/corpus/findings_budget_test.exs** — test/corpus/findings_budget_test.exs:139 — The grandfather ceilings do not ratchet down after a partial paydown: if a rule falls from 521 findings to 400 while remaining above the cap, every test passes with the ceiling still at 521, and the rule can later grow back to 521 undetected. This contradicts the stated “ratchet down only” policy and makes partial debt reductions refillable.
- **test/corpus/fix_safety_test.exs** — test/corpus/fix_safety_test.exs:292 — pre-existing: `safe_fix/2` rescues every exception and returns the original source, so an accepted rule whose fix crashes is treated as a harmless unchanged fix and this safety gate passes, masking the defect.
- **test/credence_equiv_task_test.exs** — test/credence_equiv_task_test.exs:46 — Verdicts are checked with substring matches instead of exact whole-string assertions, so wrong output such as `NOT EQUIVALENT` or `REPAIR FAILED` would satisfy several tests and let a classification regression pass.
- **test/credence_pipeline_test.exs** — test/credence_pipeline_test.exs:35 — pre-existing: `code_compiles?/1` passes fixture text to raw `Code.compile_string/2`, which executes top-level expressions; a hostile or accidentally non-terminating fixture can hang or exhaust the test process instead of being bounded by `RuleHelpers.compile_and_capture/1`.
- **test/dispatch_contention_test.exs** — test/dispatch_contention_test.exs:47 — A captured diagnostic claimed by two rules passes whenever the first rule has a lower priority. The Semantic dispatcher still prevents the second rule from running, violating the one-diagnostic/one-owner policy; this gate therefore masks overlapping `match?/1` predicates instead of requiring them to be made disjoint or merged.
- **test/dsl_safety_classification_test.exs** — test/dsl_safety_classification_test.exs:117 — `no_defp_already_defined_as_def` is a Semantic rule, but the oracle scans only `test/pattern/*_fix_test.exs`; this new allowlist entry is never exercised, so the test provides no evidence that its fix is DSL-safe.
- **test/dsl_safety_classification_test.exs** — test/dsl_safety_classification_test.exs:58 — `prefer_stdlib_gcd` and `no_list_delete_at_with_length` have no matching rule or fix-test file, while the stale-entry check explicitly ignores missing entries; these dead exemptions pass forever and would automatically exempt any future Pattern rules created under those names.
- **test/dsl_safety_classification_test.exs** — test/dsl_safety_classification_test.exs:242 — pre-existing: an exception from a rule fix is converted into unchanged output, making its construct delta empty; the classification gate therefore silently passes precisely when it failed to evaluate a rule.
- **test/dsl_static_scan_test.exs** — test/dsl_static_scan_test.exs:92 — The empty-ledger gate claims fabricated-rule controls and population floors, but defines neither. If construct detection, reachability, anchoring, or declaration classification breaks and every live rule becomes `:no_construct` or otherwise non-flagged, all tests still pass, silently disabling the DSL-safety gate.
- **test/equivalence_dimension_meta_test.exs** — test/equivalence_dimension_meta_test.exs:145 — An equivalence test can use `inputs: local_variable`; evaluation returns `:error`, the rule becomes unjudgeable, and both dimension gates silently exclude it, allowing dimension coverage to be bypassed.
- **test/equivalence_dimension_meta_test.exs** — test/equivalence_dimension_meta_test.exs:170 — The value-kind gate excludes any sensitive rule whose chosen test inputs contain no number, so an author can evade the required `1`/`1.0` trap by supplying only strings even when the rule admits numeric collections; the grapheme gate has the same circular loophole for inputs containing no string.
- **test/equivalence_dimension_meta_test.exs** — test/equivalence_dimension_meta_test.exs:181 — The discovery control compares `rules()` with `Pattern.default_rules()`, but both call the same `discover_rules` implementation, so losing any rules from discovery produces equal counts and passes while those rules receive no dimension review.
- **test/equivalence_dimension_meta_test.exs** — test/equivalence_dimension_meta_test.exs:199 — The class-population control checks only that each class retains one subject. If trigger parsing silently stops recognizing every other applicable rule, this test and both compliance gates still pass, despite the moduledoc claiming the populations are pinned.
- **test/equivalence_inputs_test.exs** — test/equivalence_inputs_test.exs:93 — `===` does not distinguish `-0.0` from `0.0`; both `Enum.find` calls select the same zero value, so the final `refute` fails even when the battery contains negative zero.
- **test/equivalence_inputs_test.exs** — test/equivalence_inputs_test.exs:18 — the predicate requires only a two-entry map containing key `1`, not key `1.0`, while the remaining assertions compare unrelated synthetic values; a battery with `%{1 => :int, other: :value}` would pass without the promised key-identity trap.
- **test/equivalence_inputs_test.exs** — test/equivalence_inputs_test.exs:132 — the registry regex never reads `@struct_dims`, so removing `Credence.EquivalenceInputs.structs/0` would leave a selectable `:structs` dimension broken while this test still passes.
- **test/fix_byte_scope_test.exs** — test/fix_byte_scope_test.exs:107 — This control explicitly blesses silently skipping a rule whose `fix/2` raises. On any witness diagnostic that triggers such a failure, the byte-scope gate reports no offender and can pass without examining the rule’s edit behavior, masking both the crashing repair and any non-code-byte corruption behind it.
- **test/fix_meta_test.exs** — test/fix_meta_test.exs:56 — pre-existing: if rule discovery regresses or returns no rules, `analyses/0` is empty and all four meta-gates pass vacuously, so no fix test is validated at all.
- **test/fix_or_drop_test.exs** — test/fix_or_drop_test.exs:195 — The “fixes” control always reports the same finding even after its rename, yet the test accepts any byte change as `:fixed`. Consequently the gate misses a rule that edits an unrelated occurrence while leaving the reported problem intact.
- **test/fix_or_drop_test.exs** — test/fix_or_drop_test.exs:208 — The candidate-index control checks only repository-wide floors. A newly added default rule with zero discovered fixtures leaves both totals above those floors, so its sweep is empty and an entirely inert rule ships undetected.
- **test/fix_tests_task_test.exs** — test/fix_tests_task_test.exs:163 — `expected_runtime_value/1` executes source extracted from a fixture with raw `Code.eval_string/3`. If the selected `expected` expression contains interpolation or another executable expression, the test process runs it without the project’s heap and deadline bounds; fixture evaluation must use `RuleHelpers.compile_and_capture/1` or avoid evaluation entirely.
- **test/mutation_sweep_test.exs** — test/mutation_sweep_test.exs:37 — The test says an unscorable count must return `nil`, but `refute` also accepts `false`, so a regression returning the wrong sentinel would pass; line 73 repeats the same weak assertion. Use exact `== nil` assertions.
- **test/no_parser_calls_in_rule_tests_test.exs** — test/no_parser_calls_in_rule_tests_test.exs:41 — pre-existing: the gate misses fully qualified calls such as `Elixir.Code.compile_string(fixture)`, because `parser_ref?/1` only recognizes one-part aliases; such a rule test passes this safety gate while compiling and executing an arbitrary fixture directly.
- **test/pattern_broken_source_test.exs** — test/pattern_broken_source_test.exs:45 — Both end-to-end repair assertions use substring matching (also line 57), so they pass if the rule makes the expected replacement while also deleting or corrupting unrelated source; assert the exact whole output instead.
- **test/pattern_patch_scope_test.exs** — test/pattern_patch_scope_test.exs:93 — The test requires a rule whose `fix_patches/2` crashes to be silently skipped. If a real rule crashes on every witness, the scope gate reports no offender and passes without inspecting that rule, masking exactly the defect the meta-gate is meant to detect.
- **test/pattern_patch_scope_test.exs** — test/pattern_patch_scope_test.exs:77 — The only positive control cuts a double-quoted string, although the gate claims to cover strings, charlists, sigils, heredocs, and comments. If masking or boundary handling for any untested construct breaks, an empty ledger still passes while patches can split that construct.
- **test/rule_card_test.exs** — test/rule_card_test.exs:276 — An unparsable `## Good` example or one that crashes its rule returns `:unparsable`/`:crash`, but the test rejects only `true`; malformed “correct” examples therefore pass the gate.
- **test/rule_crash_isolation_test.exs** — test/rule_crash_isolation_test.exs:136 — The fix-path tests cover only a crash in `fix_patches/2`, not in `check/2`. `Credence.Pattern.fix_with_trace/2` calls each rule’s `check/2` directly, so using `CrashingCheckRule` during a fix raises and aborts the whole call before `HealthyRule` can run, contradicting the file’s crash-isolation guarantee.
- **test/rule_crash_isolation_test.exs** — test/rule_crash_isolation_test.exs:151 — The healthy-rule result is checked only with `code =~ "y = 2"`, so the test still passes if the fix also changes or corrupts other code. Assert the exact complete expected source instead.
- **test/rule_duplication_test.exs** — test/rule_duplication_test.exs:111 — The gate permanently exempts every pair in `@ledger` and the stale-entry test only checks that each pair still scores. If either ledgered rule later changes until both fire on the same inputs, the pair has become a real duplicate but both tests remain green, masking the defect.
- **test/rule_helpers_ast_diff_test.exs** — test/rule_helpers_ast_diff_test.exs:81 — Most regression cases use substring assertions, so they still pass if the differ rewrites unrelated bytes or appends corrupt output; assert each complete patched source with `==` (especially the nested, empty-list, non-list, and `rewrap_list/2` cases).
- **test/rule_self_repair_test.exs** — test/rule_self_repair_test.exs:80 — The gate treats an integer trace entry as proof that the documented anti-pattern did not survive, but never checks the final source. If a later rule recreates the anti-pattern—or the pipeline oscillates back to the original text—the test passes even though users receive unrepaired code; the Semantic probe at line 205 has the same flaw.
- **test/rule_self_repair_test.exs** — test/rule_self_repair_test.exs:125 — The cascade control accepts any trace entry from the expected rule, including `:reverted`, `:patch_rejected`, `:crashed`, or `:no_op`. If another rule changes unrelated text, the first test labels the input `:repaired_by_another` and this control passes, masking a cascade rule that repaired nothing.
- **test/self_corruption_test.exs** — test/self_corruption_test.exs:236 — The controls never exercise a fix that only adds or removes trailing newline bytes. The oracle’s line differ uses `Enum.zip/2`, which discards unmatched trailing elements, so such a rule reports zero changed lines and the empty-ledger gate passes despite rewriting its own source.
- **test/semantic_pass_revert_test.exs** — test/semantic_pass_revert_test.exs:357 — The per-pass acceptance test checks only a few substrings and compilation, so it still passes if replay changes or deletes unrelated source bytes while producing compilable code; compare the entire expected output byte-for-byte to prevent the safety gate from masking corruption.
- **test/semantic_rule_card_test.exs** — test/semantic_rule_card_test.exs:123 — The Good-example gate has no population floor, so if extraction regresses and returns no Semantic `## Good` examples, the comprehension is empty and the test passes without checking anything.
- **test/semantic_rule_card_test.exs** — test/semantic_rule_card_test.exs:128 — `reports?/2` converts compilation aborts and analysis crashes into `false`, so an unanalysable Good example is accepted as valid; unlike the Bad-example gate, this path never calls `aborted?/1` to distinguish “does not report” from “could not be analysed.”
- **test/support/alpha_rename.ex** — test/support/alpha_rename.ex:59 — `protected_names/1` globally exempts every variable sharing a name with any function or module attribute in the fixture. For example, in `def f(f), do: f`, the argument and body variable remain `f`, so a rule over-fitted to that variable name can pass the alpha-renaming gate undetected.
- **test/support/alpha_rename.ex** — test/support/alpha_rename.ex:151 — the gate only reports a fixture when renaming removes every diagnostic. If a rule reports two constructs and only one matcher is wrongly keyed to a variable name, the remaining diagnostic keeps `fires?/2` true and the name-dependent defect is silently missed.
- **test/support/behaviour_equivalence.ex** — test/support/behaviour_equivalence.ex:264 — `normalize_traces/1` treats any non-empty list of `{atom, atom, integer_or_list, term}` tuples as a stacktrace. Ordinary user results with that valid shape are collapsed to `:__stacktrace__`, so two behaviorally different before/after results can compare equal and a corrupting rule can pass its equivalence test.
- **test/support/behaviour_equivalence.ex** — test/support/behaviour_equivalence.ex:385 — the AST rename rewrites every short alias matching the module header without resolving alias scope. For input such as `defmodule Example do; alias External.Example; def run, do: Example.run(); end`, it changes the external call into a self-call, so the harness tests behavior different from the supplied module and may falsely pass, fail, or recurse.
- **test/support/behaviour_equivalence.ex** — test/support/behaviour_equivalence.ex:389 — fixture modules are executed through raw `Code.eval_quoted/1` without the bounded `RuleHelpers.compile_and_capture/1`; a fixture containing a nonterminating or memory-exhausting top-level expression can hang or take down the test pipeline. The same issue is pre-existing for expression fixtures at line 356.
- **test/support/dispatch_contention.ex** — test/support/dispatch_contention.ex:71 — `claimers/2` catches exceptions from `match?/1` and treats them as declines, but the Semantic dispatcher has no such per-rule crash isolation. If a live rule raises on a captured diagnostic, this gate hides it while the real pipeline crashes.
- **test/support/dsl_static_scan.ex** — test/support/dsl_static_scan.ex:431 — Reachability scans attributes referenced directly by `fix_patches/2` or a reached helper, but never follows references or local calls inside those attribute values. A rule whose replacement is stored through chained attributes, such as `@replacement @dsl_ast`, can build a DSL-reinterpreted construct yet be classified as `:no_construct`, silently disarming the classification gate.
- **test/support/equivalence_inputs.ex** — test/support/equivalence_inputs.ex:248 — The battery claims `0.0` and `-0.0` differ under `===`, but Elixir considers them strictly equal; this input cannot detect sign normalization, and the corresponding `refute neg_zero === zero` test fails.
- **test/support/fix_byte_scope.ex** — test/support/fix_byte_scope.ex:151 — A fix that rewrites a literal and also inserts or deletes an adjacent line produces unequal `:del`/`:ins` runs, which `pair_runs/4` discards entirely; the byte-scope gate then passes despite corrupted non-code bytes.
- **test/support/fix_byte_scope.ex** — test/support/fix_byte_scope.ex:79 — Only the first diagnostic claimed by a rule is tested for each fixture. If the same fixture has multiple claimed diagnostics and corruption occurs only when `fix/2` receives a later diagnostic, the gate never exercises that repair and reports no hit.
- **test/support/fix_or_drop.ex** — test/support/fix_or_drop.ex:89 — `probe/2` calls any byte change `:fixed` without checking that the rule’s reported finding disappeared. A rule can edit an unrelated occurrence while leaving the original problem unfixed, and the gate will miss it.
- **test/support/fix_or_drop.ex** — test/support/fix_or_drop.ex:133 — `fixtures/1` claims to inspect a rule’s three test files but reads only its check and fix tests, excluding the equivalence test. A no-op exposed only by an equivalence fixture is never probed, so an unfixed finding can escape the gate.
- **test/support/fixture_healer.ex** — test/support/fixture_healer.ex:227 — pre-existing: interpolated fixture strings are executed with unbounded `Code.eval_string/3` here and again in `fixture_values/1`; an input such as `"#{Enum.to_list(Stream.cycle([1]))}"` can hang or exhaust memory while the healer runs before the suite. Use the bounded `RuleHelpers.compile_and_capture/1` path.
- **test/support/idempotency.ex** — test/support/idempotency.ex:166 — Fixture extraction evaluates interpolated strings with unrestricted `Code.eval_string/3`; an interpolation containing a nonterminating or resource-heavy expression executes during the sweep and can hang or exhaust the test VM instead of being bounded by `RuleHelpers.compile_and_capture/1`.
- **test/support/idempotency.ex** — test/support/idempotency.ex:149 — Nested module names are resolved incorrectly: `defmodule Outer do; defmodule Inner do ... end; end` defines `Outer.Inner`, but this records and purges `Inner`. The actual module remains loaded, so later fixtures can compile differently depending on sweep order and the gate can miss a non-idempotent repair.
- **test/support/meta_test_support.ex** — test/support/meta_test_support.ex:416 — `Code.eval_quoted/3` executes every equivalence test’s `inputs:` expression without isolation or a deadline; an input such as `Stream.cycle([1]) |> Enum.to_list()` hangs the test process, while expressions with filesystem or process side effects run during this structural meta-test.
- **test/support/meta_test_support.ex** — test/support/meta_test_support.ex:302 — an unevaluable `inputs:` expression returns `:error`, which the consuming gate treats as “unjudgeable” and exempts; one risky rule can therefore lose all dimension coverage while the suite remains green because the gate only checks that the global judgeable population is non-empty.
- **test/support/pattern_patch_scope.ex** — test/support/pattern_patch_scope.ex:49 — Parse failures and exceptions from `fix_patches/2` are converted to an empty result. If a real rule crashes on every candidate, the scope gate reports no offender and passes without inspecting that rule, masking the defect it is meant to detect.
- **test/support/pipeline_witness.ex** — test/support/pipeline_witness.ex:309 — Raw and escaped sigils are both passed through `forms/1`, which invents an unescaped candidate for `~S` fixtures and a raw candidate for `~s` fixtures. A rule can therefore be marked witnessed by source text that the test fixture never evaluates to, allowing an inert rule to pass this gate.
- **test/support/rule_case.ex** — test/support/rule_case.ex:167 — `call_fixed/4` compiles fixture source with raw `Code.compile_string/1`; top-level input such as `Stream.run(Stream.cycle([:x]))` executes without the project’s heap ceiling or deadline and can hang or exhaust the test runner.
- **test/support/rule_case.ex** — test/support/rule_case.ex:146 — pre-existing: `compiles?/1` likewise executes arbitrary fixture source through raw `Code.compile_string/1`, bypassing the bounded `RuleHelpers.compile_and_capture/1`.
- **test/support/rule_duplication.ex** — test/support/rule_duplication.ex:226 — `firing_sets/2` converts every exception, throw, and exit from `rule.check/2` into “did not fire.” If a rule crashes on its own or distinguishing corpus examples, its firing set can become empty and `duplicate_pairs/3` skips it, so the duplicate gate silently passes instead of exposing that it was disarmed.
- **test/support/self_corruption.ex** — test/support/self_corruption.ex:146 — `Enum.zip/2` truncates to the shorter line list, so a fix that only appends lines or deletes unchanged trailing lines is reported as zero corruption; the supposedly non-vacuous gate can therefore silently miss a destructive rule.
- **test/syntax_round_safety_test.exs** — test/syntax_round_safety_test.exs:263 — The default-rule safety cases only check that repaired fragments occur and that the result parses. On `@two_faults` or the missing-`end` fixture, an additional rule can rewrite unrelated code to different but parseable meaning and these tests still pass; assert the exact whole output and exact applied-rule trace.
- **maintainer_tools/199-no_negative_step_in_string_slice.patch** — maintainer_tools/199-no_negative_step_in_string_slice.patch:35 — The check only reports two-argument calls, so `str |> String.slice(n..-1)` is never diagnosed and therefore never repaired, even though the patch’s fixer explicitly handles that form.
- **maintainer_tools/199-no_negative_step_in_string_slice.patch** — maintainer_tools/199-no_negative_step_in_string_slice.patch:35 — The matcher rewrites `String.slice/2` even when `String` is a lexical alias for a custom module; changing `2..-1` to `2..-1//1` can change the range from descending to empty and give the custom function a different answer.
- **maintainer_tools/pr_review/agent_runner.sh** — maintainer_tools/pr_review/agent_runner.sh:31 — Provider exit failures are ignored because the script lacks `errexit` and never checks the Codex command or Claude pipeline status. If a provider exits nonzero after writing partial output, the nonempty-file check passes and the review campaign treats the failed or incomplete response as successful.
- **maintainer_tools/pr_review/campaign.sh** — maintainer_tools/pr_review/campaign.sh:48 — The completion check counts only `pending` reviews and fixes, so rows marked `error` by either loop are ignored and the script announces “drained — nothing pending to review or fix” even though files were never reviewed or findings remain unresolved.
- **maintainer_tools/pr_review/fix_file_prompt.md** — maintainer_tools/pr_review/fix_file_prompt.md:31 — The required `git status` condition is impossible after a successful fix: line 43 requires source and test changes to remain unstaged in the working tree, so those paths must appear as dirty outside `maintainer_tools/pr_review/`.
- **maintainer_tools/pr_review/fix_file_prompt.md** — maintainer_tools/pr_review/fix_file_prompt.md:50 — The `fixed` outcome says the agent committed the change, contradicting lines 43–45 and 98–99, which prohibit commits and reserve them for the wrapper; an agent following this definition can mutate Git history and leave no working-tree change for the wrapper to validate.
- **maintainer_tools/pr_review/fix_queue.sh** — maintainer_tools/pr_review/fix_queue.sh:113 — The script omits `set -e` and does not check the manifest-reading `jq` command. If `manifest.json` is malformed or unreadable, `rows` becomes empty and `sync` exits successfully without enqueueing any findings, silently disarming the fix pipeline.
- **maintainer_tools/pr_review/generate_manifest.sh** — maintainer_tools/pr_review/generate_manifest.sh:153 — Valid Git paths containing a tab or newline are emitted unquoted by `git diff` and then parsed as tab-separated records; the path is truncated or split into another record, causing manifest generation to fail or describe the wrong file.
- **maintainer_tools/pr_review/generate_merge_packets.sh** — maintainer_tools/pr_review/generate_merge_packets.sh:27 — packets are generated for only seven of the manifest’s twelve categories. All `test_*` and `other` files are omitted, so maintainers never see hundreds of reviewed files or their findings, including active blockers.
- **maintainer_tools/pr_review/generate_merge_packets.sh** — maintainer_tools/pr_review/generate_merge_packets.sh:19 — “Active findings” blindly includes every append-only summary item without consulting `fixes.json`; findings already fixed or refuted are presented as unresolved, giving maintainers incorrect merge evidence.
- **maintainer_tools/pr_review/review_file_prompt.md** — maintainer_tools/pr_review/review_file_prompt.md:13 — The prompt tells reviewers to write a verdict file, but lines 98–99 say not to write any file because the runner captures the final response. A reviewer following the first instruction may attempt an unspecified repository write or fail to return the verdict the runner needs.
- **maintainer_tools/pr_review/review_loop.sh** — maintainer_tools/pr_review/review_loop.sh:320 — Any user or background-process edit made while an agent session is running is mistaken for agent-created dirt and reverted; a newly created untracked directory is recursively deleted and a tracked edit is replaced from the index, silently destroying concurrent work.
- **maintainer_tools/pr_review/run_capped.sh** — maintainer_tools/pr_review/run_capped.sh:31 — when neither user systemd scopes nor the `prlimit`/`numfmt` fallback is available, or when `MEM` is invalid, this safety wrapper deliberately runs the command uncapped; a runaway compile can therefore exhaust machine memory and take down the host it claims to protect.
- **maintainer_tools/pr_review/selftest_manifest.sh** — maintainer_tools/pr_review/selftest_manifest.sh:14 — The script omits `set -e` and does not check `mktemp`; if temporary-directory creation fails, `WORK` is empty and the test continues creating repositories such as `/gate_shape` outside the intended throwaway directory.
- **maintainer_tools/stage_1_promote_fixable_rules/review_loop.sh** — maintainer_tools/stage_1_promote_fixable_rules/review_loop.sh:315 — Semantic and Syntax rows now exclude every test tagged `:corpus`, not merely the Pattern-only files under `test/corpus/`. This skips the rule-specific real-source regression at `test/syntax/close_unclosed_doc_heredoc_fix_test.exs:725`, so a change that breaks that Syntax rule can pass the acceptance gate and be committed and pushed.
- **.github/run-ci-locally.sh** — .github/run-ci-locally.sh:27 — Any argument other than exactly `check` or `all` skips every test and still prints “ALL REQUESTED CI JOBS PASSED” with exit status 0; a typo such as `.github/run-ci-locally.sh al` therefore reports success without running CI.
- **.github/workflows/ci.yml** — .github/workflows/ci.yml:42 — The third-party `erlef/setup-beam` action is pinned only to the mutable `v1` tag in all three jobs. If that tag or upstream repository is compromised, arbitrary code runs in CI; pin the action to a reviewed full commit SHA.
- **docs/12-improvement-proposals.md** — docs/12-improvement-proposals.md:192 — C6 calls a supervised `Task` with a timeout “sandboxed” and bounded, but a timeout does not prevent compile-time OOM and a linked task killed by a heap limit can take down the caller; the repository’s later implementation notes explicitly require an unlinked monitored process plus a heap ceiling, so this proposal could recreate the pipeline-crashing failure it claims to prevent.
- **docs/14-proposal-scrutiny.md** — docs/14-proposal-scrutiny.md:397 — The recommended fix-safety runner compiles dataset solutions and harnesses directly with `Code.compile_file/1`; top-level fixture code therefore executes without the heap ceiling or deadline provided by `RuleHelpers.compile_and_capture/1`, so a nonterminating or memory-hungry fixture can hang or exhaust the machine.
- **docs/18-final-143-disposition.md** — docs/18-final-143-disposition.md:204 — The source-of-truth table says `NoBareReturnInUnless` still silently mis-repairs early returns, although the same document says commit `86ee66b` fixed it and the live rule now restructures to `if/else`; maintainers are therefore directed to act on a resolved corruption defect.
- **docs/18-final-143-disposition.md** — docs/18-final-143-disposition.md:211 — The table and per-rule action claim no live rule fixes same-head/different-body unreachable clauses, contradicting the reconciled conclusion that the live `NonGroupedClauses` plus `RemoveUnreachableClausesAfterCatchall` pair repairs them end-to-end; this can cause redundant or conflicting rule work.
- **docs/research/credence-internals.md** — docs/research/credence-internals.md:4 — The report presents obsolete safety claims as verified current facts: compilation now runs in a heap- and timeout-bounded child process, and the Semantic round now recompiles changed output and reverts attributable regressions. Maintainers relying on §§1.3, 6.3, 6.5, and 6.11 would get the wrong answer about pipeline containment and repair safety.
- **docs/research/prior-art.md** — docs/research/prior-art.md:166 — The recommendation permits fixes to change “undefined/edge behavior,” contradicting Credence’s governing rule that every admitted input must produce exactly the same answer; following it could admit behavior-changing rewrites without a declared safety assumption.
- **docs/research/rule-quality-audit.md** — docs/research/rule-quality-audit.md:45 — The headline, §2B, §5.1, §5.3, rating tally, and final severity ranking repeatedly call `no_sort_then_reverse` and `no_double_sort_same_list` defective and recommend changing them, although the document’s own correction at line 7 says execution refuted both claims; this contradictory audit can send maintainers toward unnecessary or harmful rule changes.
- **STATUS.md** — STATUS.md:46 — The release procedure edits `CHANGELOG.md` and immediately tags and pushes without committing that edit. `v0.8.1` would therefore point to the pre-release commit whose changelog still says `Unreleased`, while the dated changelog remains only in the maintainer’s working tree.
- **priv/mutation_runner.exs** — priv/mutation_runner.exs:29 — `Code.compile_string/2` executes mutant top-level code without a heap ceiling. A mutant that allocates indefinitely can exhaust the machine before the outer wall-clock timeout kills its BEAM, despite the file explicitly admitting heap-blowing mutants; compile through the bounded `Credence.RuleHelpers.compile_and_capture/1` mechanism or launch this BEAM with an equivalent per-process heap limit.
- **lib/semantic/no_hallucinated_guard_fn.ex** — lib/semantic/no_hallucinated_guard_fn.ex:44 — claims every `is_regex/1` guard diagnostic without verifying the function is actually hallucinated; with `defp is_regex(x), do: Regex.match?(~r/a/, x)` and `def f(x) when is_regex(x)`, `FixLocalFunctionInGuard` correctly declines, then semantic fall-through hands ownership here and rewrites the intended string predicate to `is_struct(x, Regex)`, silently changing behavior.
- **lib/semantic.ex** — lib/semantic.ex:460 — diagnostics are captured once and then fixed right-to-left under the assumption that each repair only affects text to its right, but `FixPlugDependencyModuleOrder` can move entire modules; when a later-line missing `Dependency.init/1` diagnostic reorders modules before an earlier `List.max/1` diagnostic is processed, the latter retains its old line number and `UndefinedFunction` may rewrite the wrong line or decline, making one rule’s repair change another rule’s admission and output.
- **lib/source_mask.ex** — lib/source_mask.ex:343 — paired sigil delimiters can nest, but the scanner terminates at the first closing delimiter; `~s(prefix (inner) 100% done)` exposes `100% done)` as code, allowing rules such as `FixPythonModulo` to rewrite bytes inside the sigil when the file is otherwise malformed.
- **lib/rule_helpers.ex** — lib/rule_helpers.ex:276 — compilation remains in-process, so arbitrary analyzed source containing `System.halt(0)` terminates the entire Credence VM despite the child-process timeout and heap limit.
- **lib/rule_helpers.ex** — lib/rule_helpers.ex:270 — the heap and timeout bounds apply only to the compiler child; source such as `spawn(fn -> Stream.repeatedly(fn -> :binary.copy(<<0>>, 1_000_000) end) |> Enum.to_list() end)` compiles successfully and leaves an unbounded orphan consuming memory after the monitored child exits.
- **test/support/behaviour_equivalence.ex** — test/support/behaviour_equivalence.ex:389 — module fixtures are evaluated directly with no timeout or heap ceiling; a fixture with top-level `receive do after :infinity -> :ok end` hangs the suite indefinitely, while allocating or process-spawning fixtures can exhaust the runner.
- **test/support/rule_case.ex** — test/support/rule_case.ex:167 — `call_fixed/4` directly compiles and executes repaired fixture text without containment; a triggering repair that emits an infinite top-level expression or a non-returning target function bypasses `compile_and_capture/1` and can hang or OOM the whole test VM.

