{
  "generated_at": "2026-08-24T18:38:35Z",
  "reviewed": 11,
  "total": 780,
  "counts": {
    "blocker": 15,
    "concern": 27,
    "nit": 68
  },
  "repeated_root_causes": [],
  "items": [
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/close_unclosed_brace.ex",
      "finding": "lib/syntax/close_unclosed_brace.ex:160 — the \"result must parse\" guard only refuses an ambiguous `}` placement when the alternative placement would make the appended candidate unparseable; when the line after the unclosed literal begins with a binary operator both placements parse and mean different things, and the rule silently commits one. On `x = {1, 2` followed by a line `|> IO.inspect()` before `end`, the rule emits `x = {1, 2\\n|> IO.inspect()}` — the tuple `{1, IO.inspect(2)}` — where the author plausibly meant `x = {1, 2}` piped into `IO.inspect`, i.e. `IO.inspect({1, 2})`. The output parses and compiles, so neither ProgressGuard nor the all-or-nothing commit can catch a wrong choice, and no test in the battery covers an operator-continuation line (the \"ambiguous placement\" tests only cover the case where the wrong placement fails to parse). The moduledoc (lines 35–44) says the rule \"stays silent rather than picking a placement\" for either-line ambiguities, which overstates the guard. The tokenizer-faithful placement is a defensible documented policy, but a human should decide whether to also refuse (or at least pin with a test) when the following line starts with an operator, since that is exactly the shape where the LLM's dropped `}` belongs one line up.",
      "signature": "<anchor> — the \"result must parse\" guard only refuses an ambiguous `}` placement when the alternative placement would make the appended candidate unparseable; when the line after the unclosed literal begins with a binary operator both placements parse and mean different things, and the rule silently commits one. on `x = {#, #` followed by a line `|> io.inspect()` before `end`, the rule emits `x = {#, #\\n|> io.inspect()}` — the tuple `{#, io.inspect(#)}` — where the author plausibly meant `x = {#, #}` piped into `io.inspect`, i.e. `io.inspect({#, #})`. the output parses and compiles, so neither progressguard nor the all-or-nothing commit can catch a wrong choice, and no test in the battery covers an operator-continuation line (the \"ambiguous placement\" tests only cover the case where the wrong placement fails to parse). the moduledoc (lines #–#) says the rule \"stays silent rather than picking a placement\" for either-line ambiguities, which overstates the guard. the tokenizer-faithful placement is a defensible documented policy, but a human should decide whether to also refuse (or at least pin with a test) when the following line starts with an operator, since that is exactly the shape where the llm's dropped `}` belongs one line up."
    },
    {
      "severity": "concern",
      "reviewed_path": "test/syntax/close_unclosed_brace_analyze_test.exs",
      "finding": "test/syntax/close_unclosed_brace_analyze_test.exs:98 — the negative battery only covers ambiguous placements where appending `}` fails to parse, so it presents the rule's guard as stronger than it is; the untested case is a continuation line that starts with an operator, e.g. `x = {:ok, v` / `|> to_string()` / `end`, where appending the brace to the last content line yields `{:ok, v |> to_string()}` — that parses, so analyze flags it and the fix commits it, silently turning the author's likely `{:ok, v} |> to_string()` into a different program. No test in this file (or the fix file) pins that behaviour either way.",
      "signature": "<anchor> — the negative battery only covers ambiguous placements where appending `}` fails to parse, so it presents the rule's guard as stronger than it is; the untested case is a continuation line that starts with an operator, e.g. `x = {:ok, v` / `|> to_string()` / `end`, where appending the brace to the last content line yields `{:ok, v |> to_string()}` — that parses, so analyze flags it and the fix commits it, silently turning the author's likely `{:ok, v} |> to_string()` into a different program. no test in this file (or the fix file) pins that behaviour either way."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_brace_analyze_test.exs",
      "finding": "test/syntax/close_unclosed_brace_analyze_test.exs:35 — nothing pins the `@max_braces 5` backstop from the analyze side: a literal needing six closing braces produces no issue at all, and the deepest input tested anywhere in the pair needs three, so the boundary could move without a test noticing.",
      "signature": "<anchor> — nothing pins the `@max_braces #` backstop from the analyze side: a literal needing six closing braces produces no issue at all, and the deepest input tested anywhere in the pair needs three, so the boundary could move without a test noticing."
    },
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/close_unclosed_brace.ex",
      "finding": "lib/syntax/close_unclosed_brace.ex:186 — the uniqueness guard only ever asks \"would putting *all* the missing braces at the end of one earlier line also parse?\" (`close_at(lines, earlier)` appends `String.duplicate(\"}\", count)` to a single line). It never considers a placement *split* across two lines, so when the repair needs more than one `}` a second, differently-meaning reading can survive the guard unnoticed. Concrete input — `defmodule Example do` / `  def foo do` / `    x = {1, {2` / `    , 3` / `  end` / `end`: the target line `    , 3` takes two braces and yields `{1, {2, 3}}`, which parses, and the only earlier line (`    x = {1, {2`) parses with neither one brace (outer still open) nor two (`, 3` then becomes a standalone statement), so `sole_placement/3` returns `:ok` and the fix commits. The competing reading `{1, {2}, 3}` — inner closed on the first line, outer on the second — is equally faithful to the truncated source and is never tested. Output parses and compiles, so neither ProgressGuard nor the all-or-nothing commit can catch the wrong choice. Leading-comma continuation lines are rare in Elixir, so this is a narrow shape, but it is the same silent-behaviour-change class the guard was added for and the moduledoc's \"placement must be beyond doubt\" bullet (lines 46-58) reads as stronger than what the code checks. A human should decide whether to widen the guard to split placements, or narrow the moduledoc claim to \"single-line placements\".",
      "signature": "<anchor> — the uniqueness guard only ever asks \"would putting *all* the missing braces at the end of one earlier line also parse?\" (`close_at(lines, earlier)` appends `string.duplicate(\"}\", count)` to a single line). it never considers a placement *split* across two lines, so when the repair needs more than one `}` a second, differently-meaning reading can survive the guard unnoticed. concrete input — `defmodule example do` / `  def foo do` / `    x = {#, {#` / `    , #` / `  end` / `end`: the target line `    , #` takes two braces and yields `{#, {#, #}}`, which parses, and the only earlier line (`    x = {#, {#`) parses with neither one brace (outer still open) nor two (`, #` then becomes a standalone statement), so `sole_placement/#` returns `:ok` and the fix commits. the competing reading `{#, {#}, #}` — inner closed on the first line, outer on the second — is equally faithful to the truncated source and is never tested. output parses and compiles, so neither progressguard nor the all-or-nothing commit can catch the wrong choice. leading-comma continuation lines are rare in elixir, so this is a narrow shape, but it is the same silent-behaviour-change class the guard was added for and the moduledoc's \"placement must be beyond doubt\" bullet (lines #-#) reads as stronger than what the code checks. a human should decide whether to widen the guard to split placements, or narrow the moduledoc claim to \"single-line placements\"."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_brace.ex",
      "finding": "lib/syntax/close_unclosed_brace.ex:72 — the moduledoc enumerates \"four guards\" and then says \"the smallest number of `}` that makes the source parse is used\", with no mention that `@max_braces 5` is a hard ceiling: a literal needing six closers is silently refused (pinned by `test/syntax/close_unclosed_brace_analyze_test.exs:156`). A maintainer reading only the moduledoc would expect arbitrary nesting to be repaired. One sentence in the \"safe core\" section, alongside the other refusals, would make the list complete.",
      "signature": "<anchor> — the moduledoc enumerates \"four guards\" and then says \"the smallest number of `}` that makes the source parse is used\", with no mention that `@max_braces #` is a hard ceiling: a literal needing six closers is silently refused (pinned by `<anchor>`). a maintainer reading only the moduledoc would expect arbitrary nesting to be repaired. one sentence in the \"safe core\" section, alongside the other refusals, would make the list complete."
    },
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/close_unclosed_brace.ex",
      "finding": "lib/syntax/close_unclosed_brace.ex:192 — the \"placement must be beyond doubt\" guard only scans lines from `open_line - 1` downward, where `open_line` is whatever `Code.string_to_quoted/2` put in the error's `:line` field. When a literal leaves more than one `{` unclosed and those `{` sit on *different* lines (e.g. `x = {1,` on one line and `%{a: 2` on the next), that field names only one of them — on my reading of the tokenizer it is the innermost, because the error is raised against the top of the delimiter stack. If so, every line between the outer `{` and the inner one is never tried as a competing `}` placement, so on multi-line nested literals the uniqueness guard is weaker than the moduledoc's second bullet (lines 46-60) claims, and the same lines are also invisible to `split_placement?/3` (line 206), which shares the range. Nothing in either test file pins which `{` the parser names: every nested fixture (`..._analyze_test.exs:39`, `:51`; `..._fix_test.exs:145`) opens all of its braces on one line. The consequence is bounded — a `}` on a line before the inner `{` cannot close that inner `{`, and the leftover continuation lines are usually comma-led and would not stand alone as statements — but it is a coverage hole in exactly the guard two fix rounds were spent widening. A second-order effect of the same question: the issue's `meta.line` (line 96) would point at the inner `%{` rather than at the literal the user actually has to close.",
      "signature": "<anchor> — the \"placement must be beyond doubt\" guard only scans lines from `open_line - #` downward, where `open_line` is whatever `code.string_to_quoted/#` put in the error's `:line` field. when a literal leaves more than one `{` unclosed and those `{` sit on *different* lines (e.g. `x = {#,` on one line and `%{a: #` on the next), that field names only one of them — on my reading of the tokenizer it is the innermost, because the error is raised against the top of the delimiter stack. if so, every line between the outer `{` and the inner one is never tried as a competing `}` placement, so on multi-line nested literals the uniqueness guard is weaker than the moduledoc's second bullet (lines #-#) claims, and the same lines are also invisible to `split_placement?/#` (line #), which shares the range. nothing in either test file pins which `{` the parser names: every nested fixture (`<anchor>`, `:#`; `<anchor>`) opens all of its braces on one line. the consequence is bounded — a `}` on a line before the inner `{` cannot close that inner `{`, and the leftover continuation lines are usually comma-led and would not stand alone as statements — but it is a coverage hole in exactly the guard two fix rounds were spent widening. a second-order effect of the same question: the issue's `meta.line` (line #) would point at the inner `%{` rather than at the literal the user actually has to close."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_brace.ex",
      "finding": "lib/syntax/close_unclosed_brace.ex:190 — `repair/1` is quadratic-ish in reparses and the expensive path is the one that commits. For each candidate earlier line, `sole_placement/3` pays up to 5 whole-source `Code.string_to_quoted` calls in `close_at/2` plus up to 4x5 more in `split_placement?/3` — 25 reparses of the entire file per line — and `Enum.any?/2` only short-circuits when the input is ambiguous, i.e. when the rule *refuses*. `Credence.Syntax` then calls `analyze/1` and `fix/1` separately, so the whole scan is paid twice. An LLM-emitted attribute map spanning a few hundred lines whose lines do not end in `,` (values on their own continuation lines) therefore costs several thousand full-file parses before the rule fires. Memoizing `close_at/2` per line, or capping the scanned span, would remove it.",
      "signature": "<anchor> — `repair/#` is quadratic-ish in reparses and the expensive path is the one that commits. for each candidate earlier line, `sole_placement/#` pays up to # whole-source `code.string_to_quoted` calls in `close_at/#` plus up to #x# more in `split_placement?/#` — # reparses of the entire file per line — and `enum.any?/#` only short-circuits when the input is ambiguous, i.e. when the rule *refuses*. `credence.syntax` then calls `analyze/#` and `fix/#` separately, so the whole scan is paid twice. an llm-emitted attribute map spanning a few hundred lines whose lines do not end in `,` (values on their own continuation lines) therefore costs several thousand full-file parses before the rule fires. memoizing `close_at/#` per line, or capping the scanned span, would remove it."
    },
    {
      "severity": "blocker",
      "reviewed_path": "lib/syntax/close_unclosed_brace.ex",
      "finding": "lib/syntax/close_unclosed_brace.ex:223 — the \"placement must be beyond doubt\" guard only ever appends `}` to the *end* of lines *before* the target line, so it is structurally inert whenever the literal sits on one line: for `x = {1, 2 |> IO.inspect()` with `end` on the next line, `target_line/3` picks that same line (index == open_line - 1), the scan range `(start_line - 1)..(index - 1)//1` is empty, and the rule commits `x = {1, 2 |> IO.inspect()}` — even though `x = {1, 2} |> IO.inspect()` also parses and means something else. The rule's own moduledoc and test `test/syntax/close_unclosed_brace_fix_test.exs:242` declare exactly this ambiguity must be refused; it is, but only when the pipe happens to be on the next line. The same hole applies to a mid-line `}` on the target line of a multi-line literal (`x = {1,` / `2 |> IO.inspect()` is committed for the same reason).",
      "signature": "<anchor> — the \"placement must be beyond doubt\" guard only ever appends `}` to the *end* of lines *before* the target line, so it is structurally inert whenever the literal sits on one line: for `x = {#, # |> io.inspect()` with `end` on the next line, `target_line/#` picks that same line (index == open_line - #), the scan range `(start_line - #)..(index - #)//#` is empty, and the rule commits `x = {#, # |> io.inspect()}` — even though `x = {#, #} |> io.inspect()` also parses and means something else. the rule's own moduledoc and test `<anchor>` declare exactly this ambiguity must be refused; it is, but only when the pipe happens to be on the next line. the same hole applies to a mid-line `}` on the target line of a multi-line literal (`x = {#,` / `# |> io.inspect()` is committed for the same reason)."
    },
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/close_unclosed_brace.ex",
      "finding": "lib/syntax/close_unclosed_brace.ex:248 — `competing_placement?/4` enumerates only two-line shares of the `count` braces (earlier line + target line) for one earlier line at a time, so a competing reading that splits the braces across three or more lines of the literal is never tried and the repair would be committed. I could not construct a concrete input where a 3-way split parses and no 2-way split does (every candidate I tried was already caught by the pair enumeration), so this needs a human to judge whether the gap is reachable.",
      "signature": "<anchor> — `competing_placement?/#` enumerates only two-line shares of the `count` braces (earlier line + target line) for one earlier line at a time, so a competing reading that splits the braces across three or more lines of the literal is never tried and the repair would be committed. i could not construct a concrete input where a #-way split parses and no #-way split does (every candidate i tried was already caught by the pair enumeration), so this needs a human to judge whether the gap is reachable."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_brace.ex",
      "finding": "test/syntax/close_unclosed_brace_fix_test.exs:330 — the reparse-blow-up guard is a wall-clock assertion (`best < 1_000_000` µs) sitting between a measured 0.19s and the 2.2s regression it exists to catch, so it both false-fails on a ~5x-loaded machine and passes any future regression under 5x. It also exercises only the `count == 1` shape with half the literal's lines skipped by the dangling-comma rule (~2 reparses/line); the real worst case is `@max_braces + 1` reparses per line (~6x the tested work), which nothing covers.",
      "signature": "<anchor> — the reparse-blow-up guard is a wall-clock assertion (`best < #_#_#` µs) sitting between a measured #.#s and the #.#s regression it exists to catch, so it both false-fails on a ~#x-loaded machine and passes any future regression under #x. it also exercises only the `count == #` shape with half the literal's lines skipped by the dangling-comma rule (~# reparses/line); the real worst case is `@max_braces + #` reparses per line (~#x the tested work), which nothing covers."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_brace_analyze_test.exs",
      "finding": "test/syntax/close_unclosed_brace_analyze_test.exs:270 — \"no issue for a brace that only appears inside a string\" feeds the rule a source that parses cleanly, so it returns `[]` for the same trivial reason as \"leaves code without braces alone\" (the parser reports no mismatched-delimiter error at all). It therefore does not demonstrate the moduledoc's claim that a `{` inside a string/heredoc/comment is never taken for the unclosed one. An input that actually has an unclosed literal *plus* a brace inside a string on another line (e.g. `s = \"{\"` on the line above `x = {1, 2`) would exercise the claim; today nothing in either test file does.",
      "signature": "<anchor> — \"no issue for a brace that only appears inside a string\" feeds the rule a source that parses cleanly, so it returns `[]` for the same trivial reason as \"leaves code without braces alone\" (the parser reports no mismatched-delimiter error at all). it therefore does not demonstrate the moduledoc's claim that a `{` inside a string/heredoc/comment is never taken for the unclosed one. an input that actually has an unclosed literal *plus* a brace inside a string on another line (e.g. `s = \"{\"` on the line above `x = {#, #`) would exercise the claim; today nothing in either test file does."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_brace_analyze_test.exs",
      "finding": "test/syntax/close_unclosed_brace_analyze_test.exs:236 — \"no issue when the mismatched end sits on the opening line\" cannot go red if the guard it appears to pin is removed. With `end_line == open_line`, `target_line/3` builds the range `(end_line - 2)..(open_line - 1)//-1`, which for the equal case is a descending range whose first element is below its last — i.e. empty — so `Enum.find` returns `nil` and the rule refuses anyway, guard or no guard. The test is a correct behavioural assertion, but it is not the regression net for `end_line > open_line` (lib/syntax/close_unclosed_brace.ex:143) that its name suggests.",
      "signature": "<anchor> — \"no issue when the mismatched end sits on the opening line\" cannot go red if the guard it appears to pin is removed. with `end_line == open_line`, `target_line/#` builds the range `(end_line - #)..(open_line - #)//-#`, which for the equal case is a descending range whose first element is below its last — i.e. empty — so `enum.find` returns `nil` and the rule refuses anyway, guard or no guard. the test is a correct behavioural assertion, but it is not the regression net for `end_line > open_line` (<anchor>) that its name suggests."
    },
    {
      "severity": "blocker",
      "reviewed_path": "lib/syntax/close_unclosed_brace.ex",
      "finding": "lib/syntax/close_unclosed_brace.ex:231 — the \"placement must be beyond doubt\" guard can only ever test end-of-line placements on lines *before* the target line, so it is structurally inert whenever the literal opens on the line the `}` is appended to. On `defmodule E do` / `  def foo do` / `    x = {1, 2 |> IO.inspect()` / `  end` / `end`, `target_line/3` picks that same line (index == open_line - 1), the scan range `(start_line - 1)..(index - 1)//1` is empty, `sole_placement/4` returns `:ok` unconditionally, and the rule commits `x = {1, 2 |> IO.inspect()}` — the tuple `{1, IO.inspect(2)}` — although `x = {1, 2} |> IO.inspect()` parses just as well and is a different program. The identical ambiguity spread over two lines is refused (pinned by test/syntax/close_unclosed_brace_fix_test.exs:242), so the rule's answer depends on where the author happened to break the line. The same hole covers any mid-line placement on the target line of a multi-line literal: `x = {1,` / `    2 |> IO.inspect()` / `end` commits for the same reason (the `x = {1,` line is skipped by the trailing-comma rule and no mid-line `}` is ever tried). The moduledoc's \"Only one placement is faithful: at the very end of the literal's last line\" (lines 26-30) states this as policy, but the second guard's worked `|>` example (lines 46-58) promises the rule stays silent for exactly this shape.",
      "signature": "<anchor> — the \"placement must be beyond doubt\" guard can only ever test end-of-line placements on lines *before* the target line, so it is structurally inert whenever the literal opens on the line the `}` is appended to. on `defmodule e do` / `  def foo do` / `    x = {#, # |> io.inspect()` / `  end` / `end`, `target_line/#` picks that same line (index == open_line - #), the scan range `(start_line - #)..(index - #)//#` is empty, `sole_placement/#` returns `:ok` unconditionally, and the rule commits `x = {#, # |> io.inspect()}` — the tuple `{#, io.inspect(#)}` — although `x = {#, #} |> io.inspect()` parses just as well and is a different program. the identical ambiguity spread over two lines is refused (pinned by <anchor>), so the rule's answer depends on where the author happened to break the line. the same hole covers any mid-line placement on the target line of a multi-line literal: `x = {#,` / `    # |> io.inspect()` / `end` commits for the same reason (the `x = {#,` line is skipped by the trailing-comma rule and no mid-line `}` is ever tried). the moduledoc's \"only one placement is faithful: at the very end of the literal's last line\" (lines #-#) states this as policy, but the second guard's worked `|>` example (lines #-#) promises the rule stays silent for exactly this shape."
    },
    {
      "severity": "blocker",
      "reviewed_path": "lib/syntax/close_unclosed_brace.ex",
      "finding": "lib/syntax/close_unclosed_brace.ex:235 — the trailing-comma exemption in `sole_placement/4` is a raw text test (`String.ends_with?(line, \",\")`) that short-circuits *before* `competing_placement?/4`, so it also skips the swallowed-brace probe at :256. An earlier line of the literal whose trailing *comment* ends in a comma is therefore treated as \"pinned by a trailing comma\" and never probed. On `defmodule E do` / `  def foo do` / `    x = {1 # oops,` / `    |> g()` / `  end` / `end`, the target line is `    |> g()`, one `}` makes the whole source parse, the only earlier line ends in `,` and is skipped, and the rule commits `x = {1 # oops,` / `    |> g()}` — i.e. `{1 |> g()}` — where the author's `{1} |> g()` is equally faithful. The same source with the comma removed from the comment is refused, and that refusal is a deliberate, tested guard (test/syntax/close_unclosed_brace_fix_test.exs:293, whose own comment names this exact corruption). The comma reasoning quoted in the moduledoc (\"closing after a trailing comma drops an element\") only holds for a comma in code; nothing here distinguishes code bytes from comment bytes. The same text-level test at :174 is harmless because it only over-refuses.",
      "signature": "<anchor> — the trailing-comma exemption in `sole_placement/#` is a raw text test (`string.ends_with?(line, \",\")`) that short-circuits *before* `competing_placement?/#`, so it also skips the swallowed-brace probe at :#. an earlier line of the literal whose trailing *comment* ends in a comma is therefore treated as \"pinned by a trailing comma\" and never probed. on `defmodule e do` / `  def foo do` / `    x = {# # oops,` / `    |> g()` / `  end` / `end`, the target line is `    |> g()`, one `}` makes the whole source parse, the only earlier line ends in `,` and is skipped, and the rule commits `x = {# # oops,` / `    |> g()}` — i.e. `{# |> g()}` — where the author's `{#} |> g()` is equally faithful. the same source with the comma removed from the comment is refused, and that refusal is a deliberate, tested guard (<anchor>, whose own comment names this exact corruption). the comma reasoning quoted in the moduledoc (\"closing after a trailing comma drops an element\") only holds for a comma in code; nothing here distinguishes code bytes from comment bytes. the same text-level test at :# is harmless because it only over-refuses."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_brace_analyze_test.exs",
      "finding": "test/syntax/close_unclosed_brace_analyze_test.exs:9 — no flagging test in this file ever reaches the rule's competing-placement probe, so the file cannot tell \"refuses ambiguous placements\" from \"refuses everything\". Traced all eleven tests that assert an issue: for nine of them (lines 9, 23, 35, 47, 105, 293, 308, 322, 340) `sole_placement/4`'s scan range `(start_line-1)..(index-1)` is empty, and for the other two (lines 59, 72) the sole earlier line ends in `,` and is short-circuited by the trailing-comma guard before `competing_placement?/4` is called. Making `competing_placement?/4` return `true` unconditionally would therefore leave this whole file green — the three negative tests (lines 88, 159, 175) would still pass, and no positive test would notice. The sibling `close_unclosed_brace_fix_test.exs:117` (\"closes a multi-line map on its last entry\") does exercise the probe returning false, which is why this is a nit and not a concern; the gap is that the analyze battery has no negative control of its own. One test fixes it: the same multi-line-map shape (`%{` alone on line 3, `a: 1,`, `b: 2`, `end`) asserting `meta: %{line: 3}` runs the probe on line 3 and expects it to decline.",
      "signature": "<anchor> — no flagging test in this file ever reaches the rule's competing-placement probe, so the file cannot tell \"refuses ambiguous placements\" from \"refuses everything\". traced all eleven tests that assert an issue: for nine of them (lines #, #, #, #, #, #, #, #, #) `sole_placement/#`'s scan range `(start_line-#)..(index-#)` is empty, and for the other two (lines #, #) the sole earlier line ends in `,` and is short-circuited by the trailing-comma guard before `competing_placement?/#` is called. making `competing_placement?/#` return `true` unconditionally would therefore leave this whole file green — the three negative tests (lines #, #, #) would still pass, and no positive test would notice. the sibling `<anchor>` (\"closes a multi-line map on its last entry\") does exercise the probe returning false, which is why this is a nit and not a concern; the gap is that the analyze battery has no negative control of its own. one test fixes it: the same multi-line-map shape (`%{` alone on line #, `a: #,`, `b: #`, `end`) asserting `meta: %{line: #}` runs the probe on line # and expects it to decline."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_brace_analyze_test.exs",
      "finding": "test/syntax/close_unclosed_brace_analyze_test.exs:274 — \"no issue for a brace that only appears inside a string\" feeds the rule a source that parses cleanly, so `detect/1` returns `:none` before any brace reasoning happens; it returns `[]` for exactly the same reason as \"leaves code without braces alone\" two tests above, and proves nothing about a `{` inside a string. The file's own header comment at line 288 admits this (\"The test above only shows that a source which parses is left alone\"), and the four real masking tests added below it do the work. Rename it to say what it pins (a source that parses is left alone) or fold it into the neighbouring valid-source tests, so the name stops reading as masking coverage.",
      "signature": "<anchor> — \"no issue for a brace that only appears inside a string\" feeds the rule a source that parses cleanly, so `detect/#` returns `:none` before any brace reasoning happens; it returns `[]` for exactly the same reason as \"leaves code without braces alone\" two tests above, and proves nothing about a `{` inside a string. the file's own header comment at line # admits this (\"the test above only shows that a source which parses is left alone\"), and the four real masking tests added below it do the work. rename it to say what it pins (a source that parses is left alone) or fold it into the neighbouring valid-source tests, so the name stops reading as masking coverage."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_brace_analyze_test.exs",
      "finding": "test/syntax/close_unclosed_brace_analyze_test.exs:163 — the section header \"deliberately skipped: no single faithful placement for the `}`\" heads ten tests, but only three of them (lines 165, 181, 197) are refused for placement ambiguity. The other seven are refused for unrelated reasons: a dangling comma drops an element (214), the nesting is past the @max_braces depth cap (228), no single-literal repair makes the whole source parse (242), there is no line between the `{` and the `end` (258), no candidate parses because a comment swallows the brace (272), the mismatch is not a `{`/`end` one at all (284), and the source already parses (296). A maintainer reading the header will attribute the depth cap and the unrelated-error skip to ambiguity reasoning they do not implement. Moving the header down to sit only over 165-212, or rewording it to \"deliberately skipped, each for its own reason\", fixes it.",
      "signature": "<anchor> — the section header \"deliberately skipped: no single faithful placement for the `}`\" heads ten tests, but only three of them (lines #, #, #) are refused for placement ambiguity. the other seven are refused for unrelated reasons: a dangling comma drops an element (#), the nesting is past the @max_braces depth cap (#), no single-literal repair makes the whole source parse (#), there is no line between the `{` and the `end` (#), no candidate parses because a comment swallows the brace (#), the mismatch is not a `{`/`end` one at all (#), and the source already parses (#). a maintainer reading the header will attribute the depth cap and the unrelated-error skip to ambiguity reasoning they do not implement. moving the header down to sit only over #-#, or rewording it to \"deliberately skipped, each for its own reason\", fixes it."
    },
    {
      "severity": "concern",
      "reviewed_path": "test/syntax/close_unclosed_brace_fix_test.exs",
      "finding": "test/syntax/close_unclosed_brace_fix_test.exs:258 — the ambiguity battery only pins splits between ONE earlier line and the target line (\"leaves a placement split across two lines untouched\" is a 1+1 over two lines). The rule's `competing_placement?` (lib/syntax/close_unclosed_brace.ex:253) enumerates exactly those pairs — `taken` on one earlier line, `count - taken` on the target — so a rival reading that puts braces on two *different* earlier lines (e.g. 1 on line A, 1 on line B, 0-or-more on the target) is never tried. The rule's own comment at lib/syntax/close_unclosed_brace.ex:242 asserts \"those `count` shares are the whole alternative set\", which holds only for two-line splits. If such a source exists, the rule commits one of two parsing meanings — the exact failure this rule promises never to make — and no test in this file would notice. I traced four candidate three-opening sources by hand and the pair scan caught the ambiguity in every one, so this is unverified by reading rather than demonstrated; it needs the search below.",
      "signature": "<anchor> — the ambiguity battery only pins splits between one earlier line and the target line (\"leaves a placement split across two lines untouched\" is a #+# over two lines). the rule's `competing_placement?` (<anchor>) enumerates exactly those pairs — `taken` on one earlier line, `count - taken` on the target — so a rival reading that puts braces on two *different* earlier lines (e.g. # on line a, # on line b, #-or-more on the target) is never tried. the rule's own comment at <anchor> asserts \"those `count` shares are the whole alternative set\", which holds only for two-line splits. if such a source exists, the rule commits one of two parsing meanings — the exact failure this rule promises never to make — and no test in this file would notice. i traced four candidate three-opening sources by hand and the pair scan caught the ambiguity in every one, so this is unverified by reading rather than demonstrated; it needs the search below."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_brace_fix_test.exs",
      "finding": "test/syntax/close_unclosed_brace_fix_test.exs:318 — the fixture comment says the entries are \"each spread over two lines so no line ends in `,` but the last\", but 399 of the 800 generated entry lines DO end in `,` (the value lines, `        1,`). The property that actually makes this test non-vacuous is the opposite one: splitting each entry over two lines creates 400 *key* lines (`      k1:`) that do not end in `,`, and only those get probed — `sole_placement` skips every comma-ended line (lib/syntax/close_unclosed_brace.ex:235), so a one-line-per-entry fixture would make the scan do zero reparses and the timing assertion would measure nothing. Worth saying that, since the comment is the only thing telling a maintainer why the fixture has this shape.",
      "signature": "<anchor> — the fixture comment says the entries are \"each spread over two lines so no line ends in `,` but the last\", but # of the # generated entry lines do end in `,` (the value lines, `        #,`). the property that actually makes this test non-vacuous is the opposite one: splitting each entry over two lines creates # *key* lines (`      k#:`) that do not end in `,`, and only those get probed — `sole_placement` skips every comma-ended line (<anchor>), so a one-line-per-entry fixture would make the scan do zero reparses and the timing assertion would measure nothing. worth saying that, since the comment is the only thing telling a maintainer why the fixture has this shape."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_brace_fix_test.exs",
      "finding": "test/syntax/close_unclosed_brace_fix_test.exs:330 — `assert best < 1_000_000` is a wall-clock threshold against a claimed ~190ms baseline, i.e. ~5x headroom, and the work it times is ~800 whole-file reparses of an 805-line source. min-of-3 and a non-`async` module (so it runs alone, after every async module) keep the flake risk low, but a CI box 3-5x slower than the machine the 190ms was measured on lands at or past the limit, and there is no tag to exclude it — the repo already has a `@moduletag :corpus` convention for tests that are excluded by default. Either tag it or state the measuring machine next to the number.",
      "signature": "<anchor> — `assert best < #_#_#` is a wall-clock threshold against a claimed ~#ms baseline, i.e. ~#x headroom, and the work it times is ~# whole-file reparses of an #-line source. min-of-# and a non-`async` module (so it runs alone, after every async module) keep the flake risk low, but a ci box #-#x slower than the machine the #ms was measured on lands at or past the limit, and there is no tag to exclude it — the repo already has a `@moduletag :corpus` convention for tests that are excluded by default. either tag it or state the measuring machine next to the number."
    },
    {
      "severity": "concern",
      "reviewed_path": "test/syntax/close_unclosed_brace_fix_test.exs",
      "finding": "test/syntax/close_unclosed_brace_fix_test.exs:335 — the split-search test guards vacuity with `length(repaired) > 100`, but a repaired source can only produce a rival when its repair needed **two or more** braces: `brace_splits(count, 3)` demands `sum == count` with at least two non-zero lines, so it returns `[]` for every `count == 1` source and that source is searched against nothing. The guard counts the wrong quantity — if the fragment list ever drifted to single-`{` shapes (or the rule started refusing all nested cases), every one of the 15,625 sources could repair with one brace, `repaired` would be in the thousands, `rivals` would be empty because nothing was ever searched, and the test would stay green while proving nothing about the class its comment names. Guard on the number of repairs with `byte_size(fixed) - byte_size(source) >= 2`, or on the number of rival completions actually parsed.",
      "signature": "<anchor> — the split-search test guards vacuity with `length(repaired) > #`, but a repaired source can only produce a rival when its repair needed **two or more** braces: `brace_splits(count, #)` demands `sum == count` with at least two non-zero lines, so it returns `[]` for every `count == #` source and that source is searched against nothing. the guard counts the wrong quantity — if the fragment list ever drifted to single-`{` shapes (or the rule started refusing all nested cases), every one of the #,# sources could repair with one brace, `repaired` would be in the thousands, `rivals` would be empty because nothing was ever searched, and the test would stay green while proving nothing about the class its comment names. guard on the number of repairs with `byte_size(fixed) - byte_size(source) >= #`, or on the number of rival completions actually parsed."
    },
    {
      "severity": "concern",
      "reviewed_path": "test/syntax/close_unclosed_brace_fix_test.exs",
      "finding": "test/syntax/close_unclosed_brace_fix_test.exs:310 — this test runs the rule over 25³ = 15,625 sources (each `fix/1` costs several whole-file parses) and then reparses up to ~18 rival completions per repair, and test/syntax/close_unclosed_brace_fix_test.exs:415 times four `fix/1` calls plus 1,200 Sourceror parses of an 804-line fixture — both under ExUnit's default 60-second per-test timeout, with no `@tag timeout:`. Every other exhaustive test in this repo sets `@moduletag timeout: :timer.minutes(10)` (test/fix_meta_test.exs:32, test/pipeline_witness_test.exs:72). On a CI box a few times slower than the machine these were written on, the failure mode is a timeout that reads like a hang, not a regression signal.",
      "signature": "<anchor> — this test runs the rule over #³ = #,# sources (each `fix/#` costs several whole-file parses) and then reparses up to ~# rival completions per repair, and <anchor> times four `fix/#` calls plus #,# sourceror parses of an #-line fixture — both under exunit's default #-second per-test timeout, with no `@tag timeout:`. every other exhaustive test in this repo sets `@moduletag timeout: :timer.minutes(#)` (<anchor>, <anchor>). on a ci box a few times slower than the machine these were written on, the failure mode is a timeout that reads like a hang, not a regression signal."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_brace_fix_test.exs",
      "finding": "test/syntax/close_unclosed_brace_fix_test.exs:421 — the stated budget arithmetic does not add up, and the failure message at :459 repeats it. `sole_placement` probes the 400 key lines *and* the `    x = %{` line = 401 lines, two reparses each = ~802, not \"~520\". The comment's explanation (\"the scan stops at the first share that parses\") cannot apply here: a share that parses is exactly a competing placement, which makes the rule refuse — and the refusal is ruled out by `assert fix(code) != code` at :456. The real gap is the unit: `control` times `valid_syntax?` (Sourceror.parse_string, which also merges comments), while the scan reparses with `Code.string_to_quoted`, so a \"reparse of the fixture\" in the message costs more than a reparse in the scan. The 6× budget still fails the ~7000-reparse regression, so the gate works; the number a maintainer is handed when it goes red is the thing that is wrong.",
      "signature": "<anchor> — the stated budget arithmetic does not add up, and the failure message at :# repeats it. `sole_placement` probes the # key lines *and* the `    x = %{` line = # lines, two reparses each = ~#, not \"~#\". the comment's explanation (\"the scan stops at the first share that parses\") cannot apply here: a share that parses is exactly a competing placement, which makes the rule refuse — and the refusal is ruled out by `assert fix(code) != code` at :#. the real gap is the unit: `control` times `valid_syntax?` (sourceror.parse_string, which also merges comments), while the scan reparses with `code.string_to_quoted`, so a \"reparse of the fixture\" in the message costs more than a reparse in the scan. the #× budget still fails the ~#-reparse regression, so the gate works; the number a maintainer is handed when it goes red is the thing that is wrong."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_brace_fix_test.exs",
      "finding": "test/syntax/close_unclosed_brace_fix_test.exs:513 — every refusal in the analyze test is mirrored here as a byte-for-byte \"left untouched\" case (dangling comma, two literals, same-line `end`, comment swallow, unrelated error, all three ambiguity shapes) except the `@max_braces` backstop, which the analyze test covers at test/syntax/close_unclosed_brace_analyze_test.exs:228. Low risk since both callbacks route through `repair/1`, but the fix side has no case pinning that a six-brace nesting comes back unchanged.",
      "signature": "<anchor> — every refusal in the analyze test is mirrored here as a byte-for-byte \"left untouched\" case (dangling comma, two literals, same-line `end`, comment swallow, unrelated error, all three ambiguity shapes) except the `@max_braces` backstop, which the analyze test covers at <anchor>. low risk since both callbacks route through `repair/#`, but the fix side has no case pinning that a six-brace nesting comes back unchanged."
    },
    {
      "severity": "concern",
      "reviewed_path": "test/syntax/close_unclosed_brace_fix_test.exs",
      "finding": "test/syntax/close_unclosed_brace_fix_test.exs:429 — the two heavy tests in this module carry no raised timeout, and ExUnit's default is 60s per test. \"scans a long literal without a blow-up in reparses\" runs `fix/1` on an 803-line fixture four times (three timed plus the `!=` assert) and 1200 whole-file Sourceror parses in the control loops; by the file's own accounting one `fix/1` costs about one control loop, so the test spends roughly 3200 whole-file parses of that fixture. On a CI box a few times slower than the machine the numbers were measured on, that lands at or past the 60s cut and fails as a spurious `ExUnit.TimeoutError` rather than as a perf regression. Every other CPU-bound test file in this repo sets `@moduletag timeout:` for exactly this reason (test/no_parser_calls_in_rule_tests_test.exs:24 records the prior spurious timeout); this file sets none. The 15,625-source search at :310 sits in the same module and would be covered by the same tag.",
      "signature": "<anchor> — the two heavy tests in this module carry no raised timeout, and exunit's default is #s per test. \"scans a long literal without a blow-up in reparses\" runs `fix/#` on an #-line fixture four times (three timed plus the `!=` assert) and # whole-file sourceror parses in the control loops; by the file's own accounting one `fix/#` costs about one control loop, so the test spends roughly # whole-file parses of that fixture. on a ci box a few times slower than the machine the numbers were measured on, that lands at or past the #s cut and fails as a spurious `exunit.timeouterror` rather than as a perf regression. every other cpu-bound test file in this repo sets `@moduletag timeout:` for exactly this reason (<anchor> records the prior spurious timeout); this file sets none. the #,#-source search at :# sits in the same module and would be covered by the same tag."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_brace_fix_test.exs",
      "finding": "test/syntax/close_unclosed_brace_fix_test.exs:469 — the budget compares two different parsers. `best` times `fix/1`, which parses with `Code.string_to_quoted`; `control` times `valid_syntax?`, which parses with Sourceror (comments, token metadata and a literal encoder) and is therefore materially more expensive per parse of the same file. The comment at :461-468 explains the observed ~1 control loop entirely by probes failing early and never reading the rest of the file; the heavier control parse is a second, unmentioned contributor, and it means the 6× budget silently tracks Sourceror's overhead relative to the rule's parser (a dependency bump moves the ratio with nothing in the rule changing). The gate keeps working — 1 vs 20 has ample margin at 6 — but a maintainer re-tuning the number from this comment would be reasoning from half the cause. The test cannot call `Code.*` directly (test/no_parser_calls_in_rule_tests_test.exs forbids it), so the fix is to say this in the comment, not to change the control.",
      "signature": "<anchor> — the budget compares two different parsers. `best` times `fix/#`, which parses with `code.string_to_quoted`; `control` times `valid_syntax?`, which parses with sourceror (comments, token metadata and a literal encoder) and is therefore materially more expensive per parse of the same file. the comment at :#-# explains the observed ~# control loop entirely by probes failing early and never reading the rest of the file; the heavier control parse is a second, unmentioned contributor, and it means the #× budget silently tracks sourceror's overhead relative to the rule's parser (a dependency bump moves the ratio with nothing in the rule changing). the gate keeps working — # vs # has ample margin at # — but a maintainer re-tuning the number from this comment would be reasoning from half the cause. the test cannot call `code.*` directly (test/no_parser_calls_in_rule_tests_test.exs forbids it), so the fix is to say this in the comment, not to change the control."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_brace_fix_test.exs",
      "finding": "test/syntax/close_unclosed_brace_fix_test.exs:227 — the comment says the skipped cases are \"left byte-for-byte alone\", and :563 says the test \"pins that `fix/1` hands the source back byte-for-byte\", but `confirm_fix/2` (test/support/rule_case.ex:96) trims trailing newlines from both sides by design. A regression that dropped or added the file's final newline would pass every assertion in this file, positive and negative alike. The rule is in fact newline-exact today (`String.split(source, \"\\n\")` + `Enum.join(\"\\n\")` round-trips), so this is an overstated comment rather than an untested hazard — but it claims a guarantee no assertion here makes.",
      "signature": "<anchor> — the comment says the skipped cases are \"left byte-for-byte alone\", and :# says the test \"pins that `fix/#` hands the source back byte-for-byte\", but `confirm_fix/#` (<anchor>) trims trailing newlines from both sides by design. a regression that dropped or added the file's final newline would pass every assertion in this file, positive and negative alike. the rule is in fact newline-exact today (`string.split(source, \"\\n\")` + `enum.join(\"\\n\")` round-trips), so this is an overstated comment rather than an untested hazard — but it claims a guarantee no assertion here makes."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_brace_fix_test.exs",
      "finding": "test/syntax/close_unclosed_brace_fix_test.exs:11 — no positive fix case where the line being repaired carries a `{` inside a string or comment. The analyze test pins such an input (`x = %{a: \"{{{{{{\"`, close_unclosed_brace_analyze_test.exs:335) but only asserts the reported *line*; nothing in this file pins the bytes `fix/1` emits for it. A regression that counted braces textually rather than taking the parser's answer would still be caught on the analyze side, but the fix side's byte-exact contract for masked input is unpinned. Adding one `confirm_fix` case with the expected `x = %{a: \"{{{{{{\"}` closes it.",
      "signature": "<anchor> — no positive fix case where the line being repaired carries a `{` inside a string or comment. the analyze test pins such an input (`x = %{a: \"{{{{{{\"`, <anchor>) but only asserts the reported *line*; nothing in this file pins the bytes `fix/#` emits for it. a regression that counted braces textually rather than taking the parser's answer would still be caught on the analyze side, but the fix side's byte-exact contract for masked input is unpinned. adding one `confirm_fix` case with the expected `x = %{a: \"{{{{{{\"}` closes it."
    },
    {
      "severity": "blocker",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:73 — the rule's whole admission test is \"the first non-blank line after `@doc \\\"\\\"\\\"` starts with `def`\", which is also true of a *correctly closed* doc heredoc whose first content line is a `def` example. On such a file the fix inserts `\\\"\\\"\\\"` right after the opener, truncating the doc to empty, promoting the documented example to real code, and turning the doc's real closing `\\\"\\\"\\\"` into an *opening* heredoc that swallows everything down to the next bare `\\\"\\\"\\\"` line. That output can still parse — e.g. `@doc \\\"\\\"\\\"` / `def example do` / `:ok` / `end` / `\\\"\\\"\\\"` / `def a, do: 1` / blank / `@doc \\\"\\\"\\\"` / `Docs for b.` / `\\\"\\\"\\\"` / `def b, do: 2` becomes a module where `def a/0` has been eaten into a string literal and `def example/0` is a real function. Because the result parses, `ProgressGuard.verdict/4` short-circuits on `after_stage == :parses` (lib/syntax/progress_guard.ex:148) and `commit_or_roll_back/4` commits it, so neither the per-rule guard nor the round's all-or-nothing gate can see the deletion. The Syntax round only runs on source that does not parse, so reaching this needs one unrelated syntax error in the file — and if that error sits in the code the runaway heredoc swallows, swallowing it is precisely what makes the file parse again.",
      "signature": "<anchor> — the rule's whole admission test is \"the first non-blank line after `@doc \\\"\\\"\\\"` starts with `def`\", which is also true of a *correctly closed* doc heredoc whose first content line is a `def` example. on such a file the fix inserts `\\\"\\\"\\\"` right after the opener, truncating the doc to empty, promoting the documented example to real code, and turning the doc's real closing `\\\"\\\"\\\"` into an *opening* heredoc that swallows everything down to the next bare `\\\"\\\"\\\"` line. that output can still parse — e.g. `@doc \\\"\\\"\\\"` / `def example do` / `:ok` / `end` / `\\\"\\\"\\\"` / `def a, do: #` / blank / `@doc \\\"\\\"\\\"` / `docs for b.` / `\\\"\\\"\\\"` / `def b, do: #` becomes a module where `def a/#` has been eaten into a string literal and `def example/#` is a real function. because the result parses, `progressguard.verdict/#` short-circuits on `after_stage == :parses` (<anchor>) and `commit_or_roll_back/#` commits it, so neither the per-rule guard nor the round's all-or-nothing gate can see the deletion. the syntax round only runs on source that does not parse, so reaching this needs one unrelated syntax error in the file — and if that error sits in the code the runaway heredoc swallows, swallowing it is precisely what makes the file parse again."
    },
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:110 — `~r/^\\s*def/` has no word boundary, so ordinary doc prose starting with the letters \"def\" counts as a `def` line: `@doc \\\"\\\"\\\"` / `  defaults to 0 when absent.` / `  \\\"\\\"\\\"` / `  def get(m), ...` is flagged and rewritten, as are `defines ...`, `deferred ...`, and code lines like `defaults = %{}`. This widens the blocker above from \"doc containing a `def` example\" to \"doc whose first content line begins with those three letters\". Adding `\\b` (or `(?:def|defp|defmacro)\\b`) narrows it but does not fix the blocker, which is about correctly-closed heredocs.",
      "signature": "<anchor> — `~r/^\\s*def/` has no word boundary, so ordinary doc prose starting with the letters \"def\" counts as a `def` line: `@doc \\\"\\\"\\\"` / `  defaults to # when absent.` / `  \\\"\\\"\\\"` / `  def get(m), ...` is flagged and rewritten, as are `defines ...`, `deferred ...`, and code lines like `defaults = %{}`. this widens the blocker above from \"doc containing a `def` example\" to \"doc whose first content line begins with those three letters\". adding `\\b` (or `(?:def|defp|defmacro)\\b`) narrows it but does not fix the blocker, which is about correctly-closed heredocs."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:116 — `closed_before_def?/1` can never return `true` at a call site that matters. Both it and `next_nonblank_is_def?/1` stop at the *same* first non-blank line, and that line cannot both match `^\\s*\\\"\\\"\\\"` and `^\\s*def`; so whenever `next_nonblank_is_def?` is true, `closed_before_def?` is false and `not closed_before_def?(...)` is a constant. The guard's name promises \"the heredoc is closed before the def\" — it does not check that, and a reader (or a later editor) will assume it does.",
      "signature": "<anchor> — `closed_before_def?/#` can never return `true` at a call site that matters. both it and `next_nonblank_is_def?/#` stop at the *same* first non-blank line, and that line cannot both match `^\\s*\\\"\\\"\\\"` and `^\\s*def`; so whenever `next_nonblank_is_def?` is true, `closed_before_def?` is false and `not closed_before_def?(...)` is a constant. the guard's name promises \"the heredoc is closed before the def\" — it does not check that, and a reader (or a later editor) will assume it does."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "test/syntax/close_unclosed_doc_heredoc_fix_test.exs:63 — the only \"leave it alone\" fix test uses a doc body (`Finds the min and max.`) that could not trigger the rule under any reading. There is no test pinning `fix(x) == x` for a *correctly closed* `@doc \\\"\\\"\\\"` whose first content line is a `def` example or begins with \"def…\" — the exact input the blocker rewrites. Adding those two cases turns the blocker red.",
      "signature": "<anchor> — the only \"leave it alone\" fix test uses a doc body (`finds the min and max.`) that could not trigger the rule under any reading. there is no test pinning `fix(x) == x` for a *correctly closed* `@doc \\\"\\\"\\\"` whose first content line is a `def` example or begins with \"def…\" — the exact input the blocker rewrites. adding those two cases turns the blocker red."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "test/syntax_round_safety_test.exs:131 — the round-safety case that `lib/syntax/progress_guard.ex:90` and `:232` attribute to this rule (\"Observed: `CloseUnclosedDocHeredoc` closing an `@doc \\\"\\\"\\\"` that had swallowed the rest of the file\") is exercised through a `String.replace` stunt double, not the real rule, and asserts with `=~` rather than a whole-string `==`. The real rule happens to produce the same bytes today, but nothing pins that: if it stopped firing on `@runaway_heredoc`, this test stays green while the documented interaction is gone.",
      "signature": "<anchor> — the round-safety case that `<anchor>` and `:#` attribute to this rule (\"observed: `closeuncloseddocheredoc` closing an `@doc \\\"\\\"\\\"` that had swallowed the rest of the file\") is exercised through a `string.replace` stunt double, not the real rule, and asserts with `=~` rather than a whole-string `==`. the real rule happens to produce the same bytes today, but nothing pins that: if it stopped firing on `@runaway_heredoc`, this test stays green while the documented interaction is gone."
    },
    {
      "severity": "blocker",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:83 — the test that admits the input and the place the fix edits are two different lines. `def_below?`/`closing_quotes_below?` scan to end of file (a `-> false` branch in `Enum.find_value/3` means \"keep looking\", as lines 115-119 now correctly state), so the rule fires whenever there is a `def` *anywhere* below and no `\"\"\"` anywhere below — but `find_next_nonblank_offset/1` then inserts the closing `\"\"\"` before the **first non-blank line**. Whenever the unclosed doc actually has text in it (the shape the moduledoc itself describes: the LLM wrote the doc and forgot the closer), those differ and the terminator lands above the doc text instead of above the `def`. On `defmodule Sample do\\n  @doc \"\"\"\\n  1 + 1\\n\\n  def get(m, k), do: Map.get(m, k)\\nend\\n` the output is `@doc \"\"\"` / `\"\"\"` / `1 + 1` / `def get…`: the doc is emptied and its content becomes live module-body code. That output *parses*, so neither guard can catch it — ProgressGuard sees tokenize_error → parse_error (rank up, always `:keep`), and the phase-level all-or-nothing gate only rolls back sources that still fail to parse. This is the exact failure mode the new comment on line 38-44 claims the `\\b` prevents; the `\\b` only narrows the no-`def`-at-all case, not this one. When the doc text does not parse (`Finds the min and max.`) the same rewrite is kept per-rule and then discarded with the whole round, so the rule silently repairs nothing on its commonest realistic input — and if another rule repairs the remaining error in the same round, the emptied doc commits. Pre-existing in shape (base `next_nonblank_is_def?` had the same whole-file scan), but this PR documents the whole-file scan as intended without reconciling the insertion point. Correct behaviour is to insert immediately before the line that matched `@def_line`, not before the first non-blank line.",
      "signature": "<anchor> — the test that admits the input and the place the fix edits are two different lines. `def_below?`/`closing_quotes_below?` scan to end of file (a `-> false` branch in `enum.find_value/#` means \"keep looking\", as lines #-# now correctly state), so the rule fires whenever there is a `def` *anywhere* below and no `\"\"\"` anywhere below — but `find_next_nonblank_offset/#` then inserts the closing `\"\"\"` before the **first non-blank line**. whenever the unclosed doc actually has text in it (the shape the moduledoc itself describes: the llm wrote the doc and forgot the closer), those differ and the terminator lands above the doc text instead of above the `def`. on `defmodule sample do\\n  @doc \"\"\"\\n  # + #\\n\\n  def get(m, k), do: map.get(m, k)\\nend\\n` the output is `@doc \"\"\"` / `\"\"\"` / `# + #` / `def get…`: the doc is emptied and its content becomes live module-body code. that output *parses*, so neither guard can catch it — progressguard sees tokenize_error → parse_error (rank up, always `:keep`), and the phase-level all-or-nothing gate only rolls back sources that still fail to parse. this is the exact failure mode the new comment on line #-# claims the `\\b` prevents; the `\\b` only narrows the no-`def`-at-all case, not this one. when the doc text does not parse (`finds the min and max.`) the same rewrite is kept per-rule and then discarded with the whole round, so the rule silently repairs nothing on its commonest realistic input — and if another rule repairs the remaining error in the same round, the emptied doc commits. pre-existing in shape (base `next_nonblank_is_def?` had the same whole-file scan), but this pr documents the whole-file scan as intended without reconciling the insertion point. correct behaviour is to insert immediately before the line that matched `@def_line`, not before the first non-blank line."
    },
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "test/syntax/close_unclosed_doc_heredoc_fix_test.exs:11 — no test in either test file exercises an unclosed `@doc \"\"\"` that has doc text before the `def`. Every firing fixture (lines 11, 36, 155, 168, 203) has `def` as the literal first non-blank line, so the insertion point and the admission condition coincide and the blocker above is invisible. The three new negative tests (lines 124, 141, 106) pin only the \"no real `def` below\" half. A firing test with prose-then-`def`, asserting the closer lands under the prose, would pin the rule's stated purpose (\"closes a doc *before the next definition*\").",
      "signature": "<anchor> — no test in either test file exercises an unclosed `@doc \"\"\"` that has doc text before the `def`. every firing fixture (lines #, #, #, #, #) has `def` as the literal first non-blank line, so the insertion point and the admission condition coincide and the blocker above is invisible. the three new negative tests (lines #, #, #) pin only the \"no real `def` below\" half. a firing test with prose-then-`def`, asserting the closer lands under the prose, would pin the rule's stated purpose (\"closes a doc *before the next definition*\")."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:143 — `Regex.match?(@def_line, next_line) -> false` in `closing_quotes_below?` is dead: `false` and the `true -> false` catch-all are indistinguishable to `Enum.find_value/3`, so this clause has no effect on any input. The PR edits it (swapping `~r/^\\s*def/` for `@def_line`) as if it were load-bearing. It reads as \"stop scanning at the next definition\", which the function does not do. Delete the clause, or make it a real bound.",
      "signature": "<anchor> — `regex.match?(@def_line, next_line) -> false` in `closing_quotes_below?` is dead: `false` and the `true -> false` catch-all are indistinguishable to `enum.find_value/#`, so this clause has no effect on any input. the pr edits it (swapping `~r/^\\s*def/` for `@def_line`) as if it were load-bearing. it reads as \"stop scanning at the next definition\", which the function does not do. delete the clause, or make it a real bound."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:43 — \"longer spellings come first so `defmacrop` is not read as `defmacro` followed by a stray `p`\" is not what the pattern does: the list starts with the *shortest* alternative (`p`), and the trailing `\\b` makes ordering irrelevant anyway — `defmacro` + `\\b` fails between `o` and `p`, and the engine backtracks to `macrop`. The rationale as written would mislead anyone editing the alternation.",
      "signature": "<anchor> — \"longer spellings come first so `defmacrop` is not read as `defmacro` followed by a stray `p`\" is not what the pattern does: the list starts with the *shortest* alternative (`p`), and the trailing `\\b` makes ordering irrelevant anyway — `defmacro` + `\\b` fails between `o` and `p`, and the engine backtracks to `macrop`. the rationale as written would mislead anyone editing the alternation."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:138 — because `closing_quotes_below?` looks for a `\"\"\"` line anywhere below, a file with an unclosed `@doc \"\"\"` on `def a` followed by a correctly closed `@doc \"\"\"` on `def b` is never repaired: the second doc's closer vetoes the first doc's fix. The comment presents this guard as protecting correctly-closed docs; it also declines a whole class of genuinely broken files. Worth stating in the moduledoc as a known limitation.",
      "signature": "<anchor> — because `closing_quotes_below?` looks for a `\"\"\"` line anywhere below, a file with an unclosed `@doc \"\"\"` on `def a` followed by a correctly closed `@doc \"\"\"` on `def b` is never repaired: the second doc's closer vetoes the first doc's fix. the comment presents this guard as protecting correctly-closed docs; it also declines a whole class of genuinely broken files. worth stating in the moduledoc as a known limitation."
    },
    {
      "severity": "blocker",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:55 — `@def_line` is `^\\s*def…\\b`, so an *indented example* inside the doc text counts as \"the next definition\". On an unclosed doc whose body shows a `def` example — `defmodule S do` / `  @doc \"\"\"` / `  Example:` / blank / `      def add(a, b), do: a + b` / `  def add(a, b), do: a + b` / `end` — `def_offset/1` returns the example line, and the closer is inserted above it: the doc is truncated to `\"Example:\\n\\n\"` and the documented example becomes a live second `add/2` clause. The output *parses*, so `ProgressGuard.verdict/4` keeps it and `commit_or_roll_back/4` commits it — the same silent, unrevertable damage class as the two blockers fixed in rounds 1 and 2, just entered through the insertion point instead of the admission test. No test in either test file feeds an *unclosed* doc that contains a `def`-shaped line in its body; the three \"def example\" fixtures (fix_test.exs:85, :106, :124) are all correctly closed docs, which the `\"\"\"` veto declines before the boundary is ever consulted. Anchoring the def scan to the indent already captured from the `@doc` line would exclude a deeper-indented example while still matching every fixture in the suite.",
      "signature": "<anchor> — `@def_line` is `^\\s*def…\\b`, so an *indented example* inside the doc text counts as \"the next definition\". on an unclosed doc whose body shows a `def` example — `defmodule s do` / `  @doc \"\"\"` / `  example:` / blank / `      def add(a, b), do: a + b` / `  def add(a, b), do: a + b` / `end` — `def_offset/#` returns the example line, and the closer is inserted above it: the doc is truncated to `\"example:\\n\\n\"` and the documented example becomes a live second `add/#` clause. the output *parses*, so `progressguard.verdict/#` keeps it and `commit_or_roll_back/#` commits it — the same silent, unrevertable damage class as the two blockers fixed in rounds # and #, just entered through the insertion point instead of the admission test. no test in either test file feeds an *unclosed* doc that contains a `def`-shaped line in its body; the three \"def example\" fixtures (<anchor>, :#, :#) are all correctly closed docs, which the `\"\"\"` veto declines before the boundary is ever consulted. anchoring the def scan to the indent already captured from the `@doc` line would exclude a deeper-indented example while still matching every fixture in the suite."
    },
    {
      "severity": "blocker",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:134 — because the scan stops only at a `def…` line, every module attribute between the doc text and the definition is swallowed into the doc. On `defmodule S do` / `  @doc \"\"\"` / `  Adds.` / blank / `  @spec add(integer, integer) :: integer` / `  def add(a, b), do: a + b` / `end` the closer lands under the `@spec`, so the emitted module's `@doc` string ends with the spec text and the `@spec` attribute itself is gone. The result parses and compiles without a warning, so neither the per-rule guard nor the round's all-or-nothing gate can see the deleted contract. `@doc` → `@spec`/`@impl` → `def` is the ordering LLMs emit most often, and it is exactly the shape this rule exists to repair. Note this is a behaviour change from the base version, which declined this input outright (`next_nonblank_is_def?` saw the prose line and returned `false`): the PR trades \"repairs nothing\" for \"repairs it wrongly, and the wrong answer ships\".",
      "signature": "<anchor> — because the scan stops only at a `def…` line, every module attribute between the doc text and the definition is swallowed into the doc. on `defmodule s do` / `  @doc \"\"\"` / `  adds.` / blank / `  @spec add(integer, integer) :: integer` / `  def add(a, b), do: a + b` / `end` the closer lands under the `@spec`, so the emitted module's `@doc` string ends with the spec text and the `@spec` attribute itself is gone. the result parses and compiles without a warning, so neither the per-rule guard nor the round's all-or-nothing gate can see the deleted contract. `@doc` → `@spec`/`@impl` → `def` is the ordering llms emit most often, and it is exactly the shape this rule exists to repair. note this is a behaviour change from the base version, which declined this input outright (`next_nonblank_is_def?` saw the prose line and returned `false`): the pr trades \"repairs nothing\" for \"repairs it wrongly, and the wrong answer ships\"."
    },
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:106 — two `@doc \"\"\"` openers with no `\"\"\"` anywhere below them can resolve to the *same* def line, and the reduce then inserts two closers at that one index. On `defmodule S do` / `  @doc \"\"\"` / `  Docs for a.` / blank / `  @doc \"\"\"` / `  More docs.` / `  def a, do: 1` / `end` both openers compute `insert_at = 6`, and the output has `\"\"\"` twice in a row: the first terminates the doc, the second opens a heredoc that runs unterminated to EOF. That output can never parse, so the phase-level gate discards the whole round — including repairs other syntax rules made to the same file. A single closer at that index would have produced a source that parses. Nothing in the suite covers two openers that share a def (fix_test.exs:252 gives each opener its own).",
      "signature": "<anchor> — two `@doc \"\"\"` openers with no `\"\"\"` anywhere below them can resolve to the *same* def line, and the reduce then inserts two closers at that one index. on `defmodule s do` / `  @doc \"\"\"` / `  docs for a.` / blank / `  @doc \"\"\"` / `  more docs.` / `  def a, do: #` / `end` both openers compute `insert_at = #`, and the output has `\"\"\"` twice in a row: the first terminates the doc, the second opens a heredoc that runs unterminated to eof. that output can never parse, so the phase-level gate discards the whole round — including repairs other syntax rules made to the same file. a single closer at that index would have produced a source that parses. nothing in the suite covers two openers that share a def (<anchor> gives each opener its own)."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:149 — the `String.trim(next_line) == \"\" -> nil` clause in `closing_quotes_below?` is inert for the same reason round 2 deleted the `@def_line -> false` clause beside it: `Enum.find_value/3` treats `nil` and `false` identically, so the blank-line branch and the `true -> false` catch-all cannot produce different answers. Its survival still suggests blank lines are handled specially. The function is `Enum.any?(lines, &Regex.match?(~r/^\\s*\"\"\"/, &1))`, and saying that directly would retire the explanatory comment above it too.",
      "signature": "<anchor> — the `string.trim(next_line) == \"\" -> nil` clause in `closing_quotes_below?` is inert for the same reason round # deleted the `@def_line -> false` clause beside it: `enum.find_value/#` treats `nil` and `false` identically, so the blank-line branch and the `true -> false` catch-all cannot produce different answers. its survival still suggests blank lines are handled specially. the function is `enum.any?(lines, &regex.match?(~r/^\\s*\"\"\"/, &#))`, and saying that directly would retire the explanatory comment above it too."
    },
    {
      "severity": "blocker",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:153 — `doc_end_offset/2` searches for the next definition all the way to end-of-file with nothing stopping it at the enclosing module's `end`, so on a two-module file whose first module has an unclosed `@doc \"\"\"` and which contains no `\"\"\"` anywhere below (e.g. `defmodule Helpers do` / `  @doc \"\"\"` / `  Shared helpers.` / `end` / blank / `defmodule Main do` / `  def run, do: :ok` / `end`) the closer is inserted before `def run` — inside the *second* module. The result is `defmodule Helpers do` with `@doc \"Shared helpers.\\nend\\n\\ndefmodule Main do\\n\"` and `def run/0` as its own function: module `Main` has been swallowed into a docstring and silently deleted. This passes every downstream gate — the two outdented lines inside the heredoc are only a *warning* in this Elixir (the repo's own `test/semantic/outdented_heredoc_check_test.exs` pins `severity: :warning`), the output parses under Sourceror, and `compiles_no_worse?/2` compares error sets, not warnings. This is new in this delta: the pre-PR `next_nonblank_is_def?` required the *first non-blank* line below the opener to be a `def`, so the old code declined this input outright and never spanned an outdented line.",
      "signature": "<anchor> — `doc_end_offset/#` searches for the next definition all the way to end-of-file with nothing stopping it at the enclosing module's `end`, so on a two-module file whose first module has an unclosed `@doc \"\"\"` and which contains no `\"\"\"` anywhere below (e.g. `defmodule helpers do` / `  @doc \"\"\"` / `  shared helpers.` / `end` / blank / `defmodule main do` / `  def run, do: :ok` / `end`) the closer is inserted before `def run` — inside the *second* module. the result is `defmodule helpers do` with `@doc \"shared helpers.\\nend\\n\\ndefmodule main do\\n\"` and `def run/#` as its own function: module `main` has been swallowed into a docstring and silently deleted. this passes every downstream gate — the two outdented lines inside the heredoc are only a *warning* in this elixir (the repo's own `test/semantic/outdented_heredoc_check_test.exs` pins `severity: :warning`), the output parses under sourceror, and `compiles_no_worse?/#` compares error sets, not warnings. this is new in this delta: the pre-pr `next_nonblank_is_def?` required the *first non-blank* line below the opener to be a `def`, so the old code declined this input outright and never spanned an outdented line."
    },
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:35 — the moduledoc's \"Known limitation\" section records that `closing_quotes_below?/1` scans to EOF, but says nothing about `doc_end_offset/2` doing the same. A maintainer reading the moduledoc would reasonably believe the definition search is bounded by the module it started in; the `## Known limitation` text is the natural place to record the blocker above once its scope is decided.",
      "signature": "<anchor> — the moduledoc's \"known limitation\" section records that `closing_quotes_below?/#` scans to eof, but says nothing about `doc_end_offset/#` doing the same. a maintainer reading the moduledoc would reasonably believe the definition search is bounded by the module it started in; the `## known limitation` text is the natural place to record the blocker above once its scope is decided."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:83 — the issue message still says the closing `\"\"\"` is \"missing before the next `def`\", but after this change the fix may insert before a module attribute (`@spec`, `@impl`, a second `@doc`) rather than before a `def`. The message no longer describes where the rule actually acts.",
      "signature": "<anchor> — the issue message still says the closing `\"\"\"` is \"missing before the next `def`\", but after this change the fix may insert before a module attribute (`@spec`, `@impl`, a second `@doc`) rather than before a `def`. the message no longer describes where the rule actually acts."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "test/syntax/close_unclosed_doc_heredoc_fix_test.exs:280 — the `@spec` case is the only attribute stop that is tested, and only through `fix`. There is no test pinning the offset-0 attribute case (an attribute on the line immediately below the opener, which yields an empty `@doc \"\"`), and no analyze-side assertion that the attribute-stop shape is reported at all.",
      "signature": "<anchor> — the `@spec` case is the only attribute stop that is tested, and only through `fix`. there is no test pinning the offset-# attribute case (an attribute on the line immediately below the opener, which yields an empty `@doc \"\"`), and no analyze-side assertion that the attribute-stop shape is reported at all."
    },
    {
      "severity": "concern",
      "reviewed_path": "test/syntax/close_unclosed_doc_heredoc_fix_test.exs",
      "finding": "test/syntax/close_unclosed_doc_heredoc_fix_test.exs:190 — the new \"doc text must not become module-body code\" battery only covers words that *begin with* the letters \"def\" (`defaults …`, `defaults = …`), which the rule's `\\b` correctly rejects. It has no case for a doc line that begins with a *real* definition keyword. On `defmodule Sample do\\n  @doc \"\"\"\\n  defstruct fields\\n\\n  def add(a, b), do: a + b\\nend`, `doc_end_offset/2` returns 0 (lib/syntax/close_unclosed_doc_heredoc.ex:153 — `defstruct` is in `@def_word` and `fields` gives it its word boundary), so the closer is inserted directly under the opener: the doc is emptied and `defstruct fields` becomes live module-body code. That output *parses*, so the per-rule progress guard keeps it and the round's all-or-nothing gate (lib/syntax.ex:151) commits it — exactly the silent corruption the new tests were added to prevent, and exactly the case the rule's own comment claims the `\\b` rules out (\"The trailing `\\b` is what keeps English prose out\", lib/syntax/close_unclosed_doc_heredoc.ex:53-57).",
      "signature": "<anchor> — the new \"doc text must not become module-body code\" battery only covers words that *begin with* the letters \"def\" (`defaults …`, `defaults = …`), which the rule's `\\b` correctly rejects. it has no case for a doc line that begins with a *real* definition keyword. on `defmodule sample do\\n  @doc \"\"\"\\n  defstruct fields\\n\\n  def add(a, b), do: a + b\\nend`, `doc_end_offset/#` returns # (<anchor> — `defstruct` is in `@def_word` and `fields` gives it its word boundary), so the closer is inserted directly under the opener: the doc is emptied and `defstruct fields` becomes live module-body code. that output *parses*, so the per-rule progress guard keeps it and the round's all-or-nothing gate (<anchor>) commits it — exactly the silent corruption the new tests were added to prevent, and exactly the case the rule's own comment claims the `\\b` rules out (\"the trailing `\\b` is what keeps english prose out\", <anchor>-#)."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_doc_heredoc_fix_test.exs",
      "finding": "test/syntax/close_unclosed_doc_heredoc_fix_test.exs:280 — the `@spec` test pins the *wanted* half of `@attribute_word`, but the cost the rule's own comment acknowledges (lib/syntax/close_unclosed_doc_heredoc.ex:66-68: doc prose whose line starts with a bare `@word` at the doc's indent is read as the end of the doc) is unpinned. A doc line such as `  @timeout - how long to wait` truncates the doc above it and the emitted source still parses, so nothing downstream reverts it; a test would keep that cost visible if the regex is ever widened.",
      "signature": "<anchor> — the `@spec` test pins the *wanted* half of `@attribute_word`, but the cost the rule's own comment acknowledges (<anchor>-#: doc prose whose line starts with a bare `@word` at the doc's indent is read as the end of the doc) is unpinned. a doc line such as `  @timeout - how long to wait` truncates the doc above it and the emitted source still parses, so nothing downstream reverts it; a test would keep that cost visible if the regex is ever widened."
    },
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:169 — the search for \"the next definition\" runs to the end of the file and is stopped by nothing, so it will cross the enclosing module's own `end`. On `defmodule Solution do / @doc \"\"\" / Solves it. / end / (blank) / defmodule Helper do / def helper, do: :ok / end` the first line at the doc's indent that looks like a definition is `def helper` inside the *second* module, so the closer is inserted there: `end`, the blank line and `defmodule Helper do` are all absorbed into the doc string, module `Helper` ceases to exist, and `helper/0` is silently re-homed into `Solution`. The pre-PR code required the *next non-blank* line to be a `def` and therefore declined this input; the new scan admits it. Whether the damage ships or is merely wasted work turns on one thing I cannot settle by reading: the swallowed lines sit at column 0 while the inserted terminator sits at column 2, so the result is an outdented heredoc — if Elixir treats that as a hard error the round's all-or-nothing gate discards everything, if it is only a warning the corrupted source parses and is committed. A line indented *less* than the `@doc` proves the doc's enclosing block has already closed and the scan can never legitimately continue past it; the same input with the intervening code at the doc's own indent (e.g. a stray `alias`/`import` between the broken doc and its `def`) has no outdent to save it.",
      "signature": "<anchor> — the search for \"the next definition\" runs to the end of the file and is stopped by nothing, so it will cross the enclosing module's own `end`. on `defmodule solution do / @doc \"\"\" / solves it. / end / (blank) / defmodule helper do / def helper, do: :ok / end` the first line at the doc's indent that looks like a definition is `def helper` inside the *second* module, so the closer is inserted there: `end`, the blank line and `defmodule helper do` are all absorbed into the doc string, module `helper` ceases to exist, and `helper/#` is silently re-homed into `solution`. the pre-pr code required the *next non-blank* line to be a `def` and therefore declined this input; the new scan admits it. whether the damage ships or is merely wasted work turns on one thing i cannot settle by reading: the swallowed lines sit at column # while the inserted terminator sits at column #, so the result is an outdented heredoc — if elixir treats that as a hard error the round's all-or-nothing gate discards everything, if it is only a warning the corrupted source parses and is committed. a line indented *less* than the `@doc` proves the doc's enclosing block has already closed and the scan can never legitimately continue past it; the same input with the intervening code at the doc's own indent (e.g. a stray `alias`/`import` between the broken doc and its `def`) has no outdent to save it."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:99 — the issue message still says the closing quotes are \"missing before the next `def`\", but after this change the closer is inserted before an `@spec`/`@impl`/second `@doc` whenever one sits between the doc and its `def`. The message no longer describes where the repair actually goes.",
      "signature": "<anchor> — the issue message still says the closing quotes are \"missing before the next `def`\", but after this change the closer is inserted before an `@spec`/`@impl`/second `@doc` whenever one sits between the doc and its `def`. the message no longer describes where the repair actually goes."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:35 — the new \"Known limitation\" section lists the `\"\"\"`-below veto and the undocumentable-definition decline, but not the two limits a reader is most likely to trip over: the rule also declines outright when no definition appears below at the `@doc`'s own indentation, and the definition search is unbounded downward (the finding above). Both are stated in inline comments; neither reaches the moduledoc a maintainer reads first.",
      "signature": "<anchor> — the new \"known limitation\" section lists the `\"\"\"`-below veto and the undocumentable-definition decline, but not the two limits a reader is most likely to trip over: the rule also declines outright when no definition appears below at the `@doc`'s own indentation, and the definition search is unbounded downward (the finding above). both are stated in inline comments; neither reaches the moduledoc a maintainer reads first."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_doc_heredoc_fix_test.exs",
      "finding": "test/syntax/close_unclosed_doc_heredoc_fix_test.exs:336 — the delta pins check/fix agreement in one direction only. Seven of the nine new decline cases assert `analyze(code) == []` alongside `confirm_fix(fix(code), code)`, but none of the six new repair cases (@spec boundary, doc-text boundary, indented `def` example, two stacked openers, defstruct-below) asserts that `analyze/1` flags the same input. The analyze test file covers only the two original shapes (`@doc \"\"\"` immediately above a `def`, and with one blank line), and there is no `_equivalence_test.exs` for this rule. So if `unclosed_doc_heredoc?/3` stopped sharing `doc_end_offset/2` — e.g. reverted to a first-non-blank-line probe — the rule would silently stop reporting the @spec-bounded and attribute-bounded docs it still repairs, and every test in both files would stay green. Adding `assert analyze(input) != []` (or the issue's `meta.line`) beside each `confirm_fix` in the repair tests closes it. Low severity because the round calls `fix/1` unconditionally, so a divergence degrades the issue report, not the rewrite.",
      "signature": "<anchor> — the delta pins check/fix agreement in one direction only. seven of the nine new decline cases assert `analyze(code) == []` alongside `confirm_fix(fix(code), code)`, but none of the six new repair cases (@spec boundary, doc-text boundary, indented `def` example, two stacked openers, defstruct-below) asserts that `analyze/#` flags the same input. the analyze test file covers only the two original shapes (`@doc \"\"\"` immediately above a `def`, and with one blank line), and there is no `_equivalence_test.exs` for this rule. so if `unclosed_doc_heredoc?/#` stopped sharing `doc_end_offset/#` — e.g. reverted to a first-non-blank-line probe — the rule would silently stop reporting the @spec-bounded and attribute-bounded docs it still repairs, and every test in both files would stay green. adding `assert analyze(input) != []` (or the issue's `meta.line`) beside each `confirm_fix` in the repair tests closes it. low severity because the round calls `fix/#` unconditionally, so a divergence degrades the issue report, not the rewrite."
    },
    {
      "severity": "concern",
      "reviewed_path": "test/syntax/close_unclosed_doc_heredoc_fix_test.exs",
      "finding": "test/syntax/close_unclosed_doc_heredoc_fix_test.exs:1 — the file pins every boundary of the rule's downward scan except one: what happens when the scan crosses a line indented *less* than the `@doc` (the enclosing module's own `end`). Every fixture here is a single module, so no test constrains `defmodule A do / @doc \"\"\" / Shared helpers. / end / (blank) / defmodule B do / def run, do: :ok / end`, where the closer lands inside module B, module B's header is swallowed into the docstring and `run/0` is silently re-homed into A. This is the test-side of the already-reported rule finding (findings.md: blocker at lib/syntax/close_unclosed_doc_heredoc.ex:153, restated as a concern at :169) and is still open at head — flagged here only so the pin lands in this file when the rule decision is made. It must NOT be closed by asserting the rule's current output as expected.",
      "signature": "<anchor> — the file pins every boundary of the rule's downward scan except one: what happens when the scan crosses a line indented *less* than the `@doc` (the enclosing module's own `end`). every fixture here is a single module, so no test constrains `defmodule a do / @doc \"\"\" / shared helpers. / end / (blank) / defmodule b do / def run, do: :ok / end`, where the closer lands inside module b, module b's header is swallowed into the docstring and `run/#` is silently re-homed into a. this is the test-side of the already-reported rule finding (findings.md: blocker at <anchor>, restated as a concern at :#) and is still open at head — flagged here only so the pin lands in this file when the rule decision is made. it must not be closed by asserting the rule's current output as expected."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_doc_heredoc_fix_test.exs",
      "finding": "test/syntax/close_unclosed_doc_heredoc_fix_test.exs:11 — the new `reported_lines/1` header comment claims \"Every repair case below asserts this beside the string the fix emits\". Five repair cases below it do not: :20, :45, :483, :496 and :531. The claim is true of the seven cases the delta added, not of the file. A maintainer adding a repair test and trusting the stated convention would leave the check/fix agreement unpinned for it — which is exactly the drift the helper was introduced to catch.",
      "signature": "<anchor> — the new `reported_lines/#` header comment claims \"every repair case below asserts this beside the string the fix emits\". five repair cases below it do not: :#, :#, :#, :# and :#. the claim is true of the seven cases the delta added, not of the file. a maintainer adding a repair test and trusting the stated convention would leave the check/fix agreement unpinned for it — which is exactly the drift the helper was introduced to catch."
    },
    {
      "severity": "blocker",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:173 — `doc_end_offset/2` now ends the doc at the first *def-or-attribute* line at the doc's indent, so every other module-body line between the opener and that def is swallowed into the doc string and deleted. On `defmodule Server do\\n  @doc \"\"\"\\n  Starts the server.\\n  use GenServer\\n\\n  def start_link(opts), do: ...\\nend` the closer is inserted above `def start_link`, `use GenServer` becomes doc text, and the module loses the GenServer behaviour, its default callbacks and `child_spec/1`. The output parses and compiles (a stale `@impl` only warns), so `commit_or_roll_back/4` keeps it and nothing downstream reverts it. The same absorption eats an Ecto `schema \"users\" do ... end` block, an Nx `defn`, or `import`/`alias`/`require` lines sitting between a doc and its def — none of which `@def_word` or `@attribute_word` recognise. This is new in this delta: the base version required the *first non-blank* line below the opener to be a `def` and inserted immediately above it, so it could never absorb an intervening line. The rule already declines when the first definition-form line is one `@doc` cannot document (`defstruct`, `defmodule`, …); the gap is that non-`def…` module-level constructs get no such treatment.",
      "signature": "<anchor> — `doc_end_offset/#` now ends the doc at the first *def-or-attribute* line at the doc's indent, so every other module-body line between the opener and that def is swallowed into the doc string and deleted. on `defmodule server do\\n  @doc \"\"\"\\n  starts the server.\\n  use genserver\\n\\n  def start_link(opts), do: ...\\nend` the closer is inserted above `def start_link`, `use genserver` becomes doc text, and the module loses the genserver behaviour, its default callbacks and `child_spec/#`. the output parses and compiles (a stale `@impl` only warns), so `commit_or_roll_back/#` keeps it and nothing downstream reverts it. the same absorption eats an ecto `schema \"users\" do ... end` block, an nx `defn`, or `import`/`alias`/`require` lines sitting between a doc and its def — none of which `@def_word` or `@attribute_word` recognise. this is new in this delta: the base version required the *first non-blank* line below the opener to be a `def` and inserted immediately above it, so it could never absorb an intervening line. the rule already declines when the first definition-form line is one `@doc` cannot document (`defstruct`, `defmodule`, …); the gap is that non-`def…` module-level constructs get no such treatment."
    },
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:70 — the `@documented_def_word` decline is scoped to the *first* `@def_word` hit only, which is what makes the blocker above reachable: any line the two regexes do not recognise is silently classified as doc prose rather than as \"I cannot tell, decline\". A maintainer should decide whether the conservative answer (decline when a line at the doc's own indent is neither prose-shaped nor a recognised attribute/def) is affordable, given the rule's existing appetite for declining.",
      "signature": "<anchor> — the `@documented_def_word` decline is scoped to the *first* `@def_word` hit only, which is what makes the blocker above reachable: any line the two regexes do not recognise is silently classified as doc prose rather than as \"i cannot tell, decline\". a maintainer should decide whether the conservative answer (decline when a line at the doc's own indent is neither prose-shaped nor a recognised attribute/def) is affordable, given the rule's existing appetite for declining."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:104 — the issue message still reads \"the closing `\\\"\\\"\\\"` is missing before the next `def`\", but after this change the closer can land above an `@spec`/`@impl`/second `@doc` instead of above a `def`. The moduledoc was updated for this; the message was not.",
      "signature": "<anchor> — the issue message still reads \"the closing `\\\"\\\"\\\"` is missing before the next `def`\", but after this change the closer can land above an `@spec`/`@impl`/second `@doc` instead of above a `def`. the moduledoc was updated for this; the message was not."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_doc_heredoc.ex",
      "finding": "lib/syntax/close_unclosed_doc_heredoc.ex:41 — the Known-limitation text says a file whose earlier doc is broken and whose later doc is closed is \"left to the next round\", but `closing_quotes_below?/1` is a pure function of the same unchanged source, so the next round returns the same decline. Nothing in this rule will ever repair such a file; the sentence reads as if a retry would.",
      "signature": "<anchor> — the known-limitation text says a file whose earlier doc is broken and whose later doc is closed is \"left to the next round\", but `closing_quotes_below?/#` is a pure function of the same unchanged source, so the next round returns the same decline. nothing in this rule will ever repair such a file; the sentence reads as if a retry would."
    },
    {
      "severity": "concern",
      "reviewed_path": "test/syntax/close_unclosed_doc_heredoc_fix_test.exs",
      "finding": "test/syntax/close_unclosed_doc_heredoc_fix_test.exs:459 — this test pins as *correct* an output that deletes a line of the user's documentation and turns it into live code: input whose doc prose reads `@timeout - how long to wait` at the doc's own indent comes back with the doc truncated above that line and the prose promoted to a module-body expression, and because the result parses nothing downstream reverts it. The boundary that does this is `@attribute_word` (lib/syntax/close_unclosed_doc_heredoc.ex:90), a bare `^@\\w+\\b`, introduced by this PR in e626a92. The same PR (4d9ebd1, lib/syntax/close_unclosed_doc_heredoc.ex:79 and the test at :176) established the opposite policy for the identical kind of ambiguity — a `defstruct` line at the doc's indent is \"as plausibly doc prose as it is code\", so the rule declines rather than guess. A bare `@word` line is exactly as ambiguous, but here the rule guesses \"code\" and corrupts. Unlike the `defstruct` case there *is* something to tell them apart with: the repair this boundary exists to buy (`@doc` → `@spec` → `def`, test at :420) only needs the doc-adjacent attributes, so restricting `@attribute_word` to a known set (`@doc`, `@spec`, `@impl`, `@deprecated`, `@since`, `@doc`-family) — or declining, as the `defstruct` branch does — would keep every repair this file asserts while dropping the corruption. A human should decide whether the cost is still worth pinning; the moduledoc documents it (lib/syntax/close_unclosed_doc_heredoc.ex:87-89), which is why this is a concern and not a blocker.",
      "signature": "<anchor> — this test pins as *correct* an output that deletes a line of the user's documentation and turns it into live code: input whose doc prose reads `@timeout - how long to wait` at the doc's own indent comes back with the doc truncated above that line and the prose promoted to a module-body expression, and because the result parses nothing downstream reverts it. the boundary that does this is `@attribute_word` (<anchor>), a bare `^@\\w+\\b`, introduced by this pr in e#a#. the same pr (#d#ebd#, <anchor> and the test at :#) established the opposite policy for the identical kind of ambiguity — a `defstruct` line at the doc's indent is \"as plausibly doc prose as it is code\", so the rule declines rather than guess. a bare `@word` line is exactly as ambiguous, but here the rule guesses \"code\" and corrupts. unlike the `defstruct` case there *is* something to tell them apart with: the repair this boundary exists to buy (`@doc` → `@spec` → `def`, test at :#) only needs the doc-adjacent attributes, so restricting `@attribute_word` to a known set (`@doc`, `@spec`, `@impl`, `@deprecated`, `@since`, `@doc`-family) — or declining, as the `defstruct` branch does — would keep every repair this file asserts while dropping the corruption. a human should decide whether the cost is still worth pinning; the moduledoc documents it (<anchor>-#), which is why this is a concern and not a blocker."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_doc_heredoc_fix_test.exs",
      "finding": "test/syntax/close_unclosed_doc_heredoc_fix_test.exs:21 — every fixture in the file puts `@doc` at exactly two-space indentation, so the whole file stays green if the rule's captured `indent` were replaced by a literal `\"  \"` in the emitted closer (lib/syntax/close_unclosed_doc_heredoc.ex:145) and in the indent-sensitive scans (`at_indent?`, `inside_doc_block?`). Those three indent-driven decisions are the heart of the delta (`aec6c9c` added the outdent bound; `e626a92` added the at-the-doc's-own-indent rule) and none of them is pinned against a second indentation. One fixture with the broken `@doc` inside a nested `defmodule` (four-space indent), and one at column 0 — where `inside_doc_block?`'s `String.starts_with?(line, \"\")` is unconditionally true and the outdent bound therefore never fires at all — would close the gap.",
      "signature": "<anchor> — every fixture in the file puts `@doc` at exactly two-space indentation, so the whole file stays green if the rule's captured `indent` were replaced by a literal `\"  \"` in the emitted closer (<anchor>) and in the indent-sensitive scans (`at_indent?`, `inside_doc_block?`). those three indent-driven decisions are the heart of the delta (`aec#c#c` added the outdent bound; `e#a#` added the at-the-doc's-own-indent rule) and none of them is pinned against a second indentation. one fixture with the broken `@doc` inside a nested `defmodule` (four-space indent), and one at column # — where `inside_doc_block?`'s `string.starts_with?(line, \"\")` is unconditionally true and the outdent bound therefore never fires at all — would close the gap."
    },
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/close_unclosed_fn_delimiter.ex",
      "finding": "lib/syntax/close_unclosed_fn_delimiter.ex:167 — the repair is committed only if deleting the stray `end` makes the WHOLE file parse, so the rule silently declines on any file that carries a second syntax error anywhere — including a second instance of this very bug. Two `fn … end)` + stray-`end` mistakes in one file are repaired by neither this rule nor `NoUnclosedFnDelimiter` (whose `repair/1` likewise requires `parses?(fixed)`): the first rule's candidate deletions all still fail to parse because of bug #2, and the sibling's insert-only pass never parses either. Output: source returned byte-identical, `analyze` returns `[]`, no trace entry. This is the opposite of what the round is built for — `lib/syntax.ex:11-30` states that unparseable intermediate states are expected and that gating on \"must parse now\" would disable the round — and the moduledoc's \"a file unparseable for an unrelated reason is left untouched\" (line 26) does not tell a maintainer that a *second copy of the same bug* is such a reason.",
      "signature": "<anchor> — the repair is committed only if deleting the stray `end` makes the whole file parse, so the rule silently declines on any file that carries a second syntax error anywhere — including a second instance of this very bug. two `fn … end)` + stray-`end` mistakes in one file are repaired by neither this rule nor `nounclosedfndelimiter` (whose `repair/#` likewise requires `parses?(fixed)`): the first rule's candidate deletions all still fail to parse because of bug ##, and the sibling's insert-only pass never parses either. output: source returned byte-identical, `analyze` returns `[]`, no trace entry. this is the opposite of what the round is built for — `<anchor>-#` states that unparseable intermediate states are expected and that gating on \"must parse now\" would disable the round — and the moduledoc's \"a file unparseable for an unrelated reason is left untouched\" (line #) does not tell a maintainer that a *second copy of the same bug* is such a reason."
    },
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/close_unclosed_fn_delimiter.ex",
      "finding": "test/syntax/close_unclosed_fn_delimiter_fix_test.exs:106 — this is the file's only test aimed at the rule's one line-editing step (`remove_stray_end/2` deletes a whole line matching `^\\s*end\\s*$`), and it never reaches it. The fixture's first parse error is `def broken(, do: 1`, not an `fn`/`)` mismatch, so `detect/1` returns `:none` and the rule declines before any line is considered; deleting the `@moduledoc` block from the fixture entirely would not change the result. So nothing in the suite exercises the line-delete against a source whose only error IS the fn bug and which also contains a bare `end` line inside a heredoc, a comment, or a second block below the insertion point — the exact input the code comment at lines 154-157 claims to be safe on.",
      "signature": "<anchor> — this is the file's only test aimed at the rule's one line-editing step (`remove_stray_end/#` deletes a whole line matching `^\\s*end\\s*$`), and it never reaches it. the fixture's first parse error is `def broken(, do: #`, not an `fn`/`)` mismatch, so `detect/#` returns `:none` and the rule declines before any line is considered; deleting the `@moduledoc` block from the fixture entirely would not change the result. so nothing in the suite exercises the line-delete against a source whose only error is the fn bug and which also contains a bare `end` line inside a heredoc, a comment, or a second block below the insertion point — the exact input the code comment at lines #-# claims to be safe on."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_fn_delimiter.ex",
      "finding": "lib/syntax/close_unclosed_fn_delimiter.ex:154 — the comment credits the wrong mechanism. The downward scan only keeps the rule out of text *above* the insertion line; it does nothing about a bare `end` line inside a heredoc *below* it. What actually rules those out is the `parses?(candidate)` check: deleting a line inside a string/heredoc removes no `end` token, so the extra `end` is still unbalanced and the candidate never parses. Worth rewording, so that a future relaxation of the parse gate is not made on the belief that the scan direction is carrying the safety.",
      "signature": "<anchor> — the comment credits the wrong mechanism. the downward scan only keeps the rule out of text *above* the insertion line; it does nothing about a bare `end` line inside a heredoc *below* it. what actually rules those out is the `parses?(candidate)` check: deleting a line inside a string/heredoc removes no `end` token, so the extra `end` is still unbalanced and the candidate never parses. worth rewording, so that a future relaxation of the parse gate is not made on the belief that the scan direction is carrying the safety."
    },
    {
      "severity": "blocker",
      "reviewed_path": "lib/syntax/close_unclosed_fn_delimiter.ex",
      "finding": "lib/syntax/close_unclosed_fn_delimiter.ex:162 — `stray_end_error/1` accepts ANY \"`(` closed by `end`\" mismatch anywhere in the file, not only one caused by the `end` this rule just inserted; with the whole-file parse gate removed by this PR, a file whose *second, unrelated* fault has that shape makes the rule invent a stray `end` that does not exist and delete a real one. Input `defmodule Mixed do\\n  def a(list) do\\n    Enum.max_by(list, fn {_, s} -> s)\\n  end\\n\\n  def broken(, do: 1\\nend` — the parser's first error is the `fn`/`)` mismatch on line 3, so `detect/1` fires; inserting `end` there makes lines 1-4 perfectly balanced (this is NoUnclosedFnDelimiter's plain-unclosed-`fn` shape, which this rule's moduledoc:41-44 promises it never touches), but the tokenizer then reports the unrelated `def broken(` on line 6 as a `(` closed by the `end` on line 7, so `stray_end_error/1` returns `{:ok, {6, 13}}` and the rule proceeds to delete a line. Output: `def a`'s own `  end` (line 4) is deleted. Under the base version this was rejected because the candidate did not parse.",
      "signature": "<anchor> — `stray_end_error/#` accepts any \"`(` closed by `end`\" mismatch anywhere in the file, not only one caused by the `end` this rule just inserted; with the whole-file parse gate removed by this pr, a file whose *second, unrelated* fault has that shape makes the rule invent a stray `end` that does not exist and delete a real one. input `defmodule mixed do\\n  def a(list) do\\n    enum.max_by(list, fn {_, s} -> s)\\n  end\\n\\n  def broken(, do: #\\nend` — the parser's first error is the `fn`/`)` mismatch on line #, so `detect/#` fires; inserting `end` there makes lines #-# perfectly balanced (this is nounclosedfndelimiter's plain-unclosed-`fn` shape, which this rule's <anchor>-# promises it never touches), but the tokenizer then reports the unrelated `def broken(` on line # as a `(` closed by the `end` on line #, so `stray_end_error/#` returns `{:ok, {#, #}}` and the rule proceeds to delete a line. output: `def a`'s own `  end` (line #) is deleted. under the base version this was rejected because the candidate did not parse."
    },
    {
      "severity": "blocker",
      "reviewed_path": "lib/syntax/close_unclosed_fn_delimiter.ex",
      "finding": "lib/syntax/close_unclosed_fn_delimiter.ex:259 — the identity guard `same_stray_end?/2` compares the `(` position captured *before* the deletion against the one reported *after* it, relying on the invariant stated at :159-161 and :236-240 that \"the `(` is always above the line being deleted\". Nothing checks that invariant. When `stray_end_error/1` picked up a `(` that is *below* the insertion point (the blocker above), deleting any bare `end` line shifts that `(` up by one line, the two positions differ, and the guard reads the unchanged error as \"the stray `end` is gone\" — so the very first bare `end` line at or below the insertion point is accepted no matter what it closes. The same positional-not-semantic weakness lets a deletion be accepted whenever the identical structural error is merely re-reported at a *different* enclosing `(`.",
      "signature": "<anchor> — the identity guard `same_stray_end?/#` compares the `(` position captured *before* the deletion against the one reported *after* it, relying on the invariant stated at :#-# and :#-# that \"the `(` is always above the line being deleted\". nothing checks that invariant. when `stray_end_error/#` picked up a `(` that is *below* the insertion point (the blocker above), deleting any bare `end` line shifts that `(` up by one line, the two positions differ, and the guard reads the unchanged error as \"the stray `end` is gone\" — so the very first bare `end` line at or below the insertion point is accepted no matter what it closes. the same positional-not-semantic weakness lets a deletion be accepted whenever the identical structural error is merely re-reported at a *different* enclosing `(`."
    },
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/close_unclosed_fn_delimiter.ex",
      "finding": "lib/syntax/close_unclosed_fn_delimiter.ex:262 — `stopped_at_or_below?/2` does not do what the comment at :241-245 says it does. That comment claims it rejects a deletion that removed a real enclosing `end`, because the resulting unterminated block \"drags the error back above the repair\". But this repo's own `Credence.Syntax.ProgressGuard` moduledoc (lib/syntax/progress_guard.ex:43-47) records the opposite: `meta[:end_line]` is **EOF** for an unclosed delimiter. A deletion that leaves a block unterminated therefore reports `end_line` at EOF, which always satisfies `line > deleted_idx`, so this guard passes exactly the case it is documented to catch. It is the only backstop left after the whole-file parse gate was removed, and it is doing far less than the comment asserts. (Related: the rule hand-rolls a progress measure that `ProgressGuard` already provides for this round.)",
      "signature": "<anchor> — `stopped_at_or_below?/#` does not do what the comment at :#-# says it does. that comment claims it rejects a deletion that removed a real enclosing `end`, because the resulting unterminated block \"drags the error back above the repair\". but this repo's own `credence.syntax.progressguard` moduledoc (<anchor>-#) records the opposite: `meta[:end_line]` is **eof** for an unclosed delimiter. a deletion that leaves a block unterminated therefore reports `end_line` at eof, which always satisfies `line > deleted_idx`, so this guard passes exactly the case it is documented to catch. it is the only backstop left after the whole-file parse gate was removed, and it is doing far less than the comment asserts. (related: the rule hand-rolls a progress measure that `progressguard` already provides for this round.)"
    },
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/close_unclosed_fn_delimiter.ex",
      "finding": "test/syntax/close_unclosed_fn_delimiter_fix_test.exs:176 — the test \"repairs its own occurrence on a file that is also broken for an unrelated reason\" is the test that pins the unsound path. In its fixture, inserting `end` before the `)` leaves lines 1-6 fully balanced with no stray `end` at all; the only `(`-closed-by-`end` error in the file belongs to `def broken(, do: 1` on line 8, and the deletion of line 5 is accepted only because that unrelated `(` shifted from line 8 to line 7. The expected output happens to be defensible on its own, but it was reached from a signal that says nothing about line 5. A maintainer fixing the blockers must re-derive this expectation rather than preserve it — a natural guard (require the reported `(` to be at or above the insertion line) makes this test fail.",
      "signature": "<anchor> — the test \"repairs its own occurrence on a file that is also broken for an unrelated reason\" is the test that pins the unsound path. in its fixture, inserting `end` before the `)` leaves lines #-# fully balanced with no stray `end` at all; the only `(`-closed-by-`end` error in the file belongs to `def broken(, do: #` on line #, and the deletion of line # is accepted only because that unrelated `(` shifted from line # to line #. the expected output happens to be defensible on its own, but it was reached from a signal that says nothing about line #. a maintainer fixing the blockers must re-derive this expectation rather than preserve it — a natural guard (require the reported `(` to be at or above the insertion line) makes this test fail."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_fn_delimiter.ex",
      "finding": "lib/syntax/close_unclosed_fn_delimiter.ex:86 — the multi-pass loop has no pass bound, unlike the sibling `NoUnclosedFnDelimiter` (`@max_passes 100`, lib/syntax/no_unclosed_fn_delimiter.ex:32). Termination is genuinely guaranteed (one line removed per pass), but the work is not: each pass tokenizes once per bare `end` line at or below the insertion point, so a pathological file is O(n²) full tokenizations. Cheap to bound.",
      "signature": "<anchor> — the multi-pass loop has no pass bound, unlike the sibling `nounclosedfndelimiter` (`@max_passes #`, <anchor>). termination is genuinely guaranteed (one line removed per pass), but the work is not: each pass tokenizes once per bare `end` line at or below the insertion point, so a pathological file is o(n²) full tokenizations. cheap to bound."
    },
    {
      "severity": "blocker",
      "reviewed_path": "lib/syntax/close_unclosed_fn_delimiter.ex",
      "finding": "lib/syntax/close_unclosed_fn_delimiter.ex:210 — an unclosed `(` sitting ABOVE the repaired line produces the very same \"`(` closed by `end`\" complaint the rule reads as \"there is a stray `end` of mine to delete\", and `opening_line(meta) <= repair_line` admits it, so the rule deletes a real block terminator. On the input `defmodule M do` / `  def broken(, do: 1` / `` / `  def go(list) do` / `    Enum.max_by(list, fn {_, s} -> s)` / `  end` / `end` the first parse error is the `fn`/`)` mismatch on line 5, so `end` is inserted there; the leftover error is then the `(` opened on line 2 (above line 5) being closed by the `end` on line 7, which `stray_end_above?` accepts. `delete_stray_end` deletes the first `end` token below line 5 — line 6, which is `def go`'s own terminator, not a stray one — and `clears_stray_end?` (line 318) accepts the result because the remaining error is now an EOF \"missing terminator: )\" whose `closing_delimiter` is nil (so `stray_end_meta?` is false) and whose `end_line` is past the deleted line. Output is the file with `def go`'s `end` gone, plus a bogus issue reported at line 5. This is exactly the plain-unclosed-fn shape the moduledoc promises to leave to `NoUnclosedFnDelimiter`, and it is a regression introduced by this PR: the base version's whole-file parse gate declined here. Every test added for the unrelated-fault case (\"declines the plain-unclosed-fn shape even when an unrelated ( is closed by an end below\", \"does not delete a real end on the strength of an unrelated ( closed by an end below\") places the unrelated `(` BELOW the repair; the above-the-repair direction the new guard does not cover is untested. The Syntax round's all-or-nothing rollback masks this only while the file still fails to parse at the end of the round — it does not apply under `syntax_partial_repairs: true`, nor when a later rule repairs the remaining `(`.",
      "signature": "<anchor> — an unclosed `(` sitting above the repaired line produces the very same \"`(` closed by `end`\" complaint the rule reads as \"there is a stray `end` of mine to delete\", and `opening_line(meta) <= repair_line` admits it, so the rule deletes a real block terminator. on the input `defmodule m do` / `  def broken(, do: #` / `` / `  def go(list) do` / `    enum.max_by(list, fn {_, s} -> s)` / `  end` / `end` the first parse error is the `fn`/`)` mismatch on line #, so `end` is inserted there; the leftover error is then the `(` opened on line # (above line #) being closed by the `end` on line #, which `stray_end_above?` accepts. `delete_stray_end` deletes the first `end` token below line # — line #, which is `def go`'s own terminator, not a stray one — and `clears_stray_end?` (line #) accepts the result because the remaining error is now an eof \"missing terminator: )\" whose `closing_delimiter` is nil (so `stray_end_meta?` is false) and whose `end_line` is past the deleted line. output is the file with `def go`'s `end` gone, plus a bogus issue reported at line #. this is exactly the plain-unclosed-fn shape the moduledoc promises to leave to `nounclosedfndelimiter`, and it is a regression introduced by this pr: the base version's whole-file parse gate declined here. every test added for the unrelated-fault case (\"declines the plain-unclosed-fn shape even when an unrelated ( is closed by an end below\", \"does not delete a real end on the strength of an unrelated ( closed by an end below\") places the unrelated `(` below the repair; the above-the-repair direction the new guard does not cover is untested. the syntax round's all-or-nothing rollback masks this only while the file still fails to parse at the end of the round — it does not apply under `syntax_partial_repairs: true`, nor when a later rule repairs the remaining `(`."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_fn_delimiter.ex",
      "finding": "lib/syntax/close_unclosed_fn_delimiter.ex:30 — the moduledoc's locality claim (\"Each occurrence is repaired on its own\", only \"a fault the rule cannot see past\" is declined) overstates what the code does. `repair_once` only ever looks at the parser's FIRST error; if that first error is a `fn`-closed-by-`)` of the OTHER shape (a plain unclosed `fn` higher in the file), inserting `end` there leaves the genuine stray-`end` occurrence below as the next error, `stray_end_above?` is false, and the rule declines the whole file — while `NoUnclosedFnDelimiter` also declines, because its whole-file gate is blocked by the second fault. Neither rule repairs anything. Pre-existing behaviour (the base version declined too); only the doc claim is new.",
      "signature": "<anchor> — the moduledoc's locality claim (\"each occurrence is repaired on its own\", only \"a fault the rule cannot see past\" is declined) overstates what the code does. `repair_once` only ever looks at the parser's first error; if that first error is a `fn`-closed-by-`)` of the other shape (a plain unclosed `fn` higher in the file), inserting `end` there leaves the genuine stray-`end` occurrence below as the next error, `stray_end_above?` is false, and the rule declines the whole file — while `nounclosedfndelimiter` also declines, because its whole-file gate is blocked by the second fault. neither rule repairs anything. pre-existing behaviour (the base version declined too); only the doc claim is new."
    },
    {
      "severity": "blocker",
      "reviewed_path": "lib/syntax/close_unclosed_fn_delimiter.ex",
      "finding": "lib/syntax/close_unclosed_fn_delimiter.ex:305 — \"first `end` token below the repair is the stray one\" is an assumption, not a property of the bug, and when it is false the rule deletes a real block terminator and the result parses, so nothing catches it. Input: the documented bug shape where the outer `fn` body continues with a complete `do…end` block before the dangling `end` — `defmodule M do / def go(m) do / Enum.map(m, fn r -> / Enum.map(r, fn e -> / if e == 0 do 1 else e end) / if r > 0 do / IO.puts(\"big\") / end / r * 2 / end / end) / end / end`. After inserting `end` on line 5 the leftover complaint is `(` (line 3) closed by `end` (line 11), 3 <= 5, so `stray_end_above?` passes; `first_end_token_line/2` then stops at line 8 — the `if`'s own terminator — not at the real stray on line 10. Deleting line 8 yields `if r > 0 do IO.puts(\"big\"); r * 2 end`, which parses and compiles cleanly, so `clears_stray_end?/3`'s `{:ok, _ast} -> true` arm accepts it unconditionally. The function now returns `nil` for `r <= 0` instead of always returning `r * 2` — a silent behaviour change, and `analyze/1` reports line 5 with \"a stray `end` follows\", which is a false diagnosis of that line. Pre-existing: the base version's \"first bare `end` line whose removal makes the whole file parse\" scan picks the same line 8 and commits the same output. What the PR changes is that the moduledoc (lines 79-83) and the `clears_stray_end?` comment (lines 339-346) now assert this deletion is ruled out \"by the shape of the bug\", so the hazard is now documented as handled. Note the guard is one-sided: it bounds walking *past* the stray but not stopping *short* of it.",
      "signature": "<anchor> — \"first `end` token below the repair is the stray one\" is an assumption, not a property of the bug, and when it is false the rule deletes a real block terminator and the result parses, so nothing catches it. input: the documented bug shape where the outer `fn` body continues with a complete `do…end` block before the dangling `end` — `defmodule m do / def go(m) do / enum.map(m, fn r -> / enum.map(r, fn e -> / if e == # do # else e end) / if r > # do / io.puts(\"big\") / end / r * # / end / end) / end / end`. after inserting `end` on line # the leftover complaint is `(` (line #) closed by `end` (line #), # <= #, so `stray_end_above?` passes; `first_end_token_line/#` then stops at line # — the `if`'s own terminator — not at the real stray on line #. deleting line # yields `if r > # do io.puts(\"big\"); r * # end`, which parses and compiles cleanly, so `clears_stray_end?/#`'s `{:ok, _ast} -> true` arm accepts it unconditionally. the function now returns `nil` for `r <= #` instead of always returning `r * #` — a silent behaviour change, and `analyze/#` reports line # with \"a stray `end` follows\", which is a false diagnosis of that line. pre-existing: the base version's \"first bare `end` line whose removal makes the whole file parse\" scan picks the same line # and commits the same output. what the pr changes is that the moduledoc (lines #-#) and the `clears_stray_end?` comment (lines #-#) now assert this deletion is ruled out \"by the shape of the bug\", so the hazard is now documented as handled. note the guard is one-sided: it bounds walking *past* the stray but not stopping *short* of it."
    },
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/close_unclosed_fn_delimiter.ex",
      "finding": "lib/syntax/close_unclosed_fn_delimiter.ex:347 — no test exercises `stopped_at_or_below?/2` firing (the third bullet of the `clears_stray_end?` contract). Every declining test in `close_unclosed_fn_delimiter_fix_test.exs` is turned away earlier, by `stray_end_above?/2`, `bare_end?/1` or `unterminated_above_meta?/2`; the arithmetic in `stopped_at_or_below?/2` mixes a post-deletion `end_line` with a pre-deletion 0-based index, and reading alone I can only confirm the off-by-one is right, not that the branch is reachable. An unreachable guard is one that silently rots.",
      "signature": "<anchor> — no test exercises `stopped_at_or_below?/#` firing (the third bullet of the `clears_stray_end?` contract). every declining test in `close_unclosed_fn_delimiter_fix_test.exs` is turned away earlier, by `stray_end_above?/#`, `bare_end?/#` or `unterminated_above_meta?/#`; the arithmetic in `stopped_at_or_below?/#` mixes a post-deletion `end_line` with a pre-deletion #-based index, and reading alone i can only confirm the off-by-one is right, not that the branch is reachable. an unreachable guard is one that silently rots."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/close_unclosed_fn_delimiter.ex",
      "finding": "lib/syntax/close_unclosed_fn_delimiter.ex:316 — the comment says \"Two things must hold\" and then lists three bullets.",
      "signature": "<anchor> — the comment says \"two things must hold\" and then lists three bullets."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_fn_delimiter_fix_test.exs",
      "finding": "test/syntax/close_unclosed_fn_delimiter_fix_test.exs:495 — \"fixing twice changes nothing the second time\" cannot catch an idempotency defect. Its input is the two-copy `TwoBugs` file, whose repaired output parses, so the second `fix/1` call stops at `detect/1`'s `{:ok, _ast}` branch and returns the source untouched without ever reaching the insert/delete path. That is the same short-circuit already covered by \"does not modify already-valid code\" (line 96). The only fix output in this file that still does not parse is the `Local` fixture (line 182), whose repair leaves `def broken(, do: 1` behind — re-running the fix on *that* output is the case where a non-idempotent rule could actually delete a second line, and nothing asserts it.",
      "signature": "<anchor> — \"fixing twice changes nothing the second time\" cannot catch an idempotency defect. its input is the two-copy `twobugs` file, whose repaired output parses, so the second `fix/#` call stops at `detect/#`'s `{:ok, _ast}` branch and returns the source untouched without ever reaching the insert/delete path. that is the same short-circuit already covered by \"does not modify already-valid code\" (line #). the only fix output in this file that still does not parse is the `local` fixture (line #), whose repair leaves `def broken(, do: #` behind — re-running the fix on *that* output is the case where a non-idempotent rule could actually delete a second line, and nothing asserts it."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_fn_delimiter_fix_test.exs",
      "finding": "test/syntax/close_unclosed_fn_delimiter_fix_test.exs:108 — \"leaves the bug shape inside a docstring untouched when the file is broken elsewhere\" never reaches the masking code it appears to test. On that fixture the tokenizer's first complaint is the unrelated `def broken(` on line 7 being closed by the module's `end` (opening delimiter `(`, not `fn`), so `detect/1` returns `:none` and the rule hands the source straight back without looking at a single line. The test would pass byte-identically against a rule with no `SourceMask` use at all. Masking is genuinely covered — but only by the Heredoc test at :413, which is the one place the downward `end`-token scan actually runs over a shadow. (Pre-existing; unchanged by this PR.)",
      "signature": "<anchor> — \"leaves the bug shape inside a docstring untouched when the file is broken elsewhere\" never reaches the masking code it appears to test. on that fixture the tokenizer's first complaint is the unrelated `def broken(` on line # being closed by the module's `end` (opening delimiter `(`, not `fn`), so `detect/#` returns `:none` and the rule hands the source straight back without looking at a single line. the test would pass byte-identically against a rule with no `sourcemask` use at all. masking is genuinely covered — but only by the heredoc test at :#, which is the one place the downward `end`-token scan actually runs over a shadow. (pre-existing; unchanged by this pr.)"
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_fn_delimiter_fix_test.exs",
      "finding": "test/syntax/close_unclosed_fn_delimiter_fix_test.exs:372 — the comment \"The multi-pass loop is bounded now; the bound must not cut a real file short\" overclaims what the test below it proves: `@max_passes` is 100 (lib/syntax/close_unclosed_fn_delimiter.ex:92) and the fixture drives 3 passes, so the bound is never approached. Nothing in the suite covers what happens when it *is* hit: `repair/4` at lib/syntax/close_unclosed_fn_delimiter.ex:130 returns `done(source, reported)`, i.e. a file with more than 100 occurrences comes back partially repaired and is reported as `{:fixed, ...}` with issues only for the occurrences that got rewritten — silently partial rather than declined. Either drop the bound claim from the comment or add a fixture that exercises the cut-off.",
      "signature": "<anchor> — the comment \"the multi-pass loop is bounded now; the bound must not cut a real file short\" overclaims what the test below it proves: `@max_passes` is # (<anchor>) and the fixture drives # passes, so the bound is never approached. nothing in the suite covers what happens when it *is* hit: `repair/#` at <anchor> returns `done(source, reported)`, i.e. a file with more than # occurrences comes back partially repaired and is reported as `{:fixed, ...}` with issues only for the occurrences that got rewritten — silently partial rather than declined. either drop the bound claim from the comment or add a fixture that exercises the cut-off."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_fn_delimiter_fix_test.exs",
      "finding": "test/syntax/close_unclosed_fn_delimiter_fix_test.exs:539 — the `Local` fixture is duplicated verbatim from :183. The comment at :532 asserts \"this is the once-fixed `Local` source above\", but nothing ties the two copies together: editing one leaves the other's comment quietly false and the idempotency test still green on a source that is no longer the one it claims to be. Bind it once (a module attribute or a helper) so the two tests cannot drift apart.",
      "signature": "<anchor> — the `local` fixture is duplicated verbatim from :#. the comment at :# asserts \"this is the once-fixed `local` source above\", but nothing ties the two copies together: editing one leaves the other's comment quietly false and the idempotency test still green on a source that is no longer the one it claims to be. bind it once (a module attribute or a helper) so the two tests cannot drift apart."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_fn_delimiter_fix_test.exs",
      "finding": "test/syntax/close_unclosed_fn_delimiter_fix_test.exs:403 — the three-copy test's `%Issue{meta: %{line: 5}}` pattern drops the `rule:` field that the otherwise-identical two-copy assertion at :172 pins. Harmless today, but the assertion no longer proves the issues came from this rule.",
      "signature": "<anchor> — the three-copy test's `%issue{meta: %{line: #}}` pattern drops the `rule:` field that the otherwise-identical two-copy assertion at :# pins. harmless today, but the assertion no longer proves the issues came from this rule."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_fn_delimiter_fix_test.exs",
      "finding": "test/syntax/close_unclosed_fn_delimiter_fix_test.exs:48 — the same 15-line `Solution` fixture is inlined byte-identically three times (lines 48-64, 87-103, 109-125), and the near-identical 9-line `TwoBugs` fixture twice more (lines 172-182, 203-213). This PR extracted `@local_source` and `@one_occurrence` precisely so shared fixtures \"cannot drift apart\"; the same treatment was not applied to the most-duplicated ones, so editing the input of \"inserts missing end before ) and removes stray end on next line\" without editing the other two silently leaves \"fixed output no longer flags\" and \"fixed output is well-formed (parses)\" asserting against a different program than the test they were written to accompany.",
      "signature": "<anchor> — the same #-line `solution` fixture is inlined byte-identically three times (lines #-#, #-#, #-#), and the near-identical #-line `twobugs` fixture twice more (lines #-#, #-#). this pr extracted `@local_source` and `@one_occurrence` precisely so shared fixtures \"cannot drift apart\"; the same treatment was not applied to the most-duplicated ones, so editing the input of \"inserts missing end before ) and removes stray end on next line\" without editing the other two silently leaves \"fixed output no longer flags\" and \"fixed output is well-formed (parses)\" asserting against a different program than the test they were written to accompany."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_fn_delimiter_fix_test.exs",
      "finding": "test/syntax/close_unclosed_fn_delimiter_fix_test.exs:211 — four decline tests (PlainShape here, plus Reduced at :232, AboveReduced at :254, AboveWithTail at :272) name `NoUnclosedFnDelimiter` as the owner of the shape they hand off, but each fixture also carries `def broken(, do: 1`, and the sibling commits its insertion only when the *whole file* parses afterwards (lib/syntax/no_unclosed_fn_delimiter.ex:66-77). So on these four inputs the sibling declines too and no rule repairs anything — the tests assert one half of a handoff that does not happen. Only the `Both` test at :495 actually calls the sibling, and it correctly asserts \"neither fires\". Adding the same two sibling assertions to these four would turn a misleading comment into a pinned fact.",
      "signature": "<anchor> — four decline tests (plainshape here, plus reduced at :#, abovereduced at :#, abovewithtail at :#) name `nounclosedfndelimiter` as the owner of the shape they hand off, but each fixture also carries `def broken(, do: #`, and the sibling commits its insertion only when the *whole file* parses afterwards (<anchor>-#). so on these four inputs the sibling declines too and no rule repairs anything — the tests assert one half of a handoff that does not happen. only the `both` test at :# actually calls the sibling, and it correctly asserts \"neither fires\". adding the same two sibling assertions to these four would turn a misleading comment into a pinned fact."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/close_unclosed_fn_delimiter_fix_test.exs",
      "finding": "test/syntax/close_unclosed_fn_delimiter_fix_test.exs:445 — `String.duplicate(@one_occurrence, 100)` is built twice in one test (once for `fix`, again for `analyze` on :449), so the `fix` result and the `analyze` result are compared against two separately constructed 400-line inputs. The very next test (:460) binds `input` once; this is a leftover of the same fixture-binding cleanup and also doubles the cost of a test that runs 100 repair passes.",
      "signature": "<anchor> — `string.duplicate(@one_occurrence, #)` is built twice in one test (once for `fix`, again for `analyze` on :#), so the `fix` result and the `analyze` result are compared against two separately constructed #-line inputs. the very next test (:#) binds `input` once; this is a leftover of the same fixture-binding cleanup and also doubles the cost of a test that runs # repair passes."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/fix_assignment_dot_syntax.ex",
      "finding": "lib/syntax/fix_assignment_dot_syntax.ex:17 — the \"Not flagged\" list is incomplete about what the rule declines, so it reads as broader coverage than it has: a destructuring left-hand side (`{:ok, val} =.foo()`), a module attribute (`@attr =.foo()`), a second `=` on the line (`x = y =.foo()`) and more than one space before the dot (`x =  .foo()`) are all left untouched because `@bad_pattern` demands a bare identifier at line start and at most one space. All four are pinned as no-ops in the tests; only the moduledoc omits them.",
      "signature": "<anchor> — the \"not flagged\" list is incomplete about what the rule declines, so it reads as broader coverage than it has: a destructuring left-hand side (`{:ok, val} =.foo()`), a module attribute (`@attr =.foo()`), a second `=` on the line (`x = y =.foo()`) and more than one space before the dot (`x =  .foo()`) are all left untouched because `@bad_pattern` demands a bare identifier at line start and at most one space. all four are pinned as no-ops in the tests; only the moduledoc omits them."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/fix_assignment_dot_syntax.ex",
      "finding": "lib/syntax/fix_assignment_dot_syntax.ex:62 — `@bad_pattern` carries no `u` modifier, so `[a-zA-Z_]\\w*` is ASCII-only and an assignment to a non-ASCII identifier (`café =.make_ref()`, `Größe =.foo()`) is silently declined by both `analyze/1` and `fix/1`. Elixir accepts such identifiers, so this is a real missed repair — safe direction, but neither documented nor tested.",
      "signature": "<anchor> — `@bad_pattern` carries no `u` modifier, so `[a-za-z_]\\w*` is ascii-only and an assignment to a non-ascii identifier (`café =.make_ref()`, `größe =.foo()`) is silently declined by both `analyze/#` and `fix/#`. elixir accepts such identifiers, so this is a real missed repair — safe direction, but neither documented nor tested."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/fix_assignment_dot_syntax.ex",
      "finding": "test/syntax/fix_assignment_dot_syntax_fix_test.exs:219 — the one test that covers the mixed case (real code repaired in a file that also documents the broken form) asserts with two `=~` substring checks instead of the exact whole-string `==` that `confirm_fix/2` — already imported at line 4 — provides. `=~` passes on output that is correct at those two spots and wrong anywhere else in the file, which is exactly the assertion style docs/02 rules out; the sibling tests in the same file all use `confirm_fix`.",
      "signature": "<anchor> — the one test that covers the mixed case (real code repaired in a file that also documents the broken form) asserts with two `=~` substring checks instead of the exact whole-string `==` that `confirm_fix/#` — already imported at line # — provides. `=~` passes on output that is correct at those two spots and wrong anywhere else in the file, which is exactly the assertion style docs/# rules out; the sibling tests in the same file all use `confirm_fix`."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/fix_assignment_dot_syntax.ex",
      "finding": "lib/syntax/fix_assignment_dot_syntax.ex:9 — the moduledoc has no `## Bad` / `## Good` block, so this new rule lands directly on the `@bad_good_ledger` exception list in test/rule_card_test.exs:59 (Rule Standard v1 requirement 6, which that gate describes as a ratchet at 40 of 43 compliant). Substantively the rule is fine for the self-corruption oracle — its two `→` examples at lines 14-15 do sit at the start of their heredoc lines and are the exact bytes it rewrites, and test line 224 proves it no longer rewrites its own source — so this is a heading rename, not missing content.",
      "signature": "<anchor> — the moduledoc has no `## bad` / `## good` block, so this new rule lands directly on the `@bad_good_ledger` exception list in <anchor> (rule standard v# requirement #, which that gate describes as a ratchet at # of # compliant). substantively the rule is fine for the self-corruption oracle — its two `→` examples at lines #-# do sit at the start of their heredoc lines and are the exact bytes it rewrites, and test line # proves it no longer rewrites its own source — so this is a heading rename, not missing content."
    },
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/fix_assignment_dot_syntax.ex",
      "finding": "test/syntax/fix_assignment_dot_syntax_fix_test.exs:213 — \"a line that is not valid UTF-8 is repaired, not crashed on\" hides the 0xFF byte inside a trailing `#` comment, and `SourceMask.mask/1` blanks a comment byte-for-byte to 0x01 before the regex ever runs. The subject the pattern actually matches is pure ASCII, so this test passes identically with or without a `/u` modifier on `@bad_pattern` — it does not defend the byte-wise `\\x80-\\xff` design that lib/syntax/fix_assignment_dot_syntax.ex:86-93 and the test's own comment at :208-212 say it exists for. The input that would reach the pattern un-blanked is a truncated multi-byte character in *code* position (e.g. `caf<0xC3> =.make_ref()`), and nothing tests it; since `Credence.Syntax.run_rule/2` (lib/syntax.ex:113) calls `rule.fix/1` with no rescue, a later \"tidy the regex\" change to `/u` would raise out of the whole fix pipeline with the suite green.",
      "signature": "<anchor> — \"a line that is not valid utf-# is repaired, not crashed on\" hides the #xff byte inside a trailing `#` comment, and `sourcemask.mask/#` blanks a comment byte-for-byte to #x# before the regex ever runs. the subject the pattern actually matches is pure ascii, so this test passes identically with or without a `/u` modifier on `@bad_pattern` — it does not defend the byte-wise `\\x#-\\xff` design that <anchor>-# and the test's own comment at :#-# say it exists for. the input that would reach the pattern un-blanked is a truncated multi-byte character in *code* position (e.g. `caf<#xc#> =.make_ref()`), and nothing tests it; since `credence.syntax.run_rule/#` (<anchor>) calls `rule.fix/#` with no rescue, a later \"tidy the regex\" change to `/u` would raise out of the whole fix pipeline with the suite green."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/fix_assignment_dot_syntax.ex",
      "finding": "lib/syntax/fix_assignment_dot_syntax.ex:94 — the lookahead `(?=[a-zA-Z_])` is ASCII-only while the left-hand identifier class was deliberately widened to `\\x80-\\xff`, so a non-ASCII *callee* (`x =.größe_von(y)`) is declined even though it is the same syntax error and carries none of the digit ambiguity the lookahead exists to avoid. Missed repair, and the moduledoc's \"Not flagged\" list does not name it.",
      "signature": "<anchor> — the lookahead `(?=[a-za-z_])` is ascii-only while the left-hand identifier class was deliberately widened to `\\x#-\\xff`, so a non-ascii *callee* (`x =.größe_von(y)`) is declined even though it is the same syntax error and carries none of the digit ambiguity the lookahead exists to avoid. missed repair, and the moduledoc's \"not flagged\" list does not name it."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/fix_assignment_dot_syntax.ex",
      "finding": "lib/syntax/fix_assignment_dot_syntax.ex:29 — \"A second `=` on the line (`x = y =.foo()`)\" overstates the decline: only an `=` appearing *before* the dot causes it. `x =.foo(a = 1)` has a second `=` on the line and is repaired, so a maintainer reading the list gets a wider abstention than the rule makes.",
      "signature": "<anchor> — \"a second `=` on the line (`x = y =.foo()`)\" overstates the decline: only an `=` appearing *before* the dot causes it. `x =.foo(a = #)` has a second `=` on the line and is repaired, so a maintainer reading the list gets a wider abstention than the rule makes."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/fix_assignment_dot_syntax.ex",
      "finding": "lib/syntax/fix_assignment_dot_syntax.ex:99 — the pattern is `^`-anchored, so only the FIRST `=.` on a line is repaired, and after that repair `analyze/1` reports nothing at all for the line. On `a =.foo(b =.bar())` the fix emits `a = foo(b =.bar())`, which still does not parse, yet the rule now says the line is clean — so the round's phase-level \"still does not parse\" rollback discards the partial repair and a re-run cannot converge. No test pins a second occurrence on one line, and the \"round-trip / fixed output no longer flags\" test (test/syntax/fix_assignment_dot_syntax_fix_test.exs:301) reads as a self-consistency proof but would pass here for the wrong reason.",
      "signature": "<anchor> — the pattern is `^`-anchored, so only the first `=.` on a line is repaired, and after that repair `analyze/#` reports nothing at all for the line. on `a =.foo(b =.bar())` the fix emits `a = foo(b =.bar())`, which still does not parse, yet the rule now says the line is clean — so the round's phase-level \"still does not parse\" rollback discards the partial repair and a re-run cannot converge. no test pins a second occurrence on one line, and the \"round-trip / fixed output no longer flags\" test (<anchor>) reads as a self-consistency proof but would pass here for the wrong reason."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/fix_assignment_dot_syntax.ex",
      "finding": "lib/syntax/fix_assignment_dot_syntax.ex:97 — the comment justifies the byte-wise `\\x80-\\xff` class with \"a high byte reaching the pattern is part of an identifier, never of a literal\". The second half is true (masking blanks literals) but the conclusion is not: a non-identifier character in code position keeps its bytes in the shadow — `Credence.SourceMask`'s own moduledoc (lib/source_mask.ex:445-459) exists precisely because an em dash can sit in code position in source that does not parse. So `x =.—dash()` is flagged and rewritten to `x = —dash()`, which still does not parse; the rule reports and \"repairs\" a line it cannot actually help. The direction is benign (broken in, broken out) and `\\p{L}` is not available without the `/u` modifier the moduledoc deliberately rejects, so this is a claim to soften rather than a behaviour to change — but nothing in the test battery pins it either way.",
      "signature": "<anchor> — the comment justifies the byte-wise `\\x#-\\xff` class with \"a high byte reaching the pattern is part of an identifier, never of a literal\". the second half is true (masking blanks literals) but the conclusion is not: a non-identifier character in code position keeps its bytes in the shadow — `credence.sourcemask`'s own moduledoc (<anchor>-#) exists precisely because an em dash can sit in code position in source that does not parse. so `x =.—dash()` is flagged and rewritten to `x = —dash()`, which still does not parse; the rule reports and \"repairs\" a line it cannot actually help. the direction is benign (broken in, broken out) and `\\p{l}` is not available without the `/u` modifier the moduledoc deliberately rejects, so this is a claim to soften rather than a behaviour to change — but nothing in the test battery pins it either way."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/fix_assignment_dot_syntax.ex",
      "finding": "lib/syntax/fix_assignment_dot_syntax.ex:126 — `x=.foo()` (no space before the `=`) is admitted by `\\s*=` and repaired to `x= foo()`: the callback appends exactly one space after the `=` but never normalises the space before it, so the emitted line parses and means the right thing while being the one admitted shape whose output is not `mix format`-clean. No test covers a spaceless `=`, so this shape's output is unpinned.",
      "signature": "<anchor> — `x=.foo()` (no space before the `=`) is admitted by `\\s*=` and repaired to `x= foo()`: the callback appends exactly one space after the `=` but never normalises the space before it, so the emitted line parses and means the right thing while being the one admitted shape whose output is not `mix format`-clean. no test covers a spaceless `=`, so this shape's output is unpinned."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/fix_assignment_dot_syntax.ex",
      "finding": "lib/syntax/fix_assignment_dot_syntax.ex:32 — the \"Not flagged\" entry \"A second `=.` later on the same line\" reads as unconditional, but `@second_occurrence` (line 121) requires the second `=` to be preceded by a *bare identifier*, so a second `=.` whose left-hand side ends in `)`, `]` or `}` slips past it: on `a =.foo(b[:k] =.bar())` the tail is scanned as `foo(b[:k] =.bar())`, the byte before ` =` is `]`, nothing matches, and the line is prefix-repaired to `a = foo(b[:k] =.bar())` — which still does not parse and which `analyze/1` then reports as clean. That is precisely the \"dead end\" the guard and lines 39-44 say the rule refuses to create. The inputs that reach it are contrived (a second broken assignment on one line, with a destructured or indexed left-hand side), and every one of them has a left-hand side this rule declines anyway, so no repair is lost — but the moduledoc bullet and the test comment at test/syntax/fix_assignment_dot_syntax_fix_test.exs:210-215 both state the invariant more broadly than the code holds it, and no fixture pins the hole in either direction.",
      "signature": "<anchor> — the \"not flagged\" entry \"a second `=.` later on the same line\" reads as unconditional, but `@second_occurrence` (line #) requires the second `=` to be preceded by a *bare identifier*, so a second `=.` whose left-hand side ends in `)`, `]` or `}` slips past it: on `a =.foo(b[:k] =.bar())` the tail is scanned as `foo(b[:k] =.bar())`, the byte before ` =` is `]`, nothing matches, and the line is prefix-repaired to `a = foo(b[:k] =.bar())` — which still does not parse and which `analyze/#` then reports as clean. that is precisely the \"dead end\" the guard and lines #-# say the rule refuses to create. the inputs that reach it are contrived (a second broken assignment on one line, with a destructured or indexed left-hand side), and every one of them has a left-hand side this rule declines anyway, so no repair is lost — but the moduledoc bullet and the test comment at <anchor>-# both state the invariant more broadly than the code holds it, and no fixture pins the hole in either direction."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/fix_assignment_dot_syntax.ex",
      "finding": "lib/syntax/fix_assignment_dot_syntax.ex:108 — the comment justifying the em-dash trade cites `Credence.SourceMask`'s moduledoc: \"Punctuation sits in code position too: `Credence.SourceMask`'s own moduledoc exists because an em dash can, in source that does not parse.\" That moduledoc (lib/source_mask.ex:2-70) is entirely about rules rewriting the *inside of string literals* — its worked example is the four rules that corrupted `IO.puts(\"100% done\")` — and it never mentions punctuation in code position or em dashes. A maintainer following the pointer to check why a high byte in the shadow is not necessarily an identifier byte finds no such argument there. The trailing clause (\"because an em dash can, in source that does not parse\") is also missing its verb. The behaviour the paragraph describes is correct and is pinned at test/syntax/fix_assignment_dot_syntax_fix_test.exs:380; only the citation is wrong — cite this rule's own pinned test, or drop the cross-reference.",
      "signature": "<anchor> — the comment justifying the em-dash trade cites `credence.sourcemask`'s moduledoc: \"punctuation sits in code position too: `credence.sourcemask`'s own moduledoc exists because an em dash can, in source that does not parse.\" that moduledoc (<anchor>-#) is entirely about rules rewriting the *inside of string literals* — its worked example is the four rules that corrupted `io.puts(\"#% done\")` — and it never mentions punctuation in code position or em dashes. a maintainer following the pointer to check why a high byte in the shadow is not necessarily an identifier byte finds no such argument there. the trailing clause (\"because an em dash can, in source that does not parse\") is also missing its verb. the behaviour the paragraph describes is correct and is pinned at <anchor>; only the citation is wrong — cite this rule's own pinned test, or drop the cross-reference."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/fix_assignment_dot_syntax_analyze_test.exs",
      "finding": "test/syntax/fix_assignment_dot_syntax_analyze_test.exs:106 — the test is named \"second `=` on the line is not flagged\", but the rule only declines an `=` that comes *before* the dot; `x =.foo(a = 1)` also has a second `=` on the line and is flagged and repaired. The rule's moduledoc was amended for exactly this overstatement and the fix test gates the amendment (test/syntax/fix_assignment_dot_syntax_fix_test.exs:243-254), so this test name is the last unqualified copy of the wider claim. Rename it to say \"a second `=` before the dot\", and consider mirroring the `assert analyze(\"x =.foo(a = 1)\") != []` case (fix_test:240) into this file so the analyze side pins both directions of the boundary.",
      "signature": "<anchor> — the test is named \"second `=` on the line is not flagged\", but the rule only declines an `=` that comes *before* the dot; `x =.foo(a = #)` also has a second `=` on the line and is flagged and repaired. the rule's moduledoc was amended for exactly this overstatement and the fix test gates the amendment (<anchor>-#), so this test name is the last unqualified copy of the wider claim. rename it to say \"a second `=` before the dot\", and consider mirroring the `assert analyze(\"x =.foo(a = #)\") != []` case (<anchor>) into this file so the analyze side pins both directions of the boundary."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/fix_assignment_dot_syntax_analyze_test.exs",
      "finding": "test/syntax/fix_assignment_dot_syntax_analyze_test.exs:78 — neither comment test in this file would go red if `Credence.SourceMask` were removed from `analyze/1`. On the raw line `# ref =.make_ref()` the `^`-anchored pattern fails on the leading `#` (not in `[a-zA-Z_\\x80-\\xff]`), and on `x = 1 # ref =.make_ref()` (line 110) it fails because a `1` follows `= `, not a dot. Both pass by the anchor, exactly the accident the rule's own moduledoc says the \"Not flagged\" list used to rely on (lib/syntax/fix_assignment_dot_syntax.ex:65-70, docs/22 T3.10). The only analyze-side test where masking is actually load-bearing lives in the sibling file (fix_test:459-467, an assignment inside a `@moduledoc` heredoc); a heredoc case belongs in this file too, so the analyze half is not one file-move away from unguarded.",
      "signature": "<anchor> — neither comment test in this file would go red if `credence.sourcemask` were removed from `analyze/#`. on the raw line `# ref =.make_ref()` the `^`-anchored pattern fails on the leading `#` (not in `[a-za-z_\\x#-\\xff]`), and on `x = # # ref =.make_ref()` (line #) it fails because a `#` follows `= `, not a dot. both pass by the anchor, exactly the accident the rule's own moduledoc says the \"not flagged\" list used to rely on (<anchor>-#, docs/# t#.#). the only analyze-side test where masking is actually load-bearing lives in the sibling file (<anchor>-#, an assignment inside a `@moduledoc` heredoc); a heredoc case belongs in this file too, so the analyze half is not one file-move away from unguarded."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/fix_assignment_dot_syntax_analyze_test.exs",
      "finding": "test/syntax/fix_assignment_dot_syntax_analyze_test.exs:46 — the comment claims \"Only an `=` *before* the dot declines; an `=` after it is an ordinary keyword-less argument and the line is flagged like any other\" (restated at :131). That is one notch too wide: `a =.foo(b =.bar())` carries its second `=` *after* the dot and is declined, by the `@second_occurrence` check at lib/syntax/fix_assignment_dot_syntax.ex:121 (the tail after the first match is `foo(b =.bar())`, which matches, so `repair/2` returns `:decline`). The rule's own moduledoc lists that shape as a separate decline (lib/syntax/fix_assignment_dot_syntax.ex:32) and the fix battery pins `analyze(\"a =.foo(b =.bar())\") == []` (test/syntax/fix_assignment_dot_syntax_fix_test.exs:225). The real boundary is \"an `=` after the dot declines only when it forms a second `=.`\" — a maintainer reading these two comments as the spec would expect that line to be repaired. Concrete fix: qualify both comments, and add the `a =.foo(b =.bar())` no-issue case to this file, so the \"both directions\" the commit message promises is actually the three the rule distinguishes. This is also the only branch of `repair/2` that this analyze battery never exercises; the suite covers it, but only from the fix file.",
      "signature": "<anchor> — the comment claims \"only an `=` *before* the dot declines; an `=` after it is an ordinary keyword-less argument and the line is flagged like any other\" (restated at :#). that is one notch too wide: `a =.foo(b =.bar())` carries its second `=` *after* the dot and is declined, by the `@second_occurrence` check at <anchor> (the tail after the first match is `foo(b =.bar())`, which matches, so `repair/#` returns `:decline`). the rule's own moduledoc lists that shape as a separate decline (<anchor>) and the fix battery pins `analyze(\"a =.foo(b =.bar())\") == []` (<anchor>). the real boundary is \"an `=` after the dot declines only when it forms a second `=.`\" — a maintainer reading these two comments as the spec would expect that line to be repaired. concrete fix: qualify both comments, and add the `a =.foo(b =.bar())` no-issue case to this file, so the \"both directions\" the commit message promises is actually the three the rule distinguishes. this is also the only branch of `repair/#` that this analyze battery never exercises; the suite covers it, but only from the fix file."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/fix_assignment_dot_syntax_analyze_test.exs",
      "finding": "test/syntax/fix_assignment_dot_syntax_analyze_test.exs:91 — \"The two comment lines above\" points at tests that are not both above it. Only `# ref =.make_ref()` (:87) precedes the comment; the other case it names, `x = 1 # ref =.make_ref()`, is at :138, in a different `describe`. The claim itself is correct — I checked both raw lines against `@bad_pattern` and neither matches without masking, so the heredoc test at :97 really is the load-bearing one — only the locator is wrong.",
      "signature": "<anchor> — \"the two comment lines above\" points at tests that are not both above it. only `# ref =.make_ref()` (:#) precedes the comment; the other case it names, `x = # # ref =.make_ref()`, is at :#, in a different `describe`. the claim itself is correct — i checked both raw lines against `@bad_pattern` and neither matches without masking, so the heredoc test at :# really is the load-bearing one — only the locator is wrong."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/fix_assignment_dot_syntax_analyze_test.exs",
      "finding": "test/syntax/fix_assignment_dot_syntax_analyze_test.exs:11 — the `Issue` struct is matched on `rule` and `meta.line` only; `message` is never asserted here, and the fix test does not assert it either, so this rule's user-facing text (\"Extra dot after `=` in assignment — …\", lib/syntax/fix_assignment_dot_syntax.ex:180) could be emptied or made wrong with the whole suite green. No meta-gate covers issue messages. Sibling analyze tests pin it exactly — e.g. test/syntax/fix_capture_operator_syntax_analyze_test.exs:13 and test/syntax/fix_ets_match_spec_erlang_less_than_analyze_test.exs:23 both use `message == \"…\"`; one such assertion on the first flagging test would close it.",
      "signature": "<anchor> — the `issue` struct is matched on `rule` and `meta.line` only; `message` is never asserted here, and the fix test does not assert it either, so this rule's user-facing text (\"extra dot after `=` in assignment — …\", <anchor>) could be emptied or made wrong with the whole suite green. no meta-gate covers issue messages. sibling analyze tests pin it exactly — e.g. <anchor> and <anchor> both use `message == \"…\"`; one such assertion on the first flagging test would close it."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/fix_assignment_dot_syntax_analyze_test.exs",
      "finding": "test/syntax/fix_assignment_dot_syntax_analyze_test.exs:45 — the comment opens \"The flagging one of the three shapes the second-`=` boundary distinguishes;\" which is a fragment with no verb, so the sentence never says what is being claimed about that shape. The rest of the comment is accurate; only the first clause needs a verb (e.g. \"This is the flagging one of the three shapes …\").",
      "signature": "<anchor> — the comment opens \"the flagging one of the three shapes the second-`=` boundary distinguishes;\" which is a fragment with no verb, so the sentence never says what is being claimed about that shape. the rest of the comment is accurate; only the first clause needs a verb (e.g. \"this is the flagging one of the three shapes …\")."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/fix_assignment_dot_syntax_fix_test.exs",
      "finding": "test/syntax/fix_assignment_dot_syntax_fix_test.exs:183 — `f =.(1)` (anonymous-call syntax) is a decline pinned as a no-op, but it is the one decline left out of the `@declined` list, so the moduledoc-consistency test at :195 does not cover it — and the rule's `## Not flagged` list (lib/syntax/fix_assignment_dot_syntax.ex:21-32) indeed never names it; it is only implied by the \"the character after the dot must start an identifier\" sentence in a later section. The comment introducing `@declined` at :164 claims the list is \"Everything the pattern declines\", which is what makes the omission read as complete coverage. Moving `\"f =.(1)\"` into `@declined` closes the gap (and would go red until the `## Not flagged` list names anonymous-call syntax, which is the point of the gate).",
      "signature": "<anchor> — `f =.(#)` (anonymous-call syntax) is a decline pinned as a no-op, but it is the one decline left out of the `@declined` list, so the moduledoc-consistency test at :# does not cover it — and the rule's `## not flagged` list (<anchor>-#) indeed never names it; it is only implied by the \"the character after the dot must start an identifier\" sentence in a later section. the comment introducing `@declined` at :# claims the list is \"everything the pattern declines\", which is what makes the omission read as complete coverage. moving `\"f =.(#)\"` into `@declined` closes the gap (and would go red until the `## not flagged` list names anonymous-call syntax, which is the point of the gate)."
    },
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/fix_assignment_dot_syntax.ex",
      "finding": "lib/syntax/fix_assignment_dot_syntax.ex:125 — the \"second `=.` on the line\" guard only sees a second occurrence whose left-hand side ends in an identifier byte, because `@second_occurrence` starts with `[a-zA-Z_\\x80-\\xff]`. A second `=.` written after a closing bracket slips through: on `x =.foo({:ok, v} =.bar())` the rule repairs the prefix and emits `x = foo({:ok, v} =.bar())`, which still does not parse and no longer matches the `^`-anchored `@bad_pattern`, so `analyze/1` afterwards reports the line as clean and nothing ever surfaces the leftover — exactly the half-repaired dead end the moduledoc (lines 43-48) and the test at test/syntax/fix_assignment_dot_syntax_fix_test.exs:215 say the rule refuses to create. The same shape with an identifier on the left (`x =.foo(b =.bar())`) is declined, and that is the only shape either test file pins, so the gap is invisible to the suite.",
      "signature": "<anchor> — the \"second `=.` on the line\" guard only sees a second occurrence whose left-hand side ends in an identifier byte, because `@second_occurrence` starts with `[a-za-z_\\x#-\\xff]`. a second `=.` written after a closing bracket slips through: on `x =.foo({:ok, v} =.bar())` the rule repairs the prefix and emits `x = foo({:ok, v} =.bar())`, which still does not parse and no longer matches the `^`-anchored `@bad_pattern`, so `analyze/#` afterwards reports the line as clean and nothing ever surfaces the leftover — exactly the half-repaired dead end the moduledoc (lines #-#) and the test at <anchor> say the rule refuses to create. the same shape with an identifier on the left (`x =.foo(b =.bar())`) is declined, and that is the only shape either test file pins, so the gap is invisible to the suite."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/fix_assignment_dot_syntax.ex",
      "finding": "lib/syntax/fix_assignment_dot_syntax.ex:165 — the match's start offset is bound to `_match_start` and discarded, and `match_len` is then used as an *absolute* byte offset in both `binary_part(shadow, match_len, ...)` (line 166) and `binary_part(line, match_len, ...)` (line 173). That is only correct while `@bad_pattern` keeps its `^` anchor and never gains the `m` modifier; if the anchor is ever dropped the splice cuts at the wrong byte and silently emits a mangled line rather than failing. Writing `match_start + match_len` costs nothing and removes the coupling.",
      "signature": "<anchor> — the match's start offset is bound to `_match_start` and discarded, and `match_len` is then used as an *absolute* byte offset in both `binary_part(shadow, match_len, ...)` (line #) and `binary_part(line, match_len, ...)` (line #). that is only correct while `@bad_pattern` keeps its `^` anchor and never gains the `m` modifier; if the anchor is ever dropped the splice cuts at the wrong byte and silently emits a mangled line rather than failing. writing `match_start + match_len` costs nothing and removes the coupling."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/fix_assignment_dot_syntax.ex",
      "finding": "lib/syntax/fix_assignment_dot_syntax.ex:25 — the `## Not flagged` bullet `String literals (msg = \"=.not_a_dot\")` is the only entry in that list with no corresponding no-op in the `@declined` battery (test/syntax/fix_assignment_dot_syntax_fix_test.exs:171). The moduledoc gate at that file's line 194 checks only the other direction — that every pinned decline is named — so this claim is documented but unpinned. Adding the string to `@declined` closes it.",
      "signature": "<anchor> — the `## not flagged` bullet `string literals (msg = \"=.not_a_dot\")` is the only entry in that list with no corresponding no-op in the `@declined` battery (<anchor>). the moduledoc gate at that file's line # checks only the other direction — that every pinned decline is named — so this claim is documented but unpinned. adding the string to `@declined` closes it."
    },
    {
      "severity": "nit",
      "reviewed_path": "test/syntax/fix_assignment_dot_syntax_fix_test.exs",
      "finding": "test/syntax/fix_assignment_dot_syntax_fix_test.exs:429 — in `@mixed_source` the `@moduledoc` heredoc body (`doc =.example()`) sits in column 0 while its closing `\"\"\"` is indented two spaces, so the fixture is an *outdented heredoc line*; Elixir accepts it but emits a tokenizer warning (\"heredoc line does not have enough left-hand side indentation ... may cause errors in Elixir v2.0 and later\"), which `valid_syntax?/1` at lines 485 and 497 triggers on every run. Moving the closing `\"\"\"` (line 430, and the matching one in the `expected` literal at line 474) to column 0 keeps exactly what the fixture is for — a doc line that begins with the shape the rule matches — with no warning and no future-Elixir fragility.",
      "signature": "<anchor> — in `@mixed_source` the `@moduledoc` heredoc body (`doc =.example()`) sits in column # while its closing `\"\"\"` is indented two spaces, so the fixture is an *outdented heredoc line*; elixir accepts it but emits a tokenizer warning (\"heredoc line does not have enough left-hand side indentation ... may cause errors in elixir v#.# and later\"), which `valid_syntax?/#` at lines # and # triggers on every run. moving the closing `\"\"\"` (line #, and the matching one in the `expected` literal at line #) to column # keeps exactly what the fixture is for — a doc line that begins with the shape the rule matches — with no warning and no future-elixir fragility."
    },
    {
      "severity": "blocker",
      "reviewed_path": "lib/syntax/fix_bare_tuple_zero_in_type.ex",
      "finding": "lib/syntax/fix_bare_tuple_zero_in_type.ex:109 — `toggle_heredoc/2` decides \"am I inside a heredoc?\" by counting `\"\"\"`/`'''` per line and flipping on an odd count, so any line whose delimiter count is odd for a reason other than being a delimiter inverts the state for the rest of the file. On input like `@moduledoc \"\"\"` / `Charlist heredocs open with '''.` / (blank) / `  @type t :: () -> any()` / `\"\"\"`, the `'''` line flips the tracker off and the rule rewrites the documentation prose to `  @type t :: (() -> any())` — silently changing the text the module's `@moduledoc` produces, which is exactly the corruption the moduledoc at :46-50 claims this tracking prevents. The same flip runs the other way (a code line containing one `\"\"\"`, e.g. `String.split(x, ~s(\"\"\"))`, turns tracking *on* and the rule goes inert for the remainder of the file with every gate green). The project already has the primitive for this, with the opposite failure direction stated in its own docs: `Credence.SourceMask.lines/1` + `self_contained?/2` (lib/source_mask.ex:224, :255) — \"the failure direction is a missed fix on a line inside a multi-line literal, never a corrupted one\" — and `PreferSpecArrowOperator` (lib/syntax/prefer_spec_arrow_operator.ex:25-31) documents that it was converted to it precisely because it had been rewriting the `## Bad` example in its own moduledoc.",
      "signature": "<anchor> — `toggle_heredoc/#` decides \"am i inside a heredoc?\" by counting `\"\"\"`/`'''` per line and flipping on an odd count, so any line whose delimiter count is odd for a reason other than being a delimiter inverts the state for the rest of the file. on input like `@moduledoc \"\"\"` / `charlist heredocs open with '''.` / (blank) / `  @type t :: () -> any()` / `\"\"\"`, the `'''` line flips the tracker off and the rule rewrites the documentation prose to `  @type t :: (() -> any())` — silently changing the text the module's `@moduledoc` produces, which is exactly the corruption the moduledoc at :#-# claims this tracking prevents. the same flip runs the other way (a code line containing one `\"\"\"`, e.g. `string.split(x, ~s(\"\"\"))`, turns tracking *on* and the rule goes inert for the remainder of the file with every gate green). the project already has the primitive for this, with the opposite failure direction stated in its own docs: `credence.sourcemask.lines/#` + `self_contained?/#` (<anchor>, :#) — \"the failure direction is a missed fix on a line inside a multi-line literal, never a corrupted one\" — and `preferspecarrowoperator` (<anchor>-#) documents that it was converted to it precisely because it had been rewriting the `## bad` example in its own moduledoc."
    },
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/fix_bare_tuple_zero_in_type.ex",
      "finding": "lib/syntax/fix_bare_tuple_zero_in_type.ex:125 — `target/1` judges \"self-contained right-hand side\" one line at a time, so a typespec whose return type continues onto the next line is flagged and rewritten into a differently-broken shape. On `@type t :: () -> {:ok, term()}` followed by `  | {:error, term()}`, the first line passes every gate (balanced, one top-level arrow, ends on `}`) and becomes `@type t :: (() -> {:ok, term()})`, orphaning the `|` continuation. The net user-visible outcome is a missed repair rather than shipped damage — the round's all-or-nothing rollback (lib/syntax.ex:151) discards it because the result still does not parse — but the check reports a finding the fix cannot actually repair, and it burns the round for any other rule whose repair was riding along. The moduledoc's \"self-contained: brackets balanced, no second `::`, no `when` guard\" list does not mention the line-locality assumption.",
      "signature": "<anchor> — `target/#` judges \"self-contained right-hand side\" one line at a time, so a typespec whose return type continues onto the next line is flagged and rewritten into a differently-broken shape. on `@type t :: () -> {:ok, term()}` followed by `  | {:error, term()}`, the first line passes every gate (balanced, one top-level arrow, ends on `}`) and becomes `@type t :: (() -> {:ok, term()})`, orphaning the `|` continuation. the net user-visible outcome is a missed repair rather than shipped damage — the round's all-or-nothing rollback (<anchor>) discards it because the result still does not parse — but the check reports a finding the fix cannot actually repair, and it burns the round for any other rule whose repair was riding along. the moduledoc's \"self-contained: brackets balanced, no second `::`, no `when` guard\" list does not mention the line-locality assumption."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/fix_bare_tuple_zero_in_type.ex",
      "finding": "test/syntax/fix_bare_tuple_zero_in_type_analyze_test.exs:63 — `assert issue.message =~ \"Wrap the function type in parens\"` is a substring assertion on the one thing the user reads; pin the whole message with `==`, as the recent campaign commits did for the other Syntax rules.",
      "signature": "<anchor> — `assert issue.message =~ \"wrap the function type in parens\"` is a substring assertion on the one thing the user reads; pin the whole message with `==`, as the recent campaign commits did for the other syntax rules."
    },
    {
      "severity": "nit",
      "reviewed_path": "lib/syntax/fix_bare_tuple_zero_in_type.ex",
      "finding": "test/syntax/fix_bare_tuple_zero_in_type_fix_test.exs:284-301 — the 1008-combination battery is vacuous-safe only by luck: if the rule rewrote nothing at all, `rewritten?` would be `false` for every combination, `assert length(analyze(line)) == 0` would hold, and the `valid_syntax?` branch would never run, so the whole loop passes green against a dead rule. Count the rewrites and assert the total (the intended figure is derivable from the fixture lists) so the battery cannot pass on an inert rule.",
      "signature": "<anchor>-# — the #-combination battery is vacuous-safe only by luck: if the rule rewrote nothing at all, `rewritten?` would be `false` for every combination, `assert length(analyze(line)) == #` would hold, and the `valid_syntax?` branch would never run, so the whole loop passes green against a dead rule. count the rewrites and assert the total (the intended figure is derivable from the fixture lists) so the battery cannot pass on an inert rule."
    },
    {
      "severity": "blocker",
      "reviewed_path": "lib/syntax/fix_bare_tuple_zero_in_type.ex",
      "finding": "lib/syntax/fix_bare_tuple_zero_in_type.ex:92 — A multiline range return such as `@type t :: () -> 1` followed by `.. 10` is not recognized as a continuation, so the fix closes the function type on the first line and changes it to `(() -> 1) .. 10` instead of wrapping the complete `1..10` return type.",
      "signature": "<anchor> — a multiline range return such as `@type t :: () -> #` followed by `.. #` is not recognized as a continuation, so the fix closes the function type on the first line and changes it to `(() -> #) .. #` instead of wrapping the complete `#..#` return type."
    },
    {
      "severity": "concern",
      "reviewed_path": "lib/syntax/fix_bare_tuple_zero_in_type.ex",
      "finding": "lib/syntax/fix_bare_tuple_zero_in_type.ex:225 — `balanced?/1` counts bracket depth but does not require matching bracket kinds, so input such as `@type t :: () -> [integer()}` is reported and rewritten even though the rewrite cannot repair it.",
      "signature": "<anchor> — `balanced?/#` counts bracket depth but does not require matching bracket kinds, so input such as `@type t :: () -> [integer()}` is reported and rewritten even though the rewrite cannot repair it."
    }
  ]
}
